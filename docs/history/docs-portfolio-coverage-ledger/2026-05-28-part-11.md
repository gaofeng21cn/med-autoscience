# Docs Portfolio Coverage Ledger 2026-05-28 Part 11

Status: `history_provenance`
Owner: `MedAutoScience`
Purpose: `docs_lifecycle_governance_coverage_ledger_archive`
State: `history_provenance`
Machine boundary: 本文件只保存 docs governance coverage provenance；不承载 MAS runtime、controller、publication、artifact、source readiness、quality verdict、study truth、owner receipt、production readiness 或 physical-delete authorization。

### 2026-05-28 AI reviewer record / OPL attempt currentness tranche

本轮逐段复核 MAS 最近两个 currentness 修复提交对应的文档、源码和测试：AI reviewer request-attached record 的 fail-closed record shape，以及多 running OPL attempt 时 MAS progress/current-control 必须优先投影当前 owner action。目标是确认 `docs/decisions.md` 与 `docs/runtime/control/controllers.md` 的新增 currentness 规则仍匹配 live implementation；本轮不重写 MAS active truth，不关闭 OPL series 全局 `/goal`，也不把 read-model currentness 写成 paper closure、publication-ready、artifact authority、domain-ready、production-ready、owner receipt 或 App/workbench readiness。

Live truth inputs:

- MAS `AGENTS.md`, `TASTE.md`, `docs/docs_portfolio_consolidation.md`, `docs/active/mas-ideal-state-gap-plan.md`, `docs/status.md`, `docs/architecture.md`, `docs/invariants.md`, `docs/decisions.md`, and `docs/runtime/control/controllers.md`.
- Source surfaces: `src/med_autoscience/controllers/owner_route_reconcile.py`, `src/med_autoscience/controllers/owner_route_reconcile_parts/opl_provider_attempts.py`, `src/med_autoscience/controllers/domain_owner_action_dispatch_parts/action_execution_parts/ai_reviewer_record_validation.py`, and `src/med_autoscience/controllers/domain_owner_action_dispatch_parts/action_execution_parts/ai_reviewer_record_production.py`.
- Focused tests: `tests/owner_route_reconcile_cases/test_opl_provider_attempt_projection.py`, `tests/owner_route_reconcile_cases/test_current_write_routeback_projection.py`, and `tests/test_domain_owner_action_dispatch_cases/ai_reviewer_record_validation.py`.
- OPL Doc Governance doctor output from this worktree is used only as structural risk map, not as semantic proof.

Fresh semantic result:

- `docs/decisions.md` remains current for "多 running OPL attempt 时优先投影当前 owner action": `owner_route_reconcile` passes preferred owner actions into OPL provider attempt projection, and `opl_provider_attempts.live_provider_attempt_for_study` ranks live tasks by action type, executable/controller work unit and dispatch ref, with action-type-only fallback when work-unit ids differ. Focused tests cover both exact current owner action preference and the action-type fallback. This is observability/read-model currentness only; it does not mutate OPL queue/attempt/retry/dead-letter or MAS truth/package/publication gate.
- `docs/decisions.md` and `docs/runtime/control/controllers.md` remain current for AI reviewer record-only fail-closed shape: request-attached AI reviewer records must be AI reviewer-owned, publication-scoped, quality-assessment complete, current-manuscript bound, backed by a production `reviewer_operating_system`, and carry a top-level `future_facing_limitations_plan` with `limitation`, `impact_on_claim`, `required_future_analysis_data_or_design`, and `current_manuscript_wording_must_be_restrained`. Item-only `{"item": "..."}` diagnostic notes are rejected before publication workflow/materializer handoff.
- `ai_reviewer_record_validation.py` provides the live fail-closed contract by returning `ai_reviewer_record_invalid` for invalid evaluation scope, invalid reviewer OS, or item-only future-facing limitations, and `ai_reviewer_record_incomplete` for missing required record fields. `ai_reviewer_record_production.py` keeps the canonical owner callable as `publication materialize-ai-reviewer-record --build-production-trace`, writes only `artifacts/publication_eval/ai_reviewer_responses/*_publication_eval_record.json`, and forbids `publication_eval/latest.json`, `controller_decisions/latest.json`, paper, submission package and current package writes.
- No prose body correction was needed. This tranche only records coverage and links the ledger from the active governance index.

Reviewed documents / sections:

| Repo | Reviewed docs / sections | Edited docs this tranche |
| --- | --- | --- |
| `med-autoscience` | Full currentness read of the new `docs/decisions.md` section for current owner action projection; support read of the AI reviewer record-only decision entry and the `docs/runtime/control/controllers.md` AI reviewer record-only paragraph; support read of MAS repo guidance, core docs, active gap plan, source surfaces and focused tests listed above. | `docs/docs_portfolio_consolidation.md`; `docs/history/docs-portfolio-coverage-ledger/2026-05-28-part-11.md` |

Archived / tombstoned / deleted docs:

- none. The reviewed docs still have durable roles: `docs/decisions.md` remains the decision log, `docs/runtime/control/controllers.md` remains active runtime control support, and this file is coverage provenance.

Unreviewed docs:

- `med-autoscience`: no new exact-path gap is introduced by this tranche; future source/contract/read-model changes can reopen individual sections.
- Other OPL-series repos remain governed by the global coverage ledger. This MAS currentness tranche does not close the global `/goal`.

Remaining stale / retire candidates:

- Any future MAS docs that describe item-only `future_facing_limitations_plan` as acceptable AI reviewer record payload are stale unless the validator contract changes again.
- Any future MAS docs that let older live OPL attempts override a current MAS owner action in progress/current-control projection are stale unless the provider projection ranking contract changes again.
- MAS implementation/evidence tails remain separate from docs coverage: real paper-line owner receipt scaleout, memory/artifact/lifecycle receipts, human gate/resume, provider SLO long soak, OPL refs-only ledger follow-through and physical-delete authorization still require source/test/receipt evidence.

Next tranche write scope:

- Continue with OPL, RCA or App docs coverage according to the OPL series ledger; keep dirty/recent OPL developer-mode, RCA native PPT and App first-run worktrees isolated until their owners finish or hand off.
- Return to MAS only if new MAS docs appear, a live source/contract/read-model change reopens a section, or an implementation/evidence owner lane closes one of the remaining runtime/evidence tails and requires doc foldback.
