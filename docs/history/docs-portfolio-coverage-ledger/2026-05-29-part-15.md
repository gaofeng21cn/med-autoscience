# Docs Portfolio Coverage Ledger 2026-05-29 Part 15

Status: `history_provenance`
Owner: `MedAutoScience`
Purpose: `docs_lifecycle_governance_coverage_ledger_archive`
State: `history_provenance`
Machine boundary: 本文件只保存 docs governance coverage provenance；不承载 MAS runtime、controller、publication、artifact、source readiness、quality verdict、study truth、owner receipt、production readiness 或 physical-delete authorization。

### 2026-05-29 Domain-dispatch worklist currentness tranche

本轮在 OPL series fresh inventory 后，复核 MAS active truth 与 status 中的
domain-dispatch / stage evidence payload 段落。目标是把 `zero_open` 旧读数更新为
当前 OPL refs-only read-model truth：现有 open route 是 payload-required 的
MAS domain-dispatch evidence workorder，必须由 MAS domain/App/live owner refs 或
MAS-owned typed blocker 关闭，OPL 不能自造 owner receipt、typed blocker、owner-chain
或 no-regression refs。

Live truth inputs:

- MAS `AGENTS.md`, `TASTE.md`, `docs/docs_portfolio_consolidation.md`,
  `docs/active/mas-ideal-state-gap-plan.md`, and `docs/status.md`.
- OPL read-model commands run from `/Users/gaofeng/workspace/one-person-lab`:
  `./bin/opl family-runtime evidence-worklist --family-defaults --provider temporal --executor-kind codex_cli --detail full --json`,
  `./bin/opl framework readiness --family-defaults --json`, and
  `./bin/opl runtime action execute --action domain_dispatch:medautoscience:sat_55e36b0dbf2b609fb33329db:record --dry-run --payload '{"domain_receipt_refs":[],"typed_blocker_refs":[],"no_regression_refs":[],"owner_chain_refs":[],"evidence_refs":[]}'`.
- OPL Doc Governance doctor output from this checkout is used only as a structural risk map,
  not as semantic proof.

Fresh semantic result:

- OPL evidence-worklist currently reports `open_worklist_item_count=1`,
  `open_safe_action_payload_required_item_count=1`,
  `open_safe_action_payload_free_item_count=0`,
  `domain_dispatch_evidence_workorder_count=1`,
  `stage_receipt_freshness_open_workorder_count=0`,
  `closed_refs_only_item_count=307`, and
  `domain_dispatch_evidence_receipt_item_count=267`.
- The open route is
  `domain_dispatch:medautoscience:sat_55e36b0dbf2b609fb33329db:record` for
  `003-dpcc-primary-care-phenotype-treatment-gap` /
  `run_quality_repair_batch` on stage `domain_owner/default-executor-dispatch`.
- Empty-payload dry-run fails closed with missing
  `domain_receipt_refs_or_typed_blocker_refs_or_owner_chain_refs_or_no_regression_refs`.
  This confirms the route is recordable only after real domain/App/live owner refs or a
  domain-owned typed blocker are available.
- The current worklist state does not claim MAS owner receipt, paper closure,
  publication readiness, artifact mutation authorization, domain readiness or production
  readiness.

Reviewed documents / sections:

| Repo | Reviewed docs / sections | Edited docs this tranche |
| --- | --- | --- |
| `med-autoscience` | Currentness read of `docs/active/mas-ideal-state-gap-plan.md` rows `Domain dispatch / stage evidence payload` and `owner-chain dispatch ledger scaleout`; currentness read of `docs/status.md` section `后续验证 / evidence tails` owner-chain dispatch paragraph; support read of MAS repo guidance and docs governance index; live read of OPL evidence-worklist, framework readiness and payload preflight outputs listed above. | `docs/active/mas-ideal-state-gap-plan.md`; `docs/status.md`; `docs/docs_portfolio_consolidation.md`; `docs/history/docs-portfolio-coverage-ledger/2026-05-29-part-15.md` |

Archived / tombstoned / deleted docs:

- none. The edited MAS active docs remain current owners for status and active gap truth.

Unreviewed docs:

- `med-autoscience`: no newly uncovered exact path was introduced by this tranche. This was
  a semantic refresh over already-covered high-drift active currentness paragraphs.
- Other OPL-series repos remain governed by the global OPL coverage ledger. This MAS
  currentness tranche does not close the global `/goal`.

Remaining stale / retire candidates:

- Any MAS prose that freezes the previous `zero_open` OPL worklist numbers as current truth
  is stale unless a fresh read-model command reproduces those values.
- Any MAS prose that treats the current open workorder, payload preflight, provider
  completion or refs-only receipt accounting as paper closure, publication readiness,
  artifact authority, domain readiness or production readiness remains stale pollution.

Next tranche write scope:

- Continue OPL series whole-doc coverage outside MAS, prioritizing OPL support docs and
  clean repo slices.
- Return to MAS only if live source/contract/read-model changes reopen these active
  currentness paragraphs, or if MAS owner surfaces produce real refs or a domain-owned typed
  blocker for `sat_55e36b0dbf2b609fb33329db`.
