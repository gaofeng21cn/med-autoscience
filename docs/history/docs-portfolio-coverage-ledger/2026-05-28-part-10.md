# Docs Portfolio Coverage Ledger 2026-05-28 Part 10

Status: `history_provenance`
Owner: `MedAutoScience`
Purpose: `docs_lifecycle_governance_coverage_ledger_archive`
State: `history_provenance`
Machine boundary: 本文件只保存 docs governance coverage provenance；不承载 MAS runtime、controller、publication、artifact、source readiness、quality verdict、study truth、owner receipt、production readiness 或 physical-delete authorization。

### 2026-05-28 runtime projection paragraph-currentness tranche

本轮逐段复核 MAS runtime projection 支持文档中仍需语义 currentness 复查的三份文件：`artifact_inventory_projection.md`、`runtime_health_kernel.md` 与 `study_truth_kernel.md`。目标是把 projection 文档重新对齐 live reducer、artifact lifecycle inventory、truth kernel、focused tests 与 canonical docs；本轮不重写 MAS active truth，不关闭 OPL series 全局 `/goal`，也不把 docs coverage 写成 paper closure、publication-ready、artifact authority、domain-ready、production-ready、owner receipt 或 App/workbench readiness。

Live truth inputs:

- MAS `AGENTS.md`, `TASTE.md`, `docs/docs_portfolio_consolidation.md`, `docs/active/mas-ideal-state-gap-plan.md`, `docs/status.md`, `docs/architecture.md`, `docs/invariants.md`, `docs/decisions.md`, and `docs/runtime/README.md`.
- Runtime projection docs: `docs/runtime/projections/artifact_inventory_projection.md`, `docs/runtime/projections/runtime_health_kernel.md`, and `docs/runtime/projections/study_truth_kernel.md`.
- Source surfaces: `src/med_autoscience/controllers/artifact_lifecycle_inventory.py`, `src/med_autoscience/controllers/runtime_health_kernel.py`, and `src/med_autoscience/controllers/study_truth_kernel.py`.
- Focused tests: `tests/test_artifact_lifecycle_inventory.py`, `tests/test_runtime_health_kernel.py`, `tests/test_study_truth_kernel.py`, `tests/test_study_progress.py`, and owner-route runtime health cases that consume `canonical_runtime_action` / `external_supervisor_required`.
- OPL Doc Governance doctor output from this worktree is used only as structural risk map, not as semantic proof.

Fresh semantic result:

- `artifact_inventory_projection.md` remains current: inventory is projection, not artifact authority; stable roles, delivery package layout statuses `v2` / `legacy` / `unknown`, and the no-edit-authority boundary match `artifact_lifecycle_inventory.py` and `tests/test_artifact_lifecycle_inventory.py`.
- `study_truth_kernel.md` remains current: truth events, shadow/materialized snapshot split, `reconcile-truth`, stop-loss dominance, reactivation by task intake / explicit resume, supervisor-only / bundle downstream blockers, AI reviewer provenance gate and current-package projection boundary match `study_truth_kernel.py` and focused tests.
- `runtime_health_kernel.md` needed one currentness fix: retry-budget exhaustion now materializes `canonical_runtime_action=external_supervisor_required`, with manual-review allowed actions; the stale `canonical_runtime_action=escalate_runtime` wording was replaced. Runtime health remains MAS domain diagnostic projection and does not own OPL worker/session/attempt truth.
- No archive, tombstone or deletion was needed. This tranche only edits a stale runtime-health sentence, records paragraph coverage, and links this ledger from the active governance index.

Reviewed documents / sections:

| Repo | Reviewed docs / sections | Edited docs this tranche |
| --- | --- | --- |
| `med-autoscience` | Full-body paragraph currentness read of `docs/runtime/projections/artifact_inventory_projection.md`, `docs/runtime/projections/runtime_health_kernel.md`, and `docs/runtime/projections/study_truth_kernel.md`; support read of MAS repo guidance, core docs, active gap plan, runtime README, source reducers and focused tests listed above. | `docs/runtime/projections/runtime_health_kernel.md`; `docs/docs_portfolio_consolidation.md`; `docs/history/docs-portfolio-coverage-ledger/2026-05-28-part-10.md` |

Archived / tombstoned / deleted docs:

- none. The reviewed projection docs still have a durable `active_runtime_support` role.

Unreviewed docs:

- `med-autoscience`: no new exact-path gap is introduced by this tranche; future source/contract/read-model changes can reopen individual sections.
- Other OPL-series repos remain governed by the global coverage ledger. This MAS runtime projection tranche does not close the global `/goal`.

Remaining stale / retire candidates:

- Any future MAS docs that claim retry-budget exhaustion emits `canonical_runtime_action=escalate_runtime` directly from `RuntimeHealthKernel` are stale unless the live reducer changes again.
- MAS implementation/evidence tails remain separate from docs coverage: real paper-line owner receipt scaleout, memory/artifact/lifecycle receipts, human gate/resume, provider SLO long soak, OPL refs-only ledger follow-through and physical-delete authorization still require source/test/receipt evidence.

Next tranche write scope:

- Continue with OPL, RCA or App docs coverage according to the OPL series ledger; keep RCA/App root dirty lanes isolated until their owners finish or hand off.
- Return to MAS only if new MAS docs appear, a live source/contract/read-model change reopens a section, or an implementation/evidence owner lane closes one of the remaining runtime/evidence tails and requires doc foldback.
