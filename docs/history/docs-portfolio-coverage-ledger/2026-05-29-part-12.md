# Docs Portfolio Coverage Ledger 2026-05-29 Part 12

Status: `history_provenance`
Owner: `MedAutoScience`
Purpose: `docs_lifecycle_governance_coverage_ledger_archive`
State: `history_provenance`
Machine boundary: 本文件只保存 docs governance coverage provenance；不承载 MAS runtime、controller、publication、artifact、source readiness、quality verdict、study truth、owner receipt、production readiness 或 physical-delete authorization。

### 2026-05-29 AI reviewer materialization current-inputs currentness tranche

本轮逐段复核 MAS `035b2e82 Fix AI reviewer currentness materialization routing`
对应的文档、源码和测试：AI reviewer record-production work unit 在
`ai_reviewer_record_stale_after_current_inputs`、current manuscript 或
unit-harmonized rerun blocker 仍未被候选 record 覆盖时，必须保留 AI reviewer
record-production route，不得提前桥接到 writer story-surface work unit。本轮不重写
MAS active truth，不关闭 OPL series 全局 `/goal`，也不把 materializer
currentness 写成 paper closure、publication-ready、artifact authority、
domain-ready、production-ready、owner receipt 或 App/workbench readiness。

Live truth inputs:

- MAS `AGENTS.md`, `TASTE.md`, `docs/docs_portfolio_consolidation.md`,
  `docs/active/mas-ideal-state-gap-plan.md`, `docs/status.md`,
  `docs/architecture.md`, `docs/invariants.md`, `docs/decisions.md`, and
  `docs/runtime/control/controllers.md`.
- Changed source surface:
  `src/med_autoscience/controllers/domain_action_request_materializer_parts/publication_owner_materialization.py`.
- Focused test:
  `tests/domain_action_request_materializer_cases/test_ai_reviewer_story_surface_bridge.py`.
- OPL Doc Governance doctor output from this worktree is used only as
  structural risk map, not as semantic proof.

Fresh semantic result:

- `docs/decisions.md` now records the current-inputs materializer rule: when
  request lifecycle is still blocked by `ai_reviewer_record_stale_after_current_inputs`,
  `ai_reviewer_record_stale_after_current_manuscript`, or
  `ai_reviewer_record_stale_after_unit_harmonized_rerun`, the materializer must
  prove the candidate AI reviewer record covers the live currentness refs before
  consuming the record-production work unit.
- The live materializer now checks request lifecycle currentness through
  `project_ai_reviewer_request_lifecycle(...)` before treating an existing AI
  reviewer record as current. If the currentness blocker remains, the
  record-production work unit is not bridged to `run_quality_repair_batch` or
  publication gate replay; the existing AI reviewer production route stays the
  owner route.
- Story-surface bridge work-unit selection is no longer hard-coded across
  studies. The materializer derives the work unit from current reviewer
  route-back `next_work_unit` and verifies it through the canonical story-surface
  write-work-unit registry. Missing route-back work-unit falls back to generic
  `medical_prose_write_repair`; the DM002 legacy bridge remains restricted to
  `002-dm-china-us-mortality-attribution`.
- Focused tests cover both sides of the contract: a current record-production
  work unit can be consumed into a writer story-surface route, while a DM003
  `ai_reviewer_record_stale_after_current_inputs` request with stale evidence /
  claim-map refs keeps `return_to_ai_reviewer_workflow` and does not emit
  `materialized_work_unit_id`.
- `docs/runtime/control/controllers.md`, `docs/status.md`, and
  `docs/active/mas-ideal-state-gap-plan.md` remain current at their current
  abstraction level: they describe AI reviewer currentness, owner-route
  fail-closed behavior, OPL refs-only transport and remaining evidence tails
  without asserting paper closure, package freshness, domain-ready or
  production-ready status.
- No MAS prose body correction was needed beyond the already landed
  `docs/decisions.md` decision entry. This tranche only records coverage and
  links the ledger from the active governance index.

Reviewed documents / sections:

| Repo | Reviewed docs / sections | Edited docs this tranche |
| --- | --- | --- |
| `med-autoscience` | Full currentness read of the new `docs/decisions.md` decision entry; support read of AI reviewer currentness passages in `docs/runtime/control/controllers.md`, `docs/status.md`, and `docs/active/mas-ideal-state-gap-plan.md`; source/test read of the materializer and focused regression test listed above. | `docs/docs_portfolio_consolidation.md`; `docs/history/docs-portfolio-coverage-ledger/2026-05-29-part-12.md` |

Archived / tombstoned / deleted docs:

- none. The reviewed docs still have durable roles: `docs/decisions.md` remains
  the decision log, runtime/status/active docs remain current support / active
  truth surfaces, and this file is coverage provenance.

Unreviewed docs:

- `med-autoscience`: no new exact-path gap is introduced by this tranche; future
  source/contract/read-model changes can reopen individual sections.
- Other OPL-series repos remain governed by the global coverage ledger. This MAS
  currentness tranche does not close the global `/goal`.

Remaining stale / retire candidates:

- Any future MAS docs that let `ai_reviewer_record_stale_after_current_inputs`,
  current-manuscript, or unit-harmonized currentness blockers bridge directly to
  writer/gate owner routes without record-currentness proof are stale unless the
  materializer contract changes again.
- Any future MAS docs that let DM002-specific story-surface work-unit ids become
  cross-study currentness basis are stale unless the story-surface registry
  explicitly admits that route.
- MAS implementation/evidence tails remain separate from docs coverage: real
  paper-line owner receipt scaleout, memory/artifact/lifecycle receipts, human
  gate/resume, provider SLO long soak, OPL refs-only ledger follow-through and
  physical-delete authorization still require source/test/receipt evidence.

Next tranche write scope:

- Continue with OPL, RCA or App docs coverage according to the OPL series
  ledger; keep RCA native-PPT and App first-run dirty lanes isolated until their
  owners finish or hand off.
- Return to MAS only if new MAS docs appear, a live source/contract/read-model
  change reopens a section, or an implementation/evidence owner lane closes one
  of the remaining runtime/evidence tails and requires doc foldback.
