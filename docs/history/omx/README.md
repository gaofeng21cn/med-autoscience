# OMX 历史资料索引

Owner: `MedAutoScience`
Purpose: `omx_history_index`
State: `history_index`
Machine boundary: 人读 OMX-era 历史索引。当前项目规则、runtime truth 和 owner boundary 继续归 `AGENTS.md`、核心 docs、runtime contracts、contracts/source、CLI/API 行为和 owner receipts。

这里收录 `Med Auto Science` 仓库内与 OMX 时代相关的历史资料入口。

它不是当前活跃执行入口，也不是当前 worktree、session、hook、tmux 或 owner-lane 规则。
本目录只能按 2026-04-11 `OMX` 退役决策之前的历史工具手册阅读；子文档中的“当前”“推荐”“必须”“正确用法”等词保留原时代语境，不能继承为今天的 MAS workflow。
当前 MAS 规则必须回到仓库根 `AGENTS.md`、核心 docs、runtime contracts、source/tests、CLI/API read model 和 owner receipts。

不得用本目录复活项目级 `.omx` / `.codex` 状态、root hook 扫描、tmux/session 指针或旧 owner worktree 便签作为权威执行面。

如果要找当前项目规则和当前工作入口，应优先从以下位置进入：

- `../../../AGENTS.md`
- `../../README.md`
- `../../runtime/contracts/agent_runtime_interface.md`
- `../../runtime/contracts/runtime_handle_and_durable_surface_contract.md`

保留为审计与迁移背景的 OMX 历史资料：

- [OMX worktree 启动与收尾操作规约](./omx_worktree_startup_and_closeout.md)
