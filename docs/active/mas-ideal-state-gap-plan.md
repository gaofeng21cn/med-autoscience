# MAS 理想目标态差距与完善计划

Owner: `MedAutoScience`
Purpose: `single_active_truth_plan`
State: `active_plan`
Machine boundary: 本文是人读 gap / completion plan。机器真相继续归 `contracts/`、源码、CLI/MCP/API 行为、product-entry manifest、domain-handler receipt、runtime/controller durable surfaces、真实 workspace 与 generated artifact proof。
Date: `2026-06-30`

## 文档读法

- 本文是 MAS 当前唯一 single Active Truth plan：维护当前唯一真相、目标态、已落地状态、功能/结构差距、近期完善计划、完成门和历史索引。
- MAS 的 north-star 目标态读 [MAS 理想目标态](../references/positioning/mas_ideal_state.md)。本文只保留面向执行的差距和顺序。
- [MAS 当前开发线路](./current-development-lines.md) 只作为内容线索引；若它与本文冲突，以本文为准，并把仍有效内容折回本文、核心 canonical docs、runtime/contracts/policies 或 history/tombstone。
- 2026-06-30 SSOT refresh：本文只维护 MAS 功能面落地、结构收薄、历史遗留清理、source morphology、domain pack、authority function、PaperProgressPolicyAdapter / OPL transition ABI、no-active-caller/tombstone/rebuild 和 no-second-truth guard。live domain diagnostic apply、OPL outbox / StageRun live readback、DM002/DM003 fresh outcome、真实 paper artifact delta、owner receipt、stable typed blocker、human gate、provider running / long-soak 和 publication evidence 不作为本文 active gap 或 next prompt 的默认 worklist；它们只在 paper-progress、publication-ready、domain-ready、artifact mutation、provider running 或 production-ready 声明前回 owner evidence surface 做 fresh gate。
- 2026-07-01 source morphology refresh：`paper_mission_commands.py` 已通过 stage-closure terminalizer case 拆分降到 560 行，`runtime_surface_retirement.py` 已降到 951 行，`paper_recovery_state.py` 已通过 terminal closeout projection 拆分降到 995 行，`paper_mission.py` 已通过 candidate outputs / transaction payload 拆分降到 994 行；后续又把 `existing_projection_refresh.py`、`mission_summary.py` 和 `provider_admission_projection.py` 分别拆到 881 / 843 / 992 行，并把 `provider_admission_current_control.py` 的 transition-request candidate / projection helper 拆到 sibling modules 后降到 989 行；`opl_provider_attempts.py` 也已把 OPL subprocess / provider readiness / candidate task-attempt filtering runtime helper 拆出后降到 981 行。这些入口均回到 1000 行 preferred boundary 内，clean integration worktree 的 `make line-budget` 当前报告 3 条 advisory。当前剩余结构尾项是 repo-wide line-budget advisory、legacy diagnostic consumer / currentness identity/readback helper 的物理退役深度和后续自然边界收薄，不是默认 control-plane authority 缺口或 live paper acceptance。上述拆分都是 behavior-preserving source morphology cleanup，不写 runtime state、owner receipt、typed blocker authority、human gate、paper body、publication eval、controller decision、`current_package`、submission-ready 或 publication-ready。
- 2026-07-06 overengineering source morphology refresh：当前主线重新吸收 PaperMission / medical prose / OPL readback / drive readback / receipt owner consumption / package test source-thinning lanes 后，重点入口行数为 `paper_mission_commands.py` 934、`paper_mission_receipt_owner_consumption.py` 453、`paper_mission_opl_readback.py` 1185、`drive_readback.py` 1456、`quality_repair_batch/medical_prose_story_surface.py` 596；`stage_closure_terminalizer_cases/test_source_selection.py` 与 `submission_minimal_cases/package_core_and_authority.py` 均降为 4 行 thin entrypoint。对应 clear `>1500` 超线缺口已关闭，但 `scripts/line_budget.py` 仍报告 37 条 preferred `>1000` advisory，作为 source-governance backlog 留存。该刷新只覆盖结构收薄与 no-forbidden-write-preserving refactor，不声明 live paper acceptance、owner receipt、typed blocker、human gate、publication-ready、submission-ready、domain-ready 或 production-ready。
- [MAS / OPL Stage Native 状态机设计](./mas-opl-stage-native-state-machine.md) 是 Stage Native 后的下一层顶层设计与当前 foundation landing 记录：保留 stage folder artifact/evidence 面，把 Stage 状态收敛到最小 StageRun Kernel，把 Stage transition authority 收敛到 MAS owner receipt / typed blocker。它不新增 MAS controller system，不替代本文；MAS 主干已落地 StageRun profile / projection / focused synthetic canary foundation，并在 canonical path follow-through 中把 stage folder 文件名收敛到 `stage_manifest.json`、`receipts/owner_receipt.json`、`receipts/typed_blocker.json` 等当前口径。2026-06-05 DM002 / DM003 live canary 已通过 refs-only `stage-artifact-materialize` 推进到 terminal `08-publication_package_handoff` owner gate，但不声明 live paper line、publication verdict、submission-ready 或 `current_package` 已完成。
- [Co-Scientist Stage / Route 重构设计与执行规格](../runtime/designs/coscientist_stage_route_restructure.md) 是 Stage-native scientific work system 的 runtime-facing 执行规格和 provenance。2026-06-10 其剩余可学习点已收敛为 current-owner-native JIT affordance，并落到 `agent/` semantic pack、machine-readable contracts、progress-first owner ticket projection、focused tests、runtime docs 和本文：`next-delta tournament`、`bounded micro-candidate generation`、`critique-as-repair-hint`、`reusable lesson extraction`、`triggered meta-review`、`opportunistic knowledge prefetch` 默认不运行，只在 `current_delta_declares_or_implies_affordance_need` 时即时调用；它不替代本文的 single Active Truth owner，也不表示真实 paper-line 已关闭、production-ready 或任何 authority 已转移。
- [MAS / OPL Agent OS 目标运行架构与重构计划](../runtime/designs/mas_opl_agent_os_target_operating_architecture.md) 是当前顶层重构 target plan。它把目标态固定为 `OPL Agent OS + MAS Declarative Medical Research Pack + MAS Minimal Authority Kernel + Scientific Capability Registry`，并把下一层改造拆成 Pack Compiler、OPL StageRun durable execution、canonical `NextActionEnvelope` 默认 next-action authority、`current_owner_delta` owner projection、MAS authority function 收薄、evidence lineage、AI-first Quality OS、Agent Tool Arsenal / capability registry、workbench UX 和 production soak lane。本文维护这些 lane 的当前差距、顺序、完成门和证据尾项；该 target plan 不声明 live study truth、paper closure、publication-ready、artifact mutation authorization 或 production-ready。
- [MAS / OPL Paper Autonomy Supervisor 目标设计](../runtime/designs/paper_autonomy_supervisor_target.md) 与 `contracts/paper_autonomy_supervisor_contract.json` 是自动论文推进的 diagnostic / supervisor support 入口，不是默认 next-action authority。当前默认入口是 `StageOutcome -> NextActionEnvelope`；Supervisor 只能把同 identity 的 transition receipt、owner receipt、typed blocker、human gate 或 route-back evidence 解释成 consume / wait / stable blocker / owner receipt 等决策候选。Stage-route reconcile、domain diagnostic、study_progress 和 owner-gate materializer 都是 OPL transition runtime 事件与 MAS policy result 的 projection consumer，不再各自形成平行控制面；MAS 不生成 human-gate resume token。该合同已由 `tests/test_paper_autonomy_supervisor_contract.py` 覆盖；DM002/DM003 当前 domain diagnostic dry-run 形状已由 `tests/test_paper_autonomy_supervisor_live_shape.py` 固定为每篇 exactly one `paper_recovery_state.supervisor_decision`，其中 `provider_admission_pending_count=0` / `action_queue=[]` 只能作为 forbidden interpretation。该 synthetic fixture 不声明 live paper progress、publication-ready、OPL production readiness 或 DM002 / DM003 当前 blocker 已解除。
- [Progress-First Evidence Gap Policy 目标设计](../runtime/designs/progress_first_evidence_gap_policy.md) 与 `contracts/evidence-gap-decision-policy.json` / `contracts/evidence-gap-consumption-abi.json` / `contracts/schemas/evidence-gap-decision.schema.json` / `contracts/evidence-gap-decision-examples.json` 是当前 evidence gap decision 的机器合同入口。它把缺口拆成 `authority_gate`、`human_gate`、`proceed_with_assumption`、`soft_quality_gap`、`observability_backlog`、`evidence_tail` 六类，并固定 typed blocker count 只允许 authority gate、human gate、forbidden write boundary 或 stop-loss materialization 进入；soft / observability / evidence tail 只能进入 assumption、follow-up、backlog 或 completion/readiness evidence-tail 账。`EvidenceCondition`、`EvidenceBudget`、`HardGateRegistry`、`SoftGapLedger`、`AssumptionLedger`、`WorkbenchGapView` 已作为 OPL / Workbench / MCP / action-catalog 消费 ABI 落地，stage control plane 和 default executor policy 也统一为先 classify 再 progress-first。该 landing 只声明 repo 功能面、目标设计和合同边界，不声明 live runtime ready、paper progress、publication-ready、provider running 或 production-ready。
- 本文执行原则是功能/结构差距优先：先关闭 repo-source、contract、schema/readback shape、projection demotion、authority=false、no-active-caller/tombstone/rebuild、domain diagnostic / PaperProgressPolicyAdapter / OPL transition ABI 的功能边界。Live apply、owner receipt、stable typed blocker、human gate、provider running / long-soak 和 publication evidence 不在本文维护；它们不反向重开 repo/source/control-plane backlog，也不能被 focused tests、projection clean、queue empty、contract pass 或 docs foldback 替代。
- 本文和 product/read surface 后续若继续出现 `current_executable_owner_action`、provider admission、delivery mirror、queue / attempt 或 StageAttempt，默认只按 diagnostic / transport / mirror / provenance 读取。它们不能作为默认完成判断、current owner/action 入口、provider admission authority、delivery/submission completion、paper progress、publication-ready、submission-ready 或 runtime-ready 证据；完成判断必须回到 `StageOutcome -> NextActionEnvelope` 和同 identity 的 MAS owner consumption / fresh live readiness proof。
- 2026-07-01 NextAction/control-plane 完成度审计当前口径：默认 NextAction authority、缺 envelope fail-closed 行为、synthetic route / false-claim guard、authority-route DM004-style 新路线验收和合同文档口径已作为 `source/control-plane` 完成项落地；existing `progress_projection.next_action` 已是 canonical envelope 时，artifact-first mission summary 不得再用 `paper_mission_readback_missing` / `human.approval` fallback 覆盖它，materializer carrier 与 transition-request projection 也必须保留同一 envelope 的 `action_family`，不再要求旧 exact work-unit registry mapping。historical producer physical deletion 已推进到 `owner_action_diagnostics.repair_progress`、`owner_action_diagnostics.non_advancing_terminal_closeout`、`owner_action_diagnostics.action_types` 和 `owner_action_diagnostics.paper_recovery` 物理删除 / 迁移，`owner_action_diagnostics/` 空 package marker 已删除；provider-admission study-level fallback 和 domain-handler transition-request task producer 已退役；PaperRecovery obligation projection 现在缺 canonical next-action source 时 fail closed，不再从 top-level legacy `current_executable_owner_action` / `current_execution_envelope` 补 owner、action 或 work-unit；product/workbench 默认 wording 已把 OPL transport ref / owner hint 明确标为 diagnostic / observed wording，不再作为默认 next-action authority。`paper_mission_commands.py`、`runtime_surface_retirement.py`、`paper_recovery_state.py`、`paper_mission.py`、`existing_projection_refresh.py`、`mission_summary.py`、`provider_admission_projection.py`、`provider_admission_current_control.py` 和 `opl_provider_attempts.py` 已分别降到 560 / 951 / 995 / 994 / 881 / 843 / 992 / 989 / 981 行；`paper_mission_commands_cases/route_back_budget.py` 也已改为直接依赖 route-back budget 职责模块，不再把旧 CLI 聚合入口当作私有 helper 转发面。剩余 source-governance tail 是更广义的 legacy current-work-unit / current-execution / provider-admission diagnostic consumer、旧聚合测试和 repo-wide line-budget advisory。DM002/DM003 live paper acceptance 仍是 live-acceptance tail。source-governance tail 不反向重开已落地的默认 authority / fail-closed / synthetic guard，也不能写成全仓 `100%` 物理清理；live-acceptance tail 只能由 fresh owner/live surface 关闭，不能由 docs、focused tests、`scripts/verify.sh`、candidate package、refs-only ledger、queue empty、UI wording clean 或 provider completion 替代。
- [OPL Domain Progress Transition Runtime 与 MAS Paper Policy Adapter 目标设计](../runtime/designs/paper_progress_transition_kernel_target.md) 是 currentness / owner-route / owner receipt / provider admission 反复复发后的下一层顶层设计。它把通用 fixed-point controller、durable command/event log、transactional outbox、StageRun identity、projection metadata、replay harness、`NonAdvancingApply` 和 human gate resume 明确上收到 OPL；MAS 只保留 paper-specific policy adapter、authority functions、owner receipt / typed blocker / publication gate / artifact delta / forbidden write 语义。2026-06-16 first slice 已完成方向纠偏：OPL `DomainProgressTransitionRuntime` 最小 runtime slice 已落到 OPL `main`，MAS 私有 `paper_progress_transition_kernel` 已退役，provider-admission 候选统一携带 MAS `paper_progress_policy_result` 与 clean `opl_domain_progress_transition_request`；request 不夹带 `projection_metadata`、OPL event/outbox、StageRun identity、read-model generation metadata 或旧 current-control command。OPL intake 再把 request normalize 成 OPL command / event / transactional outbox / StageRun readback，MAS projection metadata 留在 policy result、candidate/read-model projection 和 study progress 派生面。2026-06-18 follow-up 修正：MAS 不再从 OPL `domain_progress_transition_runtime/command_event_log.jsonl` 自行重建 provider-admission authority；MAS public consumer 也不再暴露 command/event/outbox log readback extractor，可信 admission 必须来自 OPL 发布的 `opl_domain_progress_transition_runtime_live_readback` 完整 shape。domain diagnostic apply、owner-route reconcile、provider admission、`paper_recovery_state`、`current_work_unit`、`current_executable_owner_action`、OPL current-control 和 Workbench 后续都应消费 OPL transition event + MAS policy result，而不是各自从 queue、stage index、旧 dispatch、provider count 或 read-model refresh 重算下一步。docs、tests、projection clean、queue empty 和 domain diagnostic dry-run 不得写成 live paper progress。
- 2026-07-10 completion audit v2 foldback：`contracts/paper_progress_transition_runtime_completion_audit.json` 已删除过时的 feature/phase/work-order 逐字段账本，只保留 scoped repo-source/control-plane completion、StageOutcome / owner receipt / typed blocker 权威不变量、OPL ownership、live evidence 开口和现存 verification refs。repo-source 物理退役可由 active caller migration、no-forbidden-write、focused/meta/default verification 与当前 inventory 关闭；synthetic fixture、docs、contract、queue empty、projection clean、source-adapter manifest 或跨 identity readback仍不能替代 runtime/paper evidence。
- 2026-07-10 blueprint L0-L7 functional acceptance foldback：`contracts/paper_progress_transition_runtime_completion_audit.json#/blueprint_l0_l7_functional_acceptance` 只把 `repo/source/control-plane` 切片标为 `done` / `100%`；`#/overall` 仍为 `partial`，`#/live_runtime_readiness` 为 `evidence_required`。剩余 live-only evidence 固定为 same-identity OPL command/event/outbox/StageRun、provider admission/running、domain diagnostic exactly-one、fresh DM002/DM003 owner/paper-line outcome、OPL StateIndex owner readback和 observability/lifecycle/storage replacement readback；这些缺口不反向打开已验证的 repo-source retirement，也不能被写成 runtime-ready、publication-ready 或 production-ready。
- 2026-06-30 completion/readback tail 更新：`contracts/paper_progress_transition_runtime_completion_audit.json#/source_of_truth` 当前把 MAS repo evidence 刷到 `37be1f4a88b271fddb78dcd3e43c65535fdaa1ea`，并单列 `10d17340d4d374d7eae56b302c09a8ad2ee12b78` 作为 study-progress receipt consumption mainline evidence。legacy next-action authority retirement tail 已由 `37be1f4a88b271fddb78dcd3e43c65535fdaa1ea` 吸收到 main，避免旧 read-model / legacy authority 默认复活。该更新只补齐 repo/control-plane readback 与 completion audit 证据，不吸收当前另行存在的 `current_action_selection/current_work_unit_action` producer-retirement dirty worktree，也不声明 live acceptance。live acceptance 仍需同一 transition identity 的 OPL/Temporal StageRun live readback、domain diagnostic apply exactly-one、provider running/admission proof、MAS owner receipt、stable typed blocker、human gate、route-back evidence或 paper/gate/artifact semantic delta。
- 2026-06-18 provider-admission projection demotion follow-through：`paper_recovery_state` 现在要求完整 OPL transition live readback，才允许同 identity provider-admission candidate 压制 stale parked/operator projection；request-only `current_work_unit` 布尔 pending、MAS transition request 或 request-only candidate 都只能进入 projection repair / transition-request waiting 语义。该切片推进 `provider_admission_event_consumption` 和 projection demotion，但不关闭 live domain diagnostic apply、OPL outbox / StageRun readback、DM002 / DM003 live outcome 或 physical retirement gate。
- 2026-06-19 runtime storage maintenance foldback：`runtime_storage_maintenance` 已从 MAS-local physical maintenance shell 降为 OPL-authorized storage maintenance callable adapter。`workspace-storage-audit --apply`、`maintain-runtime-storage` 与 direct quest physical maintenance 必须消费绑定 operation、maintenance surface、workspace/quest scope、authorized outcome 和 authorization ref 的 `opl_runtime_storage_maintenance_authorization` proof；缺 proof 或 scope mismatch fail closed 为 `opl_runtime_storage_maintenance_authorization_required` typed blocker，不进入 cache/git cleanup、backend maintenance、oversized JSONL slimming、restore-proof compaction、retention apply 或 semantic raw migration。dry-run、source-retained restore-proof canary、refs-only index-only 与 planned retention/capsule projection 仍可无授权运行，但只是不写 body 的 plan/projection/canary，不代表 OPL live takeover、runtime currentness、paper progress、publication readiness 或物理退役完成。
- 2026-06-19 private runtime residue no-authority guard foldback：`src/med_autoscience/runtime_protocol/runtime_surface_retirement.py` 现在从 `contracts/runtime/mas-runtime-surface-retirement-inventory.json` 生成 `mas_runtime_surface_retirement_no_authority_audit`，统一证明所有未物理退役 surface 只能是 refs-only source adapter、consume-only readback projection、read-only projection 或 OPL-authorized callable/maintenance adapter；active caller、read-only projection、maintenance apply gate、focused tests 或 inventory 字段更新都不能单独保留 MAS private runtime authority，也不能关闭完成门。该 guard 只关闭 repo-side authority 回归口，不关闭 live OPL takeover、live every-active-caller soak、no-active-caller physical delete、owner retirement decision、DM002/DM003 live paper-line outcome 或 domain diagnostic apply exactly-one 证据尾项。
- 2026-06-19 repo-source physical retirement first foldback：MAS 私有控制面退役现在分成两栏验收。`repo-source retirement` 可以先完成：只要 active callsites 已迁移或删除，当前功能由 OPL primitive 或 MAS 最小 authority adapter / fail-closed typed blocker 承接，旧 module / alias / wrapper / compat shim 已物理删除，no-forbidden-write / no-resurrection guard、focused / meta / default verification 和 tombstone/provenance 通过，就不再等待 live runtime evidence 才删除旧面。`live-runtime readiness` 另账：OPL live takeover、provider running、domain diagnostic apply exactly-one、DM002/DM003 paper-line outcome、production readiness、publication-ready 仍必须由 fresh live readback、owner receipt、stable typed blocker、human gate、route-back 或 production soak 证明，不能由 repo-source 删除、docs、contracts 或 focused tests 代替。
- 2026-06-19 owner-dispatch / obligation-actuator retirement guard follow-up：`runtime_surface_retirement_no_authority_audit` 进一步补齐 `stage_outcome_authority` 与 `domain_diagnostic_obligation_actuator` 的专用 fail-closed validator。前者只能是 OPL proof-bound owner callable adapter，缺 trusted OPL execution authorization、exact provider-hosted StageRun、active OPL provider attempt/lease 或 bound OPL transition readback 时只能投影 `opl_execution_authorization_required` blocker；stage-native / inline dispatch / owner-callable list 候选不能自选为 current 或授权执行。后者只能消费 OPL runtime readback、MAS owner answer readback 或带 authority boundary 的 MAS domain authority readback，并要求 success proof + consumed readback identity；MAS supervisor decision engine、RecoveryObligationStore mutation、request projection success、read-model evidence ref success、actuator-private blocker write 和 provider-admission authority 都被 validator 拒绝。该 follow-up 是 repo-side 防复发证据：它支撑 repo-source private-surface retirement，不关闭 live OPL takeover、live every-active-caller soak、owner retirement decision、domain diagnostic apply exactly-one outcome、DM002/DM003 live paper-line outcome 或 production readiness。
- 2026-06-18 current-control closeout identity follow-through：OPL current-control handoff projection 现在要求 identity-bound provider admission / transition request 只能被同一 StageRun / route / idempotency / stage-packet identity 的 terminal closeout 消费；action_type + work_unit + fingerprint 弱匹配不再能清掉 runtime-backed admission。该切片继续推进 `provider_admission_event_consumption` 和 projection demotion，但仍不关闭 live domain diagnostic apply、OPL outbox / StageRun readback、DM002 / DM003 live outcome 或 physical retirement gate。
- 2026-06-18 live-readback consumer hardening follow-through：`opl_transition_readback` 现在要求 StageRun identity 至少携带 `stage_run_id` 或 `stage_run_identity_ref`，并要求 identity、causality、projection metadata 与 `latest_transaction_readback` 的 event / outbox / transaction refs 全部一致；错配 payload 不能变成 provider admission，只能回到 OPL runtime readback required。该切片关闭薄 StageRun / stale transaction ref 被 MAS consumer 信任的复发口，但仍不关闭 live domain diagnostic apply、真实 OPL outbox / StageRun readback、provider running proof、DM002 / DM003 live outcome 或 physical retirement gate。
- 2026-06-18 OPL transition ABI single-source follow-through：`contracts/opl_domain_progress_transition_runtime_contract.json` 与 `src/med_autoscience/controllers/opl_domain_progress_transition_contract.py` 现在是 MAS-facing `DomainProgressTransitionRuntime` request / postcondition / live-readback / projection-demotion ABI 的单一机器入口；`paper_progress_policy_adapter`、`opl_transition_readback`、provider-admission boundary 与 materializer transition projection 都消费同一 shape。该切片减少 request/readback/postcondition 常量漂移和各控制面重复硬编码，但仍不关闭 live domain diagnostic apply、OPL outbox / StageRun readback、provider running proof、DM002 / DM003 live outcome 或 physical retirement gate。
- 2026-06-18 OPL live-readback transaction-consistency ABI follow-through：`opl_domain_progress_transition_contract.py` 现在把 `latest_event_id` / `latest_outbox_item_id` / `latest_transaction_id`、command/event/outbox presence flags、causality refs、latest transaction refs 和 transaction consistency 布尔规则统一暴露给 JSON contract、readback validator 与 tests；`required_opl_transition_readback_shape()` 也携带这些字段族。该切片关闭 validator 与 machine contract 对 transaction consistency 要求漂移的复发口，但仍不关闭真实 OPL outbox / StageRun readback、domain diagnostic apply exactly-one outcome、provider running proof 或 DM002 / DM003 live paper-line outcome。
- 2026-06-19 provider-admission identity-bound readback follow-through：`opl_transition_readback` 新增 provider-admission 专用 live-readback consumer，完整 OPL `DomainProgressTransitionRuntime` readback 还必须匹配当前 transition 的 `study_id`、`work_unit_id`、`work_unit_fingerprint`、`route_identity_key`、`attempt_idempotency_key` 和 request idempotency key，才能把 `provider_admission_pending` 置真；`provider_admission_current_control_identity`、`paper_recovery_state` 和 `current_work_unit` 统一消费该 helper。缺 identity、错 work unit、错 route / attempt 或错 request key 的 readback 即使交易形状完整，也只能保持 request-pending / readback-required 语义。该切片关闭 cross-identity / stale readback 污染 provider admission 的复发口，但仍不关闭 live domain diagnostic apply、真实 OPL outbox / StageRun readback、provider running proof、DM002 / DM003 live outcome 或 physical retirement gate。
- 2026-06-19 owner-dispatch readback ABI follow-through：`stage_outcome_authority` 不再保留私有 weak readback binding；它与 provider admission 统一消费 `opl_domain_progress_transition_contract.py` 定义的 `DomainProgressTransitionRuntime` identity binding。缺显式 `route_identity_key`、`attempt_idempotency_key` 或 request idempotency key 的 readback 不能授权 owner callable dispatch，只能 fail closed 为 `opl_execution_authorization_required`。该切片继续关闭 MAS private dispatch residue 的 authority 复发口；live every-active-caller soak、domain diagnostic apply、OPL outbox / StageRun readback、DM002 / DM003 live outcome 仍是 live-runtime readiness 尾项，而不是 repo-source 删除前置。
- 2026-06-19 materializer projection physical-retirement gate follow-through：`domain_action_request_materializer_owner_callable_adapter_projection`、`domain_action_request_materializer_request_tasks_projection` 和 `domain_action_request_materializer_canonical_transition_request_body_projection` 现在统一带 `repo-source physical retirement first` 边界：legacy projection body / alias / local persistence API 已物理退役，active callsites 只能消费 canonical `domain_progress_transition_requests` 或 OPL readback，缺 proof 时 fail closed。`opl_materializer_projection_tail_readback` 继续作为 live-runtime readiness tail；projection demotion、`request_tasks` alias retired、refs-only transition projection、repo no-authority guard 和 focused tests 可以证明 repo-source 不再有 MAS 私有 outbox / command / event / StageRun / provider-admission 面，但不能写成 OPL live takeover、provider running、domain diagnostic apply success 或 DM002 / DM003 live outcome。
- 2026-06-18 Workbench/Portal next-action demotion follow-through：Progress Portal 顶层 `study.next_system_action`、study workbench overview、runtime workbench `user_next` / `next_action_summary`、workspace dashboard focus 与 materialized workspace-row fallback 现在统一消费 canonical `NextActionEnvelope` 派生的 owner summary；`current_owner_delta` 只作为 refs-only projection / drilldown，不作为默认 next-action authority。旧 `user_visible` / cockpit / workspace row 的 `next_system_action` 与 operator focus 只保留在 `diagnostic_legacy_projection_input`，显式 `can_generate_action=false`、`must_not_be_used_as_next_action_authority=true`。该切片关闭 Workbench/Portal 默认读面重新解释 next action 的复发口，但不关闭 OPL Workbench Shell live event-driven rebuild proof、domain diagnostic apply、OPL outbox / StageRun readback 或 DM002 / DM003 live outcome。
- 2026-06-16 顶层重设计把 MAS / OPL 理想运行模型进一步固定为 `DomainIntent -> OPL Command -> OPL Event -> OPL Transactional Outbox -> StageRun / ToolInvocation -> MAS OwnerAnswer -> Derived Projection`。这不是新增 MAS 私有控制面，也不是重开已关闭的 functional privatization gap；它是下一层 OPL 基座优化和迁移门：新增能力必须先归位到 OPL primitive、MAS authority function、MAS declarative pack、Scientific Capability Registry 或 refs-only audit sidecar。任何 MAS-local helper 若承担 command log、event log、outbox、fixed-point runtime、attempt lifecycle、worker residency、state index body store、generic lifecycle、workbench shell、tool selector 或默认 preflight，必须作为 migration input / projection / diagnostic / tombstone 处理，不得写成长期 retained surface。
- [MAS / OPL 进度运行时理想蓝图](../runtime/designs/mas_opl_progress_runtime_ideal_blueprint.md) 是两轮失败/修复调研后的顶层收敛文档：它把旧 MAS+MDS 的短闭环优势、OPL-based MAS 的 authority-path split 根因、外部成熟工程模式和 OPL 基座 primitive 统一为一条 durable transition spine。本文继续维护执行顺序和证据缺口；该蓝图定义理想态、禁止误读、OPL 基座优化要求和“一劳永逸”的架构完成门。
- [EvoScientist Progress-First Intake](../runtime/designs/evo_scientist_progress_first_intake.md) 记录 EvoScientist / EvoSkills 的 upstream learning foldback、完整目标态 sidecar execution architecture 与 repo 可调用执行面。EvoScientist v0.1.4 的 auxiliary model for background memory workers / tool selector 与 fire-and-forget observation memory，以及 EvoSkills v1.0.0 的 IDE / IVE / ESE research lifecycle memory，只作为 `ordinary progress spine` 的 async learning sidecar / auxiliary helper / fail-open tool selector / failed-path taxonomy / routing eval / stop-loss candidate；当前已落地 refs-only writer、CLI observe/read-latest、`study_progress` materialize hook 和 refs-only index family。它们不能阻断 ordinary progress spine，不能持有 MAS authority，不能替代 independent reviewer/auditor。`remaining_learning_plan=false` 是稳定机器口径，后续只允许按合同做生产 scheduler 或真实 evidence scaleout。
- [当前状态](../status.md) 只维护 current-state 摘要；dated closeout、receipt id、OPL worklist 数字、命令流水、旧 phase checklist、same-day follow-through 和 proof 过程进入 `docs/history/**`、runtime ledger、真实 workspace receipt 或提交历史。
- 差距按目标态判断。当前实现可运行只能作为迁移输入、风险和证据来源，不能反向定义 MAS 长期架构。

## 当前唯一真相

| 主题 | 当前结论 |
| --- | --- |
| MAS 身份 | MAS 是医学研究 domain agent，也是 OPL-compatible package。direct MAS app skill path 与 OPL-hosted path 必须回到同一套 MAS-owned stage、controller、durable truth、quality verdict、artifact authority、memory decision、owner receipt 和 typed blocker。 |
| 默认运行 | Hosted autonomous runtime 默认由 OPL/Temporal 承担；OPL 持有 stage attempt、queue、wakeup、retry/dead-letter、resume、human-gate transport、provider query、worker residency 和 operator projection。`Codex CLI` 是 stage 内第一公民 executor；Codex App 只承担 direct entry / 人机操作面，不作为任务启动后的外围持续 driver。 |
| MAS authority | MAS 持有 study truth、stage semantics、AI reviewer / auditor quality gate、publication route、domain transition table、publication-route memory body 与 accept/reject/blocker decision、artifact/package authority、source readiness、owner receipt、safe action refs 和 typed blocker。 |
| OPL authority | OPL 持有通用 scheduler、queue、attempt ledger、generic transition runner、lifecycle/index、memory/artifact locator、restore/retention shell、observability、repair projection、generated CLI/MCP/Skill/product-entry/domain-handler/status/workbench wrapper 和 App/workbench shell。 |
| MDS / DeepScientist / EvoScientist / EvoSkills | 只作为 source provenance、historical fixture、explicit archive import、backend audit、upstream learning、smoothness learning、progress accelerator / audit sidecar 和 parity oracle reference。可吸收经验是单循环、少默认门、持续产出、UI 体感、background memory worker、tool selector、observation memory 和 research lifecycle skill taxonomy；它们不回到 MAS 默认 backend、quality owner、artifact authority、memory authority 或 runtime owner。 |
| 当前机器面 | `agent/` 是 canonical medical research semantic pack；`contracts/foundry_agent_series.json`、`contracts/pack_compiler_input.json`、`contracts/stage_control_plane.json`、`contracts/action_catalog.json`、`contracts/generated_surface_handoff.json`、`contracts/functional_privatization_audit.json` 和 `contracts/production_acceptance/mas-production-acceptance.json` 是主要 machine-readable truth。 |
| 控制面收薄 | 稳定 execution contract 收敛为 `macro_state + owner_route + receipt_or_blocker + evidence_refs`。长 runtime/status reason、supersession reason、publication supervisor phase 和 operator/workbench 文案只做 diagnostic/read-model detail，不再成为跨入口执行授权。 |
| SQLite / State Index Kernel | OPL StateIndex Kernel 是 generic index 与 SQLite sidecar owner；MAS 只保留 body-free `opl_state_index_source_adapter`，发出 refs、locator、checksum、source fingerprint、receipt/blocker/restore 等 metadata，不保留 rows projection、本地 persistence、inspection 或 replay。study truth、publication eval、controller decisions、manuscript、package、evidence/review ledger、memory、artifact body、quality verdict 和 owner receipt authority 继续归 MAS 文件/receipt truth。 |
| StageRun Kernel foundation | Stage Native 目录产出思路保留为 artifact/evidence plane；MAS 主干已新增 `contracts/stage_run_kernel_profile.json`、`contracts/stage_run_canary_evidence.json`、`src/med_autoscience/controllers/stage_run_kernel.py` 和 `study_progress` StageRun projection binding，并把 materializer / index 收敛到 canonical `stage_manifest.json`、`receipts/owner_receipt.json`、`receipts/typed_blocker.json`、`inputs/consumed_artifact_refs.json`、`lineage/prov.json`、`projection/current_owner_delta.json` 路径，用最小 StageRun 状态壳表达 stage folder / manifest / receipt / blocker 派生状态；focused AI reviewer canary tests 已覆盖 owner receipt、typed blocker 与 provider terminal 不等于 domain accepted。2026-06-06 MAS terminal publication handoff apply 侧补齐 OPL execution authorization / closeout binding 消费：缺 OPL provider attempt / lease / execution authorization 时 fail closed 且不改 MAS owner receipt / typed blocker；有授权时只写 stage-native `handoff_owner_receipt.json` 或 `receipts/typed_blocker.json`，并把 refs-only `closeout_binding`、`current.json`、`latest_owner_answer_ref`、`hard_gate.owner_answer_*` 和 `delta_id` 绑定到 StageRun / manifest / current pointer / source fingerprint / idempotency key。MAS 只用 `OwnerReceipt` / `TypedBlocker` 关闭 Stage 和授权 route；`latest.json`、progress projection、Portal/workbench 只能是 receipt-derived read model。StageRun canary 还要求 candidate / grounded reflection / comparative selection / evolution and revision / meta-review / independent quality gate 只作为 Stage 内 advisory refs-only evidence，并验证旧 scheduler、runner、session store、status shell、workbench wrapper 没有重新获得 transition authority；controlled canary operator summary、overclaim boundary 和 legacy runtime residue guard 已作为 operator read-model-only contract/test 固定为 `platform_repair_controlled_fixture`，不授权 transition、closeout、quality/export 或 live resume；`contracts/stage_run_canary_evidence.json` 的 scope 是 `controlled_fixture_not_live_domain_progress`，不声明 DM002 / DM003 live domain progress。 |
| Ordinary progress handoff | `contracts/stage_run_kernel_profile.json#/ordinary_progress_handoff`、`contracts/stage_artifact_kernel_adoption.json#/stage_run_kernel_profile_binding` 与 `agent/stages/stage_route_contract.yaml#/ordinary_progress_handoff_policy` 已把普通推进主干机器化：默认 next-action root 是 canonical `StageOutcome -> NextActionEnvelope`，`current_owner_delta` / `stage_run_current_owner_delta` 只作 owner projection；executor 普通 step 可返回 `ProgressDeltaReceipt`，其 tier 固定为 `T0_progress_delta`，只记录 changed / produced / consumed refs、progress classification、next owner 和 next required delta。 | 该合同只解决“完成一个 concrete delta 后如何继续接力”，不授权 domain ready、publication-ready、submission-ready、quality/export ready、artifact mutation、memory accept/reject、production ready 或 physical delete；完整 Stage transition、delivery artifact 和 production evidence 继续分别走 `OwnerReceipt` / `TypedBlocker`、T2 delivery artifact、T3 production evidence 与 terminal gates。 |
| Paper Autonomy Supervisor | `contracts/paper_autonomy_supervisor_contract.json`、[Paper Autonomy Supervisor 目标设计](../runtime/designs/paper_autonomy_supervisor_target.md)、`tests/test_paper_autonomy_supervisor_contract.py`、`tests/test_paper_autonomy_supervisor.py`、`tests/test_paper_autonomy_supervisor_live_shape.py` 和 owner-gate materializer focused tests 已固定六类 supervisor decision、obligation identity、OPL-owned durable resume token consumption、recovery action owner、paper/platform/migration/observability 分账、read-model false authority、OPL substrate / MAS authority split，以及 DM002/DM003 zero-provider-admission / empty-action-queue live shape。当前状态是 `supervisor_decision_core_and_live_shape_canary_landed_runtime_tail_open`：Lane 0 机器合同和文档入口已落地；Lane 2 已有 `paper_recovery_state.supervisor_decision`、owner-gate route-back materialization 局部 focused tests，以及 DM002 `medical_paper_readiness_missing` / DM003 `publication_gate_replay_blocked` 映射到 `materialize_recovery_action` 的 focused canary。2026-06-18 obligation actuator follow-through 进一步固定 domain diagnostic apply consume-only outcome source：success 只能来自 OPL runtime readback、MAS owner-answer readback 或 MAS domain authority readback；MAS transition request projection 只能是 `transition_request_pending` 且 postcondition fail closed；不在当前 SupervisorDecision 允许集合内的 outcome 也必须 fail closed。剩余完成门不是 contract/projection landed，而是 fresh `supervisor_decision` readout 成为默认运行读根、derived surfaces consume-only、OPL substrate readback 可证明同 obligation 的 decision store / human-gate / StageRun identity、DM002/DM003 live-shape canary 持续通过，以及真实 paper-line supervisor tick 产生 running / terminal / consume closeout / next owner delta 的 fresh evidence。 |
| OPL Domain Progress Transition Runtime / MAS Paper Policy Adapter | `replay_expanded_and_projection_demotion_tail_landed_live_paper_outcome_open` | [OPL Domain Progress Transition Runtime 与 MAS Paper Policy Adapter 目标设计](../runtime/designs/paper_progress_transition_kernel_target.md) 已固定下一层重构目标：通用 transition runtime 属于 OPL，负责 typed transition、fixed-point reconciler、event log、outbox、replay、projection metadata 和 `NonAdvancingApply`；MAS 只提供 paper-specific policy adapter 和 authority result shapes。2026-06-16 已落地 OPL runtime first slice 与 MAS provider-admission adapter consumption：MAS 候选统一消费 `paper_progress_policy_result` 并发出 clean `opl_domain_progress_transition_request`，不再生成 `current_control_command_outbox_record`，也不把 projection metadata、OPL event/outbox、StageRun identity 或 read-model generation metadata 放进 request；OPL intake 负责生成 OPL-owned command / event / transactional outbox / StageRun readback，且 MAS policy 明确不持有 provider admission、fixed-point、event log 或 outbox authority。domain diagnostic `obligation_actuator` 同步降级为 MAS policy / owner-answer readback 与 fail-closed typed blocker 投影；`provider_admission_pending` 在 MAS 侧必须有 OPL live readback，只有 MAS request 时写 `NonAdvancingApply` typed blocker。PAS human gate 只消费 OPL resume token，`paper_recovery_execution_refresh` 只做 single-pass projection normalization 并标记 `authority=false` / `fixed_point_runtime_owner=one-person-lab`。2026-06-17 replay suite 扩展到 owner receipt recorded 后不推进、same-tick stale blocker / admission 冲突、`provider_admission_pending_count=0` 禁止解释、`current_work_unit` / `paper_recovery_state` 分歧、domain diagnostic/request 无 OPL readback -> `NonAdvancingApply`、human gate / route-back accepted shape，并由 `contracts/paper_progress_replay_live_evidence_status.json` 固定只读 live evidence exactly-one acceptance。2026-06-17 follow-through 进一步补齐 `projection_metadata.lag_status` 物化、request-only consume-only provider-admission split、DM003 exact StageRun / owner receipt handoff preservation，以及 OPL live readback consumer；provider admission projection 只接受同 identity 的 OPL live readback，MAS 不再从 command/event/outbox log 派生 trusted readback，domain diagnostic runtime report 也只从 fresh `progress_currentness` 合入带可信 OPL live readback 的 admission candidate，避免 study_progress 与 domain diagnostic 顶层读面再次漂移，并清理最终 projection 中 stale supervisor blocker residue。2026-06-18 再补 request/readback 互斥投影：同一 identity 一旦有 OPL live readback，就只计入 `provider_admission_pending`，不再同时占用 `transition_request_pending`；request-only candidate 才保留在 transition request projection。2026-06-18 follow-up 将 provider-admission arbiter 的 authority boundary 固定为 live-readback-only：event/outbox fragment 不是 provider admission authority，缺 live readback 的 request 必须显式投影 `transition_request_waits_for_opl_runtime` / `non_advancing_apply_required`。2026-06-18 replay follow-up 补齐 `closeout_stagerun_identity_split`：历史 closeout 中 stale StageRun packet 与 fresh current-control identity 分裂时，`current_work_unit` 必须稳定成同 current identity 的 typed blocker，并由 MAS policy adapter 只发 `RecordTypedBlocker` request。2026-06-18 paper recovery follow-up 进一步把 `paper_recovery_state` 的 provider admission currentness 读法收成 live-readback-backed：同 identity 但只有 MAS request / read-model candidate 时不能抑制 projection contradiction；只有 OPL `opl_domain_progress_transition_runtime_live_readback` 完整 shape 才能作为 provider admission currentness evidence。2026-06-18 live-readback consumer hardening 继续要求 StageRun identity 带 `stage_run_id` 或 `stage_run_identity_ref`，并要求 identity / causality / projection metadata / latest transaction refs 完全一致，错配 payload 只能 fail closed。 | 当前完成的是 OPL first slice + MAS adapter consumption + MAS projection demotion tail + representative replay expansion + closeout/StageRun identity split replay + request-only/readback identity hardening + live-readback-only consumer + pending 计数互斥投影 + arbiter live-readback authority boundary + paper_recovery_state live-readback-backed provider admission currentness + live readback transaction consistency fail-closed，不是 live paper progress。剩余完成门是 domain diagnostic apply exactly-one live outcome、OPL outbox/StageRun identity live readback、provider admission arbiter 完全消费 OPL transition event，以及 DM002/DM003 fresh live paper-line outcome。不得把本文、合同、局部测试、projection clean、queue empty、`provider_admission_pending_count=0` 或 domain diagnostic dry-run 写成能力彻底完成。 |
| Owner-callable adapter / legacy dispatch carrier | `canonical_owner_callable_readback_landed_legacy_carrier_tail_open` | 2026-06-17 将 MAS active materializer、domain diagnostic preview/same-tick、dispatcher consumer 和 paper-recovery export 从直接读取 `owner_callable_adapters` 收敛到 `owner_callable_adapters` canonical helper；旧 `owner_callable_dispatch*` readback 字段不再导出；`owner_callable_adapters/` 路径仅作为 persisted carrier path、lineage refs 和 residue cleanup provenance。`stage-outcome-authority` contract guard 现在 fail closed 拒绝 `mas_dispatch_authority=true`、`mas_creates_opl_outbox/event/stage_run=true` 或 `target_runtime_owner != one-person-lab` 的 payload；无 OPL proof 的 paper-recovery successor 不再测试为 `handoff_ready`，而是 `opl_execution_authorization_required` typed blocker。2026-06-17 追加将 `owner_callable_adapter_receipt/latest.json` 收成 legacy wire path：新写 payload 进入 `artifacts/supervision/consumer/owner_callable_adapter_receipts/latest.json`，以 `surface=owner_callable_adapter_receipt_study_latest` 为 canonical surface，并携带 `legacy_surface_alias=owner_callable_dispatch_execution_study_latest`、`owner_callable_receipt_projection=true`、`projection_authority=false`、`execution_ledger_authority=false`、`attempt_lifecycle_authority=false` 和 `queue_authority=false`；receipt-consumption helper canonical-first 读取，并将旧 legacy wire 规范化为 canonical receipt surface 后再消费，StageRun identity exception allowlist 也接入 `owner_callable_adapter_receipt`。2026-06-17 再追加：`mas_domain_progress_transition_request_projection` 不再被 dispatcher 当成未知 `unsupported_dispatch_surface`；它只有携带可信 OPL execution authorization / exact StageRun packet / runtime readback proof 时才作为 `opl_authorized_owner_callable_adapter` 进入，缺 proof 时统一落 `opl_execution_authorization_required` typed blocker，未知 surface 仍 fail closed。2026-06-17 继续收口：`provider_admission.persisted_provider_admission_candidates`、`export_study_projection._current_provider_handoff_execution` 和 `recovery_actions._latest_clean_migration_rehydrate_execution` 已改为 canonical-first 消费 `owner_callable_adapter_receipts/latest.json`，旧 `owner_callable_adapter_receipt/latest.json` 只在 canonical 缺失时被规范化为 `owner_callable_adapter_receipt` projection，并显式 `projection_authority=false`、`execution_ledger_authority=false`、`attempt_lifecycle_authority=false`、`queue_authority=false`。2026-06-17 再收口：domain-handler export 现在先消费 canonical current-control `opl_domain_progress_transition_request`，只有该 request 缺失时才回落到旧 `owner_callable_adapters` carrier；两者并存时旧 carrier 只作 provenance / fallback，不能抢先产生 MAS-local dispatch task。2026-06-18 再收口：fallback export task 保留 `task_kind=stage_outcome/opl-handoff` 仅作为 OPL StageRun ABI，但显式标记 `carrier_kind=opl_domain_progress_transition_request_carrier`、`transition_request_status=transition_request_pending`、`legacy_carrier_projection=true`、`provider_admission_pending=false`、`provider_attempt_or_lease_required=false`、`mas_private_attempt_loop_forbidden=true`、`mas_dispatch_authority=false`、`mas_creates_opl_outbox/event/stage_run=false`；export->dispatch regression 证明该 task 只能进入 OPL `DomainProgressTransitionRuntime` intake，不会请求 MAS/OPL provider admission。2026-06-18 dispatcher closeout 进一步把 repo-level authorization proof 固定到 contract：所有 `SUPPORTED_ACTION_TYPES` 的 transition request projection 在缺 OPL proof 时必须 fail closed 为 `opl_execution_authorization_required`，trusted OPL execution authorization、bound `DomainProgressTransitionRuntime` readback 和 exact provider-hosted StageRun packet 是唯一可通过的 owner-callable execution authorization families；closeout binding 仍不能授权 execution。2026-06-18 legacy latest wire follow-through：`latest_owner_callable_receipt_payload` 默认只读 canonical `owner_callable_adapter_receipts/latest.json`，`owner_callable_adapter_receipt/latest.json` 必须显式 `allow_legacy_fallback=True` 才能作为历史 replay / provenance 输入；provider admission、provider handoff export 和 clean-migration recovery current readers 在 canonical 缺失时返回无当前 receipt，不再消费 legacy latest。2026-06-18 stage-native selector follow-through：`control/next_action.json` / `ready_for_owner_action` 缺 trusted OPL execution authorization、bound `DomainProgressTransitionRuntime` live readback 或 exact provider-hosted StageRun packet 时，只能是 candidate / diagnostic carrier；默认 selector 不再把它计为 currentness score、不会让它抢占其他 current dispatch，也不会生成 provider admission 或 execution authorization；显式请求同 action 只能得到 `opl_execution_authorization_required` typed blocker。2026-06-18 owner-route basis follow-through：同一缺 proof stage-native candidate 的 explicit-action execution payload 也不得继续使用 `stage_native_workspace_next_action` authority-looking basis，必须投影为 `stage_native_workspace_next_action_blocker_projection`；只有带可信 OPL proof 的 stage-native owner callable adapter 才保留 `stage_native_workspace_next_action` basis。2026-06-19 owner-callable legacy body reader follow-through：`owner_callable_adapter_projection.owner_callable_adapters()` 已退役为 active body reader，不再把 top-level legacy list 返回给 controller / domain diagnostic / reconciler / provider-admission path；迁移期 legacy count / status 只能经 `legacy_owner_callable_adapter_refs()` / `legacy_owner_callable_adapter_diagnostics` 读取 body-free identity refs，canonical 当前 carrier 继续是 `domain_progress_transition_requests`。 | 这关闭的是 MAS active readback / contract guard / legacy latest current-reader fallback / legacy export carrier authority / stage-native next-action selector authority / stage-native owner-route basis authority 复发口，并铺好 owner-callable receipt projection 读写语义。repo-source 退役按 active caller migrated/deleted、replacement parity、no-forbidden-write、tombstone/provenance 和 no resurrection guard 关闭；`owner_callable_stage_tasks`、dispatch evidence export、residue cleanup、历史 fixtures 和显式 opt-in legacy replay fallback 只可作为 OPL transitional carrier/provenance，不是 MAS 私有控制面。dispatcher live every-active-caller soak、no-active production caller proof 或 OPL readback 继续作为 live-runtime readiness evidence tail，不再阻塞旧 carrier / alias / wrapper / compat shim 删除。 |
| RuntimeHealthKernel / runtime liveness diagnostic | `read_only_diagnostic_publisher_landed_live_runtime_tail_open` | 2026-06-17 起，`runtime_health_kernel` 的 lifecycle-looking event 必须带 OPL lifecycle proof；`launch_attempt`、`recover_attempt`、`relaunch_attempt`、`attempt_released`、`escalation_opened` 和 `escalation_resolved` 缺 proof 时 fail closed。带 proof 的 event 只作为 `mas_runtime_health_event_role=diagnostic_observation` 写入，并显式 `lifecycle_authority_owner=one-person-lab`、`attempt_lifecycle_authority=false`、`retry_or_dead_letter_authority=false`、`worker_residency_authority=false`。2026-06-18 follow-through：`reconcile_runtime_health_snapshot_from_status_payload` 已收成 read-only diagnostic publisher，只物化 `latest.json` projection，不再追加 MAS-local runtime health event log；snapshot 带 `projection_metadata.authority=false`、`derived_from_event_id`、`observed_generation`、`lag_status`，`allowed_controller_actions` 只保留 `read_runtime_status` / `open_monitoring_entry`，runtime action / retry budget / provider readiness / current executable owner action 全部标为 diagnostic hint 或 false authority。2026-06-19 follow-through：`contracts/runtime/mas-runtime-surface-retirement-inventory.json#/surfaces/runtime_health_kernel` 增加 `active_caller_boundary`、`diagnostic_projection_boundary` 和 `retirement_gate`，并由 `runtime_surface_retirement` validator fail closed 拒绝 event-log append、runtime health epoch currentness、canonical runtime action next-action、attempt liveness / retry / worker residency 非 OPL owner、provider admission 或 paper progress authority 回归。2026-06-19 identity follow-through：`runtime_health_decision_gate` 增加 `diagnostic_consumer_gate_boundary`，runtime-health recovery hint 只有在 OPL current-control / StageRun / terminal log / runtime liveness readback 与当前 recovery identity 相同时才可改写为 OPL owner-route wait；裸 `opl_*_ref`、顶层 read-model claim 和跨 identity readback 均 fail closed。 | 这关闭的是 MAS 本地 attempt/retry/epoch 私有面、runtime-health next-action 复发口，以及 consumer 侧“任意 OPL ref 即可授权恢复”的复发口，不是 live runtime currentness 或 paper progress。OPL Observability / StageRun / Route Reconciler 仍是 lifecycle owner；剩余完成门是 OPL lifecycle readback 在真实 domain diagnostic/owner-route apply 中给出同 current identity 的 exactly-one outcome、running proof、owner receipt、stable typed blocker、human gate 或 route-back evidence；inventory / validator / focused tests 不能写成 live runtime ready。 |
| Ideal operating chain / OPL primitive ABI | `target_design_landed_execution_tail_open` | [MAS / OPL Agent OS 目标运行架构与重构计划](../runtime/designs/mas_opl_agent_os_target_operating_architecture.md) 已把成熟工程经验转译为七个平面和一条可审计链：Intent / Policy、Command / Event、Transactional Outbox、Execution、Authority Answer、Projection / Workbench、Capability / Tool。OPL primitive 的目标字段族固定为 identity、causality、authority boundary、exactly-one outcome 和 projection metadata；缺任一字段族只能进入 drilldown / diagnostic。 | 该设计只关闭 top-level target clarity，不声明 OPL primitive 全部可运行、MAS 私有 residue 已物理清零、live paper line 已前进或 production-ready。下一层必须把 `DomainProgressTransitionRuntime`、Transactional Outbox、RecoveryObligationStore、HumanGateTransport、StateIndexKernel、Tool Arsenal / Capability Registry 和 Workbench Shell 分别推进到 contract、fixtures、generated/hosted consumption、live readback 或 owner blocker。 |
| MAS private progress spine fitness gate | `machine_guard_landed_physical_retirement_tail_open` | 2026-06-17 起，`module_boundary_audit` 与 `architecture_owner_boundary` 把 OPL progress spine 固定为 machine-readable owner boundary：`command_log`、`event_log`、`transactional_outbox`、`fixed_point_reconciler`、provider admission / attempt、StageRun / ToolInvocation / HumanGateTransport、RecoveryObligationStore、StateIndexKernel、Workbench Shell、Tool Arsenal / Capability Runtime 和 Observability transport 均归 `one-person-lab`。validation 会 fail closed 拒绝任何 MAS group / layer 声称这些 authority 或打开 private command/event/outbox/reconcile/attempt/state/workbench/tool-selector capability flag；MAS 只允许声明 policy adapter、owner callable adapter、authority result validator、body-free diagnostic projection、derived projection、domain authority function 或 tombstone/provenance role。 | 这关闭的是“新增/保留 MAS 私有进度控制面还能通过结构 gate”的复发口。repo-source 旧模块 / alias / wrapper / compat shim 退役按 active caller migrated/deleted、replacement parity、no-forbidden-write、tombstone/provenance 和 focused/meta/default verification 处理；`domain_action_request_materializer`、`stage_outcome_authority`、`paper_autonomy_supervisor` / `obligation_actuator`、state index / workbench / tool sidecar 的 live owner / stable blocker / OPL readback 证据另列为 runtime readiness 或 production readiness，不作为源码删除前置。 |

2026-06-17 export priority follow-through：`domain-handler export` 的 pending family task 现在先导出 current-control 派生的 canonical `mas_domain_progress_transition_request`，只有没有该 canonical request 时才回退 legacy `owner_callable_dispatch` carrier。该修复关闭的是 legacy carrier 抢在 OPL DomainProgressTransitionRuntime request 前面的 read-model priority 复发口；它仍不声明 provider running proof、domain diagnostic apply success、DM002/DM003 live paper progress、publication-ready、domain-ready 或 production-ready。

2026-06-19 legacy latest wire dispatch follow-through：`stage_outcome_authority.execution_io.execution_latest_payload`、`latest_owner_callable_receipt_payload`、`owner_callable_receipt_candidates`、`owner_callable_receipt_consumption` 和 `owner_callable_receipt_nonconsumable_closeout` 默认只读取 canonical `owner_callable_adapter_receipts/latest.json`；`owner_callable_adapter_receipt/latest.json` 只能在显式 `allow_legacy_fallback=True` 时作为 history replay / provenance merge 输入。`_persist_study_executions` 保留该显式 opt-in 只为把旧 ledger 合并进新的 non-authority owner-callable receipt projection；任何 current reader、provider admission、handoff export、clean-migration recovery、owner-route selection、current receipt consumption 或 progress/running claim 都不得把 legacy latest wire 当作 current execution authority。stage closeout packet files under `owner_callable_adapter_receipt/*.closeout.json` 仍可作为 OPL StageRun closeout / provenance evidence 参与 currentness 和 typed-blocker 逻辑，但这不是 legacy latest fallback。该修复关闭的是 domain-owner dispatch helper 与 receipt-consumption helper 默认回落 legacy wire的复发口；repo-source 旧 carrier / latest wire 退役按 canonical receipt replacement、explicit legacy opt-in、tombstone/provenance 和 no-resurrection guard 验收；live every-active-caller soak、domain diagnostic apply、OPL outbox / StageRun readback、DM002 / DM003 live paper-line progress 和 production-ready 另账。

2026-06-19 legacy latest wire current-reader follow-through：`owner_callable_receipt_candidates`、`owner_callable_receipt_consumption` 与 `owner_callable_receipt_nonconsumable_closeout` 默认也不再读取 `owner_callable_adapter_receipt/latest.json`；历史 replay / provenance 必须显式 `allow_legacy_fallback=True`。domain diagnostic 的 legacy execution fallback 即使读到旧 wire，也只投影为 `legacy_owner_callable_adapter_refs_only_stage_run_intake`，保持 `provider_admission_pending=false`、`provider_attempt_or_lease_required=false`、`legacy_wire_can_authorize_provider_admission=false`，只能作为 OPL StageRun ABI / provenance 输入。该修复关闭的是 legacy latest wire 通过 receipt candidates 或 domain diagnostic fallback 重新变成 current provider admission 的复发口；repo-source 删除看 active current reader 迁移、explicit history replay/provenance opt-in、canonical receipt replacement 和 no-resurrection guard；live every-active-caller soak、domain diagnostic apply、OPL outbox / StageRun readback、DM002 / DM003 live paper-line progress 和 production-ready 另账。

2026-06-19 legacy carrier ABI/provenance follow-through：`owner_callable_dispatch_request` 的 retirement inventory 现在显式固定为 OPL `DomainProgressTransitionRuntime` intake-only / StageRun ABI provenance-only：active caller 只能产生 `transition_request_pending`，不能保留 surface 或 runtime authority，不能标记 `provider_admission_pending`，也不能要求 provider attempt / lease；`legacy_stage_run_abi_provenance_boundary` 明确 `mas_can_create_stage_run=false`、`mas_can_mark_provider_admission=false`、`mas_can_mark_provider_running=false`，并要求 OPL runtime intake/readback。`runtime_surface_retirement` 新增 fail-closed validator，拒绝 legacy carrier 重新声明 provider admission、running 或 MAS-created StageRun；`owner_callable_receipt_candidates` 产出的 stage closeout packet 也带 `legacy_stage_run_abi_role=opl_stagerun_closeout_provenance_identity_recovery_only`、`provider_admission_authority=false`、`execution_authority=false`、`attempt_lifecycle_authority=false` 和 `queue_authority=false`。这关闭的是 legacy carrier / stage closeout packet 被误读为 provider admission、execution authorization 或 StageRun authority 的复发口；它仍不声明 live OPL outbox / StageRun readback、domain diagnostic apply exactly-one outcome、DM002 / DM003 paper-line progress、live every-active-caller soak、no-active-caller proof 或物理删除完成。

2026-06-19 legacy carrier tail-readback gate follow-through：`owner_callable_dispatch_request` 进一步增加 `opl_owner_callable_adapter_carrier_tail_readback`，把 live-runtime readiness 门固定到 OPL `DomainProgressTransitionRuntime` live readback、command/event/outbox live readback、StageRun owner-callable adapter live readback，或 no-active owner-callable carrier caller scan。`runtime_surface_retirement` 与 completion evidence layers 现在把该 tail 纳入 live required refs，并拒绝把 legacy carrier provenance、`transition_request_pending`、repo no-authority guard、focused tests、request-only carrier provider admission/running/progress 误读为 live takeover。该切片关闭的是 owner-callable carrier tail 被泛化 no-active ref 掩盖的复发口；repo-source carrier deletion 另按 active caller migrated/deleted、canonical transition request replacement、no-forbidden-write、tombstone/provenance 和 no resurrection guard 验收。

2026-06-19 legacy StageRun ABI active-caller scan follow-through：`owner_callable_adapter_receipt_latest_wire_projection.legacy_stage_run_abi_boundary.active_stage_run_abi_caller_scan` 现在显式列出仍在消费 StageRun closeout packet 的 repo caller，包括 receipt consumption、nonconsumable closeout、followthrough、domain diagnostic closeout scan、domain diagnostic work-unit projection 与 study-progress terminal logs。该 scan 的状态是 `active_callers_present_tail_open`，并固定 `no_active_stage_run_abi_caller_proven=false`、`physical_delete_allowed=false` 作为 live-readiness tail；`runtime_surface_retirement` 会拒绝 active callers 仍存在时把 no-active 或 live physical-delete 标成 true。允许的残留消费只限 terminal closeout、typed blocker、owner-route currentness identity recovery 和 paper stage log delta projection；该 scan 本身不能当作 provider admission、execution authorization、running/progress、production readiness 或 live completion 证据。repo-source latest-wire deletion 另由 canonical receipt replacement、explicit legacy fallback、tombstone/provenance 和 no-resurrection guard 验收。

2026-06-19 domain authority refs legacy-helper no-active scan follow-through（历史，已被 2026-07-10 physical retirement 取代）：`paper_progress_transition_refs.record_paper_progress_transition_ref` 移除 `persist_authority_refs_index` active opt-in，paper transition refs 改走 `opl_state_index_source_adapter.emit_paper_progress_transition_source`。该阶段的 helper scan、replay/local-inspection allowance 和 retired-caller inventory 只作迁移 provenance；current repo 已无该 helper 或 SQLite API。

2026-07-10 StateIndex retirement closeout：`refs_only_state_index_pilot.py`、`domain_authority_refs_index.py`、本地 SQLite persistence/inspection/replay API、无效果的 `db_path` 参数和旧 SQLite locator 已物理删除。MAS 只保留单一 `opl_state_index_source_adapter.py`，向 family/product-entry/provider-ready/owner-handoff 投影 body-free ref metadata；generic state index、持久化、lifecycle 与 live owner readback 继续归 OPL。旧 `domain_authority_refs.sqlite` 仅可作为历史迁移/cleanup provenance，不是 current adoption、runtime readiness 或 paper progress 证据。

2026-06-19 domain authority refs OPL family adoption follow-through（历史）：OPL family adoption 与 product-entry persistence / lifecycle ledger 不再把 `domain_authority_refs.sqlite` 的 inspection / payload 作为 current projection 输入，改为读取 `opl_state_index_source_adapter.source_adapter_manifest()` 暴露的 `/refs/state_index_source_adapter`。该阶段仍保留 legacy helper；已由 2026-07-10 physical retirement 取代。

2026-06-19 runtime health diagnostic projection active-caller scan follow-through：`runtime_health_kernel.opl_runtime_health_observability_tail_readback.active_diagnostic_projection_caller_scan` 现在显式列出仍在消费 runtime health snapshot / decision gate / read-status projection 的 repo caller，并固定 `no_active_diagnostic_projection_caller_proven=false`、`physical_delete_allowed=false` 作为 live-readiness tail。`runtime_surface_retirement` 会拒绝这些 active diagnostic projection caller 被写成 no-active caller、OPL observability readback 或 live physical-delete proof；允许消费只限 `read_runtime_status`、`open_monitoring_entry` 与 identity-bound OPL readback requirement projection。该切片只关闭 runtime health 诊断投影被误读为 OPL live observability takeover 的复发口；仍不声明 OPL Observability / RouteReconciler live readback、no-active diagnostic projection caller proof、runtime readiness、domain diagnostic apply 或 DM002/DM003 live paper-line progress。

2026-06-18 materializer request packet follow-through：`domain_action_request_materializer.request_tasks` 顶层 alias 已物理移除；旧 request task 只保留在 `legacy_request_task_diagnostics.legacy_request_task_refs` 作为 `diagnostic_ref_only` / body-free projection，包含 identity、request packet path、status、typed blocker reason、readiness identity 与 OPL transition runtime postcondition；`handoff_packet`、`source_action`、`owner_route`、operator payload、prompt contract 和 request packet body 不再从该 legacy refs surface 导出。完整 body 和 OPL intake carrier 只能来自 canonical `domain_progress_transition_requests`。该修复关闭的是 materializer 顶层 request packet body 或 alias 重新变成 MAS-local outbox / command / event / StageRun / provider admission / next-action authority 的复发口；它证明 repo-source 旧 carrier/alias 已退役，不声明 domain diagnostic apply success、live paper progress 或 production-ready。

2026-06-18 materializer canonical transition request body-retirement follow-through：canonical `domain_progress_transition_requests` 也已从 body carrier 收成 `identity_refs_and_contract_metadata_only` projection；`source_action`、`owner_route`、`prompt_contract`、`domain_intent`、`stage_transition_authority_boundary`、`progress_first_closeout_admission`、operator payload、payload authoring target、record production satisfaction 等 body 字段不再导出，只保留 `*_ref`、currentness identity、route identity、attempt idempotency、forbidden/allowed write refs、OPL transition postcondition 和 body-omitted flags。该修复关闭的是“把 request_tasks 退役后又让 canonical request 本身承载 MAS-local outbox / executable handoff body”的复发口；完整 executable body、command/event/outbox、attempt lifecycle 和 StageRun readback 仍必须由 OPL `DomainProgressTransitionRuntime` intake / readback 承载。本条仍不声明 domain diagnostic apply success、provider running proof、live paper progress、旧 dispatcher 物理删除或 production-ready。

2026-06-18 supervisor projection no-build follow-through：`study_progress.paper_autonomy_supervisor_decision.supervisor_decision_for_projection` 在缺显式 `paper_autonomy_supervisor_decision` / `paper_progress_policy_result_projection` 或 OPL SupervisorDecisionEngine readback 时，不再 fallback 调用 `paper_autonomy_supervisor.build_supervisor_decision`。它只返回 `opl_supervisor_decision_readback_required` 的 no-authority projection，并让 `provider_admission_supervisor_gate` fail closed；projection 明示 `decision_authority=false`、`read_model_can_build_supervisor_decision=false`、`requires_opl_supervisor_decision_engine_readback=true`、`mas_can_run_supervisor_decision_engine=false`、`mas_can_store_recovery_obligation=false` 与 `provider_admission_pending=false`。该修复关闭的是 study_progress / provider-admission read-model 在 OPL proof 之前重跑 MAS supervisor decision engine 的复发口；它不声明 OPL RecoveryObligationStore / SupervisorDecisionEngine 已 live 接管、domain diagnostic apply success、provider running proof、DM002/DM003 live paper-line progress 或 production-ready。

2026-06-18 paper recovery supervisor no-build follow-through：`paper_recovery_state.supervisor_decision` 不再由 `paper_recovery_state` reducer 调用 `paper_autonomy_supervisor.build_supervisor_decision` 生成。`paper_recovery_state` 现在只消费显式 `paper_progress_policy_result_projection` / `paper_autonomy_supervisor_decision` 或 OPL SupervisorDecisionEngine readback；缺显式 projection/readback 时嵌入 `opl_supervisor_decision_readback_required` 的 no-authority projection，并明示 `read_model_can_build_supervisor_decision=false`、`mas_can_run_supervisor_decision_engine=false`、`mas_can_store_recovery_obligation=false` 与 `provider_admission_pending=false`。该修复关闭的是 PaperRecovery reducer 自己把 phase / next_safe_action 提升成 PAS decision 的残留私有面；它不声明 OPL RecoveryObligationStore / SupervisorDecisionEngine live 接管、domain diagnostic apply success、provider running proof、DM002/DM003 live paper-line progress 或 production-ready。

2026-06-18 dispatch running-route proof follow-through：`scan_route_currentness.live_provider_attempt_owner_route_from_scan_payload` 现在要求 `running_provider_attempt=true` 同时具备 trusted OPL execution authorization、exact provider-hosted StageRun packet 或 bound `DomainProgressTransitionRuntime` live readback，才允许返回 `live_provider_attempt_dispatch` owner route。缺 proof 时 running hint 只保留为 diagnostic，不再参与 persisted dispatch selection、owner route basis 或 current/next-action 语义。该修复关闭的是 dispatcher selector 在执行闸之前用 running-provider hint 自行形成 current route 的复发口；它不声明 OPL every-active-caller live soak 完成、domain diagnostic apply success、provider running proof 或 live paper progress。

2026-06-18 recovery successor fixed-point follow-through：`current_executable_owner_action` 现在要求 `repair_progress_projection` 与 `paper_recovery_state.next_safe_action.successor_owner_action` 的 work-unit fingerprint / action fingerprint 匹配，才允许 repair progress 消费 paper recovery successor。旧 repair progress 的 `source_fingerprint` 或 precedence fingerprint 与当前 successor 不一致时，successor 继续保持 current owner action；这关闭 stale repair-progress projection 在 fixed-point 中把当前 recovery successor 降回旧 gate replay / owner receipt residue 的复发口。该证据仍只是 repo-side projection currentness 修复，不是 OPL outbox / StageRun live readback、provider running proof、domain diagnostic apply exactly-one outcome 或 DM002/DM003 live paper-line progress。

## 目标态

MAS 的目标态是：

`Declarative Medical Research Pack + OPL generated/hosted surfaces + minimal medical authority functions`

这表示 MAS 只长期保留医学研究语义、stage pack、quality gate、publication/artifact/memory/source authority、domain transition、owner receipt 和必要医学 helper。通用 runtime platform、queue、attempt ledger、state-machine runner、memory/artifact locator、lifecycle/restore/retention、observability、workbench 和 generated entry/status wrapper 上收到 OPL Framework / shared family layer。

AI-first quality gate 必须由独立 reviewer/auditor agent invocation 关闭。程序函数只做 schema/provenance/authority refs 校验、持久化、owner receipt 签发、typed blocker、safe action refs 和 no-forbidden-write guard；脚本、regex、scorecard、file presence、queue completion 或 executor 自审不能替代医学质量判断。

控制面目标同步收薄为 `macro_state + owner_route + receipt_or_blocker + evidence_refs`。程序只决定当前 owner、allowed action、forbidden writes、idempotency 和 receipt/blocker；开放式医学语义、写作修订、质量判断和路线判断交由 prompt、skill、executor、独立 reviewer/auditor 与 evidence refs 承接。

## 当前完成进度

| Area | 当前进度 | 当前读法 |
| --- | --- | --- |
| Standard OPL Agent source shape | `standard_agent_source_shape_landed` | `agent/` pack、stage/action/memory/artifact contracts、generated surface handoff 与 production acceptance 已成为 MAS 当前机器面；`contracts/functional_privatization_audit.json` 与 focused tests 将 `functional_structure_gap_count` 固定为 `0`。 |
| Runtime owner split | `opl_owned_default` | 默认 hosted autonomous runtime 归 OPL/Temporal；MAS 保留 study truth、quality gate、artifact authority、owner receipt 和 typed blocker。 |
| Workspace target-state cleanup | `ops_bridge_retired_visual_clean_landed` | 新 workspace / bootstrap / monolith migration 不再生成 `ops/mas` 私有 runtime bridge或 repo-local Progress Portal 静态入口；`ops/medautoscience/` 是唯一 MAS workspace ops 入口，用户可见 workbench / display 归 OPL hosted surface 消费 MAS refs-only projection。`workspace-target-state-cleanup --visual-clean` 可归档已知 legacy study/ops residue，并对未分类 entry fail closed；这是 platform repair，不是 paper-line progress。 |
| Legacy runtime no-resurrection | `guarded` | `runtime_transport`、`mas_runtime_core*`、turn runner、worker lease 和旧 lifecycle writer 只能按 tombstone/provenance 或 OPL handoff refs 读取。 |
| DM002 / DM003 anti-stall control loop | `representative_live_owner_closeout_satisfied_cleanup_done_scaleout_tail_open` | `always resolve to next owner`、AI reviewer currentness consumption、owner-route/read-model/liveness 优先级、closeout ingestion、gate-clearing receipt consumption 和 stale provider-attempt invalidation 已进入当前口径；2026-06-05 两篇先按用户要求 queue hold，随后 Stage Native live canary 用 refs-only materializer 把 stage artifact read-model 推进到 terminal `08-publication_package_handoff`。2026-06-06 fresh workspace evidence 显示 terminal handoff 已写出 MAS-owned `medical_paper_readiness_not_ready` typed blocker，并绑定 provider attempt、active lease、execution authorization decision、stage manifest、current pointer、source fingerprint 和 idempotency key；`projection/current_owner_delta.json` 当前指向 `MedAutoScience / complete_medical_paper_readiness_surface`。MAS repo 已落地该 owner callable / owner-callable policy / owner-route reason / dispatch tests：它优先按当前 `medical_paper_readiness.next_action.surface_key` 消费同 surface 的 dispatch / request / ref payload，物化 canonical surface 或写 stable owner blocker，避免 stale dispatch 重放。2026-06-06 follow-through 已落地 `literature_provider_runtime`、`study_line_selection`、`archetype_analysis_contract`、`bounded_analysis_candidate_board` 四个 surface 的 owner-authored payload / materializer、surface/action scoped idempotency 和 closeout-binding result shape；2026-06-07 follow-through 继续落地 `stop_loss_memo` owner surface、verified literature materialization fallback provenance、current readiness surface 优先级和 stop-loss stale dispatch rejection，不写 paper、package、publication truth 或 quality verdict。2026-06-15 fresh domain diagnostic apply / post-apply readback 已把代表性 live owner evidence 收到 exactly-one outcome：DM002 为 `typed_blocker_ref` / `stop_with_stable_typed_blocker` / `anti_loop_budget_exhausted`，DM003 为 `owner_receipt_ref` / `stop_with_owner_receipt`。同日 `owner-callable-adapter-residue-cleanup --apply` 已归档两篇共 23 个 stale mutable dispatch slot，并由 fresh dry-run 读回 clean；immutable dispatch provenance 保留。这关闭 representative live-owner gate 与 Yang stale mutable dispatch 物理 cleanup，但不声明 paper closure、publication-ready、package-ready 或 provider running proof。补偿链 retirement / tombstone 统一按 `contracts/runtime/legacy-active-path-tombstones.json#/stage_native_compensation_retirement_gate` 判断。 |
| Domain owner evidence shape | `shape_landed_current_pointer_owner_answer_materializer_live_scaleout_pending` | guarded apply、owner-route、body-free evidence packet、typed blocker shape、paper-line owner-chain result、domain-dispatch payload 和 OPL refs-only return shapes 已可消费；`medautosci current-owner-delta-owner-answer` 现在可把 fresh OPL `paper_autonomy/guarded-apply` current owner delta 物化为 MAS-owned `typed_blocker_ref` answer，并输出 OPL `domain-owner-payload-summary` / `stage-run-authorization` record payload。该 landing 只关闭 current pointer owner-answer handoff 的 materializer / record-payload 缺口；不写 study truth、paper body、publication eval、controller decision、current package、owner receipt 或 quality verdict，也不声明 paper closure、publication-ready、artifact mutation authorization、memory accept/reject 结论、domain-ready 或 production-ready。 |
| Research evidence pack read-model / schema / canary | `read_model_schema_canary_evidence_landed_not_publication_ready` | read-model、schema validation、Progress Portal / runtime workbench stage review projection 和 canary expectation 已同口径 fail closed；它们只关闭最低科研审计链的可读 / 可校验 / fail-closed 证据。 |
| Docs lifecycle | `single_active_truth_owner_reconfirmed_2026_06_03` | 本文持有 current gap / completion plan；`docs/status.md` 持有 current-state summary；`docs/docs_portfolio_consolidation.md` 持有 docs lifecycle rules；dated closeout 与命令流水归 history/provenance。 |
| Progress-first Co-Scientist affordance | `agent_contract_source_tests_docs_landed_not_authority` | 六项机制已落到 `agent/stages/stage_native_semantic_pack.yaml`、`agent/stages/stage_route_contract.yaml`、hypothesis portfolio contract、StageRun profile、`current_owner_ticket.progress_jit_affordance_policy`、focused tests、本文、runtime design、handoff standard、operator runbook、invariants 和 status：它们是 current-owner-native JIT affordance，不是默认增强层。ordinary path 默认不运行 tournament / candidate generation / meta-review / prefetch / memory scan；只有 `current_delta_declares_or_implies_affordance_need` 时才即时调用。 |
| EvoScientist / EvoSkills progress sidecar | `repo_callable_worker_landed_not_authority` | EvoScientist v0.1.4 的 auxiliary background memory workers / tool selector / fire-and-forget observation memory 与 EvoSkills v1.0.0 的 IDE / IVE / ESE lifecycle memory 已折回为完整 nonblocking current-owner-following sidecar architecture，并落地 repo callable worker：`write_evo_scientist_sidecar_observation`、`medautosci evo-scientist-sidecar observe/read-latest`、`study_progress` materialize hook、`artifacts/runtime/evo_scientist_sidecar/` refs-only observation/latest，以及 refs-only state index family `evo_scientist_sidecar_ref`。`tool_selector_helper`、`observation_memory_sidecar`、`failed_path_taxonomy`、`routing_eval`、`attempt_budget_stop_loss` 都只能产生 refs-only hint、reviewer briefing、memory reuse candidate、route hint 或 no-loop signal；critical path 不等待 sidecar，缺失、失败、超时、预算耗尽或低置信时不得阻断 current owner action。 |
| External-learning adoption closure | `worker_registry_landed_refs_only_not_external_runtime` | `contracts/progress_first_safety_envelope.json#/external_learning_adoption_closure_policy`、`contracts/opl-framework/family-contract-adoption.json#/external_learning_adoption_closure`、[External Learning Adoption Closure Runbook](../runtime/control/external_learning_adoption_closure.md)、`docs/decisions.md`、`docs/status.md`、reference intake 边界和 `$mas-contract-first-external-intake` skill 已固定：外部自动研究框架的 `adopt_contract`、reference intake、design doc、score、ranking、checklist 或 skill inventory 不能写成 functional landed；必须标注 `owner_surface_landed`、`read_model_landed`、`sidecar_or_worker_landed`、`sidecar_execution_slot_landed`、`contract_projection_landed` 或 `contract_only_gap` / `projection_only_gap` / `history_only_gap` / `not_landed_gap`。ARS、AutoSci / OmegaWiki、ARK、ARIS、PaperSpine 和 PaperOrchestra 已通过 single nonblocking `run_external_learning_sidecar` worker registry 落到 refs-only advisory worker surface；该面不引入外部 runtime，也不让缺 external advisory 阻断当前可执行 owner action，且不把 worker refs 写成 owner receipt、typed blocker、paper progress 或 publication quality。 |
| Progress-first safety envelope | `contract_tests_runbook_landed_not_live_preflight` | `contracts/progress_first_safety_envelope.json` 已把错误完成、伪证据、旧 read-model、重复 receipt 和 artifact authority 漂移固定为五类风险，并把 Light / Co-Scientist / EvoScientist / EvoSkills 的混合模式收敛为 `current_owner_native_jit_affordance`：ordinary path 没有常驻 sidecar、没有默认 scan / tournament / meta-review / prefetch / memory scan；触发语义固定为 `current_delta_declares_or_implies_affordance_need`。该面由 focused meta test 和 runbook/decision/status 承载；它是 safety / audit hardening，不是 paper-line progress、publication-ready、domain-ready 或 production-ready。 |
| MAS / OPL Agent OS target architecture | `docs_target_plan_landed_registry_hosted_consumption_authority_inventory_landed_real_owner_tail_open` | [MAS / OPL Agent OS 目标运行架构与重构计划](../runtime/designs/mas_opl_agent_os_target_operating_architecture.md) 已把外部成熟工程经验转译成可落地 lane：OPL Agent OS、MAS Medical Research Pack、MAS Authority Kernel、Scientific Capability Registry、Pack Compiler、StageRun / State Index / Route Reconciler、evidence lineage、AI-first Quality OS、Agent Tool Arsenal、Workbench UX 和 production evidence soak。当前已关闭方向入口、Agent Tool Arsenal contract、实际 MCP result envelope、completeness diagnostic、Scientific Capability Registry repo runtime ABI、repo-side owner-consumption / live-soak evidence packet shape、hosted OPL ordinary-path consumption evidence 和 Authority Kernel inventory/read-model；真实 paper-line owner receipt / typed blocker / reviewer receipt / route-back evidence、direct/hosted parity、provider / human gate / artifact lifecycle soak 仍是证据尾项。 |
| Authority Kernel inventory | `inventory_landed_physical_thinning_pending` | `contracts/authority_kernel_inventory.json` 与 `src/med_autoscience/authority_kernel_inventory.py::build_authority_kernel_inventory` 已把 retained Medical Authority Kernel 落成机器可读 read model，覆盖 owner receipt signer、typed blocker materializer、source readiness、publication quality gate、artifact mutation authorization、publication-route memory accept/reject、no-forbidden-write proof、refs-only helper 和 diagnostic probe；每项固定 category、owner、active caller refs、allowed writes、forbidden authority、output refs、cannot-lift-to-OPL reason 与 retirement / upcollect target。第一批 physical thinning 已把 generator 的 schema/dataclass、item builders 与 gap validation 下沉到 `src/med_autoscience/authority_kernel_inventory/`，入口只保留 thin read-model builder；contract JSON 和 generated payload 不变。该 landing 仍只关闭 inventory/read-model 与第一批 generator physical thinning，不声明 authority fully retired、physical thinning complete、production-ready、paper-line progress、publication-ready 或 artifact mutation authorized；后续按 inventory 做旧 wrapper / residue 删除门、OPL upcollect consumption、direct/hosted parity 和真实 evidence 验证。 |
| Agent Tool Arsenal / Capability Invocation OS | `agent_tool_arsenal_hosted_consumption_executor_receipt_refs_landed_real_live_soak_tail_open` | `contracts/agent_tool_arsenal.json` 已把 action catalog / MCP registry / owner callable / plugin skill / stage refs / scientific capability registry 统一成 machine-readable agent invocation ABI，并由 MCP `agent_tool_arsenal` 只读工具暴露 index、card、plan、result envelope schema、completeness diagnostic 和 hosted-consumption evidence。实际成功 MCP call result 的 `structuredContent` 已统一为 `mas_tool_result_envelope`，原 payload 保留在 `structured_payload`；diagnostic 会检查 action cards 的必备 ABI 字段、public runtime tool manifest 映射，并把 support / diagnostic MCP tools 与 current-owner-delta action cards 分账。`scientific_capability_registry` 已作为 action catalog、MCP runtime、CLI/product-entry 和 Agent Tool Arsenal card 落地，支持 `index / resolve / invoke`，把 external-learning sidecar、Light advisory、Evo sidecar、Co-Scientist affordance 和 Display Pack capability 作为 current-owner-delta-bound refs-only capability 消费；module API 已新增 `mas_scientific_capability_owner_consumption_evidence` packet，绑定 current owner delta identity、capability output refs、owner response refs、no-forbidden-write proof 和 fail-open flags。`contracts/hosted_ordinary_path_consumption.json` 与 MCP `agent_tool_arsenal(mode=hosted_consumption)` 已证明 OPL hosted ordinary path 能消费 AgentExecutionIndex、OperationalToolCard、CapabilityInvocationPlan、ToolResultEnvelope recovery、Scientific Capability Registry resolution 和 owner-consumption evidence packet，且不新增默认 preflight、Docker / DinD 或人类手动工具学习负担。`ToolResultEnvelope` / `CapabilityInvocationPlan` / `ToolUseCard` 也已暴露可选 `executor_receipt_ref` 与 `lightweight_executor_receipt_contract_ref`，用于引用 L0/L1 refs-only default executor evidence；该 ref 可缺失，不能阻断 current owner action，也不能升级为 owner receipt、typed blocker 或 paper progress。2026-06-17 boundary hardening 已把 `selection_runtime_owner`、`capability_runtime_owner`、`capability_runtime_kind`、`mas_selector_authority=false`、`mas_tool_invocation_runtime_authority=false`、`missing_refs_trigger_mutating_invocation=false` 和 capability-plan non-authority flags 写入 ToolArsenalIndex、AgentExecutionIndex、resolver contract、candidate、ToolUseCard / OperationalToolCard 与 CapabilityInvocationPlan；MAS 只发布 capability planning projection、gate metadata、allowed/forbidden refs 和 owner-answer 晋级门，selector / invocation runtime 归 OPL Capability Runtime。2026-06-19 follow-up 又把含 `*` action trigger 的 Evo / Light support sidecar 收成显式 request-only：未知 owner action 不再自动选中 wildcard sidecar，只有当前 owner delta 明确声明 capability family / id 时才可 resolve；`contracts/runtime/mas-runtime-surface-retirement-inventory.json` 和 `runtime_surface_retirement_no_authority_audit` 会拒绝 MAS selector authority、always-on sidecar 或 wildcard auto-select 回归。2026-06-19 live-soak boundary follow-up 进一步固定 `live_owner_consumption_soak_boundary`：repo-side owner-consumption evidence packet、hosted ordinary-path requirement、MCP/CLI coverage、wildcard guard 或 focused tests 都不能证明 live owner-soak、direct hosted parity、no-active caller 或 production readiness；真实 current-owner-delta live consumption、direct hosted parity 和 no-active-caller / owner decision evidence 属于 live-runtime readiness tail，不再作为 repo-source capability selector/sidecar 删除前置。剩余差距是真实 current_owner_delta live owner-soak、direct/hosted parity 和 production evidence tail。该 landing 不新增 MAS 私有 selector、always-on sidecar、默认 preflight、第 11 个 OPL 品牌模块或用户手动工具学习负担；OPL brand module taxonomy 继续由 OPL 持有，evidence packet 不声明 paper progress 或授权。 |
| Standard OPL Agent completion acceptance | `repo_morphology_and_opl_stage_route_current_verification_landed_production_and_live_tail_open` | `contracts/standard_agent_completion_acceptance.json` 把“彻底清除 MAS 历史包袱”和“让后续 OPL 标准化智能体不重复 DM002/DM003 类错误”定义成可验收机器合同；`contracts/standard_agent_completion_evidence_status.json` 把七个 gate 落成可验收 evidence ledger，逐项记录 current status、required evidence refs、observed refs、missing evidence tails 和 false-completion boundary。合同与 ledger 共同禁止把 `contract_landed`、`docs_updated`、`functional_structure_gap_count_zero`、`generated_interface_ready`、`OPL_conformance_passed`、`queue_empty`、`provider_completed`、`domain_diagnostic_observe_only`、`owner_callable_adapter_residue_cleanup_clean`、refs-only ledger、未采纳 foreground edit、partial live runtime evidence rollup 或 `typed_blocker_required` rollup 声明为彻底完成。2026-06-15 fresh domain diagnostic apply / post-apply readback 已将 representative DM002/DM003 live owner evidence 更新为 `satisfied_with_live_owner_evidence`：DM002 是 stable `typed_blocker_ref`，DM003 是 `owner_receipt_ref`。同日 Yang stale mutable dispatch cleanup receipt 与 fresh dry-run 已记录到 evidence ledger；2026-06-16 OPL `one-person-lab@e56b9a7583b64d26f275062f3d2c5561bcf4dc20` 已落地 repo-backed MAS/MAG/RCA/OMA/family-default false completion negative samples 与 OMA work-order missing-guard field samples；2026-06-18 `contracts/functional_privatization_audit.json#/physical_source_morphology_scan` 已把 physical source morphology scan 从泛 tail 收为 repo-backed scan proof；2026-06-18 OPL `one-person-lab@cca17b60401151b83f6195c6f315cbe055209c57` 的 stage-route arbiter substrate current verification 已记录到 MAS ledger，source evidence 是 OPL `stage-route-scheduler-contract`、standard-agent landing evidence status 和 focused stage-route / currentness / acceptance tests。2026-06-19 obligation actuator tail-readback boundary 已落到 runtime retirement inventory、validator、completion evidence layer 与 Paper Autonomy Supervisor contract 读法：domain diagnostic obligation actuator physical retirement 不能由 MAS projection、transition request、typed blocker authority result、focused tests 或 repo no-authority audit 关闭，仍需要 OPL RecoveryObligationStore / SupervisorDecisionEngine active-caller readback、no-active-caller proof 或 owner retirement decision。2026-07-10 `contracts/runtime/mas-live-runtime-evidence-rollup.json` 已收为 11 行 retired redirect tombstone；MAS Python/CLI evidence intake 与 rollup 已物理删除，旧 7 个 takeover/soak tail、5 个 runtime gap 和 `all_work_orders_satisfied` 只作合同历史/provenance，current live evidence intake/readback 归 OPL runtime owner。整体仍是 `evidence_tail_open_not_complete`，因为 direct/hosted generated surface production consumption、domain diagnostic obligation actuator no-active-caller 或 physical retirement owner decision、真实 owner evidence scaleout、production generated-surface caller negative samples、provider/operator long-soak 与 OPL live runtime evidence readback 仍是 open evidence tails。 |

当前 platform lane 的近期收口点是 `complete_medical_paper_readiness_surface`：把 owner action、accepted payload、payload-missing stable blocker、surface artifact path、readiness rebuild 和 OPL execution authorization / closeout binding 写成单一路径。`literature_provider_runtime`、`study_line_selection`、`archetype_analysis_contract`、`bounded_analysis_candidate_board` 和 `stop_loss_memo` 已进入该路径；下一层 functional tail 是继续按当前 readiness next action 扩展 owner-authored surface 或产出 stable blocker。2026-06-07 follow-through 补齐 `stage-outcome-authority --payload-file/--payload-json`，使 OPL/App 生成的 refs-only consumer payload 可以进入 MAS dispatch selector，并复用已有 owner callable closeout-binding 链路；同日补齐 verified `publication_eval/literature_materialization.json` fallback，使 PubMed live fetch 失败但已有 verified PubMed records 时仍能生成 provider-backed payload 与 response ledger。这些只是 transport / read-model / owner-surface repair，不写 study truth。Fresh read-only check 显示 OPL worklist / cockpit current-owner identity 已重新收敛；MAS `study progress` 默认投影 canonical `NextActionEnvelope` 派生 owner summary，`current_owner_delta` 只作为 owner projection / audit drilldown，legacy `current_executable_owner_action` 只保留 diagnostic / provenance，不作为 owner answer、dispatch source 或第二 next action；active attempt id 会随 live owner run 变化，不能单独作为 owner answer。不得把 CLI transport、zero open worklist、typed blocker context、legacy current owner action、active attempt 或结构 conformance 升级为 owner answer / domain ready。DM002 / DM003 live paper execution 不在本轮，由会话 `019e92c8-b940-7313-940b-ea892820d322` 继续处理；本轮不声明 paper readiness、publication-ready、submission-ready、quality verdict、`current_package` fresh、domain-ready 或 production-ready。这个收口是 Stage Native / StageRun / owner projection 黄金路径上的默认面压缩，不是推倒重构，也不是恢复 MAS generic runtime loop。

2026-06-09 顶层规划更新，2026-06-30 口径修正：MAS paper-line 默认推进采用 `ordinary progress spine + audit sidecar`。普通路径只看 `StageOutcome -> canonical NextActionEnvelope -> owner projection/current_owner_delta drilldown -> concrete paper/evidence/reviewer/gate delta -> ProgressDeltaReceipt / OwnerReceipt / TypedBlocker -> next StageOutcome/NextActionEnvelope`。`current_owner_delta` 是投影和 audit drilldown，不是 planning root。`complete_medical_paper_readiness_surface` 和后续 readiness surface 只能按 just-in-time delta 读取：当前缺哪个 surface，就补哪个 surface 或写 stable typed blocker；不得把 literature provider、study line、analysis contract、bounded board、journal layer 等 readiness inventory 重新变成“全部补齐后才允许继续写作/分析”的前置大门。OPL refs-only ledger、read-model reconcile、closeout binding、restore proof、long-soak、cleanup 和 production evidence 进入 audit sidecar；只有触发 owner / authority / execution authorization / closeout binding / irreversible mutation / publication-submission-delivery claim / human gate 时才升级为 hard gate。MDS / DeepScientist 的可取之处是流畅运行的单循环和少默认门；EvoScientist / EvoSkills 的可取之处是 background memory worker、tool selector、observation memory 和 lifecycle skill taxonomy 的旁路加速；它们都不能重新定义 MAS runtime、quality、memory 或 artifact authority。

## 已落地

| Area | 当前状态 | 当前证据入口 | 当前读法 |
| --- | --- | --- | --- |
| MAS semantic pack / generated handoff | `landed_with_evidence_tail` | `agent/`、`contracts/pack_compiler_input.json`、`contracts/stage_control_plane.json`、product-entry stage descriptor | `agent/` 是 canonical semantic pack；真实 stage attempt 与独立 reviewer/auditor receipt 仍属 evidence tail。 |
| Functional privatization boundary | `closed_functional_structure_gates_evidence_tail_remaining` | `contracts/functional_privatization_audit.json`、`contracts/private_functional_surface_policy.json`、`contracts/production_acceptance/mas-production-acceptance.json`、`tests/test_opl_standard_pack.py`、`tests/test_opl_family_contract_adoption.py` | `classification_gap_count=0`、`functional_structure_gap_count=0`、`active_private_generic_residue_count=0`、`repo_local_wrapper_tail_count=0`；`retirement_disposition_matrix` 把旧面分成 `tombstone_only`、`opl_primitive_replacement`、`temporary_refs_projection` 和 `retained_minimal_authority_function`。former wrapper / private runtime surface 的 repo-source 物理删除要求 active caller migrated/deleted、replacement parity、no-forbidden-write、focused/meta/default verification、no-resurrection guard 和 tombstone/provenance；owner receipt / stable typed blocker / human gate / route-back 或 OPL readback 进入 live-runtime readiness 栏。 |
| Purpose-first adapter thinning | `machine_guard_landed_evidence_tail_remaining` | `contracts/foundry_agent_series.json#/purpose_first_adapter_thinning_policy`、`tests/test_opl_standard_pack.py` | retained product/workbench/status/read-model surface 只能作为 refs-only adapter、domain handler target、minimal authority function、migration input 或 tombstone/provenance 读取；repo-source 物理删除统一要求 active caller migrated/deleted、replacement parity、no-forbidden-write、focused/meta/default verification 与 tombstone/provenance。owner receipt、typed blocker、human gate、route-back 或 OPL live readback只用于 runtime readiness / paper-line progress / production readiness。 |
| Default runtime owner | `opl_owned_default` | runtime default/backend contract、product-entry manifest、domain-handler export/read-model | 默认 runtime ref / substrate 是 `opl_hosted_stage_runtime`，默认 backend 是 `opl_provider_backed_stage_runtime`；历史 `mas_runtime_core` 只能按 retired provenance / migration input 读取。 |
| Runtime / lifecycle no-alias retirement | `retired_no_alias_guarded` | `contracts/runtime/legacy-active-path-tombstones.json`、`contracts/runtime/mas-runtime-surface-retirement-inventory.json`、functional boundary audit | `runtime_transport/`、`mas_runtime_core*`、turn runner、worker lease、旧 lifecycle refs SQLite writer 只能作为 tombstone/provenance、OPL handoff refs 或 standard Agent purity guard 读取。 |
| Owner-route / dispatch currentness | `active_current_truth` | `docs/decisions.md`、owner-route protocol contracts、domain-handler export/dispatch behavior | `mas-owner-route-attempt-protocol.v1` 要求 reason registry、priority lattice、currentness basis、source/work-unit/truth/runtime fingerprint 和 typed closeout boundary；它只授权 OPL transport，不授权 paper closure 或 package freshness。 |
| Progress-first admission and accounting | `landed_with_live_evidence_tail` | [Progress-first Stage Outcome Runbook](../runtime/control/progress_first_stage_outcome.md)、`study_progress`、`study-state-matrix`、Progress Portal、MCP compact | 非终局 study 必须投影到唯一 owner action、owner receipt、typed blocker、human gate 或 stop-loss；platform repair 不计 paper/deliverable progress。 |
| Research evidence pack projection | `landed_not_publication_ready` | schema validation、read-model projection、Progress Portal / runtime workbench stage review、canary expectation | 只证明最低科研审计链可读、可校验、可 fail closed；真实 publication-ready 仍需 MAS owner receipt、独立 reviewer/auditor record、publication gate / human gate 和 artifact/memory/lifecycle authority receipt。 |
| StageRun Kernel foundation | `profile_projection_controlled_canary_and_live_stage_artifact_gate_landed_publication_handoff_owner_answer_binding_landed_real_evidence_tail_open` | MAS `contracts/stage_run_kernel_profile.json`、`contracts/stage_run_canary_evidence.json`、`contracts/stage_artifact_kernel_adoption.json#/stage_run_kernel_profile_binding`、`src/med_autoscience/controllers/stage_run_kernel.py`、`src/med_autoscience/controllers/study_progress/stage_kernel_projection.py`、`src/med_autoscience/controllers/stage_artifact_materializer.py`、`src/med_autoscience/controllers/stage_artifact_index.py`、`src/med_autoscience/controllers/stage_outcome_authority/action_execution/publication_handoff.py`、`tests/stage_run_kernel_cases/test_ai_reviewer_stage_run_kernel.py`、`tests/test_stage_artifact_index.py`、`tests/stage_artifact_index_cases/test_legacy_contract_residue.py`、`tests/test_stage_run_kernel_profile_contract.py`、`tests/test_stage_outcome_authority_cases/publication_gate_dispatch.py`；OPL refs-only StageRun substrate / CLI / conformance 以 OPL repo live state 为准 | 已关闭 StageRun Kernel 的 MAS foundation，并补齐 terminal publication handoff callable 与 owner-answer binding：stage folder / manifest / role artifact / owner receipt / typed blocker 是当前 transition 口径；canonical stage folder 文件名已采用 `stage_manifest.json`、`owner_receipt.json`、`typed_blocker.json`、`consumed_artifact_refs.json`、`prov.json`、`current.json` 和 `current_owner_delta.json`；provider completion、file presence、`latest.json`、read-model 和 active_run_id 不能关闭 Stage。`publication_handoff_owner_gate` apply 缺 OPL execution authorization 时 fail closed，不写 MAS owner receipt / typed blocker；有授权时只写 stage-native handoff receipt / typed blocker 与 refs-only closeout binding，不写 publication eval、controller decision、paper 或 package。Co-Scientist candidate generation、grounded reflection、comparative selection、evolution / revision、meta-review 和 independent quality gate 已进入 controlled canary evidence fixture，全部是 refs-only / advisory boundary；operator summary、overclaim boundary 与 legacy runtime residue guard 已由 focused contract test 锁定为 read-model-only / fail-closed guard。2026-06-05 DM002 / DM003 live canary 已把 stage artifact current pointer 推进到 terminal publication handoff gate，并把旧 contract 文件降为 `legacy_orphan_residue` 而非 blocking orphan。真实 workspace owner receipt / typed blocker、human gate / route-back 和 real owner-chain evidence 仍开放；gate 未满足时，重复 reconcile / materialize / dispatch 补偿路径只能保留 tombstone、provenance 和 delete-gate context，不做物理退役。 |
| ARK-inspired research workflow intake | `contract_landing_in_progress` | [ARK Research Workflow Intake](../references/mainline/ark_learning_intake.md)、reviewer issue/progress ledger、display artifact manifest、source citation authority pack | 只吸收 review loop、goal anchor、issue repair validation、API-first citation、figure manifest、page adjustment 和 human-intervention UX 的 MAS-native contract shape；不引入 ARK runtime、SQLite authority、conda project model、Telegram/webapp service、代码或依赖。 |

## 当前功能/结构差距

当前没有 open functional / structural gap。`contracts/functional_privatization_audit.json` 的 `functional_followthrough_gap_summary` 和 focused tests 固定：

| Metric | 当前值 |
| --- | --- |
| `classification_gap_count` | `0` |
| `functional_structure_gap_count` | `0` |
| `remaining_functional_followthrough_gate_ids` | `[]` |
| `active_private_generic_residue_count` | `0` |
| `repo_local_wrapper_tail_count` | `0` |

### 本轮旧表面扫描读法

按 `launcher|wrapper|alias|compat|legacy|status|sidecar|product-entry|helper|facade` 关键词扫描后，没有发现可在本轮直接物理删除的孤立普通入口。以下 retained surfaces 仍有 active caller 或承担 MAS domain target / refs-only projection；它们不能被写成长期 MAS 私有 runtime，也不能在缺少退役门时删除。

| Retained surface | 保留原因与 active caller | OPL owner surface | 退役门 |
| --- | --- | --- | --- |
| `plugins/med-autoscience` canonical plugin source | repo-local plugin source、skill、MCP launcher、generated handoff 和 consumer inventory 均指向 canonical path；`plugins/mas` 与 `skills/mas` legacy alias 已物理退役，healthcheck 会阻止恢复。 | OPL wrapper / startup maintenance / Codex marketplace registration。 | 保留 canonical source；Codex marketplace 由 OPL-owned sync surface 物化，不在 MAS repo 恢复 legacy alias。 |
| repo-local `product-entry` / `status` / `workbench` projection shell | `src/med_autoscience/controllers/product_entry*`、`mainline_status*`、product-entry tests 和 README operator quick start 仍消费这些 refs-only projection / diagnostic surfaces。保留原因是给 domain handler / operator 展示 MAS owner refs，不是 MAS generic shell owner。 | OPL generated/hosted product-entry、status、workbench、App shell 和 projection surface。 | OPL generated/default caller parity、no-active MAS repo-local caller、same owner receipt / typed blocker roundtrip、no-forbidden-write proof 和 tombstone/provenance pointer 同时成立。 |
| `runtime_status_summary` / workspace status / mainline status helpers | `tests/test_runtime_status_summary.py`、workspace/status readback helpers 和 status docs 仍用它们读取稳定 runtime artifact 或 human readout。保留原因是 diagnostic/readback helper，不是 lifecycle、queue、attempt ledger 或 runtime authority。 | OPL StateIndex / Observability / Workbench shell。 | OPL StateIndex 或 hosted read-model 消费同一 body-free refs；MAS 默认 next action 仍来自 `StageOutcome -> NextActionEnvelope`；no-active helper caller scan 通过；保留 provenance 后删除或收薄。 |
| EvoScientist / EvoSkills sidecar refs | `runtime_protocol/evo_scientist_sidecar_refs.py`、`cli/evo_scientist_sidecar_commands.py` 和 `tests/test_evo_scientist_sidecar_runtime.py` 仍验证 refs-only observation、fail-open 和不能成为 next action / authority source。保留原因是 advisory/audit sidecar，不是 owner、quality、memory 或 artifact authority。 | OPL Capability Runtime / Tool Arsenal / advisory worker substrate。 | capability output 迁到 OPL ToolResultEnvelope / capability runtime；MAS 只消费 current-owner-requested refs；CLI/MCP sidecar caller 迁出；no-forbidden-write 与 fail-open guard 保持；留下 tombstone/provenance。 |
| `domain_entry` / `domain-handler export|dispatch` target | `tests/test_domain_entry.py`、MAS CLI/MCP、skill direct entry 和 product-entry manifest 仍以它作为 service-safe domain target。保留原因是 MAS domain package 的入口，不是可上收的 generic wrapper。 | OPL generated wrapper / hosted package surface 调用该 target。 | 不作为普通 wrapper 删除；只能继续收薄 generic descriptor / shell 部分，保留 MAS owner receipt、typed blocker、domain refs 和 authority function 边界。 |

近期结构收口只保留当前读法，不保留同日 commit / test / line-count ledger：

| 收口面 | 当前读法 | 后续约束 |
| --- | --- | --- |
| `paper_mission_source_test_morphology` | `paper-mission` CLI 已按 typed-blocker resolution、stage closure、receipt consumption、candidate package、materialized readback、parser registration、domain-handler dispatch、OPL handoff、one-shot migration 和 materialized terminal closeout case 等自然边界持续拆分。剩余大入口是 source/test structure advisory，不是 open functional gap。 | 后续只有在 source-governance lane 被选中时，才 fresh-read line budget / focused tests 并继续按 transaction readback、output-root 等自然边界收薄；过程证据留在 git history / history program provenance，不写回 active plan。 |
| `typed_blocker_executable_owner_route` | `paper-mission typed-blocker-resolution --apply-*` 已从 readback packet 推进为可执行 owner route：packet 暴露 executable owner route、paper-facing delta、accepted answer shape、route-back 和 verification。 | 该 surface 仍是 non-authority packet/readback，不写 owner receipt、typed blocker、human gate、`current_package`、publication eval 或 controller decision；它关闭 repo/source/control-plane owner-route 可执行性，不关闭 live paper acceptance。 |
| `legacy_active_path_guard` | legacy next-action authority、default executor、Progress Portal / Workbench next action 和 materializer fallback 只允许 diagnostic / migration / provenance / read-only projection。Canonical path 是 `StageOutcome -> NextActionEnvelope` 绑定的 typed-blocker resolution successor action。 | 不新增 active callable alias、fallback authority 或 compatibility shim；旧 producer / old test truth 的物理删除只在 caller migrated/deleted、replacement parity、no-forbidden-write proof 和 focused guard 成立后进入 source-governance lane。 |

下表只保留仍约束后续工作的 closed gate / guard；它们不能被重写成 active backlog，也不能被 OPL transport evidence 升级为 MAS paper closure。

新增或保留 MAS-local adapter 时，默认下一步必须是 `paper_progress_delta_or_mas_owned_typed_blocker`。platform repair、read-model currentness、provider completion、schema/descriptor conformance 和 generated surface readiness 只能作为证据尾项或 refs-only 投影；缺真实 paper / research / reviewer / human-gate delta 时返回 MAS-owned typed blocker，不写成结构 gap 已关闭或 paper progress。

2026-06-16 后的新增执行规则：如果某个 MAS-local adapter 的职责能映射到 `DomainIntent -> OPL Command -> OPL Event -> OPL Transactional Outbox -> StageRun / ToolInvocation -> MAS OwnerAnswer -> Derived Projection` 链中的 OPL 段，它就不能作为 MAS retained function 扩写。允许留在 MAS 的只有：声明 domain intent / policy result、签发或校验 owner answer、物化 domain authority refs、生成 typed blocker / human gate / route-back evidence、或提供 body-free diagnostic projection。否则必须创建 OPL work order、迁移门、tombstone/provenance 或 stable typed blocker，而不是增加 MAS 私有 selector、supervisor、kernel、state index、outbox-like adapter 或 workbench shell。

| Gate / guard | 当前实际 | 后续约束 |
| --- | --- | --- |
| `generated_surface_default_owner_cutover` | OPL owns generated/default CLI/MCP/Skill/product-entry/status/workbench/domain-handler shells；MAS provides pack inputs、domain handler targets、authority refs、owner receipts、typed blockers 和 no-forbidden-write guard。 | 不把 MAS direct path、domain handler target 或 read-model projection 写回 MAS-owned generic wrapper。 |
| `agent_tool_arsenal_generation` | `contracts/agent_tool_arsenal.json`、MCP `agent_tool_arsenal` 和 public runtime `scientific_capability_registry` 已把 action catalog / MCP registry / owner callable / plugin skill / stage refs / external scientific capability refs 收成 agent-facing tool cards、invocation plan、structured result envelope schema、hosted consumption evidence 和 current-delta-bound capability `index / resolve / invoke` ABI；Scientific Capability Registry 还提供 refs-only owner-consumption evidence packet API，用于记录 capability output refs 被 current owner / reviewer / route-back refs 消费的 live-soak 证据。Tool Arsenal 所有 selector / invocation-runtime 字段现在显式归 `one-person-lab` / `OPL Capability Runtime`，并 fail-closed 声明 MAS 没有 selector authority 或 private invocation runtime authority；wildcard action trigger 只能通过显式 capability request 消费，不能成为未知 action 的默认 sidecar selector。 | 新 agent-facing 工具必须继续从 `current_owner_delta` 解析适用性，提供 risk annotations、input/output schema、allowed writes、forbidden authority、human gate 条件、idempotency/fingerprint policy、receipt/blocker shape、failure class 和 OPL capability-runtime owner boundary；不得把人类 runbook 当作 agent tool-use ABI，也不得在 MAS 中新增 selector / planner / tool runtime。后续尾项是真实 paper-line owner receipts / typed blockers / reviewer receipts / route-back evidence、direct/hosted parity 和 production soak，不是 MAS 私有 selector。 |
| `authority_kernel_inventory` | `contracts/authority_kernel_inventory.json` 固定 retained authority kernel representative surfaces、owner、active caller、allowed writes、forbidden authority、output refs、cannot-lift-to-OPL reason、retirement / upcollect target 和 non-claims；focused tests 固定必填字段、forbidden authority 完整性、generic runtime owner 排除、counts/gaps 与合同/生成器一致性。第一批 physical thinning 已将 generator 私有实现拆到 `authority_kernel_inventory/`，入口保持 thin builder 和 public export。 | 该 inventory 是后续 OPL upcollect 和旧 residue 删除门的读根；它不改变 owner callable 行为，不签 owner receipt / typed blocker，不声明 authority 已退役或 production-ready。 |
| `opl_primitive_abi_guard` | 目标字段族已在 Agent OS 目标架构中固定：identity、causality、authority boundary、outcome、projection metadata。 | 新增 OPL-facing readback / hosted surface / generated caller 缺这些字段族时，只能被 MAS 读作 diagnostic；不得进入 ordinary owner route、provider admission、domain diagnostic apply success、Workbench next action 或 paper progress accounting。 |
| `mas_private_surface_retirement_gate` | `contracts/functional_privatization_audit.json#/retirement_disposition_matrix`、`contracts/private_functional_surface_policy.json#/mas_private_surface_retirement_gate_policy` 与 `contracts/runtime/mas-runtime-surface-retirement-inventory.json` 固定四类归位：OPL primitive、temporary refs projection、retained minimal authority function、tombstone-only。旧 MAS-local runtime-like 面即使仍有 active caller，也只能作为 migration input、refs-only projection、diagnostic 或 tombstone 候选；materializer / dispatch retained surfaces 还必须声明 `retirement_gate`，且 active caller 不能单独作为保留理由。Progress Portal / Workbench 的 `next_system_action` / operator intent 只能来自 canonical `NextActionEnvelope` 派生 owner summary；`current_owner_delta` 只作为 refs projection / drilldown，不能生成 action、provider admission 或 worker attempt。2026-06-19 起，repo-source physical retirement first：active callers 迁移/删除、OPL primitive 或 MAS minimal authority adapter 承接、fail-closed typed blocker、no-forbidden-write / no-resurrection guard、focused/meta/default verification 和 tombstone/provenance 足以删除旧 module / alias / wrapper / compat shim；OPL `DomainProgressTransitionRuntime` / StageRun live readback 只进入 live-runtime readiness 栏。 | 迁移门固定为 replacement parity、active caller migrated/deleted、no-forbidden-write proof、tombstone/provenance、focused regression 和 no compatibility shim / alias / facade / fallback / wrapper test；owner receipt、stable typed blocker、人类 gate、route-back 或 OPL live readback 只能支撑 runtime readiness / paper-line progress / production claim，不再阻塞 repo-source 删除。 |
| `standard_agent_purity_guard` | MAS repo source shape 已按 standard Agent purity 收口；former wrapper tail module ids 只作为 guarded provenance / delete-gate context 存在。 | repo-source 物理删除看 OPL generated/default parity、active caller migrated/deleted、no-forbidden-write、tombstone/provenance 和 focused/meta/default verification；对同一 work unit 的补偿链 live readiness 另按 `stage_native_compensation_retirement_gate` 追 live evidence / same work-unit binding。不新增 compatibility shim、alias、facade 或 wrapper。 |
| `domain_authority_refs_thinning` | storage、artifact、memory、source、owner-route、progress/status helper 只输出 refs、receipts、blockers、locators 或 diagnostic projection input。 | generic locator/index/projection shell 属于 OPL primitive；MAS retained path 只能是 domain authority refs、typed blocker、owner receipt 或必要医学 helper。 |
| `state_index_kernel_adoption` | OPL 持有 StateIndex / read-model / lifecycle kernel；MAS 只保留 `opl_state_index_source_adapter.py` 的 body-free ref metadata。MAS-local pilot、SQLite persistence/inspection/replay、旧 layout/locator 和 compatibility 参数已物理删除；owner-route 与 stage-artifact metadata 消费 adapter，`paper_progress_transition_refs` 则直接保留 body-free JSONL 并交给 OPL `DomainProgressTransitionRuntime`。`runtime_lifecycle_payload_retention` / `runtime_storage_maintenance` 仍是 OPL-authorized maintenance adapter，缺授权只能返回 typed blocker。 | OPL StateIndexKernel live owner readback、cleanup/retention policy live takeover 与 same-identity observability/storage receipt 仍是 runtime/production readiness tail；source-adapter manifest、focused tests 或 no-active scan不能替代。 |
| `state_contract_thinning_guard` | 稳定控制面只承认 `macro_state`、`owner_route`、`receipt_or_blocker` 和 `evidence_refs`；长 reason 和 supersession reason 只能作为 diagnostic detail。 | 新增执行判断必须进入 owner route 或 typed blocker，不得通过扩展 `StudyRuntimeReason`、publication supervisor phase 或 operator 文案绕开 owner ticket。 |
| `physical_source_morphology_guard` | legacy 名称只允许出现在 tombstone、history/provenance、旧状态输入测试、explicit archive/import fixture 或 standard Agent purity guard。 | current docs/tests/callers 不得把旧名读成 active runtime adapter、diagnostic fallback 或 callable alias；发现 generic queue、attempt ledger、scheduler、worker residency、runtime lifecycle owner 语义时按复活旧控制面处理。 |
| `ai_first_quality_record_boundary` | quality gate contract 和 validator 边界已落地；真实 paper-line reviewer/auditor record scaleout 属于证据 tail。 | 程序只做 validator、materializer、receipt signer 和 guard；质量结论必须追到独立 reviewer/auditor invocation、context/task record、quality receipt、route-back 或 typed blocker。 |

## Plan Completion 退役门清单

本轮起，MAS / OPL 私有控制面退役的 Plan Completion Audit 必须把 `repo-source retirement` 和 `live-runtime readiness` 分栏。`docs`、合同、read-model、preflight 分类或 focused tests 通过，只能证明对应的源码/合同约束；它们不能证明 live runtime ready、provider running、paper-line progress 或 production ready。

### 一步到位落地矩阵与完成度审计

本节是后续 Plan Completion Audit 的人工读根。所谓“一步到位”，只表示规划已经把目标态、owner split、OPL 基座落点、MAS 保留面、外部经验吸收路径和完成证据口径放进同一张矩阵；它不表示 live runtime、真实 paper-line、owner receipt、typed blocker、human gate、publication gate 或 artifact authority 已经完成。

完成度必须分两本账：

- `repo/source/control-plane`：源码、合同、schema/readback shape、projection demotion、authority=false、no-active-caller/tombstone/rebuild、replay fixture、forbidden interpretation、default path guard 和 docs/current-plan 入口。该账可以通过 repo-native diff、focused tests、contract/readback shape、static guard 或 `git diff --check` 证明。
- `live acceptance`：fresh `paper-mission inspect` / `study_progress`、domain diagnostic apply/readback、OPL command/event/outbox/StageRun readback、strict running proof、owner receipt、stable typed blocker、human gate、route-back evidence、quality gate receipt、paper/artifact semantic delta、artifact lifecycle receipt、reviewer/auditor receipt 或 production soak。该账不能被 docs、contract、tests、queue empty、projection clean、candidate package 或 refs-only ledger 替代。

Plan Completion Audit 输出时，每一行都必须给出 `status`、`repo/source/control-plane %`、`live acceptance %`、fresh evidence、缺口和下一步。`repo/source/control-plane % = 100%` 只说明结构面关闭；只有同一行的 live evidence 满足后，才能把对应 live claim 标为 `100%`。若只有文档/合同/测试证据，live acceptance 必须写 `0%` 或 `partial`，不能写 `100%`。

| Audit item | Owner split | repo/source/control-plane acceptance | live acceptance | 禁止替代 |
| --- | --- | --- | --- | --- |
| 外部经验吸收 | OPL 吸收 durable execution、outbox、state index、tool/capability/workbench 模式；MAS 只吸收医学 policy、authority answer、reviewer/auditor、source/artifact/memory 边界。 | 外部经验已进入 `MAS / OPL Agent OS`、progress runtime blueprint、capability registry、Tool Arsenal、evidence gap 和 safety envelope 的 repo-native refs / contracts / docs。 | 外部 capability 输出被真实 current owner、reviewer、route-back 或 MAS owner receipt 消费，且保持 refs-only / fail-open / no-forbidden-write。 | reference intake、score、ranking、checklist、advisory refs、contract-only gap。 |
| OPL `TransitionRuntime` | OPL 持有 command/event/fixed-point/NonAdvancingApply；MAS 只提交 DomainIntent / policy result。 | command/event/readback shape、aggregate identity、idempotency、projection metadata、forbidden interpretation 和 replay guard 落地。 | same-identity OPL transition readback 被 MAS owner surface 消费为 owner answer、stable blocker、human gate、route-back 或 paper/artifact delta。 | domain diagnostic dry-run、request-only transition、projection fresh。 |
| OPL `Outbox` | OPL 持有 transactional outbox side effect；MAS 不创建 OPL outbox record。 | outbox identity、event binding、side-effect target、no-owner-answer boundary 和 false-claim harness 落地。 | provider start、MAS owner callable、tool invocation 或 human gate 通过同一 transition outbox 发出并可 read back。 | outbox append、provider completion、queue count。 |
| OPL `StageRunKernel` | OPL 持有 attempt identity、lease、retry/dead-letter、resume、terminal closeout；MAS 只消费 closeout refs。 | StageRun / carrier / route identity / terminal reason / observed-or-missing readback shape 落地。 | live StageRun terminal closeout 以同一 identity 被 MAS consume 成 owner receipt、typed blocker、human gate、route-back 或 progress delta。 | provider completed、checkpoint、stale attempt、cross-identity closeout。 |
| OPL `StateIndex` | OPL 持有 refs-only index、generation、rebuild、current pointer drilldown；MAS 只提供 truth refs 和 authority refs。 | source-adapter、authority=false、observed_generation vs accepted_transition_ref、no MAS private persistence guard 落地。 | OPL StateIndex / workbench readback 能从 accepted event / owner answer 重建 current owner view，且不写 domain truth。 | SQLite presence、read-model freshness、high generation observation。 |
| OPL `Tool Arsenal` | OPL 生成 tool cards、invocation plan、result envelope 和 capability runtime；MAS 声明 authority boundary。 | Agent Tool Arsenal、Scientific Capability Registry、ToolResultEnvelope、hosted consumption evidence shape 和 wildcard guard 落地。 | 真实 current-owner-delta 工具调用产生 refs-only result envelope，被 owner/reviewer/route-back 消费或形成 stable blocker。 | tool list ready、MCP descriptor ready、hosted consumption contract、executor receipt ref。 |
| OPL `Workbench` | OPL 持有 App/workbench shell、trace/drilldown、operator action transport；MAS 持有 current owner truth。 | default cockpit 只读 canonical `NextActionEnvelope` 派生的 owner summary / stable outcome / next gate；`current_owner_delta` 是 projection，audit detail 降为 drilldown。 | 用户可见 action 进入合法 MAS owner action、OPL StageRun 或 human gate，且 readback 不把 UI 可见性写成 progress。 | UI visible、zero worklist、trace visible、operator text。 |
| MAS `Declarative Pack` | MAS 持有 stage semantics、quality gates、skill/knowledge refs、action/receipt/blocker contracts。 | `agent/` pack、stage route/action catalog/quality refs、generated surface input、current-delta-bound affordance 落地。 | Pack 驱动真实 stage owner action，并产出 owner receipt、reviewer receipt、paper/artifact delta、human gate、route-back 或 stable blocker。 | pack compiled、descriptor generated、conformance pass。 |
| MAS `Medical Authority Kernel` | MAS 只保留 source readiness、quality verdict materialization、artifact mutation authorization、memory accept/reject、owner receipt signer、typed blocker materializer 和医学 helper。 | `authority_kernel_inventory`、allowed/forbidden writes、cannot-lift-to-OPL、output refs、retirement/upcollect target 和 no generic runtime owner guard 落地。 | retained function 在真实 owner chain 中签出或消费 owner receipt、typed blocker、quality gate、human answer、route-back、artifact/source/memory authority。 | inventory presence、focused tests、validator pass、helper output without owner consumption。 |
| NextAction/control-plane completion audit | MAS 持有默认 NextAction authority、claim boundary 和 owner consumption；OPL 持有 transition receipt / runtime transport / workbench shell。 | 默认 `StageOutcome -> NextActionEnvelope` authority、缺 envelope fail-closed、synthetic route / false-claim guard、authority-route DM004-style action-family fixture、contract docs 和 current wording 已落地。Existing projection 已有 canonical envelope 时，artifact-first summary / missing-readback fallback / human-approval fallback 不得覆盖它；materializer carrier 和 OPL transition request projection 必须保留 envelope `action_family`，新 study / 新 work-unit 不需要补 exact-id mapping。Historical producer physical deletion 已推进到 `owner_action_diagnostics.repair_progress`、`owner_action_diagnostics.non_advancing_terminal_closeout`、`owner_action_diagnostics.action_types` 和 `owner_action_diagnostics.paper_recovery` 删除 / 迁移；`owner_action_diagnostics/` 空 package marker 已删除；provider-admission study-level legacy current-action fallback producer 和 domain-handler current-control transition-request task producer 已物理删除，缺 canonical envelope 时不能从散落 study current fields 补默认 candidate；PaperRecovery obligation projection 已 fail closed，缺 canonical next-action source 时不再从 top-level legacy action/envelope 补 owner、action 或 work-unit；product/workbench wording 已把 OPL action / owner hints 降为 diagnostic / observed surface。`paper_mission_commands.py`、`runtime_surface_retirement.py` 和 `paper_recovery_state.py` 已回到 preferred line boundary；剩余 legacy diagnostic consumer、旧聚合测试、currentness identity/readback helper 和 repo-wide source morphology 只有在 consumer 迁移/删除、replacement parity、no-forbidden-write 与 tombstone/provenance 证据齐备后，才能作为 source-governance tail 关闭。 | Workbench live 标签只投影同一 envelope / OPL receipt / MAS owner consumption，且 DM002/DM003 或下一条真实 paper line 出现 same-identity owner/live acceptance。 | docs/tests、`scripts/verify.sh`、candidate package、refs-only ledger、queue empty、projection clean、provider completion、UI visible、wording clean。 |
| Live paper-line acceptance | MAS 是 paper authority owner；OPL 是 runtime transport owner。 | harness、work orders、rollup、identity gate、false live-claim boundary 落地。 | DM002/DM003 或下一条真实 paper line 出现 same-identity `drive -> OPL intake -> StageRun/provider -> typed closeout -> MAS consume -> next owner answer` 证据。 | docs/tests、candidate package、refs-only ledger、queue empty、projection clean、provider completion。 |

当前矩阵状态：`repo/source/control-plane` 的主要 authority demotion 与 no-resurrection guard 已落地，包括 OPL transition receipt bridge、legacy next-action authority retirement、owner-action source thinning、delivery `current_package` readback projection、typed-blocker-resolution 非 authority apply surface、canonical blocked-typed successor bridge、unsupported-dispatch legacy selector guard、provider-admission study-level fallback producer 删除、PaperRecovery obligation fallback fail-closed 和 DM004-style 新 study / 新 work-unit action-family route fixture。Legacy stage-native next-action、materializer exact-id registry fallback、provider-admission study current-action fallback，以及已删除 / 迁移的 `terminal_next_forced_delta` / `owner_action_diagnostics.repair_progress` / `owner_action_diagnostics.non_advancing_terminal_closeout` / `owner_action_diagnostics.action_types` / `owner_action_diagnostics.paper_recovery` 不能按 active producer 读取。更激进的 legacy `current_work_unit` / `current_execution_envelope` / currentness identity/readback helper / provider-admission diagnostic consumer 物理删除仍是 source-governance residual：有保留价值的行为必须迁到 canonical envelope、owner-consumption readback 或 focused guard，否则随对应 diagnostic consumer 一并退役；未迁移/删除前不得写全仓 `100%` physical deletion。`paper_mission_commands.py`、`runtime_surface_retirement.py`、`paper_recovery_state.py`、`paper_mission.py`、`existing_projection_refresh.py`、`mission_summary.py`、`provider_admission_projection.py`、`domain_action_request_lifecycle.py`、`projection_payload_assembly.py`、`provider_admission_current_control.py` 和 `opl_provider_attempts.py` 已完成当前主要入口 line-budget split；repo-wide source morphology 仍以剩余 3 条 advisory 为尾项。该结论只支撑 repo/control-plane 完成度审计与 legacy producer authority demotion，不改变 `live acceptance` 总账，也不证明 DM002/DM003 submission-ready、publication-ready 或 governed acceptance。

### 私有控制面退役门

| Gate | Repo-source retirement 100% 所需 fresh evidence | Live-runtime readiness 读法 |
| --- | --- | --- |
| `no_active_caller_or_migrated_caller` | repo scan、contract/static guard、OPL generated default-caller report 或等价 source evidence 证明旧 MAS surface 没有 default/runtime caller，或所有 caller 已迁到 OPL primitive / MAS minimal authority adapter / fail-closed typed blocker。 | live every-active-caller soak、OPL takeover 或 no-active production caller proof 另账；缺失时只能阻止 runtime-ready / production-ready claim，不阻止源码删除。 |
| `replacement_parity` | OPL primitive / generated surface / MAS minimal authority adapter 消费同一 refs，并保持 MAS owner-answer boundary；缺 proof 时 fail closed typed blocker。 | 真实 current-owner-delta consumption、StageRun readback、owner receipt、route-back 或 human gate 是 live readiness evidence，不回填为 repo-source 阻塞项。 |
| `no_forbidden_write_proof` | replacement / projection 证明没有写 study truth、memory body、artifact body、`publication_eval/latest.json`、`controller_decisions/latest.json`、`current_package`、owner receipt 或 typed blocker。 | live apply / provider / production path 仍需单独证明 no-forbidden-write；测试绿不等于 runtime path 已覆盖。 |
| `tombstone_or_provenance` | 删除旧入口前已有 history / tombstone / provenance ref，且 active docs 不再把旧名写成 current adapter / callable alias。 | tombstone 不能声明 live takeover、paper progress、provider running 或 production readiness。 |
| `compat_surface_removed` | 旧 module / alias / wrapper / compat shim / 只保护旧路径的兼容测试已删除，或只剩 history/provenance/negative-test mention。 | live owner receipt、stable typed blocker、human gate、route-back evidence 或同一 current identity 的 OPL StageRun readback用于后续 runtime readiness，不作为 compat 删除前置条件。 |

## Ready / Publication 声明边界

以下内容不作为本文 active gap / next prompt 的默认工作清单维护：真实 paper-line provider apply、DM002/DM003 fresh outcome、owner-chain dispatch ledger scaleout、publication-route memory receipts、artifact lifecycle receipts、human gate / resume、provider SLO long soak、family transition live receipt、independent reviewer/auditor record scaleout、真实 `current_package` handoff 和 submission-ready gate。

这些 live evidence 只在声明 paper progress、publication-ready、domain-ready、artifact mutation authorization、`current_package` 更新、provider running 或 production-ready 时作为 hard gate。日常开发优先处理上方 `当前功能/结构差距`：source morphology、repo-source/control-plane cleanup、legacy path no-resurrection、no-second-truth guard、minimal authority function 与 OPL transition ABI 边界。Live evidence 缺失不反向重开已关闭的 repo/source/control-plane backlog；focused tests、projection clean、queue empty、contract pass 或 docs foldback 也不能替代 live evidence。

## MAS/OPL Stage 解耦风险彻底解决方案

本节回答当前 MAS/OPL 重构的主要风险如何彻底解决。目标不是新增第二套控制面，而是把每个风险收敛到一个可执行的 owner surface、一个机器合同、一个 fail-closed gate 和一组 live evidence。Clean Architecture 口径固定为：MAS 持有医学 policy / authority；OPL 持有 runtime detail / transport；所有跨边界数据使用 plain refs / command / receipt model，不能让 queue、provider、UI、read-model 或 adapter 反向拥有医学裁决。

### 目标态

```text
MAS PaperMissionTransaction
  -> MAS StageTerminalDecision
  -> OPL RouteCommand
  -> OPL StageRun / provider attempt / retry / resume / dead-letter
  -> OPL typed closeout readback
  -> MAS authority consume
  -> OwnerReceipt / TypedBlocker / HumanGate / RouteBack / ProgressDeltaReceipt
  -> next StageTerminalDecision or terminal stop
```

彻底解决的定义：任意 stage 结束后，OPL 不需要询问 MAS “医学上算不算完成”，只消费 MAS 已签出的结构化终态；MAS 也不读取 OPL queue / provider completion 作为医学完成，只读取同一 transaction identity 的 OPL closeout evidence，再由 MAS authority consume 产出下一 owner answer。

### 风险到修复矩阵

| risk | 根因 | 永久修复 | owner | 落地面 | 完成证据 |
| --- | --- | --- | --- | --- | --- |
| `provider_completion_as_stage_success` | OPL provider completion 与 MAS stage closeout 曾被读成同一个结果。 | 新增并强制 `StageTerminalDecisionPromotionGate`：OPL 只能消费 MAS `StageTerminalDecision` 派生的 `OPL RouteCommand`；provider completion 只允许进入 `runtime_closeout_evidence`，不能改 `decision_kind`。 | MAS contract + OPL runtime | `contracts/paper_mission_transaction_contract.json`、`contracts/opl_domain_progress_transition_runtime_contract.json`、OPL family-runtime intake / tick / closeout readback | Same transaction 下 provider terminal closeout 被 MAS consume 后，只能生成 owner receipt、typed blocker、human gate、route-back 或 progress delta；没有 MAS consume 时 fail closed。 |
| `candidate_or_ledger_as_authority` | candidate package、consume ledger、route handoff 是可接力证据，但容易被误写成 owner receipt 或 typed blocker authority。 | 建立 `AuthorityMaterializationGate`：candidate / ledger 永远保留 `authority_materialized=false`；只有 MAS authority consume 写出 accepted owner shape，才进入 authority result。 | MAS authority kernel | `paper-mission drive`、`package-candidate` owner packet/checklist required refs、`consume-candidate --output-root`、authority result adapter | Candidate package、consume ledger、route handoff 均可被 OPL intake；但 publication eval、controller decision、owner receipt、typed blocker authority、human gate、current package 均未被候选层写入。 |
| `self_review_quality_gate` | executor 可能把自己产出的 summary 当作 quality verdict。 | `ReviewerInvocationSeparationGate`：quality gate 必须引用独立 reviewer / auditor invocation、task/context record 和 quality receipt；同一 executor/context 输出只能是 execution receipt。 | MAS Quality OS | stage quality contract、AI-first private authority validator、reviewer/auditor receipt schema | 缺独立 reviewer/auditor receipt 时，write/review/finalize/journal-resolution stage 只能 route back 或 typed blocker，不能 pass。 |
| `legacy_path_resurrection` | 旧 domain diagnostic / owner-route / default dispatch / PaperRecovery 仍有 diagnostic / ABI / fixture 残留，可能被旧 consumer 当主线。 | `DefaultPathNoResurrectionGate`：product-entry、domain-handler default task、MCP manifest、action catalog、skill ordinary path 只能指向 `paper_mission/start_or_resume` / `PaperMissionTransaction`；legacy task 只允许 diagnostics / migration / provenance，manual dispatch fail closed。 | MAS product-entry + domain-handler | `contracts/runtime/legacy-active-path-tombstones.json`、domain-handler export/dispatch、action catalog、agent tool arsenal | 默认面扫描无 legacy paper mainline；手工 legacy dispatch 返回 tombstone receipt；旧引用只剩 allowed reference class。 |
| `stale_or_cross_identity_handoff` | OPL closeout、MAS transaction、work unit、route command 或 current owner delta 不同 identity 时可能误接力。 | `TransactionIdentityGate`：所有 route handoff、runtime carrier、StageRun readback、owner answer 必须绑定 `study_id`、`transaction_id`、`work_unit_id`、`work_unit_fingerprint`、`route_identity_key`、`attempt_idempotency_key`。缺任一字段或不匹配直接 typed blocker。 | MAS + OPL shared ABI | `PaperMissionTransaction`、`opl_runtime_carrier`、OPL StageRun readback、live evidence rollup | Same identity 的 live closeout 才能进入 MAS consume；cross-identity / stale closeout 只能进入 diagnostic，不改变 current decision。 |
| `human_gate_bypass_or_overblocking` | “不额外沟通”容易被误解为人类 gate 也不需要，或普通人工注释阻断自动推进。 | `HumanGateTransportGate`：只有 MAS owner surface 判定 human gate boundary 命中时才阻断；OPL 只运输 question / required receipt / resume token，不能自己审批或阻断。 | MAS human gate + OPL transport | `StageTerminalDecision(decision_kind=human_gate)`、human gate receipt / resume chain | 人工注释不阻断自动推进；真实 human gate 有 question、required receipt、owner、resume path 和 no-forbidden-write proof。 |
| `projection_or_ui_as_truth` | workbench/progress portal/read-model 可能把展示状态变成 current truth。 | `ReadModelProjectionBoundaryGate`：Portal / Workbench / Progress projection 只显示 refs、freshness、blocker、next owner；所有写入必须回到 MAS owner surface 或 OPL runtime command。 | MAS projection + OPL App | progress portal、workbench projection、stage artifact index | UI / projection 不写 MAS truth、不写 OPL StageRun、不生成 owner receipt；点击或 action 只返回 domain-handler / OPL route handoff ref。 |
| `sidecar_or_capability_as_admission_gate` | Co-Scientist / EvoScientist / Capability Registry 等 sidecar 可能变成默认前置流程。 | `JITAffordanceFailOpenGate`：sidecar 只能在 current delta 显式需要时产生 refs-only advisory；缺失、失败、低置信不得阻断 owner action，除非命中 source/data/authority/human hard gate。 | MAS current owner + OPL capability runtime | Scientific Capability Registry、Agent Tool Arsenal、external-learning sidecar | Sidecar output 被 owner receipt / reviewer receipt / route-back refs 消费后才计入 progress；未消费只算 advisory，不算 stage closeout。 |

### 2026-06-25 落地状态

`StageTerminalDecisionPromotionGate + TransactionIdentityGate` 的 repo/source/control-plane hardening 已落地到 MAS `main` 与 OPL `main`，但 live runtime readiness 仍另账。

- MAS `main` 已把 `PaperMissionTransaction`、OPL carrier、candidate consumption readback、stage quality / human gate / portal-workbench / capability fail-open 边界收紧到同一 transaction / study / stage / route identity；candidate、consume ledger、route handoff 和 read-model projection 继续固定为 no-authority surface。
- OPL `main` 已在 `paper_mission/stage-route` intake / runner / terminal sync / linked liveness 中要求并传播 `study_id`、`paper_mission_transaction_ref`、`opl_route_command_ref`、`command_kind`、`route_target`、`route_identity_key`、`attempt_idempotency_key` 和可选 `request_idempotency_key`；缺关键 identity 时 fail closed，且不创建 StageAttempt。
- 当前 fresh repo evidence：MAS focused suites、`make test-meta` 与 `scripts/verify.sh` 已通过；OPL `node --test` focused suite、`npm run typecheck` 已通过，默认 `scripts/verify.sh` 作为最终 closeout evidence 另跑。上述证据只证明代码、合同和 readback shape 已收口，不能声明 provider running、live runtime ready、paper progress、owner receipt、typed blocker authority file、human gate、publication-ready 或 production-ready。

### 实施顺序

1. **统一终态合同**：把 `StageTerminalDecision` 作为唯一 stage 终态输入，禁止 OPL 从 provider completion、transport queue empty、read-model status 推导医学完成。
2. **收紧 OPL intake**：OPL `family-runtime enqueue/tick/closeout` 只接受 `OPL RouteCommand` 和 `opl_runtime_carrier`，并在 StageRun readback 中回写同一 transaction identity。
3. **补 MAS consume 闭环**：MAS `paper-mission drive` / `consume-candidate` / authority consume 读取 OPL closeout 后，必须产出 owner receipt、typed blocker、human gate、route-back 或 progress delta 中一种；no-op 只能是 stable typed blocker、stop-loss 或 carry-forward receipt。
4. **删除默认旧路径入口**：所有默认 product-entry / domain-handler / MCP / skill / action catalog 入口只保留 `paper_mission/start_or_resume` 与 transaction path；旧 domain diagnostic / dispatch / recovery 保留为 bounded diagnostics 或 tombstone。
5. **上线 identity rollup**：同一 `study_id + transaction_id + work_unit_fingerprint + route_identity_key + attempt_idempotency_key` 是 runtime readback、owner answer 和 live evidence rollup 的最小比较单位。
6. **质量 gate 独立化 scaleout**：write/review/finalize/journal-resolution 的 stage closeout 必须能引用独立 reviewer/auditor record；缺失时不允许 quality pass，只允许 route-back / typed blocker / human gate。
7. **生产 live soak**：选择 DM002/DM003 或下一条真实 paper line 跑一条同 identity 链：`drive -> OPL intake -> StageRun/provider -> typed closeout -> MAS consume -> next owner answer`。

### 验收标准

| gate | 必须通过的验收 |
| --- | --- |
| contract gate | `PaperMissionTransaction`、`StageTerminalDecision`、`OPL RouteCommand`、`opl_runtime_carrier` schema / validator / focused tests 通过；contract 明确 provider completion 不是 domain completion。 |
| default path gate | product-entry、domain-handler export、MCP manifest、action catalog、plugin skill ordinary path 没有 legacy paper mainline；legacy dispatch 手工调用 fail closed。 |
| identity gate | cross-identity OPL closeout、stale route command、缺 fingerprint 的 handoff 均无法进入 MAS consume 成功路径。 |
| authority gate | candidate / ledger / route handoff 不写 authority surfaces；只有 MAS consume 后的 owner receipt / typed blocker / human gate / route-back / progress delta 可改变下一 owner。 |
| quality gate | 需要质量判断的 closeout 必须引用独立 reviewer/auditor receipt；同一 executor summary 不能关闭 quality gate。 |
| live runtime gate | 至少一条真实 paper-line same-identity 链产生 StageRun readback，并被 MAS consume 为下一 owner answer；provider completion、transport queue empty、projection clean 不得被写成 progress。 |
| no-forbidden-write gate | 整条链证明不写 study truth、paper body、publication eval、controller decision、current package、owner receipt body 或 typed blocker authority，除非对应 MAS authority surface 明确授权。 |

### 停止条件

- `done_for_contract_and_readback_shape`：只表示 repo contract、validator、CLI/read-model 和 default path 已收口。
- `done_for_opl_intake_smoke`：只表示 MAS route command 可进入 OPL queue / StageAttempt admission。
- `done_for_live_stage_closeout`：必须有同 identity OPL StageRun/provider terminal closeout readback。
- `done_for_mas_authority_consume`：必须有 MAS consume 产出的 owner receipt、typed blocker、human gate、route-back 或 progress delta。
- `production_ready`：必须同时满足 live runtime gate、authority gate、quality gate、no-forbidden-write gate、App/workbench drilldown 和 restart/re-query/retry/dead-letter soak；任何单项缺失都只能报 `partial` 或 typed blocker。

### 当前下一步

`StageTerminalDecisionPromotionGate + TransactionIdentityGate` 已从“近期先做”变成 repo/source/control-plane 已落地项。下一步不再继续扩写 read-model 或新增平台预检，而是进入 production live soak / owner-consumption evidence lane：选择 DM002/DM003 或下一条真实 paper line 跑同一 identity 的 `drive -> OPL intake -> StageRun/provider -> typed closeout -> MAS consume -> next owner answer`，并用 owner receipt、stable typed blocker、human gate、route-back evidence、paper/artifact delta 或 strict running proof 证明 live lane。缺这些 live evidence 时，只能报 `partial_live_tail_open`，不能把本轮 repo hardening 写成 runtime-ready 或 paper progress。

### 2026-06-28 OPL readback parity 与 handoff 完成度口径

Stage Closure Terminalizer 的 `contract / reducer / delivery split` 已按 repo/source/control-plane 功能面落地。当前 active plan 不再要求为 OPL readback parity 新建 MAS 平台；最低 contract 是消费或要求 OPL StageRun / closeout / attempt ledger 提供以下字段：`identity`、`attempt_idempotency_key`、duration observed-or-missing、token/cost observed-or-missing 和 `terminal_reason`。缺字段时只能形成 observability / stale-readback / cross-identity gap，不能用 checkpoint、provider completed、transport queue blocked、0 值或估算值补齐。

完成度/验收分账固定为三层：

| layer | 当前完成度读法 | Fresh evidence | 禁止替代 |
| --- | --- | --- | --- |
| `repo_control_plane_structural_completion` | docs/source/contract/readback shape 可按 repo evidence 关闭。 | 当前 checkout 的 contracts/source/docs diff、focused/meta/default verification 和 no-forbidden-write proof。 | live paper acceptance、owner receipt、publication-ready、runtime-ready。 |
| `physical_deletion_residual` | 旧源码/别名/wrapper 物理退役只看 active caller migration、replacement parity、tombstone/provenance。 | caller audit、deleted/thinned source diff、tombstone owner 和 no-active-default-caller proof。 | cross-repo live soak、真实 provider proof、paper-line outcome。 |
| `synthetic_fixture` | canary / fixture 只证明防回归或形状固定。 | focused test/canary output、fixture source refs 和 explicit non-live boundary。 | DM002/DM003 当前 blocker 解除、live acceptance、paper progress。 |
| `cross_repo_live_evidence` | live readiness / paper progress 只能由真实 owner/runtime evidence 关闭。 | fresh `paper-mission inspect` / `study progress`、OPL StageRun / TransitionReceipt readback、owner receipt、stable typed blocker、human gate、route-back、artifact delta 或 publication gate proof。 | docs、contracts、focused tests、queue state、synthetic fixture。 |
| `current_package_handoff` | repo split 已落地；live handoff 仍 open。 | `delivery-inspect`、`paper-mission inspect`、`study progress` 或 workspace manifest 证明 current source signature 已刷新到 `manuscript/current_package` / zip，且 `package_kind=current_package`、known blockers、`can_submit=false unless gate passed` 明确。 | checkpoint、candidate package、inspection package、focused tests、queue state、read-model freshness。 |
| `degraded_or_pre_submission_handoff` | terminalizer runbook 已定义；live package / handoff evidence 仍按 paper line 单独验证。 | repair budget exhausted 后产生 degraded / pre-submission handoff ref，携带 known blockers、next owner、resume condition 和 `can_submit=false`，并证明同义 repair loop 停止。 | durable final、submission-ready、quality gate passed、human approval complete。 |
| `submission_ready` | gate 清理仍 open。 | publication gate passed 或 MAS authority snapshot 明确授权 `bundle_build_allowed=true` / final package build，且 final manifest 可追踪 owner / gate evidence。 | current mirror、degraded handoff、checkpoint、tests、queue empty、provider completion。 |

因此当前 plan status 是 `done_for_repo_contract_reducer_delivery_split` + `partial_live_current_package_handoff_open` + `submission_ready_gate_open`。后续 completion audit 必须逐层报告，不得把某一层的 evidence 升级成另一层完成；尤其不得把 repo/control-plane structural completion、physical deletion residual cleanup 或 synthetic fixture 写成 live paper acceptance。

## 下一轮 Agent prompt

- Write scope: MAS active truth owner、source morphology、repo-source/control-plane cleanup、legacy path no-resurrection、minimal authority function、OPL transition ABI/policy adapter 边界和 docs lifecycle foldback。当前已拆出 `paper_mission_commands.py`、`runtime_surface_retirement.py`、`paper_recovery_state.py`、`paper_mission.py`、`existing_projection_refresh.py`、`mission_summary.py`、`provider_admission_projection.py`、`domain_action_request_lifecycle.py`、`projection_payload_assembly.py`、`provider_admission_current_control.py` 和 `opl_provider_attempts.py` 的主要结构边界，并物理删除 provider-admission study-level fallback producer；后续 source morphology cleanup 必须先 fresh-run line-budget / caller map，再按自然责任边界选择剩余 3 条 advisory，已有 dirty Python 写集存在时不得覆盖或顺手吸收。
- Non-goals: 不把 OPL provider proof、suite pass、transport queue completion、refs-only receipt、
  product-entry projection、doctor pass 或 descriptor readiness 写成 MAS paper closure、
  publication-ready、domain-ready、artifact mutation authorization、memory writeback success
  或 `current_package` 更新；不恢复旧 private runtime、retired alias、facade、wrapper 或
  fallback 文案。
- Live truth inputs: 读取 MAS `agent/` pack、contracts、owner-route/domain-handler
  exports、runtime/controller/read-model surfaces、product-entry manifest、focused tests、
  source callers、OPL transition ABI / policy adapter contracts 和 docs/history/tombstone refs；
  dated closeout、receipt id 和旧 worklist 数字只作 provenance。
- Required actions:
  1. Fresh-read line-budget advisory 和 source/caller map，选择仍超过 preferred boundary 且有自然责任边界的剩余文件；确认本轮拆分没有新增 fallback、compat shim、第二套 authority 或 forbidden write path，写入前确认同写集没有其他 owner dirty diff。
  2. 维护 Co-Scientist-inspired Stage / Route、EvoScientist sidecar、Scientific Capability Registry 等 landed surface 的 active 文档口径：它们只作 JIT advisory / fail-open accelerator / refs-only support，不成为 admission blocker、quality closure 或 paper progress。
  3. 对 read-model/currentness/dispatcher/lifecycle apply 类问题，先判断是否只是 platform repair；没有同步产生 domain delta 或 stable typed blocker时，只能记录为 platform repair，并把下一 owner 指回 MAS paper/artifact/reviewer/human gate 或 OPL generic lifecycle apply。
  4. 对 currentness / owner-route / owner receipt / provider admission 反复复发类问题，
     默认按 `OPL DomainProgressTransitionRuntime + MAS PaperProgressPolicyAdapter` 目标态处理：
     通用 event log / fixed-point controller / outbox / replay / projection metadata 放到 OPL；
     MAS 只实现 paper-specific policy、authority result shape 和 forbidden-write 语义。新增修复
     必须推进到 OPL runtime contract、MAS policy adapter、historical replay fixture、projection
     consume-only 或 `NonAdvancingApply` typed blocker 之一；不要继续在 domain diagnostic、
     `current_work_unit`、`current_execution_envelope`、`paper_recovery_state`、provider admission、study_progress 或
     Workbench 中新增平行 priority / stale guard 来掩盖同一根因。
  5. 对 terminal handoff / provider admission / recovery successor 交织问题，repo-side projection
     refresh 必须在 handoff 消费后重建 recovery state、current executable action、provider projection
     和 admission sync；当 `provider_admission_terminal_closeout_consumed` 与 handoff current
     identity 重合且无冲突时，即使 current_work_unit 仍是 `owner_receipt_recorded`，也必须优先
     投影 stable typed blocker / domain_blocked；不得保留 stale successor 或写成 provider-ready。
  6. 对文档治理，active/core 文档只保留 current conclusion、open gate、owner、
     machine boundary 和下一步；过程 proof、receipt id、命令流水和 dated worklist 数字继续归
     history/provenance。
- Verification commands: docs-only 维护运行 `rtk git diff --check`、
  `rtk rg -n "^(<<<<<<<|=======|>>>>>>>)" docs README.md README.zh-CN.md contracts tests` 和必要的
  docs/path inventory；触及 StageRun profile / canary contract 时至少运行
  `rtk scripts/run-pytest-clean.sh tests/test_stage_run_kernel_profile_contract.py`；
  触及 broader source/contracts/runtime 时运行 `rtk scripts/verify.sh`
  及对应 focused `scripts/run-pytest-clean.sh ...`、`make test-meta` 或 OPL read-model check。
- Completion gate: 本轮输出必须归类为 `source_morphology_cleanup`、`repo_source_control_plane_cleanup`、`legacy_no_resurrection_guard`、`policy_adapter_boundary_landed`、`docs_ssot_foldback`、`platform_repair` 或 `needs_owner_decision_for_irreversible_cleanup`；platform repair 不写成 paper-line progress 或 production evidence closure。Open paper-line、memory/artifact/lifecycle、human gate/resume、provider SLO long-soak 和 independent reviewer/auditor record 不作为本文 active backlog。
- Foldback target: durable 当前结论折回本文、核心五件套、runtime/contracts/policies 或
  owner docs；dated proof、receipt id、OPL worklist 数字、命令流水和 closeout 过程折回
  `docs/history/**`、runtime ledger、真实 workspace receipt 或提交历史。

## Stage-Native Kernel 完成后的下一层 TODO

本节只在 OPL Stage-Native Artifact Kernel 已完成后作为下一轮 active backlog 读取。完成条件包括：OPL 持有统一 stage folder contract，`stage.json`、`attempt.json`、`manifest.json`、`receipt.json`、`current.json` schema 已落地；`stage_artifact_index` 已降级为 derived projection；MAS/MAG/OMA/RCA 只提供 declarative domain pack 和最小 authority functions；controller/read-model/currentness 只做 repair、projection 和 diagnostic，不再决定 current stage。

2026-06-03 follow-through：MAS 当前已消费 OPL physical Stage Folder Kernel，并把 `stage_kernel_projection` 作为 Progress Portal / Workbench primary progress 的派生输入。当前完成口径提升为：physical stage folder 只有在 `current.json` 指向 latest attempt、manifest / owner receipt / domain decision receipt 成立、consumability gate 通过、lineage events / graph 可定位、retention / restore refs 覆盖时，才可被投影为 current artifact progress。`stage_artifact_index` 继续是 rebuildable diagnostic projection；缺 semantic receipt、stale current pointer、缺 retention / restore 或缺 lineage 时必须 fail closed，并产生下一 owner / blocker，而不是退回 controller/read-model currentness。

2026-07-10 operating-layer follow-through：StateIndex refs 已统一经 body-free `opl_state_index_source_adapter` 交给 OPL owner，MAS-local rows projection 已删除。`stage_kernel_projection` 保留 current pointer promotion audit 与 lineage / retention drilldown；Progress Portal / Workbench 可消费 OPL StateIndex readback，并投影 MAS/MAG/OMA/RCA cross-domain soak summary。`mas_stage_semantic_receipts` 提供 body-free semantic receipt validator，覆盖 source readiness、reviewer quality、publication gate、artifact package authority、memory accept / reject、typed blocker 与 medical owner receipt。上述 surface 都是只读 projection / validator / audit helper：不写 `current.json`、MAS study truth、canonical paper body、`publication_eval/latest.json`、`controller_decisions/latest.json`、artifact body、memory body 或 submission package；也不声明 publication ready、submission ready、quality ready 或 artifact mutation authorization。

完成上述重构后，剩余风险不再是 controller/read-model 阻塞 MAS 推进，而是 Stage Kernel 自身是否能长期承担 artifact operating layer。下一轮 TODO 按以下顺序处理：

1. `manifest_receipt_semantic_validation`：repo-side validator 已落地为 `src/med_autoscience/controllers/mas_stage_semantic_receipts.py`，只校验 `receipt_ref`、schema refs、capability refs 和 domain semantic refs；格式合法但语义缺失时 fail closed 或形成 typed blocker。后续证据尾项是更多真实 paper-line receipt 进入该 shape。
2. `current_pointer_promotion_model`：repo-side audit helper 已落地为 `src/med_autoscience/controllers/opl_stage_promotion_runtime.py`，显式检查 `attempt output -> manifest valid -> receipt accepted -> current pointer promoted -> projection rebuilt`，并覆盖 partial commit、rollback、stale pointer、orphan output 和 historical pointer tombstone。它只读 audit，不提升 projection 为 current pointer 写权限。
3. `legacy_stage_taxonomy_migration`：repo-side read-model 已落地到 `stage_artifact_index`。顶层 `legacy_taxonomy_migration` 继续持有旧 MAS `scout/idea/baseline/experiment/analysis-campaign/write/review/finalize` 到 paper/study artifact stages 的 migration manifest / role mapping；每个 stage state 现在额外投影 `legacy_taxonomy_migration_read_model`，包含 `migration_status`、`backfilled_current_pointer`、`tombstone_or_provenance_*`、`workbench_dual_truth_forbidden` 和 fail-closed `next_owner_action`。缺 current pointer backfill 或 tombstone/provenance 时只投影下一 owner/action，不把 legacy route 显示成 current truth；Workbench 仍只能显示 `paper_study_stage_pack` 单一 current truth。开放证据尾项是真实 workspace / Workbench soak 继续证明这些字段被消费且不声明 paper progress。
4. `lineage_graph_unification`：repo-side drilldown 已落地为 `src/med_autoscience/controllers/opl_stage_lineage_retention.py`，按 `stage_attempt_manifest_receipt_current_pointer` event model 汇总 lineage events、lineage graph、manifest、receipt、current pointer、restore proof 和 retention policy。UI / controller / read-model 只能消费该 refs-only family，不另造 provenance。
5. `artifact_consumability_gate`：物理文件和 content hash 只证明 artifact 存在且未变；能否进入下一 owner 还必须检查 input role、source/current truth、receipt authority、lineage、retention/restore policy 和 domain validation。
6. `workbench_information_hierarchy`：Progress Portal / Workbench 已把 `stage_operating_layer` 固定为 primary progress，并把 OPL StateIndex readback、promotion audit、lineage / retention、cross-domain soak 与 provider liveness 分层展示；repair/currentness/telemetry/evidence-tail 仍是 secondary diagnostics，不能被 UI 写成 progress 或 readiness。
7. `retention_restore_gc_protocol`：repo-side retention gate 已 fail closed：projection 永远不授权 cleanup，缺 restore contract、receipt 指向不可恢复 artifact 或 current pointer 指向 orphan output 都进入 blocker / drilldown。真实 cleanup / restore apply 仍必须等待 MAS artifact authority 或 OPL owner-authorized receipt。
8. `cross_domain_soak`：Workbench read-only projection 已覆盖 MAS/MAG/OMA/RCA lane shape、running、no-live-run、blocked 和 stale-controller-with-artifact-delta 分类。通过真实 domain lane 重建 DB、App/workbench、artifact gallery、stage_progress_log 和 next owner 仍是 evidence tail，不由 fixture 或 repo tests 声明 domain readiness。

下一层完成口径：OPL 成为 artifact operating layer；Stage Kernel 负责事实重建，Domain Authority 负责裁决，Policy Engine 负责权限，Workbench 只读展示派生视图。任何 index、controller、read-model 或 App surface 重新获得第一真相权，都按架构回归处理。

## 验证与完成门

Docs-only 维护：

- `rtk git diff --check`
- `rtk rg -n "^(<<<<<<<|=======|>>>>>>>)" docs README.md README.zh-CN.md contracts tests`
- 必要的 docs/path inventory 检查。

触及 source/contracts/runtime 时才追加：

- `rtk scripts/verify.sh`
- 按触及面追加 focused `scripts/run-pytest-clean.sh ...`、`make test-meta` 或对应 owner-route / domain-handler / product-entry smoke。
- StageRun profile / canary contract 维护至少运行 `rtk scripts/run-pytest-clean.sh tests/test_stage_run_kernel_profile_contract.py`。
- 消费 OPL refs 时追加 OPL 侧 read-model check：`rtk opl agents interfaces --repo-dir /Users/gaofeng/workspace/med-autoscience --json`、`rtk opl framework readiness --family-defaults --json`、`rtk opl runtime app-operator-drilldown --json`。

Completion gate:

- 本轮输出能归类为 domain delta / stable typed blocker / platform repair；platform repair 没有被写成 paper-line progress 或 production evidence tail closure。
- 仍 open 的真实 paper-line、memory/artifact/lifecycle、human gate/resume、provider SLO long-soak 和独立 reviewer/auditor record 回 owner evidence surface，不在本文维护。
- 新增或修改的 owner receipt / typed blocker / evidence packet 有 live refs、focused verification 和 no-forbidden-write proof；OPL transport/read-model 证据没有被升级为 MAS domain verdict。
- worktree lane 已吸收回 `main` 或明确标记为近期写入/有未提交改动而保留；最终在 `main` checkout 完成最小充分验证。

## 历史索引

| 需要追溯的内容 | 当前入口 |
| --- | --- |
| 当前状态摘要 | [当前状态](../status.md) |
| 架构与 owner 边界 | [架构概览](../architecture.md) |
| 不可变约束 | [不可变约束](../invariants.md) |
| 日期决策日志 | [关键决策记录](../decisions.md) |
| north-star 目标态 | [MAS 理想目标态](../references/positioning/mas_ideal_state.md) |
| 下一 `/goal` 执行规格 | [Co-Scientist Stage / Route 重构设计与执行规格](../runtime/designs/coscientist_stage_route_restructure.md) |
| 当前内容线索引 | [MAS 当前开发线路](./current-development-lines.md) |
| Progress-first evidence gap policy | [Progress-First Evidence Gap Policy](../runtime/designs/progress_first_evidence_gap_policy.md) |
| 文档生命周期治理 | [MAS 文档组合治理](../docs_portfolio_consolidation.md) |
| docs lifecycle coverage | [MAS Docs Portfolio Coverage Ledger](../history/docs-portfolio-coverage-ledger/README.md) 与 [MAS broader docs portfolio SSOT closeout 2026-06-07](../history/program/mas_broader_docs_portfolio_ssot_closeout_2026_06_07.md) |
| 2026-05 标准 Agent 文档过程归档 | [MAS standard agent 文档过程归档 2026-05](../history/program/mas-standard-agent-doc-process-history-2026-05.md) |
| 历史完成台账 | [Plan Completion Ledger](../history/program/plan_completion_ledger.md) |

## Active / History Foldback

| 内容 | Foldback target | 规则 |
| --- | --- | --- |
| dated closeout、attempt / receipt id、命令流水、旧 phase checklist、OPL worklist 数字 | `docs/history/program/` 或对应 `docs/history/<area>/` | 只保留 provenance；不得作为 current truth 或 active backlog。 |
| 已被 current owner surface 替代的旧 runtime/workbench/scheduler/alias 文案 | `docs/history/**` tombstone / provenance，或 machine-readable tombstone refs | active docs 只写当前 owner、删除门和 standard Agent purity guard。 |
| 已关闭功能/结构 gap | 本文 `已落地`，必要时加 compact tombstone pointer | 不保留旧 checklist chronology。 |
| ready / publication 声明证据 | owner evidence surface / runtime ledger | 不回写成结构未完成，也不写成 production/domain ready。 |

## 当前不能写成

- 不能把 OPL provider proof、suite pass、queue completion、refs-only ledger receipt 或 provider completion 写成 MAS paper closure、publication-ready、domain-ready、medical-ready、artifact mutation authorization 或 `current_package` 更新。
- 不能把 MAS production acceptance receipt 写成具体论文线 publication-ready；它只承认 structural / physical conformance 与 production-like receipt chain 的边界。
- 不能把 `mas_owner_receipt_present`、stable blocker、body-free evidence packet 或 OPL workorder preflight 写成 workspace mutation、artifact authority 放行、memory writeback success 或 paper closure。
- 不能把 MAS 已经没有任何私有程序面写成绝对清零；准确口径是私有面已收敛为 declarative pack / generated surface handoff、domain authority refs、minimal authority function 或 explicit tombstone/provenance refs。
- 不能把旧 `runtime_transport/`、turn runner、worker lease、runtime lifecycle SQLite、`lifecycle_refs_adapter.py`、MDS/DeepScientist、Hermes、local scheduler、workspace wrapper、`mas_runtime_core`、retired alias、fallback 或只保护旧路径的聚合测试写成 MAS 默认 active runtime owner、active adapter、diagnostic fallback、callable alias 或 retained caller。
- 不能把 product-entry / workbench / owner-route / progress projection shell 写成长期 MAS generic workbench、queue、attempt ledger、retry/dead-letter、worker residency、terminal transport 或 lifecycle owner；它们当前只能输出 domain projection refs、owner receipt、typed blocker、authority refs 或 diagnostic refs。
- 不能把 `study_state_matrix` 或 OPL `family-transition-runner` matrix pass 写成 paper closure、publication quality、artifact authority、submission readiness 或 domain ready。
- 不能把真实 paper apply、memory receipt、artifact receipt、human gate/resume 或 provider SLO 写成可由 repo tests、descriptor ready 或 conformance 替代。
- 不能把 `publication_quality_verdict`、`ai_reviewer_quality_decision`、`source_readiness_verdict` 或类似 verdict 写成脚本/函数直接决定；它们必须是 AI-first stage quality gate 的可审计输出。
- 不能把 executor agent 自审、同一上下文内的执行后复核、或 executor summary 改名成 reviewer/auditor output；AI-first quality gate 必须消费独立 reviewer/auditor agent invocation 的记录。
- 不能把 progress-first Co-Scientist affordance 写成默认前置流程、admission gate、quality closure、publication readiness、artifact authority、route blocking layer、paper progress、production-ready 或真实 paper-line closeout；`next-delta tournament`、bounded candidates、repair hints、reusable lesson extraction、triggered meta-review 和 opportunistic prefetch 只能在 `current_delta_declares_or_implies_affordance_need` 时服务推进选择、评审定位和失败路径复用。
- 不能把 EvoScientist / EvoSkills sidecar 写成 MAS 默认 runtime owner、executor backend、tool authority、memory authority、quality owner、admission gate、route blocking layer、paper progress、owner receipt、quality verdict、publication-ready、artifact authority、production-ready 或 independent reviewer/auditor output；async learning、tool selector、observation memory、failed-path taxonomy 和 lifecycle skill match 都只能作为 fail-open progress accelerator / audit sidecar。
- 不能把 dated specs、dated closeout、follow-through 记录、receipt id、OPL worklist 数字或历史 full record 当成 current truth；需要过程脉络时读取 history/provenance。
