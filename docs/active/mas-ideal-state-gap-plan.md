# MAS 理想目标态差距与完善计划

Owner: `MedAutoScience`
Purpose: `ideal_state_gap_plan`
State: `active_plan`
Machine boundary: 本文是人读 gap / completion plan。机器真相继续归 `contracts/`、源码、CLI/MCP/API 行为、product-entry manifest、sidecar receipt、runtime/controller durable surfaces、真实 workspace 与 generated artifact proof。
Date: `2026-05-16`

## 文档读法

- `定位`：本文是 MAS 的 active gap / implementation order，不是 north-star 目标态本身；目标态回到 `mas_ideal_state.md`。
- `当前实态`：P0 默认 scheduler owner 已迁到 OPL replacement；MAS-owned local scheduler / LaunchAgent install path 已退役，显式 `--manager local` 只保留 legacy status/remove cleanup surface，后续要用 no-active-caller proof 清掉剩余旧生成物和文案。
- `最短路径`：P0 先做 `local_scheduler_absorption_or_retirement`，再做 authority pack hardening、OPL primitive handoff、Portal/workbench read-only boundary cleanup、transition table hardening 和 focused paper-line verification。
- `验收顺序`：先 replacement proof、no-active-caller proof、CLI/status/manifest owner 迁移和 no-forbidden-write，再跑 paper-line provider apply、long provider SLO 和 live soak。
- `禁写口径`：不能把当前 LaunchAgent scheduler 写成 MAS 理想常态；也不能把 OPL provider proof、DM002 stable blocker 或 `mas_owner_receipt_present` 写成 paper closure、artifact mutation 授权或 publication-ready。
- `本轮功能面 follow-through`：验收点是 MAS 以 `functional_consumer_boundary` 和 test lane 证明自己消费 OPL generic scaffold / pack compiler / transition / memory / artifact / workbench / observability，不再默认持有 generic scheduler、daemon、queue、attempt ledger、generic runner、generic workbench 或 generated entry wrapper；这条 lane 不要求 live soak。

## 结论

本文对照 [MAS 理想目标态](../references/positioning/mas_ideal_state.md)，只维护 MAS 自己的当前差距、完善顺序、与 OPL 的 owner 边界和通用能力上收清单。OPL、MAG、RCA、One Person Lab App 与 MDS/DeepScientist 的具体完善计划不在本文维护；它们分别回到 OPL family 文档或各自仓库的理想态 / status / active surface。

MAS 当前已经具备独立 medical research domain agent 的主要边界：direct MAS app skill path、CLI/MCP/product-entry/controller、stage-led autonomy、publication-route memory、sidecar export/dispatch、functional closure projection、OPL production proof ingestion、owner receipt contract、legacy residue tombstone proof 和 body-free OPL/Aion projection 都已进入 repo surface。当前差距不能再只写成“真实 paper-line production evidence 不足”。P0 已把默认 supervision scheduler owner 迁到 OPL replacement：MAS 默认 CLI/status/bootstrap 不再安装或刷新本机 LaunchAgent，显式 `--manager local` ensure 也不再安装/刷新/触发本机 scheduler；MAS 保留 paper-progress SLO 语义、owner receipt、typed blocker、safe action refs 和 no-forbidden-write evidence。剩余功能层尾巴是显式 `--manager local` status/remove cleanup、Portal/product-entry 文案彻底清理和 no-active-caller / physical retirement proof；paper-line long soak 应作为功能边界收干净之后的验收。

MAS 的单仓计划只维护医学领域真相、声明式 medical research pack 和 minimal authority functions。descriptor、contract/schema、domain transition spec/table、quality gate、artifact locator、receipt schema、tests 和 domain entry 服务 OPL 发现、托管、审计和投影；sidecar/thin adapter、projection builder、CLI/MCP/product-entry/status wrapper 只应作为 OPL pack compiler 的生成目标或迁移桥，不构成 MAS 长期手写 surface。

2026-05-15 live evidence 更新：MAS 已补齐 OPL-hosted guarded-apply 的 task source fingerprint、MAS owner evidence source refs、source-keyed dispatch receipt 和 readiness owner blocker projector。刷新 OPL provider proof 后，DM002 export task 先以 `source_fingerprint=a22e142f0c6ceab0` 证明 OPL 能重新入队旧 dedupe key；随后 MAS 把 `002-early-residual-risk/artifacts/controller_decisions/latest.json` 作为 owner stable blocker 纳入 task source refs，再以 `source_fingerprint=a0cdbf341fae623d` 生成 OPL stage attempt `sat_df08b839ce33a33ee92974f2` 和 MAS receipt `artifacts/runtime/opl_family_sidecar/dispatch_receipts/e97b1c8cd25f3a019f90.json`。该 receipt 返回 `mas_owner_receipt_present` / `stable_blocker`，`summary.writes_performed=false`，`provider_attempt_wrote_workspace=false`，并证明 no forbidden writes。它关闭的是 OPL/MAS owner-receipt rehydrate / source-keyed dispatch gap，不关闭 paper-line publication / artifact authority。

2026-05-16 P0 migration 更新：MAS 默认 scheduler façade 已迁到 OPL replacement。代码层证据包括 `src/med_autoscience/controllers/supervision_scheduler.py` 的 `SCHEDULER_OWNER=opl_provider_runtime_manager`、`DEFAULT_MANAGER=opl`、`OPL_ADAPTER_ID=opl_family_runtime_provider`，CLI parser 默认 `--manager opl`，`workspace bootstrap` 调用 OPL replacement，不再默认安装 LaunchAgent；contract 层证据包括 `contracts/test-lane-manifest.json` 中 `default_scheduler_owner=opl_provider_runtime_manager`、`default_adapter_id=opl_family_runtime_provider` 和 `legacy_diagnostic_adapter_id=local_launchd_on_macos`。fresh CLI evidence 显示默认 `runtime-supervision-status --profile <tmp-profile>` 返回 `status=replacement_owner_active` / `scheduler_owner=opl_provider_runtime_manager` / `adapter_id=opl_family_runtime_provider`，默认 `runtime-ensure-supervision --dry-run` 返回 `action=delegated_to_opl_provider_scheduler` / `status=delegated`，默认 status projection 里的显式 local adapter 为 `status=not_installed`；显式 `runtime-ensure-supervision --manager local --dry-run` 返回 `action=retired_cleanup_only`、`status=blocked`、`reason=mas_local_scheduler_install_retired_use_opl_replacement`、`write_install_proof=false`，不生成 install proof、不写 tick script、不写 LaunchAgent plist、不触发本机 tick。`status()` / `remove()` 只用于检查和移除 legacy local 生成物；检测到旧 LaunchAgent / tick script 时只投影 `retired_legacy_cleanup_required` cleanup blocker。

2026-05-16 现有论文 workspace migration 更新：`workspace-monolith-migrate --apply` 已对 5 个真实 profile 重放并清理 14 个 study binding / quest snapshot 的 active runtime 污染，覆盖 AS biologics、DM-CVD workspace/local、Obesity 和 NF-PitNET canonical workspace。迁移后的 active binding 统一指向 `<workspace>/runtime` 与 `<workspace>/runtime/quests`，backend/engine 统一为 `mas_runtime_core` / `mas-runtime-core`；旧 `ops/med-deepscientist` quest root、runtime root、legacy backend id、legacy diagnostic、旧 baseline 绝对路径和 metric contract 绝对路径只能进入 `historical_fixture_ref` / `explicit_archive_import_ref`。本轮只写 profile/runtime binding/quest snapshot/runtime config/monolith migration report 与 retired wrapper cleanup，不写 `current_package`、`publication_eval/latest.json`、`controller_decisions/latest.json`、paper/submission package、runtime SQLite 或 restore archive；`auto_wakeup=false`，不把迁移写成论文重启或 artifact mutation 授权。

2026-05-16 legacy physical cleanup apply closeout 更新：`workspace-legacy-physical-cleanup-audit` 仍是只读 gate，`workspace-legacy-physical-cleanup-apply --dry-run|--apply` 仍只负责旧 root archive move、tombstone 和 provenance ref rewrite。真实 workspace 已执行完成：AS biologics、DM-CVD workspace/local、NF-PitNET 的旧 `ops/med-deepscientist` physical root 已移动到 `runtime/archives/legacy_mds/<timestamp>/med-deepscientist`，Obesity 已创建 absent-root tombstone；profile、workspace guidance/layout/wrapper、migration/storage/runtime-supervision、study artifact metadata、controller/autonomy/eval hygiene/evidence ledger 等旧根 refs 已改写到 archive/tombstone/ref-only surface。5 个真实 profile post audit 均为 `reference_counts={}`、`next_required_action=no_legacy_physical_cleanup_required`，旧 active root 均 absent。该 closeout 不写 `current_package`、`publication_eval/latest.json`、`controller_decisions/latest.json` 当前 authority、paper/submission package、runtime SQLite 或 restore archive，也不构成 paper closure、publication-ready 或 OPL provider live soak。

2026-05-16 功能消费方 guard 更新：`contracts/test-lane-manifest.json` 新增 `mas-functional-consumer-followthrough` focused lane，机器面明确 `generic_surface_owner=one-person-lab`，MAS 不持有 `generic_scheduler`、`generic_daemon`、`generic_queue`、`generic_attempt_ledger`、`generic_runner`、`generic_transition_runner`、`generic_workbench`、`generic_memory_locator`、`generic_artifact_lifecycle` 与 `generic_observability`。同一 guard 由 `product_entry_manifest.functional_consumer_boundary`、`sidecar_export.functional_consumer_boundary` 和 supervision `consumer_migration.functional_consumer_boundary` 投影，保留的 MAS domain authority surface 限定为 `study_truth`、`publication_quality_verdict`、`artifact_authority`、`publication_route_memory_body`、`memory_writeback_decision`、`domain_transition_table`、`owner_receipt`、`typed_blocker` 与 `safe_action_refs`；手写 shell 只作为迁移桥阅读。本轮已把 `opl_functional_harness_consumer_coverage` 明确落到机器面：OPL harness 覆盖 refs-only publication-route memory 写回链、queue / stage attempt / typed closeout、generic transition runner，以及 restart / dead-letter / repair / human gate 状态链；对应 guard 同时固定 `opl_harness_pass_is_paper_closure=false`、`opl_harness_pass_is_publication_ready=false`、`mas_owns_generic_runtime=false`。当前 `no_active_caller_proof.default_caller_count=0`，默认 CLI、workspace bootstrap、product-entry、sidecar 和 MCP 不再指向 local LaunchAgent install path；显式 local 只保留 `status/remove` cleanup diagnostic，不允许 install、trigger、loaded-state 或 install-proof 输出。这关闭的是功能面 no-active-caller / test-lane / docs 证明，不声称真实 paper-line long soak 完成。

2026-05-16 MAS functional privatization 盘点更新：`functional_consumer_boundary.functional_module_inventory` 已从 A/B/C 概念分类升级为代码路径级清单，共 18 项，含 `module_id`、`classification`、`code_paths`、`active_callers`、`active_caller_status`、`migration_action`、`retention_reason` 或 `cannot_absorb_reason`。A 类由 OPL 持有、MAS 只消费或提供 adapter/receipt，包括 `runtime_lifecycle_sqlite_reference_adapter`、`paper_work_unit_outbox_index`、`runtime_storage_maintenance`、`workspace_source_intake_shell`、`publication_route_memory_locator_transport_shell`、artifact lifecycle / storage audit shell、workbench / portal generic shell、terminal attach transport、runtime supervisor scan / consume / dispatch / reconcile shell、generic CLI/MCP/product wrappers、generic daemon/scheduler lifecycle、queue/attempt/retry/dead-letter 和 generic transition runner。B 类保留在 MAS，包括 study truth、`study_runtime_status`、`runtime_watch` domain health、AI reviewer、publication gate、artifact authority、owner receipt、domain transition table、publication-route memory body、memory writeback decision、typed blocker 与 safe action refs。C 类在 replacement proof / no-active-caller 后应退役，包括 local LaunchAgent scheduler install path、workspace-local watch service wrappers、MAS generic workbench shell、legacy scheduler default aliases、把 terminal attach / daemon-ish status 当 runtime owner 的旧 surface，以及无 active caller 的 scheduler residue。`runtime_lifecycle.sqlite` 同步降格为 MAS domain sidecar index / reference adapter：它可以索引 MAS domain receipt 和 locator，消费 OPL lifecycle index refs，但不能写 domain truth，也不能被写成 MAS generic persistence / lifecycle / restore-retention engine；对应 machine surface 已显式列出 forbidden MAS roles：`generic_persistence_engine`、`generic_lifecycle_engine`、`generic_restore_retention_owner`。OPL replacement expectation 和 audit ref 由 test-lane manifest 与 runtime lifecycle contract 持有。

代码面结论不能写成“MAS 已无私有功能实现”。当前仍活跃的 MAS 私有实现包括：`runtime_protocol/runtime_lifecycle_store.py` 的 SQLite sidecar / lifecycle store、`controllers/paper_work_unit_outbox.py` 的 paper work-unit outbox index、`controllers/runtime_storage_maintenance.py` 与其 parts 的 storage / retention / restore shell、`workspace_init.py` / `workspace_contracts.py` / `workspace_literature.py` / `literature_provider_runtime.py` 的 workspace/source intake shell、`stage_knowledge_plane.py` 与 publication-route memory parts 的 memory locator/writeback transport、`artifact_lifecycle_*` 的 artifact lifecycle shell、`progress_portal.py` / `progress_portal_parts/` / `product_entry_parts/workspace_cockpit/` 的 workbench shell、`runtime_live_console*` 的 terminal/log projection、`runtime_supervisor_scan.py` / `runtime_supervisor_consumer.py` / `runtime_supervisor_dispatch_executor.py` / `runtime_supervisor_reconcile.py` 的 generic scan/dispatch loop，以及 `cli.py` / `mcp_server.py` / `product_entry.py` 的 generic entry/product shell。它们的长期 owner 不是 MAS；当前允许存在的理由是它们仍承载 domain receipt refs、diagnostic cleanup、迁移桥或尚未由 OPL pack compiler 生成的外壳行为。后续迁移动作必须逐项消费 OPL replacement / generated surface 后删除或收薄 MAS generic shell，不能继续扩成 MAS-owned runtime platform。真正可长期保留的只应是医学 authority function：publication quality verdict、AI reviewer-backed quality decision、artifact mutation authorization、memory accept/reject、source readiness verdict、owner receipt signer 和医学 helper implementation。

2026-05-15 locator evidence 更新：`family_transition_spec_descriptor` 已明确 `study-state-matrix` 是完整 `family_transition_spec` / `family_transition_matrix_cases` 的唯一物化 surface，`product-entry manifest` 与 `sidecar export` 只暴露 descriptor locator。descriptor 的 JSON pointer 不再指向 sidecar 内不存在的完整 spec，OPL 消费方应先刷新 `study-state-matrix` 后再执行 generic transition runner。

2026-05-15 OPL workbench evidence 更新：OPL runtime stage-attempt workbench 已新增 `transition_bridge_evidence` refs-only 投影，可在 workbench 顶层、单条 attempt projection 和 operator item 中展示 owner receipt refs、no-regression evidence refs、typed blocker refs/count 与 `refs_only_no_domain_verdict_authority` 边界。MAS 只需继续提供 `study-state-matrix` spec/cases、MAS-owned owner receipt、typed blocker、route/quality/artifact refs；OPL/App 不因此获得 publication ready verdict、paper closure 或医学 transition truth。

OPL 系列项目的全局主参考是 `/Users/gaofeng/workspace/one-person-lab/docs/active/opl-family-development-reference.md`。涉及跨仓总顺序、shared primitive owner、App/workbench 通用目标和旧兼容面退役纪律时，以该主参考和 OPL docs 为准。

## 总体差距矩阵

| 目标面 | 当前实际 | 功能/结构差距 | 测试/证据差距 | 完善方向 |
| --- | --- | --- | --- | --- |
| MAS domain authority pack | 医学 stage pack、publication-route memory、AI reviewer / quality rubric、artifact authority、owner receipt schema 和 projection refs 已成主线 | runtime / workbench / lifecycle / generated wrapper 需求仍容易继续在 MAS 内复制为 generic owner | authority pack 本身已有 projection/receipt 证据；后续主要需要 guard 新入口不回流 generic owner | MAS 只保留医学研究路线、质量 judgment、artifact authority、声明式 pack 和 minimal authority functions；通用外围和可生成外壳转成 OPL primitive / pack compiler 需求。 |
| Functional consumer boundary | `mas-functional-consumer-followthrough` lane、product-entry / sidecar / supervision migration projection 已声明 MAS 不持有 generic scheduler、daemon、queue、attempt ledger、runner、workbench、memory locator、artifact lifecycle 和 observability；`opl_functional_harness_consumer_coverage` 已覆盖 refs-only memory 写回链、queue/stage attempt/typed closeout、generic transition runner 和 restart/dead-letter/repair/human gate 状态链 | 后续新增 CLI/MCP/product-entry/sidecar 入口仍需保持只消费 OPL generic primitive，不能重新实现成 MAS 默认 surface | 每个新增入口都需要 manifest / sidecar / focused lane 证明 consumer boundary；harness pass 不能写成 paper closure / publication ready | 新增 CLI/MCP/product-entry/sidecar/test lane 时先接入 `functional_consumer_boundary`，只暴露 MAS domain receipt / typed blocker / artifact-memory refs；generic scaffold、transition runner、memory locator、artifact lifecycle、workbench 和 observability继续由 OPL 提供。 |
| Functional privatization inventory | `functional_consumer_boundary.functional_module_inventory` 已列出 18 项真实代码路径和 active caller；`contracts/test-lane-manifest.json` 记录 inventory source、必需字段、期望 count 和 module ids；`runtime_lifecycle.sqlite` 明确为 MAS domain sidecar/reference adapter，不是 generic persistence / lifecycle engine | MAS 本地 SQLite/lifecycle store、paper outbox index、storage maintenance、workspace/source intake、memory locator/writeback shell、artifact lifecycle shell、Portal/Workbench、terminal attach、runtime supervisor scan/dispatch loop、CLI/MCP/product shell 和 scheduler cleanup residue 仍需逐块上收、收薄或退役 | 每块退役前需要 OPL replacement proof、no-active-caller scan、fixture/provenance 必需性检查和 focused cleanup test | A 类继续写 OPL replacement expectation/audit ref，并在 replacement 可用后把 generic owner 删除或收薄到 refs-only adapter；B 类只保留 domain truth / authority / receipt；C 类按无 active caller、无 fixture/provenance 必需、replacement proof 成立后删除，不新增 compatibility alias。 |
| OPL pack compiler / generated surface | 当前 MAS 仍主要靠手写 CLI/MCP/product-entry/sidecar/projection/status shell 暴露 OPL 可读面；`functional_consumer_boundary` 已证明这些 shell 不应成为 MAS generic owner | 缺少 OPL 统一从 MAS descriptor / stage graph / transition table / policies / receipt schema / authority manifest 派生 wrapper、sidecar、status、workbench、harness 的生产 surface；手写 shell 仍是迁移桥 | 缺 active caller 迁到 generated/replacement surface 后的 production consumption proof 和 no-regression proof | 将 MAS descriptor、stage/quality/memory/artifact/source policies、transition matrix、receipt schema、authority function manifest 作为 pack compiler 输入；OPL 生成或托管 CLI/MCP/product-entry/sidecar/status/workbench/harness，MAS 手写 shell 退到迁移桥并逐项删除。 |
| MAS scheduler / supervision | 默认 `runtime-supervision-status`、`runtime-ensure-supervision`、`runtime-remove-supervision` 和 `workspace bootstrap` 已委托 OPL `family_scheduler_replacement`；`local_adapter.py` / LaunchAgent install path 已退役，显式 `--manager local` 只保留 status/remove cleanup，不生成 tick script、LaunchAgent plist 或 install proof；`no_active_caller_proof.default_caller_count=0` 已进入 machine surface | 默认 owner 已迁移；剩余功能尾巴是旧 local adapter 物理旧生成物清理和少量 provenance/fixture 依赖 | 还需 cleanup-only path、no-active-caller、旧生成物移除和 Portal/product-entry owner wording 的持续 drift proof | 继续把 scheduler lifecycle、cadence、provider SLO、job registry/latest run projection保持在 OPL provider/runtime manager；MAS 只返回 owner receipt、paper-progress blocker、safe action refs 和 no-forbidden-write evidence。 |
| Existing workspace active binding migration | 5 个真实 profile / 14 个 study binding 与 quest snapshot 已迁到 MAS active layout：`runtime_home=<workspace>/runtime`、`runtime_root/runtime_quests_root=<workspace>/runtime/quests`、active backend/engine 为 `mas_runtime_core` / `mas-runtime-core`；旧 MDS roots、backend id、legacy diagnostic 和旧 baseline absolute refs 已被压到 archive/tombstone/ref-only provenance | binding/snapshot 与旧 physical root active pollution 已清理；暂无独立 legacy binding 功能缺口 | 真实 provider-hosted paper apply、human gate/resume、memory writeback scaleout 与 paper publication closure evidence 仍未闭合 | 继续用 `workspace-monolith-migrate` / `doctor profile` / `study-state-matrix` / `control-plane-migration-audit` 做 drift proof；发现缺 OPL runtime/workbench/provider surface 时写 handoff，不在 MAS 内重建 generic runtime。 |
| OPL-hosted paper-line apply | OPL provider proof 可被 MAS 读取；sidecar dispatch 能回到 MAS owner chain 并返回 source-keyed receipt；DM002 已证明 owner stable blocker 可进入 OPL rehydrate / dispatch loop，最新 attempt `sat_df08b839ce33a33ee92974f2` 与 receipt `e97b1c8cd25f3a019f90.json` 返回 `mas_owner_receipt_present` / `stable_blocker`，且不显示 provider workspace write | owner chain 结构已经可接回 MAS；后续结构重点是把同一 thin adapter 路径保持在 direct/hosted 共用 owner surface | 已有的是单条 paper line 的 owner stable blocker；还缺多 paper-line scaleout、连续 long-soak、artifact delta、AI reviewer update、route decision、human gate/resume 或 publication closure evidence | 用 DM002、DM003、Obesity 等真实 paper line 跑 guarded apply；每条线返回 owner receipt、artifact delta、gate replay、typed blocker、stop-loss 或 human gate，而不是把 provider completion 写成 paper closure。 |
| Publication-route memory | Markdown body、seed index、workspace pack、inventory、typed closeout proposal、router receipt、body-free OPL/Aion projection 已落地；`study-state-matrix` 现在还能消费 publication-route `memory_write_router_receipt`，只投影 router/writeback refs、accepted/rejected/blocker counts 与 body-free/non-authoritative 边界 | memory body 保持 MAS workspace/runtime owner；结构上不能复制到 OPL，也不能让 writeback receipt 授权 publication quality、submission 或 runner resume | accepted/rejected/blocked writeback receipt 还需要在更多真实 paper line 上泛化 | 继续由 MAS workspace/runtime owner 接受或拒绝 writeback；OPL/App 只展示 locator、consumed refs、receipt refs、freshness 和 grouping；transition matrix 只把 memory writeback receipt 暴露成 inspect/handoff，不驱动通用 runner。 |
| Artifact lifecycle | MAS 已暴露 artifact locator、lifecycle apply request、guarded apply proof 和 no-forbidden-write 边界 | OPL lifecycle ledger、restore/retention shell 与 operator projection 应承接通用壳；artifact mutation permission 与 canonical package authority 留在 MAS receipt | 真实 workspace cleanup / restore / retention receipt 还需要按 owner 授权闭合 | OPL 可提供 lifecycle ledger、restore/retention shell 和 operator projection；artifact mutation permission 与 canonical package authority 仍由 MAS receipt 给出。 |
| Workbench / route map | MAS 已有 route map、decision trail、Stage Review Page / Index、Portal/Workbench projection 方向 | OPL/App 通用 route graph、attention queue 和 action routing shell 仍需产品化；MAS 只输出 domain-owned route nodes/edges、decision rationale 和 refs | 普通用户仍需要从真实 workspace/projection evidence 清楚看到 paper route、阻塞、转向、owner、safe action 和证据来源 | MAS 输出 domain-owned route nodes/edges、decision rationale、source refs、review/artifact refs；OPL/App 提供通用 route graph、attention queue 和 action routing shell。 |
| State transition / decision table | `study-state-matrix` 已投影 MAS-owned JSON/Markdown `domain_transition_table` / read-model oracle，覆盖 publication gate blocker、AI reviewer re-eval、artifact delta guarded apply、human gate、stop-loss、truth conflict/fail-closed、active runtime watch、delivered package handoff、unclassified fail-closed，以及 2026-05-15 三个真实 paper-line failure mode：DM002 rebuttal coverage complete 后 yield to bundle/finalize、DM003 mechanical projection 先回 AI reviewer、Obesity AI reviewer-backed blocked verdict / owner_handoff terminal-consumed 不再重入旧 work unit；controller refresh / runtime authorization relay 已承认 `domain-transition::*` 为 controller-owned transition authority，不要求它反向出现在 `publication_eval.recommended_actions`；controller refresh 还会验证 live Codex CLI prompt 是否消费当前 work unit，旧 prompt 或缺 prompt 会触发 controller-owned fresh turn；managed Codex CLI worker 的 callable action 现在还会用受限 runtime identity 消费 `last_controller_decision_authorization`，防止 fresh prompt 再被旧 default executor dispatch 卡住；2026-05-15 继续把 `completion_receipt_consumption` 纳入 transition row、family spec receipt 与 matrix case context/expected oracle，并把 MAS `paper_repair_owner_receipt` artifact-delta apply receipt、controller stable blocker / route decision / stop-loss owner receipt 投成 owner consumed receipt，把 AI reviewer-backed `publication_eval/latest.json` 投成 `ai_reviewer_publication_eval` consumed receipt，把已批准的 `controller_confirmation_summary.json` 投成 `human_gate_resume_receipt` consumed receipt，把 publication-route memory writeback receipt 投成 `memory_writeback_receipt_consumed` / inspect handoff，且不输出 memory body、不授权 quality/submission、不允许 OPL generic runner resume；已交付 current_package / package zip 在 clear publication eval 下会优先投成 `delivered_package_handoff`，不再被 active run 重新降回 bundle finalize；task intake reducer 也会在 delivery manifest / current_package 新于用户修改请求且 gate clear 时消费旧 reviewer_revision intake，避免旧 prompt 持续重启已交付投稿包；runtime consumer guard 已接入同一 MAS domain transition oracle：`interaction_arbitration` 让 delivered package / human gate / stop-loss 压过旧 closeout redrive，并把 publication gate blocker 路由回 controller-owned resume；action projection 和 safe apply path 让 terminal / delivered / human / stop-loss / fail-closed 阻断 action queue / safe apply redrive，publication gate blocker 投到受支持的 publication gate owner action，bundle-stage finalize 通过当前 controller route 覆盖旧 analysis/write authorization；2026-05-15 本轮补齐 `memory_writeback_receipt_consumed` 的 runtime consumer guard，使 publication-route memory writeback receipt 也像 terminal/handoff decision 一样阻断 action queue / safe apply redrive，只留下 refs-only inspect/handoff；OPL 已有 generic transition runner / matrix runner 与 MAS-like fixture | OPL generic runner 接入、domain spec handoff 和 legacy study config fail-closed cleanup 仍需继续；transition read model不能替代 live provider apply、publication closure 或 submission authority | 真实 paper-line transition receipt scaleout、live provider apply、publication closure evidence 和更多 workspace owner receipt 仍不足；runtime consumer guard 是 repo-level 防回归，不等于三篇论文已完成投稿或 publication closure | MAS 继续扩展 domain transition table、relay oracle、prompt/action postcondition、consumer guard 和真实 runtime receipt coverage，再以 domain spec 接入 OPL runner；OPL 只执行 spec、attempt、receipt、retry/dead-letter 与 human-gate transport，不解释 publication verdict；单 study projection error 必须隔离为 typed blocker，不能拖垮 workspace-level transition surface。 |
| Completion / execution receipt consumption | 2026-05-15 已补真实控制面回归并进入 matrix oracle：DM002 的 `submission_authority_sync_closure` package closure / completed turn closeout 现在投成 `completion_receipt_consumed`，`next_action=do_not_redrive_completed_work_unit`；turn closeout 的 `artifact_refs` 是混合证据引用列表，图表、PDF、DOCX 等非 JSON refs 只能作为 opaque evidence refs 保留，只有 `.json` package-closure receipt 可以被 controller refresh / state matrix 当作 work-unit closure payload 消费；DM003 的更晚 `default_executor_execution/latest.json` executed receipt 现在进入 `completion_receipt_consumption` context，标记 `superseded_stale_closeout` / `honor_newer_owner_execution_receipt`，同时既有 runtime status 仍投为 `runtime_platform_repair_redrive` 而非 user wait；MAS owner apply receipt `artifacts/controller/repair_execution_receipts/latest.json` + artifact delta evidence 现在投成 `owner_apply_receipt_consumed`，`next_action=allow_mas_owner_guarded_apply`；`controller_decisions/latest.json` stable blocker / route decision owner surface 现在投成 `owner_apply_receipt_consumed`，分别记录 `record_mas_owner_stable_blocker` / `record_mas_owner_route_decision`；`controller_decisions/latest.json` stop-loss owner surface 现在投成 `mas_owner_stop_loss_receipt`，`next_action=honor_mas_owner_terminal_stop`，且 human-gated stop-loss 保持 human gate 不消费；AI reviewer-owned `publication_eval/latest.json` 现在投成 `ai_reviewer_publication_eval` receipt，保留 reviewer OS trace ref，且不声明 `quality_authorized` / `submission_authorized`；已批准或 consumed 的 MAS controller confirmation summary 现在投成 `human_gate_resume_receipt`，仅允许 resume 类 controller action 交回 MAS owner；publication-route `memory_write_router_receipt` 现在接受 `applied/blocked` receipt，要求 `memory_family=publication_route_memory`、writeback ref-chain、accepted/rejected/typed blocker 至少一个有效项，并投成 body-free `publication_route_memory_writeback_receipt` consumption | 结构规则是每个 domain transition 必须声明 `entry state -> authority receipt -> consumed closeout -> next owner/action`，并按 artifact type 区分 opaque evidence ref 与 JSON owner receipt；memory writeback receipt 只能进入 ref-only inspect handoff | 当前 matrix 覆盖 package closure、default executor execution、artifact-delta apply、controller decision、AI reviewer eval、human gate resume、memory writeback 和非 JSON closeout evidence ref 回归；真实 workspace 多 paper-line receipt scaleout 仍不足 | 每个 domain transition 必须声明 `entry state -> authority receipt -> consumed closeout -> next owner/action`；新增 completion surface 必须补 matrix fixture、focused reducer test 和 prompt/dispatch postcondition，确保旧 prompt/旧 closeout 不能压过更新或更高权威的 MAS owner receipt；读取 `artifact_refs` 时必须先按 artifact type 区分 opaque evidence ref 与 JSON owner receipt，memory writeback receipt 也只能进入 ref-only inspect handoff。 |
| Package-ready handoff | `publication_work_units` 现在要求 submission authority、delivery signatures 与 `current_package_freshness` proof 同时当前，才返回 `terminal_package_ready_handoff` / `package_ready_handoff`；缺 freshness proof 时仍保持 `submission_authority_sync_closure` controller work unit。reviewer_revision task intake 现在也会在更新的 `delivery_manifest.json`、current_package/package zip 和 clear gate 同时成立时让位给当前交付包 closeout | 结构边界已明确：MAS 只声明当前包已到用户审阅/显式恢复节点，不把 package-ready 写成 publication closure；OPL/App 只显示 attention item | 真实 workspace 还需要更多 current_package freshness proof、human review receipt 和显式 resume/revision intake 规模化证据 | MAS 只声明当前包已到用户审阅/显式恢复节点，不把 package-ready 写成 publication closure；OPL/App 可把它显示成人类 attention item。 |
| OPL generic runner / transport handoff | OPL fresh read model 已显示三仓 descriptor/stage/memory resolved，并能投影 attempt query / generic projection shell；OPL generic transition runner 基础已存在；MAS descriptor locator 已收敛为 `study-state-matrix` 持完整 spec/cases、manifest/sidecar 只持 descriptor；OPL runtime workbench 现在还能把 provider-hosted transition bridge 的 owner receipt / no-regression / typed blocker refs 投成 operator drilldown | OPL runner hardening、SLO、App workbench 产品化和 lifecycle shell 仍需由 OPL 成为可依赖 runtime substrate；MAS 不在本仓复制通用实现 | 真实 paper-line transition owner receipt scaleout、周期性 provider SLO execution 和真实 lifecycle apply evidence 仍不足 | MAS 只提供医学 transition spec、owner receipt、typed blocker、route/quality/artifact refs；`study-state-matrix` 已把 spec 与 matrix 机器面落下，`product-entry manifest` / `sidecar export` 只提供 descriptor，等待 OPL runner hardening / SLO / workbench / lifecycle 壳成熟后接入，不在 MAS 内复制通用实现。 |
| Legacy contraction | MDS default dependency、Hermes-first/local scheduler 默认语义和 legacy active-path 已有 tombstone / no-active-default-caller 证据；5 个真实 profile 的 legacy physical root archive/tombstone 与 no-active-reference proof 已完成 | 后续仍需防止新 surface 重新引用 archive 或把 archive provenance 写回 active truth；新增入口不能把旧面补成 active runtime | live soak 和 paper closure evidence 仍未完成；legacy drift 需要持续 no-active-reference / no-forbidden-write guard | MDS/DeepScientist 保持 archive/oracle/intake reference；新增入口必须持续跑 no-active-reference / no-forbidden-write guard，不把旧面补成 active runtime。 |

## 最短实施顺序

1. `local_scheduler_absorption_or_retirement`
   P0 默认迁移已完成：OPL-owned replacement / migration contract 可被 MAS 消费，默认 CLI/status/bootstrap 已迁到 `opl_provider_runtime_manager`，显式 local ensure 已退役为 cleanup-only blocker，MAS 保留 paper progress SLO 语义、owner receipt、typed blocker、safe action refs 和 no-forbidden-write evidence。当前功能面 guard 已把 no-active-caller 证明推进到 test lane / product-entry / sidecar / supervision projection；后续只允许做显式 `--manager local` status/remove cleanup proof 和物理退役，不能把 LaunchAgent scheduler 重新扩展成 MAS 长期产品内核。
2. `authority_pack_hardening`
   把 stage policy、prompt/skill、publication-route memory、AI reviewer、quality gate、route decision 和 artifact authority 收紧到 MAS owner surface；不在 MAS 内新增通用 queue、generic memory service、通用 workbench 或通用 lifecycle OS。
3. `opl_primitive_handoff`
   将通用需求转成 OPL owner primitive：provider workflow、queue/human gate transport、generic transition runner hardening、transition bridge evidence refs-only drilldown、memory locator/index、artifact lifecycle、route graph shell、attention queue、observability/SLO、repair command projection、generated entry surface 和 App grouping。MAS 不在本仓新增第二套 scheduler、daemon、queue、attempt ledger、transition runner、App workbench、memory locator、artifact lifecycle、observability OS、generic sidecar/status wrapper 或 product shell；所有新 surface 必须消费 OPL scaffold/pack compiler/generic transition/memory/artifact/workbench/observability，并只返回 MAS owner receipt、typed blocker 与 refs。
4. `pack_compiler_migration`
   把 MAS 手写 generic shell 的输入抽成声明式 pack：domain descriptor、stage graph、action intent、source/readiness policy、publication-route memory policy、artifact authority policy、transition table / oracle fixtures、receipt schemas、authority function manifest 和 no-forbidden-write contract。OPL pack compiler 可用后，迁移 CLI/MCP/product-entry/sidecar/status/workbench/harness 的 active caller 到 generated surface；原 MAS shell 若不再承载 authority function、diagnostic cleanup 或 fixture/provenance，直接删除或 tombstone。
5. `portal_workbench_readonly_boundary_cleanup`
   清理 MAS Portal / Live Console / product projection 中把 runtime supervision、provider SLO 或 operator action 写成 MAS-owned generic OS 的措辞和入口。UI/Portal 只展示 MAS domain receipt、paper-progress blocker、quality/source refs、safe action refs 和明确 owner 的 repair hint；通用 cadence、attempt ledger、queue、action routing 和 App drilldown 回到 OPL。
6. `domain_transition_table_hardening`
   维护已落地的 MAS-owned domain transition table / read-model oracle，把更多真实 paper-line transition receipt 纳入 coverage。已纳入的真实回归包括：002 的 reviewer coverage closeout 完成态消费、DM002 package closure / turn closeout 的 `completion_receipt_consumed` matrix case、MAS owner apply receipt artifact-delta 与 controller stable blocker / route decision 的 `owner_apply_receipt_consumed` matrix case、AI reviewer-backed `publication_eval/latest.json` 的 `ai_reviewer_publication_eval` matrix case、accepted/rejected/blocked publication-route memory writeback receipt 的 body-free inspect handoff、current package 已交付后的 `delivered_package_handoff` active-run 优先级、003 的 AI reviewer provenance 优先级、DM003 default executor execution supersession context、Obesity 的 publication gate 真阻断与 stale owner_handoff 授权消费、live Codex CLI stale prompt fresh-turn postcondition、managed worker action 消费当前 authorization 而非旧 dispatch、runtime arbitration / action projection / safe apply path 对 delivered package、human gate、stop-loss、fail-closed、publication gate blocker 和 bundle-stage finalize oracle 的消费，以及单 study projection error fail-closed 隔离。OPL runner 可用后，只接入这份 MAS-owned spec，不让 OPL 重写医学判断。
   2026-05-15 新增硬约束：transition table hardening 必须持续覆盖 `completion_receipt_consumption`。后续新增 work unit 时，必须同时补 `not redriven after receipt` 和 `stale receipt superseded by newer owner execution` 的 matrix/focused tests；stop-loss owner receipt 与 memory writeback receipt 都必须留在同一 oracle，其中 memory writeback 只允许 refs/counts/blocker 投影，不允许 body、quality/submission authorization 或 generic runner resume。
   2026-05-15 consumer guard 硬约束：`study_domain_transition_guard` 是 MAS-owned domain transition 的消费边界，runtime supervisor action projection、interaction arbitration 与 safe apply path 必须复用同一 decision-type 集合。`memory_writeback_receipt_consumed` 当前已列入 terminal/handoff guard，publication-route memory writeback receipt 只能进入 body-free refs/counts/blocker inspect，不得触发旧 work unit redrive、quality/submission authorization 或 generic runner resume。新增 transition 时必须同时声明是否 terminal/handoff、是否允许 runtime redrive、投到哪个受支持 action type，以及旧 prompt、旧 closeout、旧 authorization 是否应被阻断或覆盖。
   2026-05-15 AI reviewer record 硬约束：`return_to_ai_reviewer_workflow` 只能消费 AI reviewer-owned output 或 request 内完整 reviewer record。mechanical `publication_eval/latest.json` / `publication_gate_projection_v1` 是 blocker/projection/read-model 输入，不能作为 AI reviewer record 进入 workflow；request packet 必须声明 `future_facing_limitations_plan` 与 reviewer OS trace 必需字段，dispatch 缺字段时返回 typed owner blocker，不通过 workflow failure 或人工 patch current package 兜底。
7. `memory_and_lifecycle_live_receipts`
   在真实 workspace 中继续产生 accepted/rejected memory receipts、cleanup/restore/retention guarded apply receipts 和 artifact mutation permission receipts。
8. `paper_line_provider_apply_verification`
   功能边界迁移和通用 primitive handoff 之后，再用真实 paper line 验证 `OPL provider -> MAS sidecar -> MAS owner chain`。source-fingerprint rehydrate 和 source-keyed dispatch receipt 已完成；下一完成信号是 MAS owner receipt、artifact delta、gate replay、AI reviewer update、route decision、human gate、stop-loss 或 stable typed blocker 的多 paper-line scaleout。这里是 focused verification，不再作为 P0 前置条件。
9. `live_soak_and_production_closure`
   最后再跑多 paper-line provider-hosted guarded apply、长时 provider SLO、human gate/resume、owner-chain receipt、no-forbidden-write 和 App drilldown soak。live soak 只负责验收迁移后的目标边界，不负责决定是否开始迁移已知功能残留。
10. `legacy_physical_cleanup`
   当前真实 workspace tranche 已完成：旧 MDS physical root 已 archive/tombstone，targeted provenance refs 已改写到 archive/tombstone/ref-only surface，5 个 profile no-active-reference proof clean。后续只保留 drift guard 和新增 surface 审计；若出现新旧根 active ref，必须先回到 audit/apply surface，不手工 patch paper truth。

## 当前不能写成

- 不能写成 OPL provider proof 等于 MAS paper closure。
- 不能写成 MAS production-hosted paper automation 已闭合。
- 不能把 DM002 的 provider-hosted typed blocker 写成 artifact delta、AI reviewer update、route decision、human gate 或 publication closeout。
- 不能把 `mas_owner_receipt_present` / `stable_blocker` 写成 workspace mutation、artifact authority 放行或 paper closure；它只证明 MAS owner surface 已给出稳定阻塞回执。
- 不能写成 OPL 持有医学研究路线、publication-route memory body、publication quality、artifact authority 或 current package。
- 不能写成 MDS/DeepScientist 回到 MAS 默认 backend 或 OPL active Foundry Agent。
- 不能写成 MAS 还需要维护一套通用 agent OS；通用 runtime / lifecycle / workbench 能力应进入 OPL / shared family layer。
- 不能写成 MAS P0 migration 等于 live soak 完成；默认 scheduler owner 已迁到 OPL replacement，但显式 local LaunchAgent / tick script cleanup diagnostic 仍留在 MAS，需要 no-active-caller proof 后再物理退役。
- 不能写成 `workspace-legacy-physical-cleanup-apply` 是 MAS generic persistence、runtime lifecycle 或论文包清理引擎；它只是 gated archive/tombstone/reference rewrite surface。
