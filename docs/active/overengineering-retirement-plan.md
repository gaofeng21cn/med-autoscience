# MAS 过度设计退役与收薄计划

Owner: `MedAutoScience`
Purpose: `active_cleanup_plan`
State: `active_plan`
Machine boundary: 本文是人读规划与执行地图。机器真相继续归 `agent/`、`contracts/`、source、CLI/MCP/API 行为、runtime/controller durable surfaces、真实 workspace artifact、owner receipt 和 repo-native verification。

## 目标读法

MAS 长期形态收敛为 `Declarative Medical Research Pack + OPL generated/hosted surfaces + minimal authority functions`。MAS 保留医学研究 truth、stage semantics、AI reviewer / auditor quality gate、publication route、source readiness、artifact authority、owner receipt、typed blocker 和 safe action refs；generic runtime、queue、attempt、lifecycle、workspace shell、package/install、observability 与 generated UI/API 默认上收到 OPL。

完成口径分两层：

- 功能/结构完成：active caller 已迁移、legacy facade / wrapper / root artifact / vendored build helper 已删除或退为 provenance，focused tests 与 repo-native verify 通过。
- 后置证据：真实 paper-line owner receipt、publication-ready、domain-ready、provider long-soak、artifact mutation authority 另账验收。docs、focused tests、projection clean、contract pass 或 package build 不等于论文进展或 publication-ready。

## 模块定位

| 模块 | 保留职责 | 收薄方向 | 禁止承担 |
| --- | --- | --- | --- |
| PaperMission / StageOutcome | 默认 next-action authority、stage terminal decision、owner answer consumption、publication route。 | 保留 minimal authority function；删除 legacy diagnostic/current-work-unit/provider-admission producer tail 的 active facade。 | 不恢复旧 MAS-local scheduler、queue、attempt loop 或 provider carrier。 |
| Display / delivery | 医学图件 quality verdict、publication-facing renderer family、submission package authority refs。 | Display pack 作为版本化 pack / descriptor / gallery refs；build 时不靠 `setup.py` 私有复制投影制造隐藏库存。 | 不拥有 OPL generic Pack OS；display descriptor / lock 不授权 publication-ready。 |
| Runtime / control-plane tail | MAS-owned owner receipt、typed blocker、human gate、safe action refs。 | `runtime_protocol/runtime_surface_retirement` 等私有 runtime tail 只保 authority adapter / tombstone / refs-only projection；generic lifecycle 上收 OPL。 | 不维护第二套 runtime maintenance、health、storage lifecycle、attempt queue 或 workbench shell。 |
| Facade / product entry | 对外 entry 只暴露当前 domain-handler target 和 minimal product route。 | active caller 直连真实 owner module；wildcard-import facade 与历史 alias 删除或收薄。 | 不用 facade 保留旧 public projection 或兼容旧 mainline。 |
| Root artifacts / fixtures | repo 根只保源码、合同、文档和明确 fixture。 | 未引用 demo artifact 移入 history/assets 或删除；超大测试按 semantic case 拆分。 | 不把 demo PNG/PDF 当 active package、gallery truth 或 visual evidence。 |

## 落地清单

| 优先级 | 项 | 动作 | 验证 |
| --- | --- | --- | --- |
| P0 | MAS 私有 runtime/control-plane tail | 对 `runtime_protocol/runtime_surface_retirement`、legacy diagnostic/current-work-unit/provider-admission tail 做 active caller proof；可删则删，不可删则 tombstone 为 authority adapter / refs-only projection。 | focused tests；`scripts/verify.sh` 或 relevant regression lane；no forbidden MAS runtime writes。 |
| P0 | Wildcard-import facade | 迁移 active callers 到真实模块，删除/收薄 `controllers/submission_minimal.py`、`study_progress.py`、`study_runtime_decision.py`、`product_entry.py`、`medical_publication_surface.py` 等 facade tail。 | `rg` 无 active import 依赖退役 facade；focused CLI/domain-handler tests。 |
| P1 | Display pack vendoring / `setup.py` projection | 明确 display-pack 版本化来源；删除自定义复制路径，或把它降为显式 dev/provenance helper。 | package/build smoke；display focused tests；no hidden generated resource inventory。 |
| P1 | Root demo artifacts | 删除或移入 history/assets 未被 active tests/source 引用的 `Rplots.pdf`、`visual_qa_demo.png`。 | `rg` 引用证明；git diff sanity；visual/paper tests 不依赖 root artifact。 |
| P1 | ScholarSkills repo-local mirror | 保持 MAS 仓内 ScholarSkills mirror 物理退役 / 缺席；外部 `mas-scholar-skills` repo 与 workspace/quest `.codex/skills` 同步链路继续作为 canonical source / install path。 | `git ls-files 'plugins/*scholar*'` 无 tracked mirror；focused ScholarSkills capability / arsenal tests；no forbidden MAS runtime writes。 |
| P1 | Display Pack bundled fallback | 默认 Display Pack 解析只走外部 `mas-scholar-skills/packs/medical-display-core` 或显式 paper/repo config；repo-bundled `external/display-packs/medical-display-core` hidden fallback 已从打包清单、focused direct-source tests 和 tracked asset 中物理退役。 | `config/display_packs.toml` 无 fallback source；`display_pack_paths.py` 不再优先探测 `external/display-packs/medical-display-core`；focused display-pack tests；`git ls-files 'external/display-packs/**'` 无 tracked asset。 |
| P2 | 超大测试 / fixture | 将 narrative/compat/alias assertions 收敛成 semantic case modules；删除重复实现细节。 | line budget；focused tests；`scripts/verify.sh` appropriate lane。 |

## 2026-07-07 natural-boundary migration policy

- `_parts` 目录进入硬退役口径，不再作为新拆分方案、测试 discovery 入口或长期可接受的迁移输入；结构验收以 `find src tests -type d -name '*_parts'` 返回 0 为准。
- 对单纯 shard：微型 helper 合并回 owner module；过长 owner 按业务职责拆成 concrete module 或自然包名，不新增 wrapper、facade、compat alias 或二级 `_parts`。
- 对真实边界：保留 public owner entrypoint 只用于当前稳定 import contract；内部依赖应直连 concrete owner module，`import *`、dynamic `__all__`、`exec(compile(...))` 只作为退役前信号，不作为长期结构。
- 测试拆分使用既有 `*_cases/` discovery 形态、语义 case module 或自然 owner 包；源码拆分优先 concrete package name，例如 `study_domain_transition_table/`，而不是 `study_domain_transition_table_parts/`。
- `boundary_fitness` 已停止推荐 `{stem}_parts`，并把 `_parts` package marker、nested `_parts`、wildcard/dynamic export 和 exec/compile re-export 纳入 advisory；最终 hygiene gate 以 active source/test 目录零残留为准。
- 本文后续旧 evidence 行若仍出现 `_parts` 路径，只按历史 provenance 读取；不得作为 active docs、tests、source hygiene 的长期允许清单。

## P0 runtime/control-plane tail 执行注记

- 2026-07-06 active caller proof：`runtime_surface_retirement` 当前保留面按文件/函数归类，避免把 P0 runtime tail 写成泛化 blocker：
  - `private_runtime_residue_validators.py` / `private_runtime_residue_maintenance_validators.py` / `runtime_health_kernel_validators.py`：被 `runtime_surface_retirement.py` 调用的 no-authority / minimal authority validator；只能验证 MAS 侧 forbidden authority、OPL takeover tail 和 physical-delete gate，不能写 study truth、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。
  - `live_runtime_evidence_rollup.py::live_runtime_evidence_rollup_readback` 与 `public_root_commands.py live-runtime-evidence-rollup`：refs-only rollup readback；只能汇总 live-tail / live-gap evidence records 和 typed-blocker-required 状态，不能替代 live-runtime readiness、paper progress 或 production-ready evidence。
  - `live_tail_work_orders.py` / `live_runtime_gap_work_orders.py`：typed-blocker evidence guard；保留 duplicate / unknown / malformed / forbidden-source validator，防止 docs、focused tests、queue empty 或 repo-source retirement 被误读为 live runtime evidence。
  - `completion_evidence_layers.py` / `authority_flags.py` / `surface_helpers.py`：shared physical-delete gate helper；只服务 retirement audit 的 evidence-layer 分账和 forbidden-authority flag scan。
- 本轮可安全删除的是旧 private runtime residue aggregate fixture；合同与测试引用已改为具体 case module：`runtime_surface_no_authority_audit.py`、`domain_authority_refs_index.py`。这只删除重复 collection/旧路径别名，不改变 runtime authority 语义。
- 仍未删除的 runtime/control-plane helpers 均有 active caller 或 contract guard 作用；后续只能在对应 active caller 迁移、contract ref 改到 concrete module、focused tests 证明 no-forbidden-write 后继续物理收薄。
- `current_execution_envelope.build_current_execution_envelope()` 已退役删除：该函数固定返回 `{}`，active callers 仅把其结果写回 legacy diagnostic projection；caller 已改为直接投影 `{}`，只保留 `build_current_execution_evidence()` 与 explicit retired authority boundary。
- P0 runtime tail 复核结论：`runtime_surface_retirement.py` / validators、`live_runtime_evidence_rollup.py`、`live_tail_work_orders.py`、`live_runtime_gap_work_orders.py`、`current_work_unit.py` 和 legacy owner-callable receipt fallback 均仍有 active caller 或 fail-closed contract guard，不能按空 wrapper 删除。

## 停止条件

- 若某 facade 仍是唯一 public import 或 domain-handler target，先迁移 caller，再删除。
- 若某 runtime tail 仍签 MAS owner receipt、typed blocker、human gate 或 artifact authority，保留为 minimal authority function，不上收到 OPL。
- 若 display pack 改造缺 packaging proof，只能先文档化边界与删除无引用 artifact，不能声明 display-pack release-ready。

## 2026-07-07 current tranche

- `*_parts` 目录专项已从“排除项”改为硬退役验收项；各 lane 只在自身允许写集内迁移 active caller，最终合并口径必须让 `find src tests -type d -name '*_parts'` 归零，不能继续把历史 parts directory 写成长期可接受状态。
- `.venv/` 与 `outputs/` 是 ignored local residue；root 已清理，不构成 tracked code change、source morphology landing、contract delta 或 runtime evidence。
- `contracts/agent_tool_arsenal.json` 是 generated/authority contract；source ref 是 `src/med_autoscience/agent_tool_arsenal.py::build_agent_tool_arsenal_index`。不能手工缩减 JSON；后续只能修改 generator / owner surface，并跑 focused contract tests。
- Absorption gate：line-budget readback 候选与 `codex-parts-readback-v2-20260707` 同写集，wildcard reporting 候选与 `codex-parts-exports-v2-20260707` 同写集，domain-transition line-budget 剩余项与 `codex-parts-domain-transition-20260707` 同写集；这些代码候选未吸收进 `main`，等待对应 parts lane 交出或完成后再复核。

## 2026-07-08 overengineering direct-cleanup tranche

- Branding asset decision：按 owner 指令保留 raster logo `assets/branding/medautoscience-logo.png`，删除 README 不需要的 duplicate SVG `assets/branding/medautoscience-logo.svg`。README 仍使用 PNG，不声明 visual refresh 或 public branding release。
- Display gallery stale manifest：删除 `gallery/medical-display/scholar_display_pack_manifest.json`。该 manifest 指向已退役的 `external/display-packs` / `outputs/display-pack-gallery` 路径；真实 display pack source 继续归外部 `mas-scholar-skills` pack 与 MAS display-pack resolver/readback surface。
- Python package markers：删除测试树内 0 字节 `__init__.py` marker，保留 `src/med_autoscience/resources/__init__.py` 这类 package-data 边界。当前 pytest 仍通过 native discovery 与显式 aggregate/case module 工作；不恢复空 marker 当 package identity shim。
- Plugin alias retirement：删除 tracked symlink `plugins/mas` 与 `plugins/med-autoscience/skills/mas`。所有 active source / contracts / tests / docs ref 已迁到 `plugins/med-autoscience/.codex-plugin/plugin.json`、`plugins/med-autoscience/bin/medautosci-mcp`、`plugins/med-autoscience/skills/med-autoscience/SKILL.md`；`scripts/opl-module-healthcheck.sh` 现在阻止 legacy alias 恢复。
- Plugin launcher / installer currentness：`medautosci-mcp` launcher 识别 package 化后的 `src/med_autoscience/mcp_server/__init__.py`；`med_autoscience.cli` 与 `med_autoscience.mcp_server` 增加标准 `__main__.py`，让 clean runner 现有 `python -m <package>` entrypoint 不再依赖退役 file module shape。`codex_plugin_installer` 的 canonical plugin root 改为 `plugins/med-autoscience`，旧 `mas` 只作为本机 legacy symlink / test stub 清理对象。
- Public facade status：`product_entry.py` 修正 `dispatch_guarded_medical_paper_operator_action` 的 public `__all__` re-export 顺序。其余 facade 物理删除仍等待 `_parts` lane / public identity contract 更新；不能因 facade 很薄就删除仍被 public smoke、runtime controller identity 或 tests 消费的 import surface。
- `LazyModuleProxy` 保留：fresh evidence 显示它承担 controller currentness 语义，每次访问检查 `sys.modules` replacement；`tests/test_lazy_module_proxy.py` 与 `docs/runtime/control/controllers.md` 明确约束该行为。用一次性 `importlib.import_module` 替代会丢失 monkeypatch / reload currentness，不按 overengineering 删除。
- Runtime diagnostic tail 保留边界：`current_work_unit`、`current_execution_envelope` 仍有 active projection / owner-surface imports；`provider_admission` 是活跃控制面族，不是无引用 facade。后续只能在 active caller 迁移、replacement parity、no-forbidden-write 和 tombstone evidence 齐备后继续物理删除。
- Import-star status：top-level pytest aggregate entrypoints 保留以维持当前 focused nodeid / collection contract；production `import *` 主要绑定 `_parts` 退役 lane，当前 cleanup lane 不做孤立机械展开。后续在 parts lane 自然包吸收时按 concrete owner module 显式化。
- Fresh cleanup verification：`rtk proxy scripts/verify.sh` -> repo hygiene passed，smoke / line-budget 8 passed；`git diff --check` clean；active ref scan 对 `medautoscience-logo.svg`、`scholar_display_pack_manifest.json`、legacy file-module entrypoints 和 legacy plugin skill alias 无命中，只剩 retirement wording / healthcheck guard；`find tests -path '*__init__.py' -size 0 -print` 无输出，`src/med_autoscience/resources/__init__.py` 是保留的 package-data marker。
- Absorption status：此前根 checkout 被另一个 `_parts` 退役 lane 的 dirty write set 阻断；该 lane 已吸收到 `main`，本 cleanup tranche 已在其后 rebase，并按当前自然包路径保留 canonical plugin / package entrypoint cleanup。合入 `main` 仍必须走 fast-forward / evidence-bound absorption 和 post-absorption verify。

## 2026-07-06 P1 root artifact / facade cleanup evidence

- `Rplots.pdf`、`visual_qa_demo.png`：`rg --hidden --glob '!.git/**' --glob '!*.pdf' --glob '!*.png' 'Rplots\.pdf|visual_qa_demo\.png' .` 仅命中本文 P1 清单；两者是未被 active source/tests 引用的 root demo artifact，已删除而非移入 history。
- `submission_minimal.py`：已把 `study_delivery_sync/submission_delivery_descriptions.py` 的 `describe_submission_minimal_authority` 迁到 `submission_minimal.authority`；facade 仍被 `study_manual_finish.py` 作为 dynamic controller import 使用，且测试仍有 exact facade import，typed blocker 为 `facade_active_public_import_surface`。
- `study_runtime_decision.py`：已把 `domain_status_projection.py` 的 `_status_payload` / `_status_state` / `_record_quest_runtime_audits` 迁到真实 parts；facade 仍被 publication runtime / gate dynamic import 和 parts `__name__` guard 使用，typed blocker 为 `facade_active_runtime_controller_identity`.
- `study_progress.py`、`product_entry.py`、`medical_publication_surface.py`：production surface 仍存在 controller identity / `sys.modules` 依赖，测试 exact facade imports 分别为 71 / 38 / 11 个文件；本轮不做批量 public surface 删除，typed blocker 为 `facade_active_public_import_surface`.

## 2026-07-06 P1 stage route contract source cleanup evidence

- `stage_route_contract` loader 真实路径：源码 checkout 优先读取 `agent/stages/stage_route_contract.yaml`；安装包环境回落到 `med_autoscience.resources/stage_route_contract.yaml`。两者 YAML payload 等价，package resource 是显式 tracked package-data，不再靠 build hook 从 `agent/stages` 私下复制。
- 已删除 `setup.py` 私有 `build_py` / `sdist` copy hook；`pyproject.toml` 现有 `[tool.setuptools.package-data]` 继续声明 `med_autoscience.resources = ["stage_route_contract.yaml"]`，`MANIFEST.in` 显式包含 package resource 与 canonical agent source。
- 已删除 repo-tracked 第三份同内容 template YAML；当前保留口径是一个 canonical source (`agent/stages/stage_route_contract.yaml`) 加一个 packaged resource (`src/med_autoscience/resources/stage_route_contract.yaml`)。
- Fresh verification：`rtk proxy scripts/run-python-clean.sh -m pytest tests/test_stage_route_assets.py tests/test_stage_route_contract.py tests/test_stage_route_ordinary_progress_handoff_contract.py tests/test_release_workflow.py` -> 47 passed；`rtk proxy scripts/run-build-clean.sh` -> sdist/wheel built；`rtk proxy git diff --check` -> clean。
- Archive readback from `/tmp/mas-stage-route-lane-c-dist`：wheel/sdist both contain `med_autoscience/resources/stage_route_contract.yaml`; neither contains `setup.py` nor the deleted template YAML; sdist still contains `agent/stages/stage_route_contract.yaml`.
- Follow-up cleanup：`sync_agent_entry_assets` no longer writes the deleted template YAML; public-entry tests, preflight classification refs, skeleton mapping, and docs now point to canonical `agent/stages/stage_route_contract.yaml` plus packaged `src/med_autoscience/resources/stage_route_contract.yaml`.
- Follow-up verification：`rg -n "templates/stage_route_contract\.yaml" . --glob '!uv.lock'` -> no matches；`rtk proxy scripts/run-python-clean.sh -m pytest tests/test_stage_route_assets.py tests/test_stage_route_contract.py tests/test_stage_route_ordinary_progress_handoff_contract.py tests/test_cli_cases/public_entry_commands.py tests/test_dev_preflight_contract.py tests/dev_preflight_contract_cases/classification_and_surface_cases.py tests/test_opl_family_persistence_adapter.py -k 'stage_route or sync_agent_entry_assets or preflight or skeleton'` -> 123 passed, 37 deselected；`rtk proxy scripts/run-build-clean.sh --outdir /tmp/mas-stage-route-lane-c-dist-v2` -> sdist/wheel built；archive readback confirms package resource present and deleted template absent.
- 本 lane 只收薄 stage-route contract packaging/source duplication；不写 study truth、paper body、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。

## 2026-07-06 P1 ScholarSkills repo-local mirror retirement evidence

- 两个 legacy ScholarSkills plugin mirror 目录在本 lane checkout 中均无 tracked 文件；MAS 程序仓不再携带 ScholarSkills repo-local mirror。
- `docs/decisions.md` 与 `docs/runtime/designs/mas_opl_capability_module_operating_model.md` 已把 mirror 口径从 `dev/review` 改为 `physically retired / tombstone-only`；外部 `mas-scholar-skills` repo、`scientific_capability_registry`、capability map truth 和 workspace/quest `.codex/skills/<skill_id>` 同步链路保持 canonical。
- `src/med_autoscience/scholarskills_local_install.py`、`src/med_autoscience/scholarskills_capability_modules.py` 与 `contracts/agent_tool_arsenal.json` 中的 ScholarSkills local install surface 已把 MAS program repo mirror 标为 `physically_retired` / `must_exist=false`；该 readback 只表达 no-authority install/tombstone 边界，不授权 MAS 复制、安装或执行 repo-local mirror，也不输出应存在的 repo-local mirror path。
- 2026-07-07 follow-up：`agent/primary_skill/SKILL.md` 与 `agent/principles/domain-specialization.md` 已写回 AI-first skill exposure boundary：先按写作、审稿、文献、统计、表图、数据治理和投稿等逻辑能力审计，再决定物理暴露方式。默认把弹性判断吸收到既有 ScholarSkills professional skill；只有复用、版本、安装/发现或 metadata 隔离收益明确时才新增或拆分 physical skill。MAS 本仓继续只保 owner/runtime/contract authority 与 generated descriptor / runtime projection 的轻量 locator、receipt、allowed-write、sync/install/readback 边界。

## 2026-07-06 P2 test surface cleanup evidence

- `tests/test_cli_cases/paper_mission_command_cases/materialized_readback.py` 已把 stage-closure-ledger 相关 case 拆到 `tests/test_cli_cases/paper_mission_command_cases/materialized_readback_cases/test_stage_closure_ledger_readback.py`；原入口从约 1705 行降到约 1471 行，回到 preferred boundary advisory 范围。
- `tests/test_cli_cases/paper_mission_command_cases/receipt_owner_consumption.py` 已把 route checkpoint / stage closure 相关 case 拆到 `tests/test_cli_cases/paper_mission_command_cases/receipt_owner_consumption_cases/route_checkpoint_stage_closure.py`；原入口从约 1610 行降到约 1320 行，退出 clear `>1500` 超线范围。
- `tests/test_cli_cases/paper_mission_commands_cases/stage_closure_terminalizer.py` 已把 source-selection / route-back precedence case 拆到 `tests/test_cli_cases/paper_mission_commands_cases/stage_closure_terminalizer_cases/test_source_selection.py`；原入口从约 2493 行降到约 1154 行，新 case 文件约 1350 行，退出 clear `>1500` 超线范围。
- `tests/test_quality_repair_batch_cases/medical_prose_write_repair.py` 已把 reviewer-revision intake story refresh case 拆到 `tests/test_quality_repair_batch_cases/medical_prose_write_repair_cases/test_reviewer_revision_refresh.py`；原入口从约 1547 行降到约 1463 行，退出 clear `>1500` 超线范围。
- `tests/test_cli_cases/paper_mission_command_cases/drive_and_route_handoff.py` 已把 consumption ledger readback、domain transition route、direct next action / OPL stage route case 拆到 `tests/test_cli_cases/paper_mission_command_cases/drive_and_route_handoff_cases/`；原入口从 3168 行降到 965 行，保留顶层 thin import 入口维持既有 nodeid/discovery。拆分时暴露的 route-checkpoint recursion 已在 `paper_mission_receipt_owner_consumption.py` 修复为只从 explicit / ledger / existing decision 生成 synthetic carrier，focused drive handoff tests 32/32 通过。
- 该拆分只改变测试组织，不改 PaperMission / StageOutcome 语义，不写真实 study artifact、publication eval、controller decision、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。
- 当前 P2 完成口径是 first safe slice landed；剩余超大测试 / fixture 继续按 source-governance lane 逐个语义拆分，不能把 line-budget advisory 当成 runtime / publication / domain-ready blocker。

## 2026-07-06 MAS source morphology cleanup evidence

- `src/med_autoscience/cli/paper_mission_commands.py` 已把 stage-closure terminalizer readback helper cluster 与 consumption-ledger readback helper cluster 收敛到 canonical `src/med_autoscience/cli/paper_mission_commands/`；旧复数目录 `paper_mission_commands/` 已退役。主入口从 2775 行降到 934 行，回到 preferred boundary 内。吸收时保留了 terminalizer currentness 修复中的 stage-packet identity / semantic priority 排序。
- `src/med_autoscience/controllers/paper_mission_receipt_owner_consumption.py` 已拆到 `src/med_autoscience/controllers/paper_mission_receipt_owner_consumption/`，主入口从 1497 行降到 453 行；吸收时保留 newer stage-closure decision 不被 stale route carrier 覆盖的 currentness 修复。
- `src/med_autoscience/controllers/quality_repair_batch/medical_prose_story_surface.py` 已把 DPCC display repairs、manuscript、table 与 common helper 拆到 `src/med_autoscience/controllers/medical_prose_story_surface/`；主入口从约 2730 行降到 596 行。
- `src/med_autoscience/paper_mission_opl_readback.py` 已把 route identity helper cluster 拆到 `src/med_autoscience/paper_mission_opl_readback/route_identity.py`；主入口从约 1630 行降到 1185 行，退出 clear `>1500` 超线范围。
- `src/med_autoscience/cli/paper_mission_commands/drive_readback.py` 已把 direct next action helper cluster 拆到 `src/med_autoscience/cli/paper_mission_commands/drive_readback/direct_next_action.py`；主入口从约 1596 行降到 1456 行，退出 clear `>1500` 超线范围。
- `tests/test_cli_cases/paper_mission_commands_cases/stage_closure_terminalizer_cases/test_source_selection.py` 和 `tests/submission_minimal_cases/package_core_and_authority.py` 已分别拆成 thin entrypoint + semantic case modules，两个入口均降到 4 行。
- `src/med_autoscience/cli/paper_mission_commands/stage_closure_terminalizer_readback.py` 已删除重复不可达 return，并收紧等价布尔 / transaction-stage helper 表达式；文件从 1506 行降到 1500 行，退出 clear `>1500` 超线范围。
- `tests/test_paper_mission_opl_readback.py` 已把 route-back precedence case 拆到 `tests/test_paper_mission_opl_readback_cases/route_back_precedence_cases.py`；顶层从 1434 行降到 1195 行，保留 thin import 入口维持既有 discovery。
- `tests/test_cli_cases/paper_mission_command_cases/materialized_readback.py` 已把 governed consumption ledger case 拆到 `tests/test_cli_cases/paper_mission_command_cases/materialized_readback_cases/test_governed_consumption_ledger.py`；顶层从 1471 行降到 1047 行，保留 preferred `>1000` advisory。
- `tests/test_cli_cases/paper_mission_command_cases/materialized_readback.py` 已继续把 stage-closure domain-transition suppression case 拆到 `tests/test_cli_cases/paper_mission_command_cases/materialized_readback_cases/test_stage_closure_domain_transition.py`；顶层从 1047 行降到 980 行，退出 preferred `>1000` advisory。
- `tests/test_cli_cases/paper_mission_command_cases/typed_blocker_resolution.py` 已把 submission-authority owner-gate retirement case 拆到 `tests/test_cli_cases/paper_mission_command_cases/typed_blocker_resolution_cases/test_submission_authority_owner_gate.py`；顶层从 1049 行降到 947 行，退出 preferred `>1000` advisory。
- `tests/test_cli_cases/paper_mission_command_cases/consume_submission_package.py` 已把 route handoff / transaction readback tail case 拆到 `tests/test_cli_cases/paper_mission_command_cases/consume_submission_package_cases/test_route_handoff_readback.py`；顶层从 1095 行降到 939 行，退出 preferred `>1000` advisory。
- `tests/test_gate_clearing_batch_cases/direct_migration_transportability_registry_sync.py` 已把 registry display-id drift case 拆到 `tests/test_gate_clearing_batch_cases/direct_migration_transportability_registry_sync_cases/test_registry_drift.py`；顶层从 1027 行降到 975 行，退出 preferred `>1000` advisory。
- `tests/display_surface_materialization_cases/cohort_flow_layout_materialization.py` 已把 legacy summary-card layout-qc case 拆到 `tests/display_surface_materialization_cases/cohort_flow_layout_materialization_cases/test_layout_qc_legacy_summary.py`；顶层从 1020 行降到 922 行，退出 preferred `>1000` advisory。
- `tests/test_opl_standard_pack.py` 已把 ordinary default-stage case 拆到 `tests/test_opl_standard_pack_cases/stage_contract_cases.py`；顶层从 1005 行降到 988 行，退出 preferred `>1000` advisory。
- `tests/test_quality_repair_batch_cases/dm002_writer_delta_preservation.py` 已把 after-story-repair route case 拆到 `tests/test_quality_repair_batch_cases/dm002_writer_delta_preservation_cases/test_after_story_repair_route.py`；顶层从 1047 行降到 957 行，退出 preferred `>1000` advisory。本 lane 只用 moved case 与排除既有 `current_hardening_preserves_external_validation_writer_delta` baseline 红的 focused 验证作为证据。
- `tests/test_display_pack_e2e_runtime.py` 已把 subprocess renderer case 拆到 `tests/test_display_pack_e2e_runtime_cases/subprocess_renderer_cases.py`；顶层从 1050 行降到 987 行，退出 preferred `>1000` advisory。本 lane 单独验证 moved subprocess case `1 passed`；全 e2e runtime suite 在 clean/current main 均保留既有 real-R / cohort-flow `{render_mode}` placeholder baseline 红，需另走 display renderer repair lane。
- `tests/test_agent_lab_medical_manuscript_quality.py` 已把 refs-only surface、feedback self-evolution、obesity quality profile tail cases 拆到 `tests/test_agent_lab_medical_manuscript_quality_cases/refs_only_feedback_obesity.py`；顶层从 1123 行降到 944 行，退出 preferred `>1000` advisory。Focused 验证 `tests/test_agent_lab_medical_manuscript_quality.py tests/test_agent_lab_medical_manuscript_quality_cases` 为 9 passed。
- `tests/test_display_pack_agent.py` 已把 render / CLI tail cases 拆到 `tests/display_pack_agent_cases/test_render_and_cli.py`；顶层从 1098 行降到 861 行，退出 preferred `>1000` advisory。非 baseline 子集验证为 38 passed / 5 deselected；完整 suite 在拆分前后均保留 5 个既有 display-pack behavioral failures（`needs_repair` vs `ready_to_render`、cohort-flow dependency preflight、typed repair owner、subprocess `{render_mode}` placeholder），本 lane 不改 `src/**` 或测试语义。
- 2026-07-07 读回：`scripts/line_budget.py` advisory 从 32 降到 29；新增吸收的三项均为测试组织拆分，不声明 display runtime readiness、publication-ready、paper progress、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。
- `tests/test_quality_repair_batch_cases/medical_prose_write_repair.py` 已把 route-context / publication-gate replay / registry-story recovery tail cases 拆到 `tests/test_quality_repair_batch_cases/medical_prose_write_repair_cases/test_route_context_and_registry_recovery.py`；顶层从 1461 行降到 925 行，退出 preferred `>1000` advisory。Focused 验证 `tests/test_quality_repair_batch_cases/medical_prose_write_repair.py` 为 11 passed。
- `tests/test_study_progress_mission_summary.py` 已把 next-action / typed-blocker / stage-closure ledger tail cases 拆到 `tests/test_study_progress_mission_summary_cases/next_action_stage_closure.py`；顶层从 1368 行降到 989 行，退出 preferred `>1000` advisory。Focused 验证 `tests/test_study_progress_mission_summary.py tests/test_study_progress_mission_summary_cases` 为 26 passed。
- `tests/test_cli_cases/paper_mission_command_cases/receipt_owner_consumption.py` 已把 route-checkpoint / stage-closure cluster 拆到 `tests/test_cli_cases/paper_mission_command_cases/receipt_owner_consumption_cases/route_checkpoint_stage_closure.py`；顶层从 1447 行降到 927 行，退出 preferred `>1000` advisory。Focused 验证 `receipt_owner_consumption.py receipt_owner_consumption_cases` 为 13 passed。
- 2026-07-07 第二批读回：`scripts/line_budget.py` advisory 从 29 降到 26；本批仍只做测试组织拆分，不声明 runtime readiness、publication-ready、paper progress、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。
- `tests/test_domain_transition_table_ai_reviewer_routeback.py` 已把 materialized-controller / story-recheck tail cases 拆到 `tests/test_domain_transition_table_ai_reviewer_routeback_cases/materialized_controller_story_recheck.py`；顶层从 1226 行降到 954 行，退出 preferred `>1000` advisory。Focused 验证 `tests/test_domain_transition_table_ai_reviewer_routeback.py tests/test_domain_transition_table_ai_reviewer_routeback_cases` 为 20 passed。
- `tests/test_submission_minimal_display_surface.py` 已把 display entry / catalog path / generated asset cases 拆到 `tests/test_submission_minimal_display_surface_cases/test_display_entries_and_assets.py`；顶层从 1269 行降到 889 行，退出 preferred `>1000` advisory。Focused 验证 `tests/test_submission_minimal_display_surface.py tests/test_submission_minimal_display_surface_cases` 为 19 passed。
- `tests/display_surface_materialization_cases/basic_displays_and_renderers.py` 已把 official-shell output 与 pack-runtime renderer cases 拆到 `tests/display_surface_materialization_cases/basic_displays_and_renderers_cases/`；顶层从 1248 行降到 972 行，退出 preferred `>1000` advisory。Focused 验证 `basic_displays_and_renderers.py basic_displays_and_renderers_cases` 为 24 passed，`scripts/line_budget.py --list` 对该目标无命中。
- `tests/test_cli_cases/paper_mission_command_cases/drive_and_route_handoff_cases/direct_next_action_and_opl_stage.py` 已把 direct next-action handoff cluster 拆到 `tests/test_cli_cases/paper_mission_command_cases/drive_and_route_handoff_cases/direct_next_action_and_opl_stage_cases/test_direct_next_action_handoff.py`；顶层从 1278 行降到 988 行，退出 preferred `>1000` advisory。Focused 验证 `direct_next_action_and_opl_stage.py direct_next_action_and_opl_stage_cases` 为 16 passed，`scripts/line_budget.py --list` 对该目标无命中。
- 2026-07-07 第三批读回：`scripts/line_budget.py` advisory 从 26 降到 22；本批继续只做测试组织拆分，不声明 runtime readiness、publication-ready、paper progress、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。
- `tests/test_paper_story_repair_executor.py` 已把 DM002 story-surface / body-authority tail cases 拆到 `tests/test_paper_story_repair_executor_cases/test_dm002_story_surface.py`；顶层从 1208 行降到 967 行，退出 preferred `>1000` advisory。Focused 验证 `tests/test_paper_story_repair_executor.py tests/test_paper_story_repair_executor_cases` 为 15 passed，`scripts/line_budget.py --list` 对该目标无命中。
- `tests/test_cli_cases/paper_mission_commands_cases/stage_closure_terminalizer.py` 已把 terminal-outcome tail cases 拆到 `tests/test_cli_cases/paper_mission_commands_cases/stage_closure_terminalizer_cases/test_terminal_outcomes.py`；顶层从 1154 行降到 895 行，退出 preferred `>1000` advisory。Focused 验证 `stage_closure_terminalizer.py stage_closure_terminalizer_cases` 为 22 passed，`scripts/line_budget.py --list` 对该目标无命中。
- `tests/test_paper_mission_opl_readback.py` 已把 runtime running/readback tail cases 拆到 `tests/test_paper_mission_opl_readback_cases/runtime_running_cases.py`；顶层从 1195 行降到 974 行，退出 preferred `>1000` advisory。Focused 验证 `tests/test_paper_mission_opl_readback.py tests/test_paper_mission_opl_readback_cases` 为 51 passed，`scripts/line_budget.py --list` 对该目标无命中。
- 2026-07-07 第四批读回：`scripts/line_budget.py` advisory 从 22 降到 19；本批继续只做测试组织拆分，不声明 runtime readiness、publication-ready、paper progress、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。
- `tests/test_cli_cases/paper_mission_command_cases/receipt_owner_consumption.py` 已继续把 apply-mode tail cases 拆到 `tests/test_cli_cases/paper_mission_command_cases/receipt_owner_consumption_cases/test_apply_modes.py`；顶层从 1021 行降到 888 行，退出 preferred `>1000` advisory。Focused 验证 `receipt_owner_consumption.py receipt_owner_consumption_cases` 为 14 passed。
- `src/med_autoscience/cli/paper_mission_commands/stage_closure_terminalizer_readback.py` 已把 owner-consumption carrier alignment helper cluster 拆到 `src/med_autoscience/cli/paper_mission_commands/owner_consumption_alignment.py`；主入口从 1563 行降到 1365 行，退出 clear `>1500` 超线范围，但仍保留 preferred `>1000` advisory，需后续继续按自然 readback 责任边界收薄。
- 2026-07-07 第五批读回：在 consumed-carrier readback fix 吸收后，`scripts/line_budget.py` advisory 从 20 降到 19，且当前没有 clear `>1500` 超线项。Root focused 验证 `receipt_owner_consumption.py receipt_owner_consumption_cases materialized_readback_cases/test_receipt_owner_consumption.py stage_closure_terminalizer.py stage_closure_terminalizer_cases` 为 47 passed。本批不声明 runtime readiness、publication-ready、paper progress、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。
- `src/med_autoscience/controllers/study_domain_transition_table.py` 已压缩常量格式噪声，主入口从 1002 行降到 998 行，退出 preferred `>1000` advisory。Focused 验证 `tests/test_domain_transition_table_ai_reviewer_routeback.py tests/test_domain_transition_table_ai_reviewer_routeback_cases` 为 20 passed。
- `src/med_autoscience/policies/medical_publication_surface.py` 已压缩三组 `frozenset` 常量格式噪声，文件从 1007 行降到 991 行，退出 preferred `>1000` advisory。Focused 验证 `tests/test_medical_publication_surface.py` 为 110 passed。
- 2026-07-07 第六批读回：`scripts/line_budget.py` advisory 从 19 降到 17；root 组合 focused 验证 `tests/test_domain_transition_table_ai_reviewer_routeback.py tests/test_domain_transition_table_ai_reviewer_routeback_cases tests/test_medical_publication_surface.py` 为 130 passed。本批只做 source morphology 收薄，不声明 runtime readiness、publication-ready、paper progress、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。
- `src/med_autoscience/controllers/study_progress/projection.py` 已压缩 import 区格式噪声，文件从 1021 行降到 996 行，退出 preferred `>1000` advisory。静态验证 `py_compile`、`git diff --check`、`scripts/line_budget.py` 通过；`tests/test_study_progress.py` 在 main 基线已保留既有 projection 行为红，不作为本 lane 通过证据。
- `src/med_autoscience/controllers/study_progress/mission_summary.py` 已压缩 mission-summary 表达与重复换行，文件从 1039 行降到 944 行，退出 preferred `>1000` advisory。Focused 验证 `tests/test_study_progress_mission_summary.py tests/test_study_progress_mission_summary_cases` 为 26 passed。
- `src/med_autoscience/controllers/submission_minimal/shared_base.py` 已压缩 submission shared-base helper 表达，文件从 1032 行降到 985 行，退出 preferred `>1000` advisory。Focused 验证 `tests/test_submission_minimal_display_surface.py tests/test_publication_profiles.py` 为 16 passed。
- 2026-07-07 第七批读回：`scripts/line_budget.py` advisory 从 17 降到 14；root 组合 focused 验证 `tests/test_study_progress_mission_summary.py tests/test_study_progress_mission_summary_cases tests/test_submission_minimal_display_surface.py tests/test_publication_profiles.py` 为 42 passed。本批不声明 runtime readiness、publication-ready、paper progress、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。
- `src/med_autoscience/external_learning_adoption_closure.py` 已把 framework catalog helper cluster 拆到 `src/med_autoscience/external_learning_adoption_closure/framework_catalog.py`；主入口从 1073 行降到 955 行，退出 preferred `>1000` advisory。Focused 验证 `tests/test_external_learning_adoption_closure.py` 为 5 passed，root 组合验证为 8 passed。
- `src/med_autoscience/controllers/submission_minimal/package_builder.py` 已把 supplementary material / figure export / combined review helper cluster 拆到 `src/med_autoscience/controllers/submission_minimal/package_builder/supplementary_material.py`；主入口从 1280 行降到 883 行，退出 preferred `>1000` advisory。Focused 验证 `tests/submission_minimal_cases tests/test_publication_profiles.py` 为 2 passed；root 补跑 display smoke `test_create_submission_minimal_package_preserves_display_surface_metadata` 为 1 passed。
- `src/med_autoscience/controllers/display_surface_materialization/materialize.py` 已把 pre-materialized markdown table helper cluster 拆到 `src/med_autoscience/controllers/display_surface_materialization/materialize/pre_materialized_tables.py`；主入口从 1109 行降到 989 行，退出 preferred `>1000` advisory。Root focused 验证 `tests/display_surface_materialization_cases/basic_displays_and_renderers.py tests/display_surface_materialization_cases/basic_displays_and_renderers_cases` 为 24 passed。
- `src/med_autoscience/controllers/medical_prose_story_surface/dpcc_tables.py` 已把 bounded supplementary table helper cluster 拆到 `src/med_autoscience/controllers/medical_prose_story_surface/dpcc_tables/bounded_tables.py`；主入口从 1103 行降到 726 行，退出 preferred `>1000` advisory。Root focused 验证 `tests/test_quality_repair_batch_cases/medical_prose_write_repair.py tests/test_quality_repair_batch_cases/medical_prose_write_repair_cases` 为 18 passed。
- 2026-07-07 第八批读回：`scripts/line_budget.py` advisory 从 14 降到 10；root worktree 清理到只剩 `main`，4 条并行 source morphology lane 均已 patch-equivalent 吸收到 `main` 并删除临时 worktree/branch。本批只做行为保持源码拆分和 line-budget 收薄，不声明 runtime readiness、publication-ready、paper progress、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。
- `src/med_autoscience/cli/paper_mission_commands/materialized_mission_readback.py` 已把 owner-consumption / stage-attempt identity helper cluster 拆到 `src/med_autoscience/cli/paper_mission_commands/materialized_owner_consumption.py`；主入口从 1210 行降到 943 行，退出 preferred `>1000` advisory。
- `src/med_autoscience/paper_mission_opl_readback.py` 已把 OPL task closeout / running-attempt readback helper cluster 拆到 `src/med_autoscience/paper_mission_opl_readback/opl_task_readback.py`；主入口从 1185 行降到 718 行，退出 preferred `>1000` advisory。
- `src/med_autoscience/scientific_capability_registry.py` 已把静态 capability catalog 与 `_capability` builder 拆到 `src/med_autoscience/scientific_capability_registry_catalog.py`；主入口从 1154 行降到 509 行，退出 preferred `>1000` advisory。
- `src/med_autoscience/controllers/agent_lab_medical_manuscript_quality.py` 已把 refs / JSON / JSONL helper cluster 拆到 `src/med_autoscience/controllers/agent_lab_medical_manuscript_quality/refs_json_helpers.py`；主入口从 1152 行降到 903 行，退出 preferred `>1000` advisory。
- `src/med_autoscience/controllers/medical_prose_story_surface/dm002_external_validation.py` 已把 display payload / figure catalog / table catalog helper cluster 拆到 `src/med_autoscience/controllers/medical_prose_story_surface/dm002_external_validation/catalog_payloads.py`；主入口从 1144 行降到 933 行，退出 preferred `>1000` advisory。
- 2026-07-07 第九批 / final source morphology 读回：五条并行 lane 均已 patch-equivalent cherry-pick 吸收到 `main`，临时 worktree/branch 已删除。Root fresh verification：`scripts/line_budget.py` exit 0 且无 advisory；`python -m py_compile` 覆盖五条 lane 的 touched modules；focused pytest 组合为 `materialized_readback` / OPL readback 77 passed、scientific registry / agent lab 48 passed、DM002 story repair 26 passed；`git diff --check` clean。
- 当前 source/test morphology 完成口径：本轮 line-budget 规划已从 32 个 advisory 收敛到 0 个 advisory；所有本轮目标文件均退出 preferred boundary。本轮只做行为保持源码/测试组织收薄，不声明 runtime readiness、publication-ready、paper progress、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。

## 2026-07-06 MAS wildcard-import facade final slice

- `submission_minimal.py`、`study_runtime_decision.py` 已删除 facade 内 `import *`，保留 public module identity 但改为按固定 parts 列表 re-export；与 main 对比，两个 facade 的可见 symbol 集合保持一致。
- `study_progress.py`、`product_entry.py` 已删除 facade 内 `import *`，改为与 `submission_minimal.py` / `study_runtime_decision.py` 相同的固定 parts re-export；public module identity 保留，两个 facade 不再依赖 wildcard import 扩散 parts namespace。Focused verification：product-entry / truth projection / study progress import surfaces 159 passed；`rg -n "import \*" src/med_autoscience/controllers/study_progress.py src/med_autoscience/controllers/product_entry.py` 无命中；`scripts/line_budget.py` exit 0。
- `medical_publication_surface.py` 已删除 facade 内 5 个 `import *`，改为同一固定 parts re-export 模式；public module identity 保留。Package-import symbol parity 与旧 wildcard 版本一致：`current=125 / old=125 / missing=[] / added=[]`；5 个候选 facade 文件均无 `import *`。
- Active caller 收薄：CLI `export-submission-minimal` 与 `submission_targets.export_submission_targets()` 改为调用 `submission_minimal.package_builder`；`study_manual_finish` 改为调用 `submission_minimal.authority`；publication gate / study progress / stage outcome publication-eval materialization 改为函数内直连 `study_runtime_decision.publication_and_submission`，避免恢复 facade import 依赖。
- Active caller 继续收薄：`real_paper_autonomy_live_supervisor_canary.py` 不再通过 `controllers.study_progress` facade 调用 `read_study_progress()`，改为直连 `study_progress.projection.read_study_progress()`；focused canary tests `tests/test_real_paper_autonomy_live_supervisor_canary.py` 为 11 passed。
- Active caller 继续收薄：`domain_action_request_materializer/fresh_progress_current_action.py` 不再通过 `controllers.study_progress` facade 读取 fresh progress，改为直连 `study_progress.projection.read_study_progress()`；focused materializer tests `tests/domain_action_request_materializer_cases/test_current_work_unit_action_authority.py` 为 14 passed。
- Active caller 继续收薄：`stage_outcome_authority/stage_native_dispatch_selection.py` 不再通过 `controllers.study_progress` facade 读取 stage-native fresh progress，改为直连 `study_progress.projection.read_study_progress()`；focused currentness selector tests `tests/test_stage_outcome_authority_currentness_selectors.py` 为 9 passed。
- Active caller 继续收薄：`stage_outcome_authority/current_writer_handoff.py` 不再通过 `controllers.study_progress` facade 读取 current writer handoff fresh progress，改为函数内直连 `study_progress.projection.read_study_progress()`；静态验证 `py_compile`、`scripts/line_budget.py`、`git diff --check` 通过，`tests/domain_action_request_materializer_cases/test_quality_repair_writer_handoff.py` 在当前 main 基线已有 3 failed / 1 passed，不作为本 lane 通过证据。
- Active caller 继续收薄：`cli/paper_mission_commands/consumption_ledger_readback.py` 不再通过 `controllers.study_progress` facade 读取 consumption ledger overlay progress，改为直连 `study_progress.projection.read_study_progress()`；focused consumption-ledger readback tests `tests/test_cli_cases/paper_mission_command_cases/drive_and_route_handoff_cases/consumption_ledger_readback.py` 为 6 passed。
- Active caller 继续收薄：`owner_route_handoff/domain_handler_export.py` 不再通过 `controllers.study_progress` facade 读取 domain-handler fresh study progress，改为函数内直连 `study_progress.projection.read_study_progress()`；静态验证与 direct projection reader check 通过，worktree focused pytest 红因 sibling worktree display-pack source 解析问题，不作为本 lane 阻断。
- Active caller 继续收薄：`paper_mission_owner_surface.py` 与 `paper_mission_owner_surface/canonical_inputs.py` 不再通过 `controllers.study_progress` facade 读取 owner-surface progress projection，改为直连 `study_progress.projection.read_study_progress()`；focused owner-surface tests `tests/paper_mission_owner_surface_cases/test_current_controller_route_closure.py tests/paper_mission_owner_surface_cases/test_ai_reviewer_record_os_currentness.py` 为 13 passed。
- Active caller 继续收薄：`submission_minimal/post_materialization_sync.py` 不再通过 `controllers.study_progress` facade 读取 post-materialization progress，改为函数内直连 `study_progress.projection.read_study_progress()`；focused post-materialization sync test `tests/submission_minimal_cases/frontiers_profile_and_sync.py -k post_materialization_sync` 为 1 passed。
- Active caller 继续收薄：`product_entry/workspace_cockpit/cockpit_payload.py` 不再通过 `controllers.study_progress` facade 读取 workspace cockpit study snapshot，改为直连 `study_progress.projection.read_study_progress()`；focused truth projection test `tests/test_truth_projection_surfaces.py::test_workspace_cockpit_study_item_carries_truth_snapshot_summary` 为 1 passed。
- Active caller 继续收薄：`study_runtime_decision/status_projection_shell/read_model_projection_assembly.py` 不再通过 `controllers.study_progress` facade 组装 status read-model progress projection，改为直连 `study_progress.projection.build_study_progress_projection()`；focused status projection tests `tests/test_progress_projection_evidence_adoption.py tests/test_control_plane_generalization_cases/test_runtime_facts.py` 为 10 passed。
- Active caller 继续收薄：`domain_entry.py` 的 `study-progress` handler 不再通过 `controllers.study_progress` facade 读取 progress，改为直连 `study_progress.projection.read_study_progress()`；测试 patch seam 同步改到 domain-entry 模块内 concrete reader。Focused direct seam test `tests/test_domain_entry.py::test_external_caller_can_consume_domain_entry_contract_without_repo_local_helper` 为 1 passed；静态验证 `py_compile`、`scripts/line_budget.py`、`git diff --check` 和 facade-call `rg` check 通过。完整 `tests/test_domain_entry.py` 在当前 worktree 仍因 display-pack source 相对路径解析到不存在的 worktree sibling `../mas-scholar-skills` 红 2 个 display-pack case，不作为本 lane 阻断。
- Active caller 继续收薄：`cli.py` / `cli/study_read_commands.py` 的 `study-progress` CLI handler 不再通过 `controllers.study_progress` facade 读取或渲染 progress，改为注入 concrete `study_progress.projection.read_study_progress()` 与 `study_progress.markdown_projection_rendering.render_study_progress_markdown()`；focused CLI tests `runtime_and_quality_commands` / `public_entry_commands` / `study_progress_monitoring_commands` 相关 8 tests passed。
- Active caller 继续收薄：`product_entry/shared_base.py` 与 `workspace_cockpit/launch_surface.py` 不再暴露或调用 `controllers.study_progress` facade；launch surface 改为直连 `study_progress.projection.build_study_progress_projection()`，cockpit/product-entry tests patch seam 改到 concrete function 或 `workspace_cockpit.cockpit_payload._read_study_progress`；focused product-entry tests 为 81 passed。
- `study_runtime_decision.py` 已在生成 `__all__` 前删除 loader helper `import_module`，避免 facade 把 re-export 实现细节暴露为 public symbol；focused regression 覆盖该 symbol 不在 facade / `__all__` 中。
- 该批次后的剩余 blocker 已由 2026-07-07 gate / publication helper dependency thinning 继续收窄；以后一节的 current status 为准。

## 2026-07-07 Gate / publication helper dependency thinning

- `gate_clearing_batch_repair_fingerprints.py` 的 production `repair_unit_fingerprint()` 路径不再接收 `submission_minimal` controller object；`create_submission_minimal_package` fingerprint 直接按需导入 `submission_minimal.shared_base` 与 `submission_minimal.profile_config` 中的具体 helper。`gate_clearing_batch.py` 删除未调用的 `_candidate_values_include_root` / `_catalog_asset_fingerprints` 转发层，并物理删除无剩余引用的 `gate_clearing_batch/path_selection.py`。
- `gate_clearing_batch._submission_minimal_fingerprint_payload()` legacy private test wrapper 已退役；fingerprint primitive tests 改为直连 `gate_clearing_batch_repair_fingerprints.submission_minimal_fingerprint_payload()` 并显式传入 fingerprint helpers。
- `publication_gate` 已移除通过 `submission_minimal` facade 获取 profile 常量、submission-surface QC builder 与 authority helper 的依赖；相关 parts 和 focused tests 改为直连 `submission_minimal.authority`、`submission_minimal.markdown_surface_qc` 与 `publication_profiles.GENERAL_MEDICAL_JOURNAL_PROFILE`。
- `gate_clearing_batch_write_routes.py` 已不再接收 `submission_minimal` / `study_delivery_sync` controller object；submission package materialization 直连 `submission_minimal.package_builder.create_submission_minimal_package()`，delivery sync 直连 `study_delivery_sync.sync_orchestration.sync_study_delivery()`。`gate_clearing_batch.py` 删除 `submission_minimal` lazy controller，测试 patch 点改到现有 `_create_submission_minimal_package` / `_sync_submission_minimal_delivery` callable seam。`study_delivery_sync` lazy controller 仅因 `batch_context.build_gate_clearing_batch_context()` 仍需要 `can_sync_study_delivery()` 判定而保留，不能归类为 route-bound package/delivery wrapper blocker。
- `batch_context.build_gate_clearing_batch_context()` 已不再接收 `study_delivery_sync` controller object；delivery-sync eligibility 改为注入 concrete `study_delivery_sync.delivery_context.can_sync_study_delivery()` callable。`gate_clearing_batch.py` 删除 `study_delivery_sync` lazy controller，gate-focused tests 的 patch 点改到 `module.can_sync_study_delivery`。
- Fresh root verification：`scripts/run-pytest-clean.sh tests/test_gate_clearing_batch_cases/planning_and_replay_primitives.py tests/test_gate_authority_currentness.py -q` -> 18 passed；publication gate focused tests `drift_and_state_cases.py::test_build_gate_report_uses_authoritative_source_markdown_path_for_submission_surface_qc` 与 `submission_manifest_paths.py::test_build_gate_report_resolves_v2_submission_manifest_source_markdown_root` -> 2 passed；touched module `py_compile` passed；`scripts/line_budget.py` exit 0；`git diff --check` clean；`scripts/verify.sh` -> repo hygiene passed and smoke/line-budget 8 passed.
- Worktree caveat：在 `/Users/gaofeng/workspace/med-autoscience-worktrees/*` 下运行完整 `tests/test_gate_clearing_batch.py` 会因为 `config/display_packs.toml` 的 `../mas-scholar-skills` 相对路径解析到 worktree sibling 而触发 display-pack source failure；root checkout 的 sibling path 解析正确。本 lane 不扩大到 display-pack loader / worktree path resolution。
- 该批次后的剩余 blocker 已由 2026-07-07 final facade active-caller thinning 继续收窄；以后一节的 current status 为准。

## 2026-07-07 Final facade active-caller thinning

- `cli.py` 的 `export-submission-minimal` 路径已把误导性 `submission_minimal` lazy 变量名改为 `submission_package_builder`，真实 loader 继续直连 `submission_minimal.package_builder.create_submission_minimal_package()`；CLI patch seam 同步改到 concrete builder。Root focused verification：`scripts/run-pytest-clean.sh tests/test_cli_cases/domain_handler_and_submission_commands.py -q` -> 24 passed。
- `domain_entry.py` 的 `launch-study` / `submit-study-task` handler 不再 import `controllers.product_entry` facade，改为直连 `product_entry.workspace_cockpit.launch_surface.launch_study()` 与 `product_entry.entry_runtime.submit_study_task()`；测试 patch seam 改到 domain-entry 模块内 concrete function。Root focused verification：`scripts/run-pytest-clean.sh tests/test_domain_entry.py -q -k "launch or submit or external_caller"` -> 2 passed / 11 deselected。完整 `tests/test_domain_entry.py` 在 sibling worktree 仍会因 display-pack source 相对路径解析失败而红，root checkout sibling path 正常；本 lane 不扩大到 display-pack loader。
- `cli.py` 的 `medical-publication-surface` command 不再通过 `controllers.medical_publication_surface` facade 调用 `run_controller()`，改为 lazy-load `medical_publication_surface.controller`。同时 `medical_publication_surface.py` 显式 re-export `controller` part，`medical_publication_surface/reporting.py` 删除底部 `from .controller import ...` 反向 re-export，避免为 public facade symbol parity 保留循环导入。Root focused verification：`scripts/run-pytest-clean.sh tests/test_cli_cases/domain_handler_and_submission_commands.py tests/test_cli_cases/runtime_and_quality_commands.py -q` -> 55 passed；`PYTHONPATH=src python - <<'PY' ...` 确认 public facade `run_controller` / `write_surface_files` 与 concrete controller 同一对象。
- `cli.py` 的 `launch-study` / `submit-study-task` command 不再把整个 `product_entry` controller proxy 传给 `study_action_commands.handle_study_action_command()`；handler 改为接收 `launch_study`、`render_launch_study_markdown`、`submit_study_task`、`render_submit_study_task_markdown` 四个 concrete callables。Root focused verification：`scripts/run-pytest-clean.sh tests/test_cli_cases/runtime_and_quality_commands.py tests/test_cli_cases/launch_study_entry_mode.py -q` -> 32 passed。
- `product_entry/attention_projection.py` 不再通过 `sys.modules["med_autoscience.controllers.product_entry"]` 偷取 `build_same_line_route_truth`，改为直接复用 `evaluation_summary.build_same_line_route_truth`；focused product-entry attention/status tests -> 24 passed。
- `medical_publication_surface/shared_base.py` / `controller.py` 已删除 `sys.modules["med_autoscience.controllers.medical_publication_surface"]` override seam，直接使用 `quest_state.load_runtime_state()`、`resolve_paper_root_context()` 与 `runtime_protocol_report_store.write_timestamped_report()`；medical-publication focused tests -> 110 passed。
- `tests/test_medical_writing_quality_progress_projection.py`、`tests/stage_outcome_authority_helpers.py`、`tests/test_legacy_active_path_retire.py` 中不再为了 concrete helper 调用或 monkeypatch 而导入 `controllers.study_progress` / `controllers.product_entry` facade；测试 patch 点已迁到 `study_progress.projection` 与 `product_entry.manifest_surfaces`。Root focused verification：`scripts/run-pytest-clean.sh tests/test_medical_writing_quality_progress_projection.py::test_study_progress_projects_medical_writing_quality_surfaces tests/test_legacy_active_path_retire.py::test_product_entry_default_mainline_has_no_legacy_domain_diagnostic_or_dispatch_command ... -q` -> included in 6 passed；`stage_outcome_authority_helpers.py` 由 `py_compile` 覆盖。
- `product_entry/shared_base.py` 已删除 `_controller_override()` 与 `sys.modules["med_autoscience.controllers.product_entry"]` 读取；`program_surfaces.py`、`manifest_surfaces.py`、`workspace_cockpit/cockpit_payload.py` 改为直接使用 concrete dependency，测试 patch seam 迁到 concrete parts module。Root focused verification：`scripts/run-pytest-clean.sh tests/test_product_entry.py tests/test_truth_projection_surfaces.py tests/test_runtime_health_projection_surfaces.py tests/product_entry_cases -q` -> 129 passed；static scan 对 product-entry override seam 无命中。
- `tests/test_product_entry.py`、`tests/test_runtime_health_projection_surfaces.py`、`tests/test_truth_projection_surfaces.py`、`tests/test_opl_family_persistence_adapter.py` 中仅为调用 manifest / workspace surface / artifact inventory concrete helper 的 `controllers.product_entry` exact imports 已迁到 `product_entry.manifest_surfaces`、`workspace_surfaces` 与 `generated_status_projection`。Root focused verification：`scripts/run-pytest-clean.sh tests/test_product_entry.py tests/test_runtime_health_projection_surfaces.py tests/test_truth_projection_surfaces.py tests/test_opl_family_persistence_adapter.py -q` -> 128 passed；四个文件的 exact `controllers.product_entry` import scan 无命中。
- `study_progress/shared_base.py` 已删除 `_controller_override()` 与 `sys.modules["med_autoscience.controllers.study_progress"]` 读取；freshness 与 publication runtime helper 改为 concrete module dependency，测试 patch seam 迁到 `study_progress.shared` / `study_progress.publication_runtime`。Root focused verification：freshness / reviewer-handoff seam tests -> 6 passed；同一 11 个 `tests/test_study_progress.py` broad projection nodeid 在 root main 基线与本 lane 均为 11 failed，属既有 projection suite 红，不作为本 override-seam lane 阻断。
- `tests/paper_mission_owner_surface_cases/test_publication_gate_specificity_targets.py` 与 `tests/study_progress_cases/publication_eval_ai_reviewer_currentness_projection.py` 中仅为调用 `_materialize_publication_eval_from_gate_report()` 的 `controllers.study_runtime_decision` exact imports 已迁到 `study_runtime_decision.publication_and_submission`；`tests/test_study_runtime_typed_surface_cases/status_type_cases.py` 中 runtime handoff helper 调用迁到 `progress_projection.ProgressProjectionStatus` 与 `study_runtime_decision.runtime_events.runtime_summary`。Root focused verification：`scripts/run-pytest-clean.sh tests/paper_mission_owner_surface_cases/test_publication_gate_specificity_targets.py tests/study_progress_cases/publication_eval_ai_reviewer_currentness_projection.py tests/test_study_runtime_typed_surface_cases/status_type_cases.py -q` -> 52 passed；剩余唯一 `controllers.study_runtime_decision` exact import 是 public facade contract 测试 `test_study_runtime_decision_facade_does_not_export_loader_helper`。
- `tests/medical_publication_surface_cases/numeric_trace_contracts.py`、`statistical_disclosure_contracts.py`、`reference_citation_coverage.py` 中仅为调用 `build_surface_report(build_surface_state(...))` 的 `controllers.medical_publication_surface` exact imports 已迁到 `medical_publication_surface.reporting` 与 `medical_publication_surface.shared_base`。Worktree focused verification 在临时 sibling symlink 复现 root display-pack source 后为 13 passed，symlink 已删除；root focused verification 同命令为 13 passed。
- `display_pack_contract.py` 的 pack-level `default_execution_mode` 已允许 `declared_by_template`，但 template-level `execution_mode` 仍只允许 `python_plugin` / `subprocess`；`contracts/display-pack-contract.v2.json` 同步声明 pack default 与 template execution 的枚举边界。该修复解除 root main 已存在的 `mas-scholar-skills/packs/medical-display-core/display_pack.toml` contract drift，不放宽 evidence template runtime。Root focused verification：`scripts/run-pytest-clean.sh tests/test_display_pack_contract.py tests/test_display_pack_v2_contract.py -q` -> 17 passed；带临时 sibling symlink 的 `tests/medical_publication_surface_cases/report_terms_sections_and_charter.py` -> 23 passed，symlink 已删除。
- `tests/medical_publication_surface_cases/formal_language_review_and_model_entries.py` 中 report/state helper 已迁到 `medical_publication_surface.reporting` / `shared_base`，非正式问句 scan 迁到 `medical_publication_surface.manuscript_checks`；`catalog_tables_and_public_data_contracts.py`、`figure_narrative_and_renderer_contracts.py` 中 report/state helper 已迁到 `reporting` / `shared_base`，catalog story roles 与 display surface coverage 迁到 `medical_publication_surface.catalog_checks`；`report_terms_sections_and_charter.py` 中 report/state/render markdown helper 已迁到 `reporting` / `shared_base`，charter 投影仍由 reporting surface 产出。Root combined focused verification：`scripts/run-pytest-clean.sh tests/medical_publication_surface_cases/numeric_trace_contracts.py tests/medical_publication_surface_cases/statistical_disclosure_contracts.py tests/medical_publication_surface_cases/reference_citation_coverage.py tests/medical_publication_surface_cases/formal_language_review_and_model_entries.py tests/medical_publication_surface_cases/catalog_tables_and_public_data_contracts.py tests/medical_publication_surface_cases/figure_narrative_and_renderer_contracts.py tests/medical_publication_surface_cases/report_terms_sections_and_charter.py -q` -> 89 passed；上述 7 个文件的 exact `controllers.medical_publication_surface` import scan 无命中。
- `tests/medical_publication_surface_cases/controller_and_surface_state.py` 中仅为调用 concrete controller/state helper 的 `controllers.medical_publication_surface` exact imports 已迁到 `medical_publication_surface.controller` 与 `medical_publication_surface.shared_base`；保留首个 public facade smoke import 证明 facade module identity 与 public `run_controller()` surface 仍可用。Root focused verification：`scripts/run-pytest-clean.sh tests/medical_publication_surface_cases/controller_and_surface_state.py -q` -> 10 passed；worktree/branch 已清理并记录 absorbed ledger。
- `tests/test_policy_integration.py` 中 medical publication surface policy 集成 case 已从 `controllers.medical_publication_surface` exact import 迁到 `medical_publication_surface.controller`；保留 `tests/test_study_runtime_execution_evidence_adoption_cases/test_standard_agent_purity_boundary.py` 与 `tests/medical_publication_surface_cases/controller_and_surface_state.py` 两处 public facade identity / absence contract。Root focused verification：`scripts/run-pytest-clean.sh tests/test_policy_integration.py -q` -> 5 passed。
- `tests/product_entry_cases/manifest_launch_and_task_intake.py` 中 manifest/status/rendering/task-intake helper exact imports 已迁到 `product_entry.manifest_surfaces`、`manifest_rendering` 与 `entry_runtime`；该文件 `controllers.product_entry` exact import 降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/manifest_launch_and_task_intake.py -q` -> 48 passed；worker worktree/branch 已吸收并清理。
- `tests/submission_minimal_cases/source_markdown_and_materialized_refs_cases/surface_qc.py` 中 submission package builder 与 manuscript surface QC helper exact imports 已迁到 `submission_minimal.package_builder` 与 `submission_minimal.markdown_surface_qc`；该文件 `controllers.submission_minimal` exact import 降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/submission_minimal_cases/source_markdown_and_materialized_refs_cases/surface_qc.py -q` -> 8 passed；worker worktree/branch 已吸收并清理。
- `tests/product_entry_cases/product_entry_task_submission_cases.py` 中 task-intake helper exact imports 已迁到 `product_entry.entry_runtime`；该文件 `controllers.product_entry` exact import 降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/product_entry_task_submission_cases.py -q` -> 57 passed；worker patch rebase 时已等价 upstream，worktree/branch 已清理。
- `tests/submission_minimal_cases/source_markdown_and_materialized_refs.py` import-thinning lane 已关闭未提交：该文件当前 focused suite 在未改 HEAD 上也失败，失败用例为 `test_create_submission_minimal_package_uses_catalog_markdown_for_long_measure_value_tables`，期望 catalog markdown 表头 `| Phenotype | Measure | Value |`，实际输出为 landscape LaTeX table。该文件仍有 22 个 `controllers.submission_minimal` exact import；后续需先修复或隔离该基线失败，再做行为保持 import thinning。
- `tests/product_entry_cases/product_entry_builder_cases.py` 中 shared builder / program surface / manifest surface helper exact imports 已迁到 `product_entry.shared_base`、`program_surfaces` 与 `manifest_surfaces`；仅保留 1 个 public facade lightweight smoke import。Root focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/product_entry_builder_cases.py -q` -> 58 passed；worker worktree/branch 已吸收并清理。
- `tests/product_entry_cases/attention_queue_and_cockpit_base.py` 中 attention queue、study item、workspace cockpit reader / markdown helper exact imports 已迁到 `product_entry.attention_projection`、`workspace_surfaces` 与 concrete `mainline_status`；该文件 `controllers.product_entry` exact import 降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/attention_queue_and_cockpit_base.py -q` -> 10 passed；worker worktree/branch 已吸收并清理。
- `tests/submission_minimal_cases/package_core_and_authority_cases/authority_and_sources.py` 中 package builder、authority、compiled path 与 source-contract helper exact imports 已迁到 `submission_minimal.package_builder`、`authority`、`shared_base` 与 `source_contract`；该文件 `controllers.submission_minimal` exact import 降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/submission_minimal_cases/package_core_and_authority_cases/authority_and_sources.py -q` -> 18 passed；worker worktree/branch 已吸收并清理。
- `tests/product_entry_cases/cockpit_status_and_entry_status_focus_cases/test_medical_paper_readiness_v2_actions.py` 中 workspace cockpit、product-entry status 与 guarded operator action helper exact imports 已迁到 `product_entry.workspace_surfaces`、`manifest_surfaces`、`medical_paper_operator_actions` 与 concrete `mainline_status`；该文件 `controllers.product_entry` exact import 降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/cockpit_status_and_entry_status_focus.py -q -k 'long_horizon_paper_operations_action_cards or v2_action_card or guarded_operator_action'` -> 7 passed, 29 deselected；worker worktree/branch 已吸收并清理。
- `tests/submission_minimal_cases/package_core_and_authority_cases/core_materialization.py` 中 package builder 与 authority helper exact imports 已迁到 `submission_minimal.package_builder` 与 `authority`；该文件 `controllers.submission_minimal` exact import 降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/submission_minimal_cases/package_core_and_authority_cases/core_materialization.py -q` -> 11 passed；worker worktree/branch 已吸收并清理。
- `tests/product_entry_cases/action_catalog_parity_cases/provider_cases.py` 中 product-entry manifest helper exact imports 已迁到 `product_entry.manifest_surfaces.build_product_entry_manifest()`；该文件 `controllers.product_entry` exact import 降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/action_catalog_parity_cases/provider_cases.py -q` -> 8 passed；worker worktree/branch 已吸收并清理。
- `tests/submission_minimal_cases/frontiers_profile_and_sync.py` 中 package builder、profile config、delivery sync 与 markdown surface QC helper exact imports 已迁到 concrete owner modules；该文件 `controllers.submission_minimal` exact import 降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/submission_minimal_cases/frontiers_profile_and_sync.py -q` -> 16 passed。该 lane 同时把两个 README 文案断言从旧 `paper/...` 路径改为当前 `build_submission_minimal_readme()` 输出的 canonical `submission/` / `submission/journal_packages/<profile>/` 文案；root main 未改前同一文件为 14 passed / 2 failed，失败根因是 stale assertion，不是 import-thinning 引入。
- `tests/product_entry_cases/delivery_inspection_visibility.py`、`tests/product_entry_cases/action_catalog_parity_cases/action_catalog_cases.py`、`tests/product_entry_cases/product_entry_supervision_and_boundary_cases.py` 中 product-entry manifest/status/cockpit/action helper exact imports 已迁到 `product_entry.manifest_surfaces`、`manifest_rendering`、`workspace_surfaces` 与 concrete `mainline_status`；三个文件 `controllers.product_entry` exact import 均降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/delivery_inspection_visibility.py tests/product_entry_cases/action_catalog_parity_cases/action_catalog_cases.py tests/product_entry_cases/product_entry_supervision_and_boundary_cases.py -q` -> 70 passed；worker worktree/branch 已吸收并清理。
- `tests/test_submission_minimal_display_surface.py` 中 package builder helper exact imports 已迁到 `submission_minimal.package_builder.create_submission_minimal_package()`；该文件 `controllers.submission_minimal` exact import 降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/test_submission_minimal_display_surface.py tests/product_entry_cases/delivery_inspection_visibility.py tests/product_entry_cases/action_catalog_parity_cases/action_catalog_cases.py tests/product_entry_cases/product_entry_supervision_and_boundary_cases.py -q` -> 84 passed。Worker worktree 因 sibling display-pack source 路径缺失需要临时 symlink 到 `/Users/gaofeng/workspace/mas-scholar-skills` 复现 root 环境，验证后 symlink 已删除。
- `tests/submission_minimal_cases/v2_layout_and_legacy_cases.py`、`tests/product_entry_cases/manifest_launch_and_task_intake_cases/test_explicit_wakeup.py`、`tests/test_submission_minimal_display_surface_cases/test_display_entries_and_assets.py` 中 exact facade imports 已迁到 concrete owner modules；三个文件目标 facade import 均降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/submission_minimal_cases/v2_layout_and_legacy_cases.py tests/product_entry_cases/manifest_launch_and_task_intake_cases/test_explicit_wakeup.py tests/test_submission_minimal_display_surface_cases/test_display_entries_and_assets.py -q` -> 13 passed。Display-assets worker worktree 同样因 sibling display-pack source 路径缺失需要临时 symlink，验证后已删除。
- `tests/test_authority_write_route_cases/test_submission_delivery_routes.py` import-thinning lane 已关闭未提交：候选 patch 可清零 exact import，但该文件 focused suite 在原始基线 `95773a697` 上已为 5 failed / 3 passed，无法证明行为保持；worktree/branch 已清理。后续需先修复该 baseline 或指定替代 verification gate。
- Fresh exact facade import scan after this slice：`rg 'import_module\("med_autoscience\.controllers\.(medical_publication_surface|product_entry|study_runtime_decision|submission_minimal)"\)' tests src` -> `medical_publication_surface=2`、`study_runtime_decision=1`、`product_entry=40`、`submission_minimal=38`。剩余项是 public facade identity / test override guard、legacy exact-import tests 与上述 baseline-red protection lanes，不是 production helper caller 依赖。
- Fresh production scan：`rg` 检查 `src/med_autoscience` 中 `submission_minimal`、`study_runtime_decision`、`product_entry`、`medical_publication_surface` 的 direct facade import、helper call、lazy controller identity、`_load_controller("product_entry"|"medical_publication_surface")` 和 `study_domain_handlers` 均无命中；`scripts/line_budget.py` 与 `git diff --check` 均通过。
- 当前剩余 blocker 继续收窄为 `facade_public_identity_and_test_override_guard`：public module identity、public facade contract / exact facade import tests、runtime payload 字符串 identity 和 docs/contract inventory guard 仍存在。生产 helper caller 与 `product_entry` / `study_progress` facade override seam 已从本批写集移除；后续要删除 public facade 文件本身，必须先迁移或退役对应 public import contract / exact facade tests，不能用本批 focused tests 直接声明 public facade 可物理删除。
- `tests/product_entry_cases/entry_status_focus_cases.py` 中 product-entry status / entry status helper exact imports 已迁到 concrete owner modules；该文件 `controllers.product_entry` exact import 降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/entry_status_focus_cases.py -q` -> 14 passed；target scan 无 exact facade import / `module.` 命中。
- `tests/submission_minimal_cases/package_core_and_authority_cases/supplementary_material.py` 与 `profile_exports.py` 中 package builder / profile helper exact imports 已迁到 `submission_minimal.package_builder`、`profile_config`、`display_assets` 与相关 concrete modules；两个文件 `controllers.submission_minimal` exact import 均降为 0。Root combined focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/entry_status_focus_cases.py tests/submission_minimal_cases/package_core_and_authority_cases/supplementary_material.py tests/submission_minimal_cases/package_core_and_authority_cases/profile_exports.py -q` -> 22 passed；target scan 无 exact facade import / `module.` 命中。
- `tests/product_entry_cases/cockpit_status_and_entry_status_focus_cases/test_medical_paper_readiness.py` 中 workspace cockpit / readiness helper exact imports 已迁到 `product_entry.workspace_cockpit.cockpit_payload`、`workspace_cockpit.cockpit_markdown` 与 concrete `mainline_status` / `medical_paper_readiness`。该 nested case 文件按 `tests/conftest.py` 被排除直接收集，合法 pytest 入口是 `tests/product_entry_cases/cockpit_status_and_entry_status_focus.py`；root focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/cockpit_status_and_entry_status_focus.py tests/product_entry_cases/entry_status_focus_cases.py tests/submission_minimal_cases/package_core_and_authority_cases/supplementary_material.py tests/submission_minimal_cases/package_core_and_authority_cases/profile_exports.py -q` -> 58 passed；target scan 无 exact facade import / `module.` 命中。
- Fresh exact facade import scan after this follow-up slice：`rg -o 'import_module\("med_autoscience\.controllers\.(medical_publication_surface|product_entry|study_runtime_decision|submission_minimal)"\)' tests src` -> `medical_publication_surface=2`、`study_runtime_decision=1`、`product_entry=32`、`submission_minimal=32`。最大剩余块仍是 protected / baseline-red `tests/submission_minimal_cases/source_markdown_and_materialized_refs.py`（23）与 `tests/test_authority_write_route_cases/test_submission_delivery_routes.py`（4）；后续需先修复或隔离基线失败，不能用 import-thinning lane 直接吸收。
- 下一批低风险候选按单文件分离：`tests/product_entry_cases/manifest_launch_and_task_intake_cases/launch_study_surfaces.py`（3）、`tests/product_entry_cases/action_catalog_parity_cases/memory_and_skeleton_cases.py`（3）、`tests/product_entry_cases/repo_shell_and_handoff_templates.py`（2）、`tests/product_entry_cases/product_entry_markdown_preview_cases.py`（2）以及 cockpit nested singletons。继续采用单文件 worktree、focused aggregator verification、target exact-import scan 与 root absorption，不碰 study truth、paper body、owner receipts、typed blockers、human gates、runtime queues 或 provider attempts。
- `tests/product_entry_cases/manifest_launch_and_task_intake_cases/launch_study_surfaces.py`、`tests/product_entry_cases/action_catalog_parity_cases/memory_and_skeleton_cases.py`、`tests/product_entry_cases/product_entry_markdown_preview_cases.py`、`tests/product_entry_cases/repo_shell_and_handoff_templates.py` 中 helper exact imports 已迁到 concrete product-entry / OPL-provider modules；四个文件 `controllers.product_entry` exact import 均降为 0。Root combined focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/manifest_launch_and_task_intake_cases/launch_study_surfaces.py tests/product_entry_cases/action_catalog_parity_cases/memory_and_skeleton_cases.py tests/product_entry_cases/product_entry_markdown_preview_cases.py tests/product_entry_cases/repo_shell_and_handoff_templates.py -q` -> 109 passed；target scan 无 exact facade import / `module.` 命中；`git diff --check` clean。
- Fresh exact facade import scan after this product-entry batch：`rg -o 'import_module\("med_autoscience\.controllers\.(medical_publication_surface|product_entry|study_runtime_decision|submission_minimal)"\)' tests src` -> `medical_publication_surface=2`、`study_runtime_decision=1`、`product_entry=22`、`submission_minimal=32`。剩余 `product_entry` 主要是 cockpit nested singletons、public facade smoke / identity tests 和 small parity cases；继续单文件推进，不把 public facade file physical deletion 写成已完成。
- `tests/product_entry_cases/cockpit_status_and_entry_status_focus_cases/test_status_cards.py`、`test_medical_paper_ops_health.py`、`test_autonomy_runtime_control.py` 中 workspace cockpit / status helper exact imports 已迁到 concrete cockpit payload / markdown / manifest surfaces / workspace surface helpers；三个文件 `controllers.product_entry` exact import 均降为 0。Root focused verification 走合法聚合入口：`scripts/run-pytest-clean.sh tests/product_entry_cases/cockpit_status_and_entry_status_focus.py -q` -> 36 passed；target scan 无 exact facade import / `module.` 命中；`git diff --check` clean。
- Fresh exact facade import scan after cockpit follow-up：`rg -o 'import_module\("med_autoscience\.controllers\.(medical_publication_surface|product_entry|study_runtime_decision|submission_minimal)"\)' tests src` -> `medical_publication_surface=2`、`study_runtime_decision=1`、`product_entry=16`、`submission_minimal=32`。剩余 `product_entry` 当前为 16 个单点文件，需继续按 single-file lane 验证；`submission_minimal` 的两个最大块仍按 baseline-red/protected 处理。
- `tests/product_entry_cases/transition_spec_descriptor.py`、`product_entry_markdown_and_skill_catalog.py`、`cockpit_status_and_entry_status_focus_cases/test_quality_lane.py`、`test_projection_error_isolation.py` 中 helper exact imports 已迁到 concrete manifest / skill-catalog / cockpit payload surfaces；四个文件 `controllers.product_entry` exact import 均降为 0。Root combined focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/transition_spec_descriptor.py tests/product_entry_cases/product_entry_markdown_and_skill_catalog.py tests/product_entry_cases/cockpit_status_and_entry_status_focus.py -q` -> 111 passed；target scan 无 exact facade import / `module.` 命中；`git diff --check` clean。
- Fresh exact facade import scan after this follow-up：`rg -o 'import_module\("med_autoscience\.controllers\.(medical_publication_surface|product_entry|study_runtime_decision|submission_minimal)"\)' tests src` -> `medical_publication_surface=2`、`study_runtime_decision=1`、`product_entry=12`、`submission_minimal=32`。剩余 product-entry 仍为单点文件，可继续单文件收薄；public facade identity / smoke tests 与 baseline-red submission blocks 仍不能按完成删除。
- `tests/product_entry_cases/action_catalog_parity_cases/stage_descriptor_cases.py`、`life_science_source_pack_cases.py`、`authority_operation_manifest.py`、`functional_consumer_boundary.py` 中 manifest helper exact imports 已迁到 `product_entry.manifest_surfaces.build_product_entry_manifest()`；四个文件 `controllers.product_entry` exact import 均降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/action_catalog_parity.py tests/product_entry_cases/action_catalog_parity_cases/life_science_source_pack_cases.py tests/product_entry_cases/authority_operation_manifest.py tests/product_entry_cases/functional_consumer_boundary.py -q` -> 27 passed；target scan 无 exact facade import / `module.` 命中；`git diff --check` clean。
- Fresh exact facade import scan after this manifest-helper slice：`rg -o 'import_module\("med_autoscience\.controllers\.(medical_publication_surface|product_entry|study_runtime_decision|submission_minimal)"\)' tests src` -> `medical_publication_surface=2`、`study_runtime_decision=1`、`product_entry=8`、`submission_minimal=32`。剩余 product-entry 单点继续按是否为 real helper caller vs public facade smoke/identity 分流。
- `tests/product_entry_cases/functional_closure_projection.py`、`open_auto_research_projection.py`、`paper_orchestra_operator_projection.py`、`opl_current_control_state_handoff_projection.py` 中 product-entry helper exact imports 已迁到 concrete owner modules；四个文件 `controllers.product_entry` exact import 均降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/functional_closure_projection.py tests/product_entry_cases/open_auto_research_projection.py tests/product_entry_cases/paper_orchestra_operator_projection.py tests/product_entry_cases/opl_current_control_state_handoff_projection.py -q` -> 4 passed；target scan 无 exact facade import / `module.` 命中；`git diff --check` clean。
- Fresh exact facade import scan after this projection-helper slice：`rg -o 'import_module\("med_autoscience\.controllers\.(medical_publication_surface|product_entry|study_runtime_decision|submission_minimal)"\)' tests src` -> `medical_publication_surface=2`、`study_runtime_decision=1`、`product_entry=4`、`submission_minimal=32`。剩余 product-entry 为 3 个 cockpit helper singletons 与 1 个 public facade lightweight smoke import；后者不是 helper dependency，不能作为物理删除依据。
- `tests/product_entry_cases/cockpit_status_and_entry_status_focus_cases/test_gate_clearing_followthrough.py`、`test_ai_first_operations.py`、`test_cross_study_completion.py` 中 workspace cockpit helper exact imports 已迁到 concrete `mainline_status`、`workspace_cockpit.cockpit_payload.read_workspace_cockpit()` 与 `workspace_cockpit.cockpit_markdown.render_workspace_cockpit_markdown()`；三个文件 `controllers.product_entry` exact import 均降为 0。Root focused verification：`scripts/run-pytest-clean.sh tests/product_entry_cases/cockpit_status_and_entry_status_focus.py -q` -> 36 passed；target scan 无 exact facade import / `module.` 命中；`git diff --check` clean。
- Fresh exact facade import scan after this cockpit-helper slice：`rg -o 'import_module\("med_autoscience\.controllers\.(medical_publication_surface|product_entry|study_runtime_decision|submission_minimal)"\)' tests src` -> `medical_publication_surface=2`、`study_runtime_decision=1`、`product_entry=1`、`submission_minimal=32`。剩余唯一 `product_entry` exact import 是 `tests/product_entry_cases/product_entry_builder_cases.py` 的 public lightweight import smoke，用于证明 facade import 不加载 heavy controllers；不能按 helper dependency 继续删除。
- `tests/submission_minimal_cases/direct_current_body_figure_catalog_filter.py`、`clean_migration_guard.py` 与 `tests/test_artifact_lifecycle_inventory.py::test_submission_surface_docx_inspector_is_read_only_for_generated_projection` 中 submission helper exact imports 已迁到 concrete `submission_minimal.package_builder.create_submission_minimal_package()` 与 `submission_minimal.docx_surface.inspect_submission_docx_surface()`；三个目标写集均无 `controllers.submission_minimal` exact import。Root focused verification：`scripts/run-pytest-clean.sh tests/submission_minimal_cases/direct_current_body_figure_catalog_filter.py tests/submission_minimal_cases/clean_migration_guard.py tests/test_artifact_lifecycle_inventory.py::test_submission_surface_docx_inspector_is_read_only_for_generated_projection -q` -> 4 passed；target exact scan 无 `submission_minimal` facade import；`git diff --check` clean。
- Fresh exact facade import scan after this submission-helper slice：`rg -o 'import_module\("med_autoscience\.controllers\.(medical_publication_surface|product_entry|study_runtime_decision|submission_minimal)"\)' tests src` -> `medical_publication_surface=2`、`study_runtime_decision=1`、`product_entry=1`、`submission_minimal=28`。剩余 `submission_minimal` 主要是 protected baseline-red `source_markdown_and_materialized_refs.py`、baseline-red `test_submission_delivery_routes.py`、`shared_base.py` 的 public export monkeypatch seam，以及 public facade identity/contract surfaces；不能用本批 focused tests 宣称 facade 文件可物理删除。
- `tests/submission_minimal_cases/source_markdown_and_materialized_refs.py` 中 23 个 `submission_minimal` facade exact imports 已迁到 concrete `submission_minimal` owner helpers；同时修复该文件既有红基线：`markdown_surface_tables.build_catalog_backed_table_blocks()` 现在允许明确 `long_measure_value_table_to_avoid_pdf_header_overlap` / `prefer_markdown_for_submission_pdf` 的 catalog markdown 覆盖已 materialized 的 draft 宽表，避免长 measure/value 表被误转成 landscape LaTeX。Root focused verification：`scripts/run-pytest-clean.sh tests/submission_minimal_cases/source_markdown_and_materialized_refs.py tests/submission_minimal_cases/package_core_and_authority.py tests/submission_minimal_cases/package_core_and_authority_cases/profile_exports.py::test_export_pdf_uses_submission_layout_header_for_fixed_figures_and_wide_tables -q` -> 62 passed；target exact scan 无 `submission_minimal` facade import / `module.`；`git diff --check` clean。
- Fresh exact facade import scan after this source-markdown slice：`rg -o 'import_module\("med_autoscience\.controllers\.(medical_publication_surface|product_entry|study_runtime_decision|submission_minimal)"\)' tests src` -> `medical_publication_surface=2`、`study_runtime_decision=1`、`product_entry=1`、`submission_minimal=5`。剩余 `submission_minimal` 为 `tests/submission_minimal_cases/shared_base.py` 的 public export monkeypatch seam 与 `tests/test_authority_write_route_cases/test_submission_delivery_routes.py` 的 authority-route baseline-red block；后者当前 5 failed / 3 passed，失败跨 authority snapshot semantics、current-package projection 和 invalid placeholder PDF fixture，需独立 authority-route repair lane，不能作为本 facade cleanup 的行为保持证明。
- `tests/test_authority_write_route_cases/test_submission_delivery_routes.py` baseline 已修复并完成 import thinning：测试断言更新到当前 authority-route contract，即缺 authority snapshot 时允许 materialize audit-source package、study delivery current package 写入 canonical `submission/` 与 `submission.zip`，fixture PDF 改为最小有效 PDF。该文件 4 个 `controllers.submission_minimal` exact imports 已迁到 concrete `submission_minimal.package_builder.create_submission_minimal_package()`，`study_delivery_sync` 调用也迁到 concrete orchestration module。Fresh root verification：`scripts/run-pytest-clean.sh tests/test_authority_write_route_cases/test_submission_delivery_routes.py -q` -> 8 passed；target scan 对该文件 exact `controllers.submission_minimal` import / `module.` 无命中。
- Fresh exact facade import scan after this authority-route repair slice：`rg -o 'import_module\("med_autoscience\.controllers\.(medical_publication_surface|product_entry|study_runtime_decision|submission_minimal)"\)' tests src` -> `medical_publication_surface=2`、`study_runtime_decision=1`、`product_entry=1`、`submission_minimal=1`。剩余 `submission_minimal` 是 `tests/submission_minimal_cases/shared_base.py` 的 public export monkeypatch seam；其余 4 个剩余 exact imports 分别是 public facade identity / smoke / contract guard，不能按 helper dependency 继续删除。
- P2 line-budget follow-up 已把 5 个 remaining advisory 收敛为 0：`source_markdown_and_materialized_refs.py` 1033 -> 841 行，`route_back_source_precedence.py` 1099 -> 858 行，`test_paper_mission_consumption_currentness.py` 1050 -> 383 行，`receipt_owner_consumption.py` 1165 -> 853 行，`study_progress/projection.py` 1044 -> 991 行。新增 case/helper 模块均按既有 `*_cases/` 或 concrete helper module 命名，只移动语义连续测试或 private refresh helper，不改变 runtime / paper authority。
- Fresh verification for the line-budget batch：source markdown focused tests -> 19 passed；route-back precedence focused tests -> 7 passed；paper mission consumption currentness focused tests -> 16 passed；receipt owner consumption focused tests -> 13 passed；projection helper py_compile passed；projection helper focused tests -> 9 passed；`scripts/line_budget.py` -> exit 0；`git diff --check` -> clean。Provider-admission broader `-k existing_projection_refresh` selector仍有 root-main 既有 5 failures，本批不把它当作新回归或完成证据。
- 2026-07-07 closeout scan：`scripts/line_budget.py` 仍为 exit 0；final exact facade scan 仅剩 5 处：`submission_minimal_cases/shared_base.py` public export monkeypatch seam、`product_entry_builder_cases.py` lightweight import smoke、`status_type_cases.py` study-runtime facade contract、`test_standard_agent_purity_boundary.py` 与 `medical_publication_surface_cases/controller_and_surface_state.py` public facade / tombstone guards。它们不是 helper dependency，后续不能作为“可直接删除 facade 文件”的证据；若要物理删除 public facade，必须先变更 public contract / smoke / identity policy 并补对应 focused verification。

## 2026-07-06 P1 runtime dependency / CLI alias / branding asset cleanup evidence

- `pyproject.toml` 已删除 runtime dependency `packaging>=24.0`；仅测试侧 `Requirement` 解析需要 `packaging`，因此保留在 `dependency-groups.dev`，`python_environment_contract` 仍不 import `packaging`。
- `pyproject.toml` 已删除 core runtime dependency `pandas>=2.2`；`python_environment_contract` 默认 runtime 只要求 `matplotlib>=3.9`，`pandas>=2.2` 保留在 `analysis` optional bundle，用于 analysis harmonization / rerun evidence 路径。
- `pyproject.toml` 已删除 `mas` console-script alias，只保留 `medautosci` 与 `medautosci-mcp`；plugin installer 本来已删除本机 `mas` wrapper，本轮同步更新 scaffold assertion，避免把 `mas` 作为 PATH readiness 证据。
- `assets/branding/medautoscience-hero.png` 与 `assets/branding/medautoscience-overview.png` 是未被 README/source active 引用的重复 raster；README 当前使用 `medautoscience-overview-v2.png`，branding preflight fixture 已改到 v2 后删除旧图。
- 2026-07-07 follow-up：`dev_preflight_contract/category_specs.py`、`contracts/README.md` 与 `contracts/nature_skills_learning_adoption.json` 已移除当前 contract/preflight 面中指向退役 `external/display-packs/medical-display-core` tracked asset 的 stale source refs，改为 MAS 真实消费面 `display_pack_dependency_environment.py` / `display_dependency_environment_os_target.md` 与外部 `mas-scholar-skills:packs/medical-display-core/*` source ref。测试 fixture 中的 `external/display-packs` 仍只表示 tmp workspace local-dir pack，不恢复 MAS repo bundled fallback。
- 本 lane 只证明 repo-source packaging / CLI alias / asset surface 收薄；focused tests 与 `git diff --check` 不声明 runtime readiness、publication-ready、paper progress、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。

## 2026-07-06 Lane D test helper / old fixture / wrapper thinning evidence

- Display golden CH / DEG / F 三套重复 `shared_base.py` 与 `golden_json_helpers.py` 已收敛到 `tests/display_shared_golden_regression_helpers.py`；各 case 目录只保留 thin `shared.py`，pytest 入口 `tests/test_display_{ch,deg,f}_golden_regression.py` 维持原 discovery 形状。
- 旧 private runtime residue aggregate fixture 已删除；`contracts/runtime/mas-runtime-surface-retirement-inventory.json` 与 `contracts/paper_progress_transition_runtime_completion_audit.json` 的 evidence refs 改指 `runtime_surface_no_authority_audit.py` 与 `domain_authority_refs_index.py` surviving concrete cases。
- `quality_repair_batch.py` 直接从 `med_autoscience.lazy_module_proxy` import `lazy_controller_module`，旧 controller proxy wrapper 已删除。
- 本 lane 只做 test/helper/wrapper 收薄与 evidence ref 更新，不写 study truth、paper body、owner receipts、typed blockers、human gates、runtime queues 或 provider attempts。

## 2026-07-06 source empty package marker cleanup evidence

- 已删除源码树中无内容、无导出、无 package-data 语义的 0 字节 `__init__.py` marker；保留 `src/med_autoscience/resources/__init__.py`，因为该 package 承载 tracked resource 边界。
- 删除范围只覆盖 PEP 420 namespace package 可承载的源码空 marker，不改 CLI/controller/test discovery 语义，不触碰有 `from __future__`、显式导出或 resource 边界的 `__init__.py`。测试目录 marker 不删除，因为现有 case 使用相对 import，pytest collection 依赖 package identity。
- 本 lane 只做包 marker 收薄；focused import / pytest / diff checks 不声明 runtime readiness、publication-ready、paper progress、owner receipt、typed blocker、human gate、runtime queue 或 provider attempt。
