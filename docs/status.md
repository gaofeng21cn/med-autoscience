# 当前状态

**更新时间：2026-05-10**

## 当前角色

- `Med Auto Science` 是面向专病研究的独立医学研究 domain agent，单一 MAS app skill 承接稳定可调用面，负责研究问题进入、工作区语境、证据推进、人话进度和论文相关文件交付。
- 仓库首页负责用户入口；`CLI`、`MCP`、`controller` 负责操作与自动化入口。
- `Med Auto Science` 作为独立 medical research domain agent，对外先由单一 MAS app skill 统一承接；direct path 和经过 OPL 的 integration handoff 共享同一套研究语义。
- 对外稳定 capability surface 继续是本地 CLI、workspace commands / scripts、durable truth surface 与 repo-tracked contract，方便 `Codex` 直接调用。
- `OPL` 是上层 family-level session/runtime/projection 整合入口，并维护 shared modules/contracts/indexes；它不改写 MAS 的 domain owner 语义。
- 当前 OPL family shared release pin 已对齐到 `2b08c7efd8acd80355e870087d4ce5be7b45d4d1`；`pyproject.toml` 与 `uv.lock` 共同持有这条 Python shared package pin。
- `product-entry-manifest` 现已导出 MAS-owned `family_action_catalog`，并从同一份 action definition 投影 CLI、MCP、single app skill command metadata 与 product-entry operator metadata；同一 manifest 也暴露标准 `family_stage_control_plane` 和深度 `family_stage_control_plane_descriptor`，把 Stage-Led Autonomy 的 route contract source、stage knowledge plane、stage packets、memory closeout/router/recall、evidence/review/controller/publication refs 和 authority boundary 投影给 family-level indexing / display / freshness。`OPL` 只消费 shared schema/helper/validator/discovery，不写入 MAS study truth、runtime truth、quality authority 或 publication gate。
- `OPL Runtime Manager` 是 OPL 侧新增的薄运行管理/投影目标层，负责把 MAS registration/projection 接到 native helper catalog、高频状态索引与 doctor/repair/resume 面；它不持有 MAS study truth、publication gate 或 evidence/review ledger。Hermes-first family topology 中，`Hermes-Agent` 由 OPL 管理并作为 OPL Full online runtime 的默认外部 substrate。
- `MAS supervision scheduler contract` 现在是 MAS standalone/local diagnostics 的外层监管调度 owner：默认 adapter selection 是 MAS-owned `local` scheduler，macOS 本机落到 LaunchAgent；OPL Full online runtime 由 OPL-managed Hermes gateway/cron/webhook 唤醒 `opl family-runtime tick`，再通过 `medautosci sidecar dispatch` 进入 MAS owner surface。`MedDeepScientist` 不再是默认 MAS operation 或默认 diagnostic 依赖，只保留 frozen source archive、historical fixture、source provenance、explicit archive import 和 backend-audit reference 角色。
- MAS 已暴露 `medautosci sidecar export --profile <profile> --format json` 与 `medautosci sidecar dispatch --task <task.json> --format json`。该桥接用于 OPL typed family queue 的 read-only projection、runtime recovery recommendation、notification receipt 和 guarded dispatch receipt；禁止写 domain truth、publication quality verdict、artifact gate 或 current package。
- Lane C 的入口边界已落到 `contracts/test-lane-manifest.json` 的 `focused_lanes.mas-entry-boundary`：`OPL` / `Hermes-Agent` 是 family online substrate，MAS sidecar export/dispatch 是受控桥接，`CLI`、`MCP`、action catalog 与单一 MAS app skill 只是 projection / entry surface，不成为 study truth、publication quality、artifact/package 或 runtime authority owner。
- 2026-05-01 之后的当前主线新增了两层 reducer truth：`StudyTruthKernel` 统一 study 级 next action / package authority / publication gate 解释，`RuntimeHealthKernel` 统一 worker liveness / retry budget / recovery escalation；普通 read 只生成 shadow projection，materialized truth 只能由显式 reconcile、controller tick 或 runtime watch apply 写入。2026-05-06 又把用户宏观状态和执行路由收口为 `study_macro_state/latest.json` 与 `owner_route`，让 matrix、supervisor consume、execute-dispatch 和 lifecycle report 共享同一 current truth。
- 同一轮主线还把医学稿件初稿质量从“gate 后置拦截”前移为 pre-draft runtime concern：`medical journal prose`、IMRAD section contract、reporting-guideline obligations 与 first-draft generation model 应在 study charter / quality OS 中可见。
- AI-first 的当前语义是可运行质量线：pre-draft quality runtime、AI reviewer workflow、artifact rebuild proof、operations state 和真实论文 soak。文档只做人工可读收敛，不新增 wording gate，不改测试或 preflight contract。
- 本轮 no-history provenance 校准读取的本机 `med-deepscientist` root checkout 为 `35976b7d6e3b99b15b57ec44ff5f5d959b342ecc`，archive sha256 为 `f8dc31822dc52ecc6e073f54c8b5c95cd46646e299a67cd1c1f6f7f3764e0d5b`；它只作为 source provenance / historical fixture / upstream comparison 参考，不作为 MAS 默认运行依赖、OPL 公共真相或 release-facing contract 输入。

## 当前推荐使用方式

- 用户视角：给出病种、数据、目标问题和期望论文结果，在同一个工作区里持续推进研究。
- 研究推进视角：围绕同一条课题线管理问题定义、证据补足、进度反馈和文件交付。
- 命令行操作视角：当前最小操作路径仍是 `product-entry-status`、`workspace-cockpit`、`submit-study-task`、`launch-study`、`study-progress` 这一组接口；对外它们都收口在单一 `Med Auto Science` app skill 之下，并继续通过 repo-tracked command contracts 被调用。`product-entry-status` 只是内部操作回路与 projection contract，不是独立公开产品入口。
- 用户可视化入口：`MAS Progress Portal` 已落成 MAS-native 固定进度入口，默认位置为 `ops/mas/progress/index.html`。它采用静态快照 + 可选本地服务的双层形态，由 MAS 生成 domain-owned progress payload / HTML / hosted packaging manifest，只消费现有 progress/cockpit/runtime/package/read-model，不成为新的 truth owner；开发合同见 [MAS Progress Portal](./runtime/display/progress_portal.md)。这不是 MDS 函数级 1:1 搬迁，而是把日常研究进度、路线、阻塞、artifact pickup、optional hosted runtime packaging 和 OPL handoff 的用户可见入口收口到 MAS-owned read-model。Portal 的 repo contract 覆盖 per-study/per-paper 工作台、deep link、研究路线地图、路线/决策、stage/artifact grouping、执行器对话时间线、study-scoped Live Console link，以及显式 `--serve --enable-actions` 下的 pause/resume/stop runtime owner apply；`focused_lanes.portal-route-decision-trail` 固定 `mas_progress_portal_route_decision_trail` 和 `mas_progress_portal_route_map` 为 read-only contract。2026-05-10 校准：此前 repo contract 已有路线/对话 read model，但真实 workspace 单篇页会因为只消费 workspace study row 或按原始 timeline 截断而看不到路线变化和用户消息；当前实现已改为单篇页优先读取 canonical `study-progress`，并把路线/决策渲染成 SVG 研究路线地图、把执行器对话渲染成 timeline。当前仍不能说完整等价旧 MDS WebUI：真实多论文 workspace 的长期刷新、route input 完整性、source ref 可读性和 interactive terminal attach 仍需 evidence-gated polish；评估见 [MDS WebUI User Parity Gap Review](./references/mds-parity/mds_webui_user_parity_gap_review.md)。同一目的接入 OPL App 时，OPL 最优形态是 family-level dashboard 只消费 `latest.json` 内 `opl_handoff` 的 payload refs、freshness、source refs、artifact locators 和 workspace-local Portal deep link，不重新解释 study truth，不写 MAS truth，也不接管 runtime/publication/package authority；`browser_url` 只表示 hosted/runtime monitoring URL，Portal 入口继续使用本地 `portal_path` / `portal_url` / `deep_link`；评估见 [Progress Portal OPL App Integration](./references/integration/progress_portal_opl_app_integration.md)。
- MDS 行为等价口径已校准：MAS 默认 operation 不要求外部 MDS repo / daemon / runtime root / WebUI，但这不等于旧 MDS resident daemon 行为完全等价。当前 MAS standalone/local diagnostics 的外层 supervision contract owner 是 `MAS supervision scheduler contract`；默认 `local` adapter 每 300 秒调用 MAS-owned tick script 并执行 supervisor scan / consume / dispatch sequence。OPL Full online runtime 的长期在线能力由 OPL-managed Hermes substrate + `opl family-runtime` 提供，MAS sidecar 接收受控 dispatch，不接管 paper/package authority。MAS Live Console 已承接旧 WebUI terminal/log observation 的 read-only purpose equivalence。旧 resident WebSocket terminal attach、terminal input/resize/detach 不是当前 landed scope，也不能写成已放弃；它们属于后续 interactive parity candidate。UI-issued runtime control 已先落到 Progress Portal 本机 `--enable-actions` 的 pause/resume/stop MAS runtime owner apply，terminal input 仍必须另过 attach gate。connector background threads、GitOps runtime lifecycle 和 workspace-local launchd/systemd/cron/docker service 不是 active MAS default behavior。行为差异见 [MDS Behavior Equivalence Gap Matrix](./references/mds-parity/mds_behavior_equivalence_gap_matrix.md)，scheduler 合同见 [Supervision Scheduler Contract](./runtime/control/supervision_scheduler_contract.md)。
- Runtime evidence closeout 已把后续三件优化落成可用 surface：`outer_supervision_slo` 让用户入口看到外环监管是 fresh、due、stale、missing 还是 blocked，并给出 canonical `runtime-supervisor-reconcile --dry-run` 安全加速命令；当前输入来自 MAS scheduler tick 和 supervisor reconcile receipt，默认 local adapter 与 optional Hermes adapter 必须输出同构 SLO。`portal_console_soak` 对真实 workspace 生成 Progress Portal / Live Console 只读 evidence；`paper_autonomy_stability_evidence` 合并 profile inventory、supervisor reconcile dry-run、workspace migration dry-run 和 soak monitor，按 blocker/next action 解释真实论文自治稳定性。三者均不恢复 MDS daemon、不恢复旧 workspace-local service、不写 `current_package`、`publication_eval/latest.json`、`controller_decisions/latest.json` 或 runtime SQLite authority。
- Runtime Watchdog / LLM cost closeout 已把旧 MDS daemon 的低延迟 worker/session 感知价值落到 MAS per-run wrapper：真实 run wrapper 托管 `codex exec`、刷新 `worker_lease` heartbeat / stdout cursor / last output / monitor state / child exit，并在 child exit 后立即 `complete_turn_and_normalize`。outer supervision 现在通过 `MAS supervision scheduler contract` 触发，默认 local adapter 与 optional Hermes adapter 都只做 300 秒 fail-safe；Portal、Live Console、study-progress、runtime watch 和 supervisor receipt 会显示 `observe_only` / `reconcile_dry_run` / `controller_apply` / `codex_worker_dispatch`、`will_start_llm`、dispatch counters 与 duplicate guard，避免高频监管误触发额外 LLM。
- 自动论文推进降级修复已落成 repo-level closeout：`paper_progress_degradation_classifier=landed`、`owner_handoff_repeat_suppression_closeout=landed`、`paper_progress_stall_safe_reconcile=landed`、`paper_progress_degradation_evidence=landed_read_model`、`production_blocker_impact_projection=landed`。当前 P0 真实降级风险已固定为 owner handoff 丢失、repeat suppression 压掉 handoff、stalled work-unit 无法 safe redrive；对应 guard 要求 evidence adoption 后回到 publication gate / AI reviewer / writer / next owner，同一 fingerprint 不重复启动同一 analysis lane，dry-run reconcile 零 Codex dispatch，apply 只有在 fresh owner_route、未 parked/completed、无 human gate、无 publication gate missing、retry budget 可用且 action fingerprint 新鲜时才能启动 worker。用户入口现在能显示 next owner、why not running、same fingerprint / handoff、`will_start_llm`、safe reconcile command 和 source refs；这些投影不授权 quality ready、publication ready 或 submission ready。
- Durable autonomy closeout 已把 `autonomy_progress_slo_status.state=breach` 固定为可解释 read model：每个 breach 必须带 `breach_reason` / `breach_explanation`，并归类到 owner route、human gate、bundle blocker、quality repair、worker recovery 或 safe reconcile dry-run。解释 payload 会串起 worker lease、checkpoint lineage、retry budget、owner_route / idempotent dispatch、controller apply receipt 与 safe reconcile dry-run command；它只读投影 MAS-owned continuity，不恢复 MDS daemon，不写 publication/controller/paper/package truth。
- Paper Progress SLO closeout 已把论文可验证增量提升为自动推进闭环的最高运行目标：worker live、controller packet、gate audit 或 repair handoff 都只能算中间状态；用户可见进度必须同时投影 `actual_write_active`、`package_delivered`、`meaningful_artifact_delta`、`next_owner` 与 `why_not_progressing`。有效进度只认 canonical manuscript/table/figure/result 变化、submission source/current package freshness proof、AI reviewer judgement 更新、publication gate replay 后 owner 前进。`owner_callable_registry` 固定 `MAS/controller`、`ai_reviewer`、`publication_gate`、`quality_repair_batch`、`gate_clearing_batch`、`delivery_sync` 六类 callable owner；`owner_callable_surface_missing` 在 `requires_user_input=false` 时不再投影成真实用户等待，而是 controller-consumable blocker 或 repo-level callable gap。2026-05-10 稳定性重构进一步把该 SLO 落成 repo implementation：`paper_progress_state` 是固定用户面 read model，`paper_progress_reconciler` 是 level-triggered reconcile receipt，`paper_work_unit_outbox` 与 SQLite sidecar 表 `paper_work_unit_receipts` 记录 idempotent work-unit receipt / duplicate fingerprint / fail-closed conflict，Progress Portal workspace dashboard 直接展示 workspace attention、live count、freshness 和每条论文线下一步。DM002/DM003/Obesity 当前仍按 read-only/live-surface 解释，不把“系统在忙”写成“论文已前进”，也不手工 patch live study artifact；controlled live apply 只能在 repo gates 全绿后执行。
- AI-first paper autonomy closure 是当前最高优先级 paper loop program，入口是 [AI-first Paper Autonomy Closure Program](./program/ai_first_paper_autonomy_closure_program.md)。2026-05-10 repo implementation 已落地：`paper_repair_executor` 能执行 `analysis_repair`、`text_repair`、`evidence_ledger_repair`、`review_ledger_repair`、`claim_downgrade` 与 `route_decision`，并写 owner receipt、canonical artifact delta、evidence/review ledger update、revision log、gate replay request、AI reviewer recheck request 和 canonical package freshness proof；缺结构化 canonical patch 的 text repair、`display_rebuild`、`package_refresh` 等返回 typed blocker。`route_decision_orchestrator` 已把弱结果/负结果/反向结果输出为 executable owner task plan，`sidecar export/dispatch` 已暴露 `paper_autonomy/*` task 给 OPL/Hermes family queue，`real_paper_autonomy_soak_projection` 能只读报告 DM002/DM003/Obesity 的 artifact delta、gate replay、AI reviewer re-eval、route decision、stop-loss、human gate、continuing repair 或 stable blocker。该 program 不承诺所有科学假设最终通过；它承诺每个符合自治条件的非 human blocker 都进入 artifact delta、gate replay、reviewer judgment、route decision、明确止损或 typed blocker。
- 当前完成矩阵必须按计划拆开理解：MAS AI-first paper autonomy 的 repo callable loop 已落地并推送到 `main`；OPL family-runtime 的 MAS paper task projection 已在 OPL 仓落地；Hermes Full App packaging、MAG/RCA paper-equivalent adapters、Hermes gateway 真实 24h restart soak、跨仓 notification / approval / dead-letter E2E、以及 DM002/DM003/Obesity controlled live apply 到最终投稿级 closure 还不能写成已完成。真实论文验收仍以 live MAS truth surface 为准，不把 repo test、queue 入队或 read-only projection 伪装成论文已经交付。
- 2026-05-10 fresh reassessment：MAS monolith 当前的主差异已经不是默认依赖，而是用户体验和受控交互。机器矩阵显示 `17` 个 MDS behavior surface 中 `2` 个完全行为等价、`6` 个目的等价但 timing/实现不同、`4` 个部分等价、`4` 个按新架构退役、`1` 个只保留为历史 fixture。per-study/per-paper Portal、执行器对话时间线、study-scoped Live Console、authorized pause/resume/stop apply receipts、研究路线地图/路线决策只读 contract 和 terminal attach fail-closed gate 已进入 repo contract；本轮修正了真实页面未优先读取 canonical study-progress、执行器对话被 runtime event 噪音挤掉的问题，并把路线可视化从文字列表升级为地图。后续优先做真实 workspace 长时间 soak、route/source-ref polish，以及 MAS-native interactive terminal attach owner 实现评审。connector background delivery、GitOps runtime lifecycle、MDS daemon lifecycle controls 与 workspace-local host service 仍保持 retired，不作为默认能力缺口。

## 当前执行与监管模型

- 当前仓库跟踪主线继续按 `Auto-only` 理解。
- 默认 concrete executor 仍继承本机 `Codex` 配置；MAS 仓库侧监管围绕 MAS-owned scheduler contract 做状态检查和恢复判断。OPL Full online runtime 依赖 OPL-managed Hermes substrate 提供常驻唤醒、session/delivery/approval transport 和 family queue tick。
- 2026-05-08 Runtime Turn Lifecycle correction 已把旧 MDS daemon 的自动科研连续主循环吸收到 MAS-owned `mas_runtime_core`：`resume_quest` 进入 `schedule_turn(reason=explicit_resume)`，`chat_quest` 持久入队用户消息并触发 turn，runner completion 通过 `complete_turn_and_normalize` 清理 `active_run_id` / `worker_running` 后按 pending user message、human gate、continuation policy 和 terminal state 决定下一 turn。真实 runner 由 `CodexExecTurnRunner` 启动并由 MAS runner monitor 在 subprocess 退出后归一化；`auto_continue` 由 kernel 内部 delayed timer 低延迟消费，同时 `inspect_turn_lifecycle` / `runtime_watch` 保留 crash-recovery drain。`runtime_watch` / supervisor / scheduler adapter 只做外层 reconcile、wakeup 和 stale recovery，不再被解释为自动科研主循环 owner。
- 方向锁定后的普通科研推进、论文质量判断与 `bounded_analysis` 一类有限补充分析默认由 `MAS` 自主完成。
- 通用研究阶段纪律继续从 upstream `DeepScientist` 方法论学习并在 `MAS` owner surface 中表达；旧 `MDS` base skill 只保留为 source provenance、historical fixture 或 explicit archive import reference。医学质量、医学稿件规则、submission hygiene 与 `publication gate` blocker 继续由 `MAS` 的 study charter、overlay、ledger、controller surface 承担。
- human gate 收口到方向重置、重大 claim 边界变化和投稿前最终审计。
- 关键持久表面继续围绕 `study_charter`、`evidence_ledger`、`review_ledger`、`study_runtime_status`、`runtime_watch`、`publication_eval/latest.json`、`controller_decisions/latest.json`。
- 文件生命周期管理已进入 SQLite runtime authority + restore-proof archive 双轨：runtime watch/report state、workspace storage audit、archive refs、migration ledger、quest lineage、workspace allocation、runtime snapshot、snapshot file refs、revision diff 与 Canvas projection 已可写入或只读投影到 `artifacts/runtime/runtime_lifecycle.sqlite`；`runtime lifecycle-read` 已支持 `lineage_route`、`workspace_allocation`、`runtime_snapshot`、`revision_diff`、`canvas_projection` 这些 SQLite-only surface，缺 DB / 缺表时 fail-closed 为 read-only missing / capability gap，不回落到 Git。现有 `latest.json`、timestamped JSON/Markdown、publication/controller truth 与投稿产物继续保留文件 authority。2026-05-05 restore-proof closeout 已把 NF-PitNET、DM-CVD / DPCC 和 AS biologics 的 11 个 migrated quest `.ds` 从约 1,024,244 个文件压到 772 个文件；这是 closeout ledger 口径。2026-05-06 已按用户确认窗口完成 DPCC004、DM002、DM003 的 post-closeout drift compaction：DPCC004 当前约 292 个文件、约 1.0G；DM002 从本轮 fresh gate 的 20,185 个文件 / 6.5G 降到 58 个文件 / 6.3G；DM003 从 16,226 个文件 / 4.7G 降到 52 个文件 / 4.5G。三者新增 restore proof 均 verified、errors 为 `0`；NF-PitNET 和 AS biologics 仍保持已处理状态。
- 新建 workspace 默认已经切到 MAS-first 分层目录：`runtime/quests/`、`runtime/archives/`、`runtime/restore_index/`、`artifacts/runtime/`、`ops/mas/`；`ops/med-deepscientist/runtime/quests/` 仅作为旧 workspace compatibility reader / restore proof / Git ignore 兼容面存在。
- Git 退役已完成 current-project active-path cutover：repo-side contract、默认 MAS-first layout、SQLite authority 边界、current eligible workspace storage compaction、SQLite lineage/snapshot/allocation writer、SQLite-only reader projection、普通目录 quest materializer CLI、quest Git inventory surface / CLI、默认 lifecycle fallback retirement、legacy runtime reader cutover 和 compatibility-retirement guard 已完成。`runtime lifecycle-quest-git-cutover` 已提供 Q5 safe dry-run/apply 入口：只对 `paused/parked/stopped/completed/failed/cancelled` 且 `active_run_id=null`、`worker_running=false` 的 quest `.git` 做 archive/remove proof；`active/running/live/unknown` 一律 audit-only/skipped。2026-05-06 real workspace apply 已把 NF-PitNET、AS biologics 和 HeRR 的 active-path quest `.git` 清零并写 verified ledger；2026-05-07 趁 DM-CVD DM002/DM003 无 live worker 窗口，经 `pause-runtime` 显式暂停后完成最后两个 quest `.git` archive/remove，并写入 Q5/Q6 verified ledger。当前 current projects 可记录 `quest_git_retired_for_current_projects=true`：默认 CLI/MCP/controller/product entry 不再走 MDS Git compatibility fallback；只保留显式 historical fixture / explicit archive import reference 作为只读恢复入口。
- Workspace root Git full retirement 已对 current workspaces 完成：`workspace-storage-audit --git-only --apply --retire-workspace-root-git` 已把 NF-PitNET、DM-CVD / DPCC 和 AS biologics 的 root `.git` archive/remove，并写入 `artifacts/runtime/lifecycle_migration/workspace_root_git_retirement/latest.json`；HeRR 本来就是 no root Git。`init-workspace` / MCP `init_workspace` 默认 no root Git / no quest Git；root Git 不再作为 workspace status、runtime lifecycle 或论文 truth 的默认读取面。runtime lifecycle 由 `artifacts/runtime/runtime_lifecycle.sqlite`、`runtime/quests` manifest、`runtime/restore_index`、`artifacts/runtime/lifecycle_migration` ledger 和文件 authority 承接。
- Quest materializer 已补上防回流 guard：如果 active quest root 已存在 `.git`，apply 会 fail-closed 为 `blocked/audit_only`，不会写 materialization manifest，也不会把 Git repo 重新纳入 runtime lifecycle。
- 终局止损文件生命周期已成为 dry-run contract：只有 materialized macro state 明确 `parked/none/stop_loss` 且 `reopen_allowed=false`，才会在 lifecycle report 中生成 `terminal_study_file_lifecycle_plan` 候选。当前不会自动物理压缩或删除 runtime history；后续 apply 必须先有 manifest、sha256、restore index 与 restore proof。
- 关键身份继续围绕 `program_id`、`study_id`、`quest_id`、`active_run_id`；用户面优先呈现 `study_id`、任务摘要、阻塞和下一步。

## 当前 active tranche

- 当前 tranche 固定为“质量闭环结构化 + 用户可见真相投影 + 对应 proof/soak 口径”。
- 2026-05-02 长线目标架构已固定为 `MAS AI-first Research OS`：MAS Core、Quality OS、Runtime OS、Artifact OS、Evaluation OS、Observability OS 与 MDS Deconstruction 共同定义后续 owner、authority、contract 与 parity proof。当前阶段继续按能力逐步落地，不做一次性物理迁移。
- 质量闭环结构化的 owner 继续落在 `study_charter`、`paper evidence ledger`、`review ledger`：把方向锁定后的普通科研推进、论文质量裁决、`bounded_analysis` 边界、reviewer concern 与 submission hygiene 压成同一套 `MAS` quality contract。
- AI-first 质量边界的当前目标是把 `publication_gate` / `medical_reporting_audit` 生成的机械投影，与 AI reviewer 读取 manuscript、evidence ledger、review ledger 和 study charter 后形成的质量判断分开。机械投影表达 blocker/projection/replay，AI reviewer workflow 才能承接科学质量、医学写作质量和 submission-facing readiness。
- 投稿前质量授权口径保持 fail-closed：只有由 AI reviewer 读取 manuscript、evidence ledger、review ledger 与 study charter 后形成的 judgement，才能进入 scientific quality、medical writing quality、publishability 或 submission-facing readiness 判断。
- 当前 repo-side 可用表面应能表达质量修复路线：当前是同线写作修复、`bounded_analysis` 一类有限补充分析、finalize / bundle 收口，还是 human gate。文档不把这些 route 词汇变成机械 wording gate。
- `publication_eval/latest.json`、`controller_decisions/latest.json`、`study-progress` 与 `product-entry` 是当前用来投影 quality closure、route-back、controller action 和用户可见下一步的主要表面；具体 route 能否闭合，以真实 study 的 durable surface 和 artifact rebuild proof 为准。
- 用户可见真相投影的 owner 继续落在 `study_runtime_status`、`runtime_watch`、`publication_eval/latest.json`、`controller_decisions/latest.json`：用户与维护者都应能从同一条 `MAS` 主线上读到当前阶段、关键证据、阻塞、下一步、恢复点与 human gate 原因。
- `study_macro_state` 是用户眼里唯一的论文状态源；`study-progress.user_visible_projection`、CLI markdown、MCP compact/markdown、`workspace-cockpit` 和 `product-entry-status` 只从它派生 `writer_state`、`user_next`、`reason`、`package_delivered`、`actual_write_active`、`user_action_required` 与人话标签。缺少 canonical macro state 或 writer 冲突时显示 `inspect/conflict`，不回退到旧 top-level 字段。
- `study-progress`、`workspace-cockpit`、`product-entry-status` 应围绕同一组 operations state 解释自动推进、runtime recovery、restore point、artifact pickup 和 human gate；它们不能制造第二套研究判断。
- `StudyTruthKernel` shadow layer 与 `RuntimeHealthKernel` shadow layer 是当前读状态基础：普通 read 不刷新 materialized truth；materialized truth / health 只能由显式 reconcile、controller tick 或 runtime watch apply 写入。
- Portable supervisor 现在已经把 request ownership、默认执行派单与 submission milestone parking 收口进运行合同：`publication_gate_specificity_required` 先生成 `publication_gate` owner 的 request packet，再由 `runtime-supervisor-execute-dispatch` 调用 publication gate owner surface 物化带具体 targets 的 `publication_eval/latest.json`；`return_to_ai_reviewer_workflow` 只生成 `ai_reviewer` owner 的 request/dispatch，缺结构化 reviewer record 时明确落账 `owner_callable_surface_missing`；`runtime-supervisor-consume` 只消费 queue 并写 handoff/dispatch task。consumer request handoff 和 executor dispatch 都必须通过 `owner_route.allowed_actions`，executor 只执行 workspace-level consumer latest 中当前 ready 的 dispatch。外层 supervisor 不能修改 paper/current_package，也不能替代 AI reviewer 或放宽 publication gate authority。
- 外层监管延迟现在有正式 SLA read model：`outer_supervision_slo` 以 scheduler tick 和 supervisor reconcile receipt 为输入，投影 `fresh/due/stale/missing/blocked`、监管年龄、blocked/missing reason 与 safe reconcile dry-run 命令。local adapter 和 optional Hermes adapter 都必须提供同构 receipt；`due/stale` 只说明可安全触发一次 one-shot reconcile，读入口不能据此直接 relaunch worker、写 paper/package、写 publication gate 或写 controller decision。
- 旧 portable workspace-local service manager 已退役：显式 `--manager systemd|cron|launchd|docker` 只返回 retired fail-closed diagnostic，不再生成旧 scheduler 模板或安装命令；检测到旧 service 时作为 cleanup evidence 处理。当前可用 OS backend 只能通过 MAS-owned `local` adapter 进入，macOS LaunchAgent 已落地，其他 persistent backend 仍 fail-closed。
- medical journal prose 质量合同已经进入当前文档口径：first draft 应是医学期刊可读的 manuscript-shaped prose，不能把 controller checklist、figure/table anchor、author-confirmation placeholder、内部 claim-boundary 标签或 operations/review 语言带入正文。
- workspace Git/storage 边界已收紧为默认 no root Git、current workspace root Git restore-proof retirement、generated/runtime/artifact 排除与 explicit storage audit repair；runtime-storage apply-mode release estimate 必须反映实际 apply strategy，而不是 gross candidate size。
- `run_gate_clearing_batch`、quality review follow-through、same-line route truth 与 autonomy soak 相关表面都是当前 proof/soak 的候选证据面；它们是否已经形成可发表工作流证据，仍需真实论文线继续 soak。
- proof/soak 口径当前围绕真实 study 的长期自治、pre-draft 质量运行、AI reviewer workflow、artifact rebuild proof 与 operations state 是否闭合，不围绕 `MDS` 再造一套长期 owner 面；`MDS` 只保留 frozen source archive、historical fixture、explicit archive import / provenance reference 和 future upstream source intake review 角色。
- 真实 paper autonomy stability 现在由 `paper_autonomy_stability_evidence` 单独关闭：repo/read-model 已可用，但产品结论必须看真实 evidence 是否有 blocker。human gate、publication gate、parked handoff、profile unreadable 或 migration skipped 都是有效 evidence，不会被伪装成 landed。
- 当前 tranche 的 repo-side 落点是单项目 owner truth、用户可见边界和 program/mainline 口径收紧；no-history physical absorb 已落为 MAS-authored guard/parity/retained-capability/default-runtime-retirement closeout，没有导入 MDS git history，也没有把 `MDS` 重新解释成并行产品面。
- `build_product_entry.return_surface_contract`、`skill-catalog` domain projection、`mainline-status` / `mainline-phase` / `product-entry-status` / `product-entry-manifest` 继续作为 caller 读取 MAS/MDS owner boundary、runtime continuity、artifact locator 与 progress truth 的入口；这些入口只能投影 MAS durable truth，不能替代研究质量判断。
- OPL 或其他 caller 读取这些入口时，只能把它们当作 MAS durable truth 的投影与内部 command contract；不得把 MDS、product-entry-status 或 product-entry manifest 写成并行 owner 面。
- 当前已增加 architecture owner boundary fitness function：`mas_mds_architecture_owner_boundary_report` 与 [MAS/MDS Owner Boundary Refactor Plan](./policies/runtime-governance/mas_mds_owner_boundary_contract.md) 明确确认重复 authority 风险，并把修复路线固定为 owner matrix、strangler refactor 和 automated fitness tests。它的作用是防止 entry projection、observability、MDS oracle/backend 或 runtime adapter 在下一轮重构中重新制造第二套 study / quality / runtime / publication owner。
- README、status、policy 与 reference 的语义边界现在由 `mas_mds_doc_reference_semantic_guard` 统一约束；它只允许文档继续做人读面，不允许它们自己生成新的 owner truth、默认 MDS 依赖或默认 MDS WebUI 入口。
- 当前 tranche 的通过条件是：`MAS` 能默认自治推进方向锁定后的研究与有限补充分析，用户可见 truth 与 durable surface 对齐，major boundary 与最终投稿审计之外不再把 human 判断留在 `MDS` 或隐藏 owner 面里。真实论文 soak 仍是整体 AI-first 质量闭环的主要证据面；Open Auto Research 的真实 study soak 已用 DM002 完成，不再是 OAR repo-level capability 缺口。

## 当前验收与 proof 口径

- 验收先看结构是否闭合：study charter 质量总合同、evidence/review ledger 执行记录、runtime/progress truth projection 三层要能沿同一条 `MAS` 主线解释。
- 验收还要看语义是否可读：当质量闭环要求回到某条现有主线时，`MAS` 能说清“回到哪条线、当前关键问题是什么、为什么先做这一步”。
- proof 先看 owner 是否单一：质量判断、有限补充分析推进、运行恢复与用户面进度解释默认都由 `MAS` 负责；`MDS` 只提供 historical fixture、source provenance 与 explicit archive import reference，不承担长期双 owner。
- soak 先看真实 study 能否长期成立：长时间运行、停滞后的恢复、pre-draft 质量判断、AI reviewer workflow、artifact rebuild proof、human gate 触发、投稿前审计前的持续推进，都要在真实 durable surface 上读得出来。
- 同线 continuation proof/soak 现在也包括 `run_gate_clearing_batch`：proof 看 controller 是否能沿 `publication_eval -> controller_decisions -> gate_clearing_batch record -> publication_gate replay` 保持单一 owner truth，soak 看真实 study 是否能经由这一步继续回到同一条 paper line，而不是新增人工 owner 或第二条治理面。
- 当前 stage 要求 `MDS` 的存在只能解释为 frozen archive、historical fixture、explicit archive import / provenance reference，而不是另一条并行产品主线、默认执行后端或默认进度入口。
- 当前 no-history physical absorb closeout 是 repo-level default-dependency retirement 和 retained-capability absorption，不是把上游历史接入 MAS，也不是声明真实论文质量 soak 已完成。
- 当前 parity/proof 口径固定为：`behavior_equivalence_oracle`、`study_progress_projection_contract`、`publication_eval/latest.json` 与 `controller_decisions/latest.json` 共同支撑迁移期等价证明；MDS mechanical signal 不能授权 medical quality、publication ready 或 submission ready。

## 当前边界

- `Med Auto Science` 负责研究入口、工作区权威语义、证据推进和论文交付。
- `Med Auto Science` 的 direct/handoff 两条入口路径共享同一套 study truth 与 authority boundary。
- `MedDeepScientist` 保留为 frozen source archive、historical fixture、source provenance、explicit archive import、backend-audit reference 和 future upstream source intake review reference，不再承担默认运行依赖、默认诊断依赖、独立产品入口、长期治理面或长期双 owner 语义。
- 研究者与课题负责人继续负责方向设定、重大边界变化和投稿前审计。
- 期刊投稿和外部系统交互继续由人工监督。
- `OPL` 集成、`product-entry manifest`、`handoff envelope` 和其他机器可读桥接继续留在集成层与参考层阅读。
- `MAS` 已声明 `OPL` family contract adoption：`contracts/opl-gateway/family-contract-adoption.json` 与 `docs/references/integration/opl_family_contract_adoption.md` 把 runtime attempt、medical quality projection、incident learning 与 product operator projection 映射回 MAS-owned durable surfaces；`OPL` 只消费投影，不持有 study truth 或 publication judgment。
- 更大的平台化结构调整和外部 runtime substrate 选择继续按各自 contract 推进；它们不得把 external MDS repo 重新变成默认 MAS dependency，也不得把 Hermes 提升为 MAS quality/artifact owner。
- 自有长期常驻 MAS daemon 不是当前 MAS 侧目标。OPL Full online runtime 使用 OPL-managed Hermes substrate 和 OPL typed queue；MAS 负责 sidecar export/dispatch、domain recovery decision 与本地 degraded diagnostics。

## 当前维护重点

1. 保持 `README*` 与 `docs/README*` 继续面向医生、课题负责人和潜在使用者。
2. 保持 `docs/project.md`、`docs/status.md`、`docs/architecture.md` 对齐同一套产品边界、执行回路和 owner 层级。
3. 保持 `product-entry-status`、`workspace-cockpit`、`submit-study-task`、`launch-study`、`study-progress` 作为 MAS 的核心可执行回路，同时保持 `CLI`、`MCP`、`controller` 作为正式操作与自动化入口。
4. 保持 `OPL Runtime Manager`、OPL-managed `Hermes-Agent` online substrate、MAS sidecar bridge、MAS supervision scheduler contract 与 MAS durable projection 的 owner split 清晰；维护者细节继续留在 reference / program 层。
5. 把“医学论文质量 + 长时间全自动驾驶优化”正式收口到 `MAS` 单项目主线，由 `controller_charter / runtime / eval_hygiene` 共同承担 owner；`MDS` 已降级为 frozen source archive、historical fixture、explicit archive import / provenance reference。
6. 把 study charter 作为质量总合同入口；`paper evidence ledger` 与 `review ledger` 作为该合同的执行与审阅记录，统一承载主结果、`bounded_analysis`、reviewer concern 与 submission hygiene 的落地状态。
7. 把用户可见真相投影压实到 `study_runtime_status`、`runtime_watch`、`publication_eval/latest.json`、`controller_decisions/latest.json` 这一组 durable surface 上，让当前阶段、关键证据、阻塞、下一步、恢复点、artifact pickup 与 human gate 原因都能被同一条 `MAS` 主线读取；当前统一出口已经收口成 `autonomy_contract + restore_point + autonomy_soak_status`、`quality_closure_truth + quality_closure_basis + quality_review_followthrough` 与 `research_runtime_control_projection` 三条互补 truth。
8. 把 `StudyTruthKernel`、`RuntimeHealthKernel` 与 first-draft prose quality contract 视为当前读状态与 pre-draft quality runtime 的核心文档面；维护者先读 [Study Truth Kernel Contract](./runtime/projections/study_truth_kernel.md)、[Runtime Health Kernel Contract](./runtime/projections/runtime_health_kernel.md)、[Medical Manuscript First-Draft Quality Policy](./policies/quality/medical_manuscript_first_draft_quality.md) 和 [AI-first Quality Boundary Policy](./policies/quality/ai_first_quality_boundary.md)。
9. 把“持续学习 `DeepScientist` 方法论”收口为 `MAS` 的长期 program lane：维护者先读 [DeepScientist Latest-Update Learning Protocol](./references/med-deepscientist/deepscientist_latest_update_learning_protocol.md)、[MedDeepScientist Method Learning Disciplines](./references/med-deepscientist/med_deepscientist_method_learning_disciplines.md)、[MedDeepScientist Continuous Learning Plan](./references/med-deepscientist/med_deepscientist_continuous_learning_plan.md) 和 [MedDeepScientist Upstream Source Provenance](./references/med-deepscientist/med_deepscientist_upstream_source_provenance.md)。当用户说“学习一下 `DeepScientist` 的最新更新”时，默认由 `MAS` 直接启动 upstream audit、decision matrix、并行 worktree 落地、验证、吸收回 `med-autoscience/main` 和清理的完整流程；先区分“upstream learned common research discipline”和“MAS-owned governance / medical-quality surfaces”，再决定哪些 lesson 进入 `controller_charter`、`runtime`、`eval_hygiene` owner 面，哪些只保留为 source provenance、historical fixture、explicit archive import reference 或 watch/reject。2026-05-08 intake 已记录在 [DeepScientist Learning Intake 2026-05-08](./history/program/deepscientist_learning_intake_2026_05_08.md)：upstream `bd0b92b..a3ba701` 只新增 Claude adapter probe ordering、README 社群图片和 AISB release-asset catalog 修正；MAS 本轮只把 Claude prompt/tool-argument 分离作为 future provider-probe watch lesson，README/AISB 变化按 provider/product/catalog 面拒绝进入 MAS mainline authority。上一轮真正吸收的 summary recall、all-negative exploration-depth gate 与 future-facing limitations 仍记录在 [DeepScientist Learning Intake 2026-05-05](./history/program/deepscientist_learning_intake_2026_05_05.md)。
10. 把外部 agent orchestration 学习记录作为后续编排参考入口：维护者先读 [External Agent Orchestration Learning Intake 2026-04-30](./history/program/external_agent_orchestration_learning_intake_2026_04_30.md)，只吸收可加强 MAS 长期自治、work-unit 状态、隔离 workspace、retry/backoff/reconciliation、observability、hosted worker trust boundary、structured handoff、evidence-over-claims 与 AI reviewer gate 的合同或模板。继续学习时按该记录的 `Continued Learning Saturation Protocol` 执行：固定 source SHA、记录新增 source file coverage、分类 adopt/watch/reject，并在只剩外部 owner、tracker-specific mechanics、generic persona routing、marketing/product lifecycle、non-medical QA label、cryptographic identity runtime 或重复表述时，把当前 snapshot 标记为 `MAS-actionable saturated`。
11. 把近期 open-source Auto Research 项目族学习记录作为 PaperOrchestra 相邻参考入口：维护者先读 [Open Auto Research Learning Intake 2026-05-04](./history/program/open_auto_research_learning_intake_2026_05_04.md)。PaperBench-style hierarchical rubrics、PaperQA2-style scientific literature evidence graph、STORM/Open Deep Research-style perspective-first evidence compression、LangGraph/OpenHands/SWE-agent-style runtime trajectory proof、AI-Scientist-v2 / AutoResearchClaw-style candidate path graph 已落成 MAS-owned read-model，并通过 `study_progress`、`workspace-cockpit`、`product-entry-status` 和 MCP 只读投影暴露 ready / blocked / needs_review 与下一步动作；`open_auto_research_projection` 当前是 read-only status surface，读入口只走 `build_*`，正式 artifact materialization 必须由显式 controller/runtime authority 入口承担。rubric score、trajectory replay 和 candidate path decision 只能作为 calibration / observability / planning evidence，不能替代 `publication_eval/latest.json`、`controller_decisions/latest.json`、`study_runtime_status`、study truth 或 submission readiness owner；这些 lesson 只能落到 MAS `Quality OS`、`Evaluation OS`、`Runtime OS`、`Artifact OS`、`Observability OS` 或 operator projection，不能把外部 framework、skill pack、provider runtime、generic role library、non-medical benchmark 或 auto-paper generator 提升为 MAS study truth、publication owner、controller decision owner 或 artifact authority。DM002 真实 study soak 已在 `002-dm-china-us-mortality-attribution` 上完成：controller-authorized soak 只写 OAR read-model source artifacts 与 `artifacts/runtime/open_auto_research_soak/latest.json`，四类 capability 投影为 `3 ready / 1 needs_review / 0 blocked`，`study_progress` 读入口仍不创建 `open_auto_research_projection/latest.json`，`product-entry-status`、`workspace-cockpit` 与 MCP 均能显示四个下一步动作；当前 blocked verdict 来自 AI reviewer required、publication gate blocked 与 runtime recovering 这些论文/runtime truth，不是 OAR repo-level 功能缺口，也不构成 submission-ready 声明。
12. MAS/MDS owner-boundary 重构入口是 [MAS/MDS Owner Boundary Refactor Plan](./policies/runtime-governance/mas_mds_owner_boundary_contract.md)。维护者新增 bridge、projection、oracle、runtime adapter 或 MDS intake 时，必须先说明当前 owner、目标 owner、authority surface、promotion gate、parity proof 与 rollback surface；`architecture_owner_boundary` meta guard 和 MDS strangler registry 是本轮落地的最低防回归面。
13. 当前 mainline 收口记录见 [Plan Completion Ledger](./references/verification/plan_completion_ledger.md) 的 `2026-05-04-final-mainline-cleanup`、`2026-05-04-mas-mainline-closeout-cleanup`、`2026-05-04-v6-research-quality-and-test-hardening-closeout`、`2026-05-04-current-state-and-oar-delivery-authority-closeout`、`2026-05-07-progress-projection-unique-state` 与 `2026-05-07-mas-modularity-fitness-wave`：OAR/DM002、v6 hardening、Developer Supervisor Mode、portable supervisor scheduler、storage governance/backfill/safe-cache/continuous-soak、runtime/control owner-route 收口、progress projection unique state 和模块化 fitness wave 相关 repo-level 能力保持 landed truth；重复 DM002 closeout 提交已进入远端历史，不做历史重写。当前 `open_auto_research_projection` 会把真实 study soak / delivery-journal usability guard 作为只读状态投影，明确 `submission_ready_authorized=false`、下一步仍是 AI reviewer / publication authority；OAR/DM002 soak 不替代 `publication_eval/latest.json`、`controller_decisions/latest.json` 或 `study_runtime_status`。本地 `main` 在模块化 closeout 后继续领先 `origin/main`；完成性以本 ledger、验证记录和 push 状态为准，不把单个 transient SHA 写成产品 truth。后续文档修正必须继续和运行状态机改动分开处理。
14. Developer Supervisor Mode 已正式定义为开发环境外层巡检/排队能力：普通 MAS 运行默认保留 `internal_only`，portable 外层可用 `external_observe`，本机开发环境使用 `developer_apply_safe`。该模式只允许写 supervision/control/autonomy request、handoff packet、action queue、why_not_applied 投影和 owner-visible request packet；不得 patch `paper/current_package` 或 `manuscript/current_package`，不得放宽 publication/quality gate。`developer_apply_safe` 的 authority 优先来自 OPL family 用户级配置 `developer-supervisor.json`；`enabled=auto` 时才使用 GitHub 登录作为默认探测信号，`gaofeng21cn` 默认启用，其他用户默认降级到 `external_observe`，但可以通过受控用户级配置手动开启或关闭。
15. Submission/finalize 里程碑停车现在按 controller-owned parking 处理：如果 quest 已 stopped 但 controller truth 表明它只是 submission milestone parking，supervisor 只能刷新 parked controller decision、确认/停止 runtime 资源，并写 `artifacts/autonomy/repair_lifecycle/latest.json` 为 `state=parked` / `authority=controller_stop`。这条路径明确禁止 paper package mutation、manual study patch、quality gate relaxation 与 medical claim authoring；后续稿件反馈仍必须走 durable revision intake 与 MAS-controlled relaunch/resume。
16. Runtime lifecycle storage 的当前落地面是 SQLite runtime authority + 文件 truth authority + restore-proof archive：MAS 已把 runtime report store、runtime event index、workspace storage audit、compatibility read/export、migration ledger、lineage/allocation/snapshot/diff/Canvas projection、plain quest materialization、quest Git inventory、显式 historical fixture / explicit archive import reference、safe quest Git cutover surface 和 workspace root Git retirement surface 登记进 repo-level contract / CLI / tests；历史 MDS-side runtime maintenance 只作为 cold/archive/restore proof 参考。2026-05-05 restore-proof closeout 已完成 NF-PitNET、DM-CVD / DPCC 和 AS biologics 的 eligible quest compaction：DM002 从约 496,470 个 `.ds` 文件降到 129，DM003 从约 201,744 降到 178，DM001 legacy 从 5,815 降到 14，NF legacy 002 从 17,318 降到 14，AS001 从约 62,383 降到 56；11 个 migrated quest 合计从约 1,024,244 个文件降到 772 个文件，restore proof 均为 verified、errors 为 0。2026-05-06 follow-up 已处理 DPCC004、DM002、DM003 的 post-closeout drift：DPCC004 proof verified `24,403/24,403`；DM002 新增 proofs verified `22,949/22,949`，final `.ds` 58 files / 6.3G；DM003 新增 proofs verified `16,920/16,920`，final `.ds` 52 files / 4.5G；三者 errors 均为 `0`。2026-05-06 Q5 active-path quest Git cutover 已执行 safe apply：NF-PitNET `3/3` retired、AS biologics `1/1` retired、HeRR `0/0` verified、DM-CVD 先退役 `3/5`。2026-05-07 追加处理 DM-CVD 剩余 DM002/DM003：两者 fresh state 为 `active_run_id=null`、`worker_running=false`，经 `pause-runtime` 写入 user pause contract 后 archive/remove 最后两个 quest `.git`，DM-CVD Q5 ledger `runtime-lifecycle-20260507-quest-git-cutover-dm-cvd-final-ledger` verified，Q6 compatibility retirement ledger `runtime-lifecycle-20260507-quest-git-cutover-dm-cvd-q6-retirement` 显示 `compatibility_retirement.allowed=true`。2026-05-07 root Git full retirement 已把 NF-PitNET、DM-CVD / DPCC 和 AS biologics 的 workspace root `.git` archive/remove；NF archive sha256 `35a210df512ee034deca3ef6a39bfbfb167de8387d6becad1c5120359ddf5b78`，DM archive sha256 `8b6aeeb0dcda363eaccbc5e73c6d83e2791550d6f70c37e732a9acb84461baa8`，AS archive sha256 `28e7f0fefc18bd170b7420559ecc37e5729b64ad5d010ca49311b93d76285404`；DM 的 1 个 root Git stash 已记录在 restore manifest 中；HeRR 原本 no root Git。NF-PitNET 是无功能垂体瘤的 canonical workspace；`/Users/gaofeng/workspace/Yang/无功能垂体瘤` 只是本机轻量 stale alias/scaffold，不作为独立项目重复迁移统计，也不得作为 repo-tracked `nfpitnet` profile 或 fixture 的真实 workspace root。后续维护按 [Runtime Lifecycle SQLite Migration Program](./program/runtime_lifecycle_sqlite_migration_program.md) 继续执行：quest `.git` 和 workspace root Git 对 current projects 已退为 restore diagnostic archive。Agent 查状态或做 runtime lifecycle 操作时，优先读取文件 authority、`artifacts/runtime/runtime_lifecycle.sqlite`、`artifacts/runtime/lifecycle_migration` ledger、`runtime/quests` manifest 和 `runtime/restore_index`，不查 Git。任何 DM001 live writer 或自动恢复意图仍应先归类为 MAS-controlled 状态机问题。event/run/bash/codex history metadata、cursor pagination 与 retention ledger 优先进入 SQLite，原始 terminal/stdout/session payload、tar/gzip 冷归档、paper/manuscript/package/dataset restore contract 继续保持文件或归档形态。
17. Delivery L4 已从 README 结构投影推进到 controller-authorized 写入：`sync_study_delivery` 现在会在真实 `manuscript/current_package/README.md` 写入医生友好四段式说明，并在 delivery manifest 暴露 `controller_authorized_doctor_readme`；`delivery_visibility` 的 build/inspect 仍是只读 projection，不写 `current_package`。L3 journal-family fixture matrix 也已要求 cover letter template、submission checklist、supplement naming convention 三类具体 refs；该 matrix 仍是 observability/projection，不授权 scientific quality、publication readiness 或 submission。
18. MAS 单项目吸收 MDS 的长线入口继续是 [MAS Single-Project MDS Absorb Program](./program/mas_single_project_mds_absorb_program.md)。2026-05-06 已把该 program 对齐到当前四层文件管理合同：`study_macro_state/owner_route` 持有 current truth，SQLite 持有 runtime lifecycle authority、read model、receipt 和 cursor，`portfolio/research_memory` 与 learning read model 只做记忆和校准，paper/manuscript/package/delivery 继续由 canonical paper truth 和 rebuild proof 持有 authority。L1 workspace layout contract 已落地：新 scaffold 默认生成 MAS-owned `runtime/`、`artifacts/runtime/`、`ops/mas/`，旧 `ops/med-deepscientist` 只保留兼容读取和 ignore。2026-05-08 `workspace_layout_de_mds_ds` 已继续把静态 profile template、doctor/show-profile、bootstrap/quickstart/agent-runtime 文档收口到 MAS-owned visible layout：`runtime/quests` 和 `runtime/` 是默认可见 runtime 路径，`med_deepscientist_*` 只作为 historical fixture / explicit archive import reference / backend-audit alias。同日 `profile_entry_compat_retirement` 已把默认入口兼容继续收口：MCP 旧 `med_deepscientist_upgrade` mode 被拒绝，profile JSON 顶层改为 `source_provenance`、`historical_fixture_ref` 与 `explicit_archive_import_ref`，workspace contract 使用 managed runtime / controlled backend 命名，product-entry 与 progress 默认 `executor_owner` 为 `controlled_research_backend`。2026-05-06 repo-level fast-track 又落地了 plain quest materializer、quest Git inventory、默认 fallback retirement、historical reader cutover 与 safe quest Git cutover CLI：`runtime quest-materialize` / `runtime-quest-materialize` 只生成 `runtime/quests/<quest_id>` 普通目录和 `artifacts/runtime/materialization_manifest.json`，manifest 明确 `git_runtime_used=false`、`quest_git_active_path_retired=true`；`runtime lifecycle-quest-git-cutover` 负责把安全窗口内的旧 quest `.git` 归档后移出 active path。真实 workspace Q5/Q6 已推进到 NF-PitNET、AS biologics、HeRR 和 DM-CVD verified；current projects active-path quest Git 已退役。2026-05-07 workspace root Git full retirement 也已完成 current workspace archive/remove/verify。2026-05-08 no-history physical absorb campaign 已落地：source provenance JSON 记录真实 MDS ref/hash/license refs，author guard 阻断上游 contributor footprint，capability parity harness 与 retained capability absorb 把 runtime execution、artifact inventory、paper health、coverage、stage discipline、memory/lesson intake 固定为 MAS-owned surface 或 oracle fixture。2026-05-08 functional monolith completion 又把默认 runtime binding、live task intake、workspace helper、Progress Portal、product-entry/progress/runtime contract 和 MDS retained role 收口到 MAS Runtime OS / Artifact OS / Quality OS；external MDS 现在只保留 frozen source archive、historical fixture、explicit archive import / provenance reference，不再是默认 operation、默认 diagnostic、default runner 或 WebUI dependency。2026-05-08 behavior equivalence audit 进一步固定：functional monolith landed 只表示默认独立，不表示 MDS daemon 行为完全等价；该差异由 `mds_behavior_equivalence_matrix` 和 behavior gap matrix 持有。
19. `docs/program/` 的当前唯一 portfolio 治理入口是 [Program Portfolio Consolidation](./program/program_portfolio_consolidation.md)。MAS 吸收 MDS 是总执行计划，Runtime Lifecycle SQLite Migration 是其 runtime/Git 子计划；两者可以按 disjoint write set 并行实现，但必须共享同一 authority contract、migration ledger、restore proof 和 compatibility retirement gate。后续 Agent 应按 portfolio 的 next execution queue 自动选择 lane、worktree、验证、吸收和清理；已落地 closeout、activation package 与 recurring learning lane 的 dated intake snapshot 归入 `docs/history/program/`，稳定规则归入 `docs/policies/`，MDS 学习、parity、ledger 与技术参考归入 `docs/references/`。DeepScientist、external orchestration、PaperOrchestra / open auto research 这类非主线学习任务仍按 `docs/status.md` 和对应 reference / snapshot protocol 触发，历史目录只保存单轮记录，不表示 support lane 退役。
20. 2026-05-07 repo 目录治理已按一次性计划落地到 `main`：新增 repo hygiene gate，固定 root display-pack source truth，root 小治理目录迁入 `docs/` / `contracts/`，三条顶层测试已拆为薄 aggregator + semantic case modules，六个超长源码 facade 已拆入对应 `_parts`。root display-pack 的高扇入 `shared.py` 已拆成兼容薄出口 + `shared_parts/`，`.sentrux/baseline.json` 已刷新到目录治理后的拓扑：quality `6142`、cycles `0`、god files `3`，后续 `scripts/verify.sh structure` 以该拓扑防退化。本轮只清理自身 `mas-*` 临时 worktree/branch，不把外部会话 worktree 当作清理目标；完成性证据记录在 [Plan Completion Ledger](./references/verification/plan_completion_ledger.md) 的 `2026-05-07-repo-directory-governance-cleanup`。
21. Boundary governance 现在补充为全仓 `_parts` advisory 报告面：`boundary_fitness` 继续把机械 `part/chunk/split` 编号作为 hard violation，同时把 nested `_parts`、膨胀的 `shared_base.py`、接近 1000 行的单 part、`exec(compile(...))` 拼接加载列为 non-blocking advisory。当前口径只服务后续自然边界治理，不改变 public contract，不触碰 live artifacts，不替源码 lane 写完成结果；计划入口记录在 [Plan Completion Ledger](./references/verification/plan_completion_ledger.md) 的 `2026-05-07-boundary-governance-advisory-entry`。
22. 2026-05-07 全仓 `_parts` 自然边界治理已经按六条并行 lane 吸收到 `main`：progress/product、publication/delivery、runtime/control、runtime model/transport、display/MCP 和 boundary governance docs 都保留原 public facade，同时把高优先级大桶拆到自然子域。`study_progress_parts/projection.py`、`product_entry_parts/workspace_surfaces.py`、publication/delivery shared helpers、`runtime_watch_parts`、`gate_clearing_batch_parts`、`study_outer_loop_parts`、runtime decision/execution/status、`runtime_transport/med_deepscientist_parts`、display schema/shared/MCP server 都已完成内部边界收口；`workspace_surfaces.py` 的 `exec(compile(...))` 拼接加载已移除。最终 integration 只降低新边界文件中的复杂函数，不改 payload shape、CLI/MCP tool surface、controller callable surface 或 import facade；该轮 lane worktree/branch 已清理。后续若出现外部 active worktree，应按 owner 单独管理，不把它写成该轮治理未完成。完成性证据记录在 [Plan Completion Ledger](./references/verification/plan_completion_ledger.md) 的 `2026-05-07-parts-natural-boundary-governance`。
23. 2026-05-07 follow-up 又把 `product_entry_parts/shared_base.py` 的残余大桶继续拆成自然边界：boundary contract payload/validation/markdown 进入 `boundary_surfaces.py`，CLI command/profile/study selector helpers 进入 `command_surfaces.py`，human status narration 与 operator/recovery labels 进入 `human_status_view.py`。public import facade、product-entry payload shape、CLI/MCP surface 保持兼容；focused product-entry/truth tests 与 `scripts/verify.sh structure` 已验证，Sentrux quality `6142 -> 6157`、cycles `0 -> 0`。
24. 2026-05-07 模块化评估与 architecture fitness wave 已记录在 [MAS Modularity Assessment 2026-05-07](./references/mainline/mas_modularity_assessment_2026_05_07.md)。当前结论是：MAS 已达到继续推进真实 product/runtime 工作的结构门槛，Sentrux DSM `above_diagonal=0`、boundary fitness `0 blocking / 0 advisory`；但 `product_entry`、`study_progress`、runtime/control、MCP/display 仍是高 fan-in/fan-out 热点，后续按 portfolio queue 推进时必须带 architecture fitness budget。Runtime Control 和 Progress Projection 不再作为独立大重构主题反复打开；它们现在是所有后续 lane 的固定 contract。本轮只修改 repo-tracked source/test/docs，未写 live study workspace、`current_package`、`publication_eval/latest.json`、`controller_decisions/latest.json`、runtime SQLite 或 restore archive。
25. 2026-05-08 program portfolio closeout：`program_portfolio_consolidation.md` 仍是唯一总控入口。Git/runtime 子计划已经完成 current-project quest/root Git 退役并进入 guard/diagnostic 维护口径；`workspace_layout_de_mds_ds` 已落地到静态 profile template、doctor/show-profile 输出、bootstrap/quickstart/agent-runtime 文档和旧 MDS/DS reader 的显示语义；`profile_entry_compat_retirement` 已落地到 MCP doctor audit mode、profile JSON、workspace contract、product-entry/progress executor owner 和相关测试；`no_history_physical_absorb` 已落地到 provenance、author guard、parity harness、retained capability absorb、external dependency retirement 和 docs/ledger closeout。下一步不是再规划 Git 清理、全仓结构治理、可见 layout 清理、默认兼容入口清理或 MDS 默认依赖迁移；root Git 不再作为 MAS workspace 的可选日常维护模式，未来新发现的 root `.git` 只走显式 inventory/archive/remove/verify diagnostic。
26. 2026-05-08 Hub Role Hardening 已吸收到本地 `main`：runtime supervisor scan、product cockpit、MCP server 和 display validation 的中心 hub 现在分别按 canonical input/fact projection、read-model projection、tool adapter/rendering、validator registry 角色收口；`module_boundary_audit` 与 `architecture_owner_boundary` 增加 `authority` / `read_model` / `adapter` / `materializer` hard guard。外部 CLI/MCP/controller callable surface、JSON payload 字段语义和 display registry id 保持兼容；本轮没有写 live study workspace、`current_package`、`publication_eval/latest.json`、`controller_decisions/latest.json`、runtime SQLite 或 restore archive。
27. 2026-05-08 test lane governance 已把默认 smoke 收窄为 manifest-backed 最小入口，把 family-owned preflight tests 从 meta 中移出，取消 meta 对 architecture-owner boundary 的显式二次执行，并把 publication/submission materialization-heavy tests 从默认 regression 中拆到 submission lane。`contracts/test-lane-manifest.json` 现在记录 smoke、submission 和 focused `control-plane` 的 lane intent / overlap policy；测试治理参考见 [MAS Test Lane Governance 2026-05-08](./references/mainline/test_lane_governance_2026_05_08.md)。本轮只修改 repo-tracked test/config/docs/contract surface，没有写 live study workspace、`current_package`、`publication_eval/latest.json`、`controller_decisions/latest.json`、runtime SQLite 或 restore archive。
28. 2026-05-08 runtime evidence closeout 已完成 repo-level 实现与文档收口：outer supervision latency SLO、Portal/Live Console real workspace soak runner、paper autonomy stability evidence read model、focused lane manifest 和对应 docs/ledger 已对齐。完成口径是 `outer_supervision_slo=landed`、`portal_console_soak=landed`、`paper_autonomy_stability_evidence=evidence_read_model_landed`；真实 `paper_autonomy_stability=landed` 仍需无 blocker 的真实 evidence，不能从 functional monolith 或 Portal/Console 可视化自动继承。本轮不写 live study workspace、`current_package`、`publication_eval/latest.json`、`controller_decisions/latest.json`、runtime SQLite 或 restore archive。
29. 2026-05-09 paper progress degradation closeout 已把 monolith 收尾中的自动论文推进风险落到 `contracts/test-lane-manifest.json` 的 `focused_lanes.paper-progress-degradation-closeout`。该 lane 绑定 P0/P1 focused tests，禁止恢复 default MDS daemon、default MDS WebUI 或旧 workspace-local service，禁止 read model/UI 写 `current_package`、`publication_eval/latest.json`、`controller_decisions/latest.json`、runtime SQLite、restore archive 或任何 `quality_ready` / `publication_ready` / `submission_ready`。真实 workspace 若仍因 human gate、publication gate、parked handoff、profile unreadable 或 source evidence 缺失阻塞，必须作为 blocker 和下一 owner 暴露，不得被 repo-level closeout 伪装成论文已可自动完成。
30. 2026-05-10 Paper Progress SLO closeout 把三篇近期论文暴露出的共同失败模式收成同一条 contract：每个 paper work unit 必须具备 `owner`、`callable_surface`、`required_inputs`、`required_outputs`、`artifact_delta_predicate`、`gate_replay_target`、`idempotency_key` 和 `source_fingerprint` 语义；terminal success 需要 owner receipt + required output + artifact delta 或 gate replay result。DM002 的 `submission_minimal_refresh` / delivery sync / gate replay 链路必须忽略旧 MDS worktree 绝对路径作为 current source；DM003 的 `blocked_turn_closeout_waiting_for_owner` 若 `requires_user_input=false`，应由 MAS/controller 消费并进入 submission QC / figure legend work unit；Obesity 的 AI reviewer queued action 必须由 callable `ai_reviewer` owner 消费，publishability 未放行前 delivery 缺失只显示为 downstream blocker。两份 dirty patch 已归档到用户级非 repo patch 目录，并按 main/integration 现状判定为 superseded 或最小移植；本轮仍不写 live `current_package`、submission package、`publication_eval/latest.json`、`controller_decisions/latest.json`、runtime SQLite 或 restore archive。
31. 2026-05-10 Paper Progress Reconciler 稳定性重构已在 integration 中把上一条 contract 变成可执行 repo surface：`PaperProgressState` 固定七个公开状态 `progressing`、`awaiting_controller_redrive`、`blocked_controller_route`、`awaiting_callable_owner`、`awaiting_human`、`downstream_only`、`terminal_delivered`；`runtime-supervisor-reconcile` 现在携带 `paper_progress_reconcile`，按 current truth 重算 desired/current/delta/decision/action receipt；`paper_work_unit_outbox` 对同一 `idempotency_key` 重放等价 receipt，对同 key 不同 intent fail-closed，对同 `source_fingerprint` 避免重复启动 worker 但不阻断 handoff；runtime retry budget exhausted 在 route/owner/gate 可解释时转为 controller-owned redrive，而不是泛化为 external supervisor。line-budget / boundary gate 的 9 个 blocker 已通过自然边界拆分归零，Progress Portal workspace dashboard 已补用户面总览。该条是 repo capability landed；三篇论文的 controlled live apply、artifact delta、gate replay、owner 前进和 package freshness proof 仍需在最终 repo gates 通过后单独记录。
32. 2026-05-10 MAS AI-first paper autonomy closure 已把 reviewer finding、负结果路线、稿件修复、gate replay、AI reviewer recheck、OPL family queue projection 和 real-paper soak projection 串成 repo-level callable loop：`paper_repair_executor` 只写 canonical manuscript / evidence ledger / review ledger / revision log / controller receipt / request proof，不写 `current_package`；`canonical_manuscript_package_loop` 只产出 rebuild / freshness / delivery manifest proof，`current_package_write_authorized=false`；`route_decision_orchestrator` 对 adverse analysis slice 产出 executable owner task；MAS sidecar dispatch 消费 `paper_autonomy/repair-recheck` 与 `paper_autonomy/ai-reviewer-recheck`，并保留 OPL/Hermes 只做 queue/wakeup/dispatch 的 owner boundary；`real_paper_autonomy_soak_projection` 固定 DM002、DM003、Obesity 的只读验收口径。真实论文结论仍看 live surface，不把 repo test 通过或 queue 派发写成投稿级完成。
