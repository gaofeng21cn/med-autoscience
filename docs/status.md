# 当前状态

**更新时间：2026-05-22**

Owner: `MedAutoScience`
Purpose: `current_truth_summary`
State: `active_current_truth`
Machine boundary: 本文是人读 current-state 摘要。机器真相继续归 `agent/` pack、`contracts/`、CLI/MCP/API 行为、product-entry manifest、sidecar receipt、runtime/controller durable surfaces、真实 workspace artifact、owner receipt 和 generated artifact proof。

## 当前角色

`Med Auto Science` 是医学研究 domain agent，也是 OPL-compatible package。单一 MAS app skill 是 direct path 的稳定入口；经 OPL 托管时，OPL/Temporal 是默认启用的 hosted autonomous runtime，承载持久在线 stage-led runtime、attempt、queue、wakeup、retry/dead-letter、resume、human gate transport、generated surface、projection 和 App/workbench shell。Codex App 不是任务启动后的外围持续 driver。

MAS 持有医学研究 truth、stage semantics、AI reviewer / auditor quality gate、publication route、domain transition table、publication-route memory body/writeback decision、artifact/package authority、runtime-facing owner receipt/projection、typed blocker 和 safe action refs。OPL 不写 MAS study truth、memory body、publication verdict、artifact authority 或 `current_package`。

`Codex CLI` 是当前第一公民 executor。其他 executor adapter 只能显式接入，并只保证接入、生命周期、回执与审计边界，不承诺行为效果等价。

MDS / DeepScientist 当前只作为 source provenance、historical fixture、explicit archive import、backend audit、upstream learning 和 parity oracle reference，不是 MAS 默认 backend。

## 当前机器事实

- `agent/` 是 canonical medical research semantic pack：`prompts/`、`stages/`、`skills/`、`quality_gates/`、`knowledge/` 持有医学研究 stage / prompt / skill / quality / knowledge 语义。
- `contracts/pack_compiler_input.json`、`contracts/stage_control_plane.json`、`contracts/action_catalog.json`、`contracts/generated_surface_handoff.json`、`contracts/functional_privatization_audit.json` 和 `contracts/production_acceptance/mas-production-acceptance.json` 是 OPL handoff、generated surface、functional boundary 与 production acceptance 的主要机器面。
- Direct MAS app skill path 与 OPL-hosted path 共享同一 MAS-owned stage、controller、durable truth、quality verdict 和 artifact surface。
- OPL generated / hosted surfaces 可以生成或托管 CLI / MCP / Skill / product-entry / status / workbench descriptor，并 dispatch allowlisted MAS task；它们不能写 MAS study truth、publication-route memory body、AI reviewer verdict、publication verdict、artifact authority、source body 或 `current_package`。
- Workspace/file lifecycle 已按 repo-source 与 live/runtime 写集分层：开发 checkout 只承载 semantic pack、机器合同、authority-function descriptor/receipt refs、domain handler/native helper 和人读治理；真实 workspace state、runtime artifact、receipt instance、paper/package/export artifact 和临时 build/cache/venv/pycache/pytest cache/install sync 副产物必须进入受控 study workspace/runtime artifact root 或用户级 runtime state。
- `DEFAULT_MANAGED_RUNTIME_BACKEND_ID` 已切到 `opl_provider_backed_stage_runtime`；`runtime_backend_default_operation_contract`、product-entry manifest 与 sidecar export 声明默认 generic runtime owner 为 `one-person-lab`，默认 backend 为 `opl_provider_backed_stage_runtime`，delegated domain adapter 为 `mas_runtime_core`。`runtime_backend_default_operation_contract.default_autonomous_runtime`、`provider_topology.default_autonomous_runtime`、`managed_temporal_state_consistency.default_autonomous_runtime` 与 `runtime_transport_handoff_projection.default_caller_policy` 共同声明：hosted autonomous runtime 默认启用，provider 为 `temporal`，wakeup/retry/resume owner 为 OPL，`codex_app_outer_driver_required=false`，`mas_daemon_scheduler_attempt_loop_allowed=false`。`mas_runtime_core` 的角色是 `mas_domain_owner_receipt_adapter` / diagnostic adapter，不是 MAS-owned generic runtime platform。
- `functional_consumer_boundary` 已关闭未分类 generic owner 回流，并把 MAS 私有面限定为 declarative pack / generated surface handoff、refs-only adapter、minimal authority function 或 legacy tombstone/provenance gate。
- MAS stage control plane 已为 6 个 runtime-guard stage 声明 `trust_boundary.runtime_event_refs` 与 `stage_contract.runtime_event_refs`；stage cohort-loop refs 也已声明 source scope、cohort query、OPL queue trigger、monitor 和 dashboard metric refs。
- `contracts/production_acceptance/mas-production-acceptance.json` 只承认 structural / physical conformance 与 production-like receipt chain；它不授权 domain ready、publication ready、medical ready、artifact mutation 或 `current_package` 更新。
- 2026-05-21 physical cleanup follow-through 已删除 current product-entry / sidecar 上的旧 residue audit 程序面；当前旧 residue 只通过 `legacy_retirement_tombstone_proof`、`functional_consumer_boundary.retired_legacy_residue_tombstones` 和 `contracts/runtime/legacy-active-path-tombstones.json` 保留 tombstone/provenance refs，不再作为 current manifest audit surface 暴露。
- `paper_line_guarded_apply_evidence` 已作为 OPL-ingestable body-free ref packet 固化。它可暴露 progress delta、AI reviewer/gate receipt、artifact movement、human gate/resume 和 stable typed blocker refs；真实 closeout 仍需要 MAS owner receipt 或 stable typed blocker。
- 2026-05-21 `paper_line_guarded_apply_evidence.opl_stage_evidence_receipt_handoff.stage_owned_typed_blocker_handoff` 已为 6 个 MAS stage 提供 MAS-owned typed blocker refs，并已被 OPL `stage_production_evidence_receipt_record|verify` refs-only ledger 消费。当前 OPL App/readiness/production closeout 不再把 MAS stage expected receipt / monitor freshness 显示为 open；这不声明任何具体 paper-line、publication quality、artifact mutation、memory writeback、human gate 或 long-soak 已完成。
- 2026-05-21 已纠正 stopped controller work-unit redrive 的边界：MAS 不再把该问题作为私有 `progress_projection` redrive 仲裁扩写；MAS 只发布 controller authorization、domain route、owner receipt、typed blocker 与 OPL 可消费 owner-route refs。OPL runtime manager 负责 liveness、queue hydration、attempt retry、dead-letter、provider resume/relaunch 和 operator status projection；MAS focused tests 现在应锁定 refs-only handoff、不写 `.ds/user_message_queue.json`、不调用 generic runtime chat。
- 2026-05-21 owner-route handoff 的写面已继续收窄：`quest_waiting_opl_runtime_owner_route` 不再向 `.ds/runtime_state.json` 写 `wait_for_opl_runtime_owner` / `last_opl_runtime_owner_route_handoff`，也不清 `active_run_id` / `worker_running`；MAS 只写 `artifacts/supervision/owner_route_handoff/latest.json`，再由 `sidecar export` 暴露 `domain_route/reconcile-apply` pending task 给 OPL queue/hydration。
- 2026-05-22 `sidecar export` 的 `domain_route/reconcile-apply`、`paper_autonomy/guarded-apply`、`publication_aftercare/*` 与 `domain_owner/default-executor-dispatch` pending task 已补 `domain_dispatch_evidence_record_payload`。该 payload 复用 body-free evidence packet 约束，只包含 MAS-owned typed blocker refs、evidence refs、no-regression refs 和 OPL `agents evidence apply` 可记录的 receipt ref，用来让 OPL `domain_dispatch_evidence_receipt_record|verify` ledger 消费真实 owner-chain pending / stable blocker refs；default-executor payload 额外携带 dispatch request、prompt contract、owner receipt contract 与 owner-route currentness refs。它不生成 owner receipt、不声明 domain ready、publication ready、artifact mutation、writer closeout 或 `current_package` authority。当前 live OPL ledger 可用这些 refs 关闭对应 open workorder 为 `closed_by_domain_owned_typed_blocker`；真实 paper-line closeout 仍需要 MAS owner chain 返回 owner receipt、typed closeout、progress delta、gate replay、human gate、stop-loss 或 stable typed blocker 的 workspace ref。
- 2026-05-22 `mas_real_paper_line_provider_canary_closeout` 已把 owner receipt、stable typed blocker、progress delta、AI reviewer/gate、artifact movement、human gate/resume 和 no-forbidden-write refs 物化为标准 `body_free_evidence_packets`。该 packet 只复用既有 MAS owner-chain refs，禁止 artifact/memory/current-package body，供 OPL App / Agent Lab / evidence ledger 摄取；它不新增 owner receipt、不执行 artifact mutation、不声明具体 paper-line closure、publication-ready、domain-ready 或 production-ready。
- 2026-05-22 本轮真实 live ledger consumption 已选择一条 OPL `paper_autonomy/guarded-apply` workorder：MAS guarded-apply proof 在 DM-CVD workspace 返回 DM002 / DM003 的 3 条 owner receipt refs、Obesity 的 stable typed blocker `mas_owner_apply_receipt_missing:obesity` 和 no-forbidden-write ref；OPL `domain_dispatch:medautoscience:sat_d7e796a0dca5a1bc5b954474:record|verify` 已把这些 refs 记录并验证为 refs-only external evidence receipt。该 evidence 证明 MAS owner-chain canary payload 能被 OPL safe-action shell 消费；它不执行 workspace mutation、不更新 `current_package`、不授权 publication quality / artifact mutation / memory writeback，也不关闭多条 paper-line scaleout 或 long-soak。
- 2026-05-22 本轮继续把 `publication_aftercare/reviewer-refresh` owner-chain pending refs 推进到一次真实 OPL ledger consumption：MAS sidecar export 对 `001-lineage-pfs` 返回 stable typed blocker `mas-domain-dispatch-typed-blocker:medautoscience:publication_aftercare-reviewer-refresh:001-lineage-pfs:reviewer_refresh_owner_route_ref:owner-receipt-or-live-paper-line-closeout-pending`、7 条 reviewer refresh evidence refs 和 no-forbidden-write/no-regression ref；OPL `domain_dispatch:medautoscience:sat_9ae0f4daae22db1d42397e3b:record|verify` 已记录并验证为 `opl://external-evidence/medautoscience/domain_dispatch:medautoscience:sat_9ae0f4daae22db1d42397e3b`。post-check 中 family open worklist 81→80，closed refs-only 34→35，MAS reviewer-refresh workorder 8→7。该 evidence 只证明 aftercare typed blocker payload 可被 OPL safe-action shell 消费；不执行 reviewer refresh、不写 `publication_eval/latest.json`、不写 paper/package/current package、不授权 publication quality / artifact mutation / memory writeback，也不关闭 aftercare long-soak 或多 paper-line scaleout。
- 2026-05-22 本轮后续把 Obesity `paper_autonomy/guarded-apply` stable typed blocker 作为身份匹配的真实 payload 提交给 OPL refs-only ledger：目标 attempt `sat_67b0aa4ff4787aa8e35a7f3b` 绑定 `study_id=Obesity` 与 `source_fingerprint=5e889ab22ead5374`；MAS guarded-apply proof 返回 `mas_owner_apply_receipt_missing:obesity`、Obesity controller / publication eval / controller decision refs 和 `real_paper_autonomy_provider_hosted_guarded_apply_receipt/forbidden_write_guard` no-regression ref；OPL `domain_dispatch:medautoscience:sat_67b0aa4ff4787aa8e35a7f3b:record|verify` 已验证为 `opl://external-evidence/medautoscience/domain_dispatch:medautoscience:sat_67b0aa4ff4787aa8e35a7f3b`。post-check 中 family open worklist 85→84、closed refs-only 35→36。该 evidence 只证明 Obesity stable blocker payload 可被 OPL safe-action shell 消费；不执行 workspace mutation、不更新 `current_package`、不授权 publication quality / artifact mutation / memory writeback，也不关闭 owner-chain success、App user path 或 long-soak。
- 2026-05-22 本轮继续把 DM003 `paper_autonomy/guarded-apply` owner receipt refs 作为身份匹配 payload 提交给 OPL refs-only ledger：目标 attempt `sat_025e3a63d22d0b651800da60` 绑定 `study_id=DM003`、`source_fingerprint=0638984119cf89a0` 与 `profile_name=dm-cvd-mortality-risk`；MAS guarded-apply proof 返回 `003-dpcc-primary-care-phenotype-treatment-gap` 的 repair execution receipt、gate replay receipt、owner-chain ref、controller / publication eval / controller decision refs 和 `real_paper_autonomy_provider_hosted_guarded_apply_receipt/forbidden_write_guard` no-regression ref。OPL record dry-run 的 `identity_binding.status=matched` 后，`domain_dispatch:medautoscience:sat_025e3a63d22d0b651800da60:record|verify` 已验证为 `opl://external-evidence/medautoscience/domain_dispatch:medautoscience:sat_025e3a63d22d0b651800da60`；App drilldown 显示该 attempt `dispatch_evidence_receipt_status=verified` 且 `domain_ready_claimed=false`。fresh worklist 当前为 open 84、closed refs-only 37。该 evidence 只证明 DM003 owner receipt refs 可被 OPL safe-action shell 消费；不执行 workspace mutation、不写 `publication_eval/latest.json`、paper/package/`current_package`、memory body 或 artifact body，也不关闭 owner-chain scaleout、App user path、memory/artifact/lifecycle receipt 或 long-soak。
- 2026-05-22 本轮追加一条 DM-CVD `publication_aftercare/reviewer-refresh` stable typed blocker ledger consumption：MAS sidecar export 对 `001-dm-cvd-mortality-risk` 返回 stable typed blocker `mas-domain-dispatch-typed-blocker:medautoscience:publication_aftercare-reviewer-refresh:001-dm-cvd-mortality-risk:reviewer_refresh_owner_route_ref:owner-receipt-or-live-paper-line-closeout-pending`、18 条 reviewer refresh evidence refs 和 no-forbidden-write/no-regression ref；OPL record dry-run 的 `identity_binding.status=matched` 后，`domain_dispatch:medautoscience:sat_0bd9cfca5fb9c0c54a8dc93d:record|verify` 已验证为 `opl://external-evidence/medautoscience/domain_dispatch:medautoscience:sat_0bd9cfca5fb9c0c54a8dc93d`。post-check 中 family worklist open 92→91、closed refs-only 37→38；App full drilldown 显示该 attempt `dispatch_evidence_receipt_status=verified`、`domain_ready_claimed=false`。该 evidence 只证明 `001-dm-cvd-mortality-risk` aftercare typed blocker payload 可被 OPL safe-action shell 消费；不执行 reviewer refresh、不写 `publication_eval/latest.json`、paper/package/`current_package`、memory body 或 artifact body，也不授权 publication-ready、artifact mutation、memory writeback、domain-ready、production-ready、App user path 或 long-soak。
- 2026-05-22 本轮继续追加一条 `004-dpcc-longitudinal-care-inertia-intensification-gap` `publication_aftercare/reviewer-refresh` stable typed blocker ledger consumption：MAS sidecar export 返回 stable typed blocker `mas-domain-dispatch-typed-blocker:medautoscience:publication_aftercare-reviewer-refresh:004-dpcc-longitudinal-care-inertia-intensification-gap:reviewer_refresh_owner_rou:owner-receipt-or-live-paper-line-closeout-pending`、7 条 reviewer refresh evidence refs 和 no-forbidden-write/no-regression ref；OPL record dry-run 的 `identity_binding.status=matched` 后，`domain_dispatch:medautoscience:sat_6a77d2c24622cfdfbefb43f9:record|verify` 已验证为 `opl://external-evidence/medautoscience/domain_dispatch:medautoscience:sat_6a77d2c24622cfdfbefb43f9`。post-check 中 family worklist open 91→90、closed refs-only 38→39；App full drilldown 显示该 attempt `dispatch_evidence_receipt_status=verified`、`domain_ready_claimed=false`。该 evidence 只证明 `004` aftercare typed blocker payload 可被 OPL safe-action shell 消费；不执行 reviewer refresh、不写 `publication_eval/latest.json`、paper/package/`current_package`、memory body 或 artifact body，也不授权 publication-ready、artifact mutation、memory writeback、domain-ready、production-ready、App user path 或 long-soak。
- 2026-05-22 本轮追加 default-executor dispatch ledger scaleout：使用当前 repo 模块入口 `med_autoscience.cli sidecar export` 从 DM-CVD workspace 读取 `domain_owner/default-executor-dispatch` payload，并将 `002-dm-china-us-mortality-attribution`、`003-dpcc-primary-care-phenotype-treatment-gap`、`004-dpcc-longitudinal-care-inertia-intensification-gap` 各一条 MAS-owned typed blocker / no-forbidden-write / currentness refs payload 提交到 OPL `domain_dispatch_evidence_receipt_record|verify`。对应 attempt `sat_417d96e3ef1517fb5706925b`、`sat_e2bfddb10bf39fd95823132d`、`sat_868c0fd8a4e8cd1732db5d56` 均已 verified；003/004 的 dry-run identity preflight 为 `matched`。post-check 中 family worklist open 96→93、closed refs-only 39→42，MAS `domain_owner/default-executor-dispatch` workorder group 8→5，App drilldown 对三条 attempt 均显示 `evidence_status=typed_blocker_observed`、`domain_ready_claimed=false`。该 evidence 只证明 default-executor dispatch currentness / pending typed blocker refs 可被 OPL ledger 消费；不执行 writer 修复、不写 `publication_eval/latest.json`、paper/package/`current_package`、memory body 或 artifact body，也不授权 writer closeout、publication-ready、artifact mutation、memory writeback、domain-ready、production-ready、App user path 或 long-soak。
- 2026-05-22 本轮继续消费剩余 5 条 default-executor dispatch typed-blocker payload：`003-dpcc-primary-care-phenotype-treatment-gap` 的 `sat_794849270c351fe7ba27131f`，以及 `002-dm-china-us-mortality-attribution` 的 `sat_743ed7d96e4630f072dec2b4`、`sat_b946eabfda204e1201c54ede`、`sat_7f1bd71b148e9b2d574ed70a`、`sat_c0652752654d47bff59515d6` 均已完成 OPL record / verify。全部 dry-run preflight 均为 `selected_payload_path=typed_blocker_path`、`identity_binding.status=matched`；`sat_743ed7d96e4630f072dec2b4` 首轮 record 后未出现在 OPL external evidence ledger，已按当前 action route 重新 record / verify 并复查为 verified。post-check 中 family worklist open 93→88、closed refs-only 42→47，MAS `domain_owner/default-executor-dispatch` workorder group 清空，App drilldown 对 8 条 default-executor attempts 均显示 `evidence_status=typed_blocker_observed`、`domain_ready_claimed=false`、`provider_completion_is_domain_ready=false`。该 evidence 只证明 default-executor dispatch refs-only typed-blocker accounting 已消费到当前 open group 清零；不执行 writer direct-fix、不写 `publication_eval/latest.json`、paper/package/`current_package`、memory body 或 artifact body，也不授权 writer closeout、publication-ready、artifact mutation、memory writeback、domain-ready、production-ready、App user path 或 long-soak。
- 2026-05-22 本轮继续把真实 workspace sidecar payload 推进到 OPL refs-only ledger：NF-PitNET `publication_aftercare/reviewer-refresh` 的 `004-invasive-architecture` / `sat_335500750666aca9065d7ec0`、`003-endocrine-burden-followup` / `sat_5741ef6ba6d5fa0a61689078`、`001-lineage-pfs` / `sat_04c5728abd9b4d031135dc45` 均以 sidecar-provided typed blocker payload 完成 OPL record / verify；DM-CVD `domain_route/reconcile-apply` 的 `002-dm-china-us-mortality-attribution` / `sat_004aec01f2d7c15b2b2332fc` 与 `004-dpcc-longitudinal-care-inertia-intensification-gap` / `sat_7c96ddd33131ce6d373c81c1` 也完成 record / verify。5 条已写入 payload 的 dry-run identity preflight 均为 `matched`。DM003 的 `sat_5503afff51e6ad1a3fa640d8` 被 OPL identity guard 判定为 `domain_dispatch_evidence_receipt_conflict`，因为目标 expected fingerprint 是 `runtime_controller_redrive_required`，payload fingerprint 是 `quest_waiting_opl_runtime_owner_route`，因此未写 ledger；对应 `quest_waiting` attempt 已另有 verified refs-only receipt。post-check 中 family worklist open 88→83、closed refs-only 47→52，MAS reviewer-refresh workorder 5→2，MAS domain_route workorder 22→20。该 evidence 只证明更多真实 sidecar typed blocker / no-forbidden-write payload 可被 OPL identity guard 和 external evidence ledger 正确消费或 fail-closed；不执行 reviewer refresh、不触发 runtime redrive、不写 `.ds` runtime truth、`publication_eval/latest.json`、paper/package/`current_package`、memory body 或 artifact body，也不授权 writer closeout、publication-ready、domain-ready、production-ready、App user path 或 long-soak。
- 2026-05-21 Progress Portal actions 与 submission milestone parking runtime action 已从 MAS backend control 改为 OPL owner-route handoff：Portal `pause/resume/stop` 只写 `runtime_owner_route_request` receipt；submission milestone parking 只刷新 MAS parked controller decision 并写 runtime stop handoff，已 stopped 状态只记录 already-parked lifecycle。两者都不直接调用 `pause_quest` / `resume_quest` / `stop_quest`。
- 2026-05-21 controller refresh/current authorization 路径也已收窄为 OPL runtime-owner handoff：`refresh_controller_decisions_for_current_publication_eval` 不再调用 `ensure_study_runtime` / `pause_study_runtime`，不写 `.ds/runtime_state.json` 的 `last_controller_decision_authorization`，不改 pending user message continuation，也不强制 fresh turn。它只返回 `runtime_owner_handoff`、proposed runtime-state delta、current work-unit refs 和 authority boundary，由 OPL queue/provider attempt 消费。
- 2026-05-21 `study_progress` 已把 current `domain_transition` 作为只读 progress read-model 字段透出，并让 current domain route-back 本身优先于 submission metadata / package handoff / manual-finish parking / stale runtime-supervision projection。历史 `runtime_supervision/latest.json` 只有在当前 status 仍是 active/running 且没有 current domain transition 时才可补足 live display，不得覆盖 failed/null-active-run 的 current study truth。该项只修 MAS 人读进度面与 owner-route 可见性，不生成 generic runtime attempt，也不把 OPL liveness、queue、retry/dead-letter 或 provider resume 下沉回 MAS。
- 2026-05-21 `family_action_catalog` 新增 read-only `study_state_matrix` action，并同步到 product-entry shell、CLI/Skill descriptor 和 descriptor-only MCP projection。该 action 只物化 MAS-owned `study_state_matrix` / `domain_transition_table` / `family_transition_spec` / `family_transition_matrix_cases`，供 OPL generic `family-transition-runner` 消费；它不写 study truth、不执行 domain action、不授权 publication quality / submission readiness，也不会作为 public MCP runtime tool 暴露。
- 2026-05-21 hard-methodology callable routing 已收口：`unit_harmonized_validation_uncertainty_and_grouped_calibration` 这类 work unit 现在经 managed worker authorization 映射到 `analysis_harmonization_owner.unit_harmonized_external_validation_rerun_or_typed_blocker`，并由 `domain-owner-action-dispatch` 执行 MAS owner callable。该路径只产出 unit-harmonized rerun evidence 或 MAS-owned typed blocker，不落回 generic `ensure_study_runtime`、quality prose repair、OPL transition runner 或 MAS 私有 control-plane 扩张；它不写 paper、`publication_eval/latest.json`、`controller_decisions/latest.json`、submission package 或 submission readiness。
- 2026-05-21 已新增 [MAS Stage / Route / Handoff 标准](./runtime/stage_route_handoff_standard.md)：当前固定口径是 stage 为 OPL provider-backed attempt admission 单位，route 为 MAS domain transition / owner-chain recommendation，handoff 为 body-free refs-only 交接包。指定杂志后的格式整理应进入 `finalize_and_publication_handoff` stage 下的 `journal-resolution` / `finalize` route transition，由 OPL stage graph 调度 journal requirement、format delta、artifact authority、independent review 和 submission handoff 子节点；MAS 只保留目标 journal 解释、publication/artifact authority、owner receipt、typed blocker 和 human gate schema。
- 2026-05-22 本轮把 route/stage 残留边界的机器面收敛到当前 owner-route/read-model/receipt 名：`owner_route_reconcile`、`progress_projection`、`domain_health_diagnostic`、`domain_decision_authority`、`owner_receipt_lifecycle_ref_index` 与 `owner_route_dispatch_receipt`。`functional_privatization_audit.route_stage_residue_boundary.legacy_surface_names_current_active=false` 且 `all_residual_surfaces_physically_retired=true`，这里的 physical retirement scope 是旧 MAS 私有 runtime/route surface 名；仍有 active caller 的当前实体继续受 no-active-caller、OPL replacement parity、MAS receipt parity、focused tests、no-forbidden-write proof 和 tombstone refs 删除门约束。

## 当前功能/结构状态

当前已关闭的结构 gate 是：

| gate | 当前口径 |
| --- | --- |
| `generated_surface_active_caller_cutover` | OPL generated / hosted surface 与 MAS domain handler target 边界已闭合；MAS hand-written shell 只允许继续承担 direct domain entry、domain handler、AI-first validator、owner receipt signer、diagnostic cleanup 或 provenance fixture。 |
| `refs_only_adapter_thinning` | runtime lifecycle SQLite、paper outbox、storage maintenance、publication-route memory transport、artifact lifecycle audit、terminal attach、runtime transport 和 domain route shell 已收薄为 refs-only domain sidecar / locator / receipt / blocker / authority-ref / diagnostic surface。 |
| `legacy_cleanup_physical_retirement` | local LaunchAgent install path、workspace-local wrapper、旧 status/remove cleanup diagnostic、旧 alias/facade/test entry 已进入 physical retirement / tombstone / provenance 口径。`manager=local` direct call 必须 fail closed。 |
| `opl_app_workbench_drilldown` | MAS route/source/quality/artifact/memory/blocker/action refs 已作为 OPL App/workbench drilldown 输入；MAS 不复制通用工作台 owner。 |
| `lifecycle_locator_retention_restore_ledger_reconciliation` | lifecycle locator、retention、restore、cleanup ledger 与 workspace runtime artifact root locator 已按 OPL generic lifecycle shell / MAS artifact authority receipt 边界对账。 |
| `family_transition_materialization_handoff` | MAS 只暴露 read-only `study_state_matrix` materialization；OPL 负责消费 spec/cases 并执行 generic transition matrix runner。MAS 不持有 generic state-machine runner，也不把 matrix pass 写成 publication / submission ready。 |
| `hard_methodology_callable_routing` | HDL/unit harmonization 与 grouped calibration 这类 hard-methodology work unit 已路由到 `analysis_harmonization_owner` authority callable；MAS 只保留医学方法学 owner evidence / typed blocker，generic runner 与 App/workbench shell 仍归 OPL。 |

这些 gate 的关闭不等于真实 paper closure、publication-ready、artifact mutation authorization、provider long-soak 或 MAS runtime transport / SQLite refs index 物理删除。

## 当前物理源码形态差距

MAS 已完成 owner/contract/read-model 收薄，并完成 domain route / domain SLO projection active source 命名收口。剩余差距是 retained adapter / diagnostic 的物理删除门，不是 MAS generic runtime owner 复活，也不是已经清零：

- 私有实现 / OPL 迁移台账已落到 [MAS 私有实现与 OPL 迁移台账](./runtime/opl_private_implementation_migration_inventory.md)。该台账把 `domain_status_authority.py` 明确列为 `needs_split_before_migration` 的 control-plane thinning item：通用状态机、runner、queue、watch shell 和 status/projection 外壳长线迁 OPL；publication quality verdict、study truth、artifact mutation、owner receipt、source readiness 和 typed blocker 留 MAS。该台账是当前迁移治理索引，不表示迁移或物理删除已经完成。
- 2026-05-21 本轮已把 `domain_status_authority.py` 的 status finalization / refs-only projection attachment 拆入 `study_runtime_decision_parts/status_projection_shell.py`，并继续把明显 generic 的 truth/control-plane snapshot 与 progress projection 组装拆入 `status_projection_shell_parts/read_model_projection_assembly.py`。这只把 runtime summary、supervision refresh、runtime event、family companion、worker/parked projection 与 read-model attachment 从主决策文件中分离并进一步收薄；MAS decision authority、publication gate、manual finish dominance、controller authorization、runtime health semantics、artifact/source/owner boundary 均未迁出，也不能写成 OPL status/control plane 已接管。
- 2026-05-21 `runtime_transport/__init__.py` 的 package-level generic callable facade 已退役；默认 OPL provider 通过 explicit `runtime_backend` registry 验证，`runtime_transport` package 本身不再导出 `create_quest` / `resume_quest` / `schedule_turn` 这类 runtime callable。`runtime_transport/` 显式子模块、turn runner、worker lease 与 `lifecycle_refs_adapter.py` 仍有 active domain / diagnostic caller；当前角色只能是 domain receipt adapter、refs-only lifecycle refs index、guarded apply / typed blocker bridge 或 standalone diagnostic，不能写成已经物理删除。
- 2026-05-21 本轮继续把 `mas_runtime_core_turns.py` 的 JSON/state/queue/snapshot 壳拆入 `mas_runtime_core_turn_state.py`，主 turn lifecycle 文件从约 993 行降到约 944 行，并保留同名 public helper 委托。该 tranche 只关闭 turn lifecycle retained adapter 的文件边界收薄，不关闭 `runtime_transport/`、turn runner、worker lease、lifecycle refs SQLite index 的 active caller、OPL parity、真实 paper-line owner receipt parity 或 physical delete gate。
- 2026-05-21 本轮继续把 worker wrapper 的 Codex managed-home / config / virtualenv isolation helper 拆入 `mas_runtime_core_worker_codex_isolation.py`，`mas_runtime_core_worker_wrapper.py` 从 942 行降到约 729 行，只保留 wrapper command、Codex subprocess、PTY terminal bridge、lease update 和 turn normalization 壳。focused proof 覆盖 quest-local cache、isolated Codex HOME/auth/config、host Codex binary preservation、controlled PTY bridge 和 runtime transport behavior 不变。该 tranche 只关闭 `worker_lease_residency_projection` retained adapter 的文件边界收薄，不关闭 worker lease active caller、OPL worker residency parity、真实 paper-line receipt parity、no-active-caller 或 physical delete gate。
- 2026-05-21 本轮继续把 turn authorization retained adapter 收薄：`mas_runtime_core_turn_authorization.py` 的 runtime prompt / controller action contract / AI reviewer redrive contract / claimed-message filtering 已拆入 `mas_runtime_core_turn_prompt.py`，主 authorization 文件从约 950 行降到约 725 行，继续只持有 runtime-state sanitization、controller currentness、owner-handoff arbitration 和 authorization sync。focused proof 覆盖 runtime core turn、blocked closeout、pause/stopped relaunch、worker watchdog、worker cache/env、OPL provider default 与 runtime backend 行为不变。该 tranche 只关闭 turn authorization prompt shell 的文件边界收薄，不关闭 `runtime_transport/`、turn runner、worker lease、lifecycle refs SQLite index、真实 paper-line owner receipt parity、no-active-caller 或 physical delete gate。
- 2026-05-22 本轮继续把 `mas_runtime_core_turns.py` 的 user-message queue / claim / restore shell 拆入 `mas_runtime_core_turn_messages.py`，主 turn lifecycle 文件从约 944 行降到约 916 行，并保留同名 public helper 委托。`functional_privatization_audit.active_path_residue_cleanup_gates.runtime_turn_runner_closeout_adapter.current_paths` 已把新模块列入 retained turn adapter gate。该 tranche 只关闭 user-message delivery shell 的文件边界收薄，不关闭 `runtime_transport/`、turn runner、worker lease、lifecycle refs SQLite index、OPL queue/provider parity、真实 paper-line owner receipt parity、no-active-caller 或 physical delete gate。
- 2026-05-21 手写 MAS CLI 聚合器已做自然边界收薄：`src/med_autoscience/cli.py` 从约 1391 行降到约 941 行，stage-memory、workbench/portal、watch/supervision、workspace/data/bootstrap dispatch 迁入 `cli_parts/*_commands.py`，末尾不可达的重复 `study-runtime-status` 分支已删除。这只是 direct CLI adapter 体积收薄；OPL generated CLI / Skill / MCP / product-entry default caller 尚未因此完成。
- 2026-05-21 本轮已物理退役 MAS repo-local `runtime domain-health-diagnostic --loop` / `run_watch_loop` / `run_managed_supervisor_loop` 长循环 shell；`ops/medautoscience/bin/watch-runtime` 新生成与 legacy upgrade 均只调用 one-shot `runtime domain-health-diagnostic` domain diagnostic tick。OPL provider/runtime manager 继续持有 cadence、wakeup、retry/dead-letter、attempt 与 operator projection；MAS 只保留 runtime health、owner-route、typed blocker、safe-action refs 与 AI doctor repair receipt 解释边界。该 tranche 不删除 `runtime_transport/`、turn runner、worker lease、lifecycle refs SQLite index，也不声明真实 paper-line owner receipt parity 或 long-soak 完成。
- `product_entry_parts/workspace_cockpit/`、product-entry manifest/status 与 runtime status projection 仍承担 direct MAS path、OPL handoff 输入或 diagnostic read model；generic sidecar provider lifecycle CLI 已退役，owner-route handoff active bridge 只剩 `owner_route_handoff` 的 export/dispatch owner-route。剩余 product/status/workbench shell 在 OPL generated product/status/workbench shell 成为 production/default caller 前不能删除。
- 2026-05-22 本轮把 `owner_route_handoff_adapter` 的删除门从泛化 gate 细化为 `deletion_readiness_worklist`，并同步到 product-entry / sidecar functional consumer boundary、physical retirement matrix 和 OPL standard pack audit。当前机器面明确：该 adapter 仍有 `active_caller_count=1`，只能作为 `domain_owner_route_handoff_adapter` 保留；删除前必须同时具备 no-active-caller、OPL generated sidecar default caller parity、真实 paper-line owner receipt 或 stable typed blocker parity、focused sidecar tests、no-forbidden-write proof 与 history/tombstone refs。该 tranche 不删除 `owner_route_handoff`，也不声明 App/workbench closeout、publication-ready、artifact mutation authorization 或 physical delete 完成。
- 2026-05-22 本轮继续推进 `owner_route_handoff_adapter` 的物理源码收薄：`owner_route_handoff.py` 已收为 dispatch facade / public export 壳，sidecar export/read-model/pending-task 组装迁入 `owner_route_handoff_parts/export_projection.py`，dispatch 编排继续留在 `owner_route_handoff_parts/dispatch_orchestration.py`。`functional_privatization_audit` 和 physical retirement matrix 现在把这些路径列为同一个 retained sidecar gate 的当前路径，并记录 `sidecar_export_projection_split_to_parts_facade_retained` thinning evidence；该 evidence 只证明文件边界收薄，不关闭 active caller、OPL default caller parity、真实 paper-line owner receipt parity、no-forbidden-write 或 physical delete gate。
- 2026-05-22 本轮继续推进 study-progress projection / workbench shell 收薄：`study_progress_parts/projection.py` 已从约 1116 行降到 935 行，quality/module surface selection 拆入 `projection_quality_surfaces.py`，live runtime supervision absorption 与 supervision-health display shell 拆入 `projection_runtime_surfaces.py`。新 runtime helper 已纳入 control-plane structure canonical fact-resolver guard，新 helper 也纳入 preflight control-plane 分类；该 tranche 只降低 status/progress projection shell 耦合，不迁移 study truth、publication verdict、source readiness verdict、artifact/package mutation、owner receipt 或 typed blocker，也不声明 OPL workbench/status default caller 已完成。
- 2026-05-22 本轮继续推进 product/status/workbench projection shell 收薄：product-entry runtime inventory、task lifecycle、session continuity、progress projection、artifact inventory 和 skill runtime continuity 的 refs-only payload assembly 已移入 `product_entry_parts/generated_status_projection.py`；workspace attention queue 与 operator brief assembly 已移入 `product_entry_parts/attention_projection.py`。`program_surfaces.py` 继续保留 phase/entry/program 入口，`workspace_attention.py` 只保留兼容导出壳，`manifest_surfaces.py` 继续作为 product-entry manifest/status/preflight/start 入口。该 tranche 只关闭 product/status/workbench payload assembly 的文件边界收薄，不触碰 `publication_eval/latest.json`、`controller_decisions/latest.json`、paper/package/`current_package`、AI reviewer verdict 或 artifact mutation authority，也不声明 OPL generated product/status/workbench default caller 已完成；runtime/watch/transport 仍需 OPL provider/default caller parity、MAS receipt parity、focused tests、no-forbidden-write proof 和 tombstone/provenance refs。
- 2026-05-21 本轮继续把 `lifecycle_refs_adapter.py` 的 SQLite connection、schema、inspection、report-index 和 index-result 壳抽入 `lifecycle_refs_adapter_parts/sqlite_refs_index.py`，主文件保留 public API。focused proof 覆盖 runtime lifecycle store 与 OPL family persistence adapter 仍按原表结构、refs-only inspection 和 cleanup gate 输出工作。该 tranche 只关闭 lifecycle refs SQLite index 的代码形态收薄，不关闭 `lifecycle_refs_sqlite_index` 的 active caller、OPL lifecycle index parity、真实 paper-line owner receipt parity、no-active-caller 或 physical delete gate。
- 2026-05-22 本轮继续把 `lifecycle_refs_adapter.py` 的 watch state、runtime report、workspace storage audit、runtime event 和 archive ref record/index implementation 拆入 `lifecycle_refs_adapter_parts/runtime_record_indexes.py`。该 tranche 只把 generic lifecycle record shell 作为 OPL lifecycle index / App workbench lifecycle projection primitive 的迁移输入单独成域；不关闭 lifecycle refs SQLite index active caller、不声明 OPL lifecycle/default caller 已接管、不生成 owner receipt、不写 `publication_eval/latest.json`、`controller_decisions/latest.json`、paper/package/`current_package`，也不授权 publication-ready、source-ready、artifact mutation、memory writeback、domain-ready 或 production-ready。
- 2026-05-22 本轮继续把 `study_task_intake.py` 的 task-intake artifact path、latest read、markdown/runtime-context render、startup-brief block 和 JSON/Markdown write shell 拆入 `study_task_intake_surfaces.py`，主入口从约 997 行降到约 842 行，继续保留 task-intake domain semantics、reviewer revision / stop-loss / manual hold / submission closeout arbitration 和 public wrapper。该 tranche 只把 generic artifact surface / render / materialization shell 作为 OPL task-intake locator/read-model/App projection primitive 的迁移输入；不迁移 study task intake truth、AI reviewer / publication quality verdict、source readiness verdict、artifact/package authority、owner receipt、typed blocker 或 live message queue，也不声明 OPL task-intake/default caller 已接管。
- 2026-05-22 本轮继续把 `mainline_status.py` 的 Phase 2 user-product loop、Phase 3 clearance、Phase 4 backend deconstruction 和 platform target 组装拆入 `mainline_status_parts/program_surfaces.py`，主入口从约 1085 行降到约 661 行。该 tranche 只关闭 mainline status 私有 program/status projection assembly 的文件边界收薄；MAS owner truth、single-project boundary、capability owner boundary、publication/source/artifact/receipt/blocker authority 未迁出，也不能写成 OPL App/status/workbench default caller 已完成。
- 2026-05-22 本轮继续把 `stage_knowledge_plane.py` 的 paper-soak memory apply proof read-only projection shell 拆入 `stage_knowledge_plane_parts/paper_soak_memory_apply_proof.py`，主入口从约 1110 行降到约 878 行。该 tranche 只把 stage closeout refs、MAS router receipt refs、workspace writeback receipt refs、sidecar dispatch receipt refs、OPL/Aion read-only receipt projection 和 body-free input-ref assembly 作为 OPL generated stage-knowledge proof / App workbench primitive 的迁移输入；不迁移 publication-route memory body、memory accept/reject routing、typed closeout authority、publication quality verdict、source readiness verdict、artifact/package mutation、owner receipt 或 typed blocker，也不声明 OPL stage-knowledge/default caller 已完成。
- 2026-05-22 本轮继续把 `gate_clearing_batch.py` 的 closed-batch read-model shell 拆入 `gate_clearing_batch_parts/closed_batch.py`，主入口从约 1004 行降到约 943 行。该 tranche 只把 closed-batch replay report payload、current package freshness proof reuse、publication work-unit lifecycle normalization、duplicate-eval/current-gate check 和 authority-sync settled projection 作为 OPL generated controller/batch read-model、lifecycle wrapper 与 App/workbench display primitive 的迁移输入；不迁移 publication gate replay authority、repair-unit selection semantics、artifact/package write-route guard、current package authority interpretation、publication verdict、source readiness verdict、artifact mutation authority、owner receipt 或 typed blocker，也不声明 OPL controller/batch/default caller 已完成。
- 2026-05-21 本 worktree 已对 product-entry / workspace cockpit / workbench projection shell 做一处最小 alias 清理：旧 `needs_physician_decision` / `legacy_*physician*` / `study_physician_decision_gate` 不再作为 product/workbench human-gate 输出或 fallback；`needs_user_decision` 是该层唯一 human-gate 字段。该项只减少 wrapper/facade residue，不声明 status/progress runtime truth 物理删除。
- 当前 active cleanup gate 需显式审计三项 retained current residue：`workbench_shell_domain_projection_refs` 是 OPL workbench 的 domain projection refs，`owner_route_dispatch_receipt` 是 MAS owner-route dispatch receipt current surface，`progress_projection` 是 MAS domain truth status projection current surface。三项都仍有 active caller，`physical_delete_permitted=false`，只能在 active caller 清零、OPL replacement parity、MAS receipt parity、focused tests、no-forbidden-write proof 和 tombstone/provenance refs 同时成立后删除或 tombstone；旧 surface id 只能作为 retired legacy id 出现在 provenance 或 delete-gate 映射中。
- developer repair / worktree / verification 元数据不得长期留在 MAS domain handler；目标是迁入 OPL Agent Lab / developer repair lane 或 explicit contract refs。

关闭门槛固定为：active caller proof、OPL generated/provider parity、MAS domain receipt parity、focused tests、no-forbidden-write proof 和 provenance/tombstone refs 同时成立。满足后直接物理删除、archive 或 tombstone，不保留 compatibility alias、wrapper、facade 或兼容聚合测试。

## 当前测试/证据差距

以下是后续真实 paper-line / workspace scaleout 验证范围，不能替代真实 paper closure、publication-ready 或 artifact mutation authorization，也不再作为结构标准化缺口计数：

- 真实 paper-line provider apply：OPL provider -> MAS sidecar -> MAS owner chain 在真实论文线上留下 attempt query、typed closeout、MAS owner receipt，并通过 Lane 4A ref packet 暴露 progress delta、AI reviewer/gate receipt、artifact movement、human gate/resume、stop-loss 或 stable typed blocker。
- owner-chain body-free packet scaleout：当前 canary closeout 与 provider-hosted guarded-apply dispatch receipt 已能输出标准 `body_free_evidence_packets`，但这只是 refs 物化和摄取能力；仍需真实 paper-line canary 持续产出 owner receipt、stable blocker、artifact/memory/human-gate receipt 与 no-forbidden-write proof。
- domain dispatch owner-route / aftercare ledger scaleout：当前已用 NF-PitNET 与 DM-CVD 真实 workspace sidecar payload 额外关闭 5 条 refs-only ledger workorder，并用一条 DM003 payload mismatch 证明 OPL identity guard 能 fail-closed 阻断错误回填；这仍只是 payload 消费 / route-back hygiene，不替代 reviewer refresh 执行、runtime redrive 执行、writer direct-fix、owner receipt success、artifact/memory/human-gate receipt 或 long-soak。
- publication-route memory receipt scaleout：更多真实 paper line 产生 accepted/rejected/blocked writeback receipts，并可被后续 stage 按 refs 检索。
- artifact lifecycle receipt scaleout：真实 workspace 产生 artifact mutation permission、cleanup/restore/retention guarded receipt 和 rebuild/freshness proof。
- human gate / resume：approval、pause、human takeover、explicit wakeup 和 resume 操作链进入 MAS owner route，并证明不会越过 publication gate、AI reviewer gate 或 artifact authority。
- provider SLO long soak：长时 provider-hosted run、restart/re-query、retry/dead-letter、no-forbidden-write 和 App/workbench drilldown 在真实 domain activity 中持续成立。
- 状态转移 focused proof：stopped / waiting / paused / live 等 runtime 状态与 controller authorization、domain transition、submission metadata handoff 的组合路径需要继续用 focused tests 锁定。2026-05-21 已补 `stopped controller_work_unit_pending` 不被 metadata parking 覆盖的回归测试；后续真实 paper-line canary 仍需证明对应 redrive 能产出 owner receipt、progress delta、gate replay、human gate、stop-loss 或 stable typed blocker。
- family transition materialization proof：`study_state_matrix` action 与 OPL generic matrix runner 已有 focused proof；后续真实 paper-line canary 仍需证明 matrix route/work-unit 能进入 MAS owner chain，并产出 owner receipt、typed blocker、progress delta、gate replay、human gate 或 stop-loss。
- stage graph / route-as-transition proof：OPL 已有承载 route-as-transition 的 stage graph / transition runner / provider attempt 基础面；MAS 仍需用真实 paper-line 与指定 journal 格式整理 canary 回填 owner receipt、artifact authority receipt、independent reviewer/auditor record、human gate 或 stable typed blocker。该 proof 是 production evidence tail，不回写成 MAS 私有 runtime 或 publication-ready 结论。

## 当前完善顺序

1. 并行推进 Codex pack canonicalization、OPL-generated default caller cutover 和 runtime_transport / SQLite physical retirement gate 盘点；只关闭具备 no-active-caller、replacement parity、domain receipt parity 和 no-forbidden-write proof 的删除项。
2. 同步推进 workbench / sidecar / status retirement：OPL generated product/status/workbench shell 成为 production/default caller 后，MAS 只保留 direct skill target、domain handler、receipt signer、typed blocker 和 authority refs。
3. 跑真实 paper-line canary，验证 OPL provider attempt -> MAS sidecar -> MAS owner chain 能产出 owner receipt、progress delta、gate replay、route decision、human gate、stop-loss 或 stable typed blocker。
4. canary 之后扩展 publication-route memory、artifact lifecycle、human gate/resume 和 provider SLO long-soak evidence；这些属于测试/证据 tail，不回写成结构 closure，也不替 publication gate、AI reviewer 或 artifact authority 宣布 ready。
5. 每个物理删除动作都必须带 no-active-caller、replacement parity、domain receipt parity、focused tests、no-forbidden-write proof 和 provenance/tombstone refs；满足即删除、archive 或 tombstone，不新增兼容 alias。

## 当前不能声明

- 不能声明 OPL provider proof 等于 MAS paper closure、publication-ready 或 artifact mutation authorization。
- 不能声明 MAS production acceptance receipt 等于具体论文线 publication-ready、medical-ready、artifact mutation authorization 或 `current_package` 更新。
- 不能把 descriptor ready、conformance passed、classification closed、legacy cleanup ledger ready 或 selected proof 写成 production ready、paper closure 或物理源码清零。
- 不能把 `runtime_transport/`、domain route、turn runner、worker lease 或 `lifecycle_refs_adapter.py` 的存在写成 MAS 仍拥有 generic runtime。current active boundary 只允许使用当前 owner-route/read-model/receipt/ref-index surface；旧 MAS 私有 runtime/route surface id 只能作为 retired legacy id 留在 provenance 或 delete-gate 映射中。仍有 caller 的当前实体继续受删除门约束。
- 不能把 OPL `stage_production_evidence_receipt_record|verify` 写成 MAS production ready；它只是 expected receipt / monitor freshness 的 refs-only record/verify route。
- 不能把 MDS/DeepScientist、Hermes、local scheduler 或旧 workspace wrapper 写成 MAS 默认 active runtime owner。
- 不能把 Codex App 外围会话、MAS daemon/scheduler、MAS attempt loop 或 `mas_runtime_core` 写成任务启动后的默认持久在线调度 owner；默认托管自治运行 owner 是 OPL/Temporal。
- 不能把 dated specs、dated closeout、follow-through 记录或历史 full record 当成 current truth。

## 下一跳

- 目标态：[MAS 理想目标态](./references/positioning/mas_ideal_state.md)
- 差距与顺序：[MAS 理想目标态差距与完善计划](./active/mas-ideal-state-gap-plan.md)
- 当前执行地图：[MAS 当前开发线路](./active/current-development-lines.md)
- 文档治理：[MAS 文档组合治理](./docs_portfolio_consolidation.md)
- 过程归档：[MAS standard agent 文档过程归档 2026-05](./history/program/mas-standard-agent-doc-process-history-2026-05.md)
