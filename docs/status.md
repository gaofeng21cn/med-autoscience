# 当前状态

Owner: `MedAutoScience`
Purpose: `current_truth_summary`
State: `active_current_truth`
Machine boundary: 本文是人读 current-state 摘要。机器真相继续归 `agent/` pack、`contracts/`、CLI/MCP/API 行为、product-entry manifest、domain-handler receipt、runtime/controller durable surfaces、真实 workspace artifact、owner receipt 和 generated artifact proof。具体 study 当前状态必须 fresh 读取 live `study_progress`、workspace artifacts、controller decisions、publication eval、OPL current-control 和 owner receipts。

文档更新时间：`2026-06-15`

当前状态 anchors：

| Theme | Current owner / provenance |
| --- | --- |
| Active gap / completion plan | [MAS 理想目标态差距与完善计划](./active/mas-ideal-state-gap-plan.md) |
| Live study truth | Fresh `study_progress`、workspace artifacts、controller decisions、publication eval、OPL current-control 和 owner receipts |
| Runtime / currentness / stage-route repair | [Progress-First Stage Outcome Runbook](./runtime/control/progress_first_stage_outcome.md)、[PaperRecovery 事故 replay 与 authority 边界](./runtime/control/paper_recovery_replay.md)、[Stage-route reconcile 目标设计](./runtime/designs/stage_route_reconcile_target.md)、[Paper Autonomy Supervisor 目标设计](./runtime/designs/paper_autonomy_supervisor_target.md)、`contracts/paper_autonomy_supervisor_contract.json`、`contracts/paper_recovery_kernel_contract.json`、`contracts/stage_route_reconcile_contract.json` |
| Stage folder / storage / authority refs provenance | [Domain Authority Refs Index Guard](./runtime/domain_authority_refs_index_guard.md) |
| Paper-line pause and dated closeouts | [history/program](./history/program/README.md) |
| Docs lifecycle | [MAS 文档组合治理](./docs_portfolio_consolidation.md) 与 [coverage ledger](./history/docs-portfolio-coverage-ledger/README.md) |

Plugin native profile pointer: `contracts/opl-native-profile.json` 只声明 OPL Flow / OPL Doc 插件同步与 drift 检查所需的 repo-native profile；它不是 MAS medical truth、runtime truth、publication verdict、artifact authority、owner receipt 或 production-ready 证据。

Stage-route / currentness top-level governance：当前人读口径已收口为 Paper Autonomy Supervisor durable supervisor transaction。2026-06-15 owner-receipt / successor-action 修复族归类为同一类 authority-path split 修复 provenance：它证明局部 read-model / provider admission / old dispatch / export path 分裂会导致“下一步”漂移，但不把该修复族本身写成 live paper progress。目标态是从同一 `paper_autonomy_obligation` / `current_owner_delta` identity 进入 `execute_current_owner_delta`、`consume_terminal_closeout`、`materialize_recovery_action`、`wait_for_owner_with_resume_token` 或 `stop_with_stable_typed_blocker` 的闭合 transaction。read-model、queue、old dispatch、transport status、provider completion、worker heartbeat、active run、trace/span、stage artifact index 和 `provider_admission_pending_count=0` / `action_queue=[]` 只作为 diagnostic / audit / transport evidence；它们不能声明 DM002/DM003 paper progress、quality verdict、publication-ready、owner receipt closure、domain-ready 或 current package freshness。

DM002/DM003 representative live owner closeout：2026-06-15T15:08:10Z fresh DHD apply 已对两篇代表性 paper line 产出 exactly-one obligation actuator outcome，并且两个 outcome 均 `postcondition_ok=true`。DM002 当前 outcome 是 `typed_blocker_ref`，指向 Yang workspace 中 `studies/002-dm-china-us-mortality-attribution/artifacts/supervision/consumer/default_executor_execution/sat_67e10efde628859185249aa0.closeout.json#typed_blocker`，post-apply `study_progress` 在 2026-06-15T15:09:08Z 仍读为 `stop_with_stable_typed_blocker` / `anti_loop_budget_exhausted`。DM003 当前 outcome 是 `owner_receipt_ref`，指向 `studies/003-dpcc-primary-care-phenotype-treatment-gap/artifacts/controller/gate_clearing_batch/latest.json`，post-apply `study_progress` 在 2026-06-15T15:09:08Z 仍读为 `stop_with_owner_receipt`。这关闭 `contracts/standard_agent_completion_evidence_status.json` 的 representative live owner evidence gate；但这不是论文完成、publication-ready、submission-ready、current package fresh、domain-ready 或 provider running proof。历史 ready dispatch / request residue 已通过受控 migration surface 完成物理 cleanup：2026-06-15T15:57:03Z `default-executor-dispatch-residue-cleanup --apply` 归档 DM002/DM003 共 23 个 mutable dispatch slot，保留 396 个 immutable provenance JSON；2026-06-15T16:09:23Z fresh dry-run 读回 `status=clean`、`cleanup_candidate_count=0`、顶层 mutable dispatch JSON 数量为 0。该 cleanup 只关闭 stale mutable carrier 物理 residue，不能覆盖当前 owner receipt 或 stable typed blocker，也不能声明论文恢复。

Default-executor mutable dispatch residue cleanup：`medautosci default-executor-dispatch-residue-cleanup --profile <profile> --studies <study_id>... --dry-run|--apply` 是当前默认执行器旧 mutable dispatch carrier 的受控清理入口。该命令用 `study_progress` 的 canonical `current_work_unit` / `paper_recovery_state` 作为 currentness gate；存在 `current_executable_owner_action` 或缺 terminal / blocked owner outcome basis 时 fail closed，只在当前权威面已经收敛到 owner receipt、stable typed blocker、human gate 或 terminal stop-loss 后，才把 `artifacts/supervision/consumer/default_executor_dispatches/*.json` 移入 migration archive，并保留 `immutable/**/*.json` provenance。2026-06-15T15:57:03Z 对 DM002 / DM003 的 live apply 归档 23 个 mutable slot，写入两个 `artifacts/migration/default_executor_dispatch_residue_cleanup/latest.json` receipt；2026-06-15T16:09:23Z fresh dry-run 显示 `status=clean`、两个 study `currentness_gate.pass=true`、`cleanup_candidate_count=0`、顶层 mutable dispatch JSON 数量均为 0。该工具不写 paper truth、publication verdict、runtime truth、不启动 provider、不运行 DHD apply，也不声明论文恢复或投稿包 ready。

Progress-first / AI-first operator runbook: [Progress-First Stage Outcome Runbook](./runtime/control/progress_first_stage_outcome.md) 是当前 DM002/DM003 同类 receipt/currentness/read-model/preflight 空转的稳定人读入口；它固定非终局 stage outcome、paper/deliverable 与 platform repair 分账、AI-first admission、hard gates、live verification commands 和错误路径。该 runbook 不写 study truth，也不替代 live `study_progress`、OPL current-control、owner receipt、AI reviewer verdict 或 publication gate。

DM002/DM003 recovery operator landing：default executor dispatch 与 DHD report 现在必须把 provider admission candidate、developer-supervisor `execution_gate` 和 `running_provider_attempt` 分账；`provider_admission_pending_count>0` 但 `execution_gate.blocked=true` 时是 `pending_but_execution_gate_blocked`，不是 running、idle bug 或 hydrate/redrive 请求。terminal stage accounting 也允许从合同规定的 `paper_stage_log` / `user_stage_log` / `stage_log_summary` 读取 duration/token/cost fallback；这只关闭 observability，不计为 paper progress、quality verdict 或 publication readiness。

DHD acceleration docs / acceptance landing：`docs/runtime/control/controllers.md` 和 `docs/runtime/control/progress_first_stage_outcome.md` 现在把 DHD 慢路径根因固定为 clean runtime bootstrap、broad DHD scan 和 duplicate readbacks，并记录落地语义：checkout-external clean runner env/cache、`--scope full|currentness-only|owner-route|provider-admission`、`--apply` post-apply readback summary。`currentness-only` 是 read-only scope，不支持 `--apply`；scope 具体 CLI 行为必须继续由 fresh `domain-health-diagnostic --help`、schema 和 parser / focused-scope tests 对齐。该 landing 只服务 controller / operator path 加速与验收边界，不写 study truth、paper body、package、`publication_eval/latest.json`、`controller_decisions/latest.json`、owner receipt 或 typed blocker，也不声明 DM002/DM003 live paper progress。

Selected stage packet identity hardening：accepted closeout evidence 现在必须保留 closeout 原生 `dispatch_ref`、`stage_packet_ref` 和 `stage_packet_refs`。provider admission arbiter 只有在 action、work-unit、fingerprint 与 selected stage packet identity 都匹配时，才允许旧 closeout 消费当前 pending admission。2026-06-13 fresh dry-run 回归样本显示，DM003 旧 `sat_f8e1cfe49a3aa3cf95d0584d` closeout 绑定旧 mutable dispatch，而当前 admission 绑定 immutable `run_quality_repair_batch/77fa1796dc1d50c2b7687a9f.json`；修复后 DHD dry-run 应投影为 `provider_admission_pending_count=1` / `pending_provider_admission`，而不是 `accepted_closeout_consumed_pending`。该读面仍只是 admission truth，不启动 provider、不声明 paper progress 或 publication-ready。

Top-level admission readback hardening：`study_progress` 顶层 `owner_action_admission`、`provider_admission_pending_count` 和 `provider_admission_candidates` 必须与 `progress_first_monitoring_summary`、`paper_recovery_state` 和 DHD provider-admission arbiter 使用同一 current-work-unit-bound identity。若 canonical `current_work_unit.status=executable_owner_action` 且 `state.provider_admission_pending=true`、dispatch/currentness/fingerprint 能通过 DHD arbiter，顶层 readback 不得继续显示 admission null 或 0/[]；若 canonical `current_work_unit` 是 typed blocker、running、blocked、parked 或缺强 identity，仍按 arbiter fail closed。该规则只修默认读面一致性，不启动 provider、不消费 closeout、不写 Yang runtime/study artifacts，也不计为 paper progress。

Accepted repair progress / gate replay precedence hardening：同一 publication work unit 的 accepted repair progress 已给出 `paper_delta_observed=true` 和 `accepted_owner_receipt=true` 后，旧 `publication_eval.recommended_actions.readiness_blocker_repair` 不能重新生成同一 write repair admission。若 repair follow-up 已进入 gate replay 并 terminal 为 typed blocker / next forced delta / owner blocker，MAS 默认读根必须展示 gate replay terminal path 或 successor owner path；`blocked` / `typed_blocked` gate replay closeout 不能被同名 replay filter 吃掉。default-executor closeout consumption 也必须携带 `owner_route_currentness_basis` / `canonical_work_unit_identity`，只在 truth epoch、runtime health epoch、work-unit id 和 fingerprint 全部一致时允许 source eval refresh 差异。该规则用于防止 DM003 同类“已完成 repair 却回退旧 `medical_prose_write_repair`”空转；它不写论文、不声明 publication-ready、不启动 provider，也不把旧 provider completion 计为 paper progress。

Accepted typed closeout currentness hardening：当 OPL / default executor closeout 已给出同一 stage attempt、work-unit fingerprint 或 `action_type + work_unit_id` 匹配的 typed blocker 时，MAS `opl_current_control_state_handoff` 必须把该 closeout 投影为显式 `typed_blocker`，并消费同一 handoff action queue；`current_work_unit`、`current_execution_envelope` 和 `current_execution_evidence.action_queue` 不得再把旧 action 显示为下一步。`domain_closeout_provided_incomplete_user_stage_log`、`medical_prose_review_request_rehydrate_required`、`stage_packet_not_current_selected_dispatch` 和 `typed_closeout_packet_required` 这类 terminal closeout blocker 的 canonical owner 是 `one-person-lab`，embedded domain `next_owner` 只作为被阻塞 work unit provenance。`current_execution_evidence` 是 drilldown/evidence surface，不能绕过 consumed handoff filtering 来复活 stale executable queue。该规则用于防止 DM002/DM003 同类 “已接受 typed closeout 但仍回到旧 provider admission / heartbeat 监督” 的空转。它只修 currentness/read-model，不声明 paper progress、publication-ready、domain-ready、current package freshness 或 provider running proof。Fresh live 读法仍以 `study progress --format json`、`runtime domain-health-diagnostic --dry-run`、OPL current-control / queue / attempt 状态、owner receipt / typed blocker refs 为准。

Fresh progress identity enrichment hardening：fresh `current_executable_owner_action`、`current_work_unit`、accepted closeout owner-route basis 与 owner-route currentness basis 现在只能补强同一 PaperRecovery / current-owner obligation 的 identity，不再成为第二 authority。`allowed_actions` 可以提供 action type，但 dispatch selection 仍必须绑定同一 study、owner、action、work-unit 和共享 `work_unit_fingerprint` / `action_fingerprint`；任一侧存在 `source_eval_id` 时必须双方一致。`source_eval_id` 只用于 currentness disambiguation，不能替代 fingerprint，也不能把缺 fingerprint 的旧 dispatch 选成 fresh owner action。DHD report 会把 current-control envelopes / action queue 同步回 report runtime surfaces，并用 fresh progress path context rebuild report-level `paper_recovery_state`，避免 report 层丢失 recovery next-safe-action 后回到 queue-empty / idle 解释。该规则修的是 currentness / projection / dispatch selector，不写 study truth、owner receipt、human gate、paper body、publication eval、controller decision 或 OPL provider attempt；`status=ready` / `ready_default_executor_dispatch_count` 只表示 action/dispatch carrier readiness，不等于 paper-quality ready、publication-ready、provider running proof 或 owner receipt。

OPL execution authorization owner split：`opl_execution_authorization_required` 是 OPL runtime / execution binding blocker。Fresh `study progress` 必须把 canonical `current_work_unit.owner` 与 `current_execution_envelope.owner` 投影为 `one-person-lab`，并让 `paper_recovery_state.current_authority.owner=one-person-lab`；embedded typed blocker / PaperRecovery obligation 仍保留原始 domain owner（例如 `gate_clearing_batch`、`run_gate_clearing_batch`、`publication_gate_replay`）作为被阻塞 work unit provenance。该分账只修 operator 默认读根和恢复 owner，不写 Yang study/runtime artifacts、owner receipt、typed blocker、human gate、paper body、publication eval、controller decision 或 OPL provider attempt；后续推进仍需要同一 current identity 的 OPL provider attempt、lease、execution authorization decision、closeout receipt binding、human gate、owner receipt、stable typed blocker supersession、route-back 或 canonical changed surface。

Running attempt / terminal closeout currentness hardening：`current_work_unit` 和 `current_execution_envelope` 在投影 `running_provider_attempt` 时，必须优先使用当前 owner-route/currentness basis 的 work-unit id/fingerprint；OPL stage attempt id、workflow id、active run id 只能作为 provider proof refs，不能冒充论文当前 work unit。record-only / archive-materialized / executed terminal closeout status 必须被识别为 terminal，不能继续投影成 running。该规则用于防止 DM002/DM003 同类旧 closeout 或 stale attempt 让 operator 误以为论文还在推进；它只修 read-model/currentness，不写 study truth、paper、publication verdict、controller decision、package、owner receipt 或 typed blocker。

Running provider top-level projection hardening：当 canonical `current_work_unit.status=running_provider_attempt`、`current_execution_envelope.state_kind=running_provider_attempt`、`progress_first_monitoring_summary.running_provider_attempt=true` 或 strict OPL handoff liveness 证明同一 current identity 正在运行，且没有 matching terminal closeout 时，`study_progress` 顶层用户可见面必须覆盖旧 `runtime_preflight` / `auto_runtime_parked` / `parked_state`，投影为 `current_stage=managed_runtime_active`、`intervention_lane=runtime_running_watch`、`active_run_id=opl-stage-attempt://...`，并从 `operator_status_card` 移除 parked 字段。该规则只说明“现在应监督当前 provider attempt，不能重复 hydrate/tick/redrive 同一 work unit”；它不消费 terminal closeout，不声明 paper progress、publication-ready、owner receipt、typed blocker、current package freshness 或 domain-ready。attempt terminal completed 且有 closeout refs 后，仍必须由 `runtime domain-health-diagnostic --apply` 或等价 MAS authority consumer 消费 closeout，再由 fresh `study_progress` 给出下一 owner / typed blocker / changed surface。

Running provider arbiter precedence hardening：DHD provider-admission arbiter 必须把 strict live provider liveness、worker liveness 和 current execution slot 读成同一 `running_identity_observed` precedence。如果同一 study 已有 live provider attempt 持有当前 execution slot，即使旧 pending admission 的 action/work-unit identity 不同，旧 candidate 也应被压制为 stale pending，而不是继续以 `weak_provider_admission_identity`、queue-empty 或 next-step 缺失解释。该 precedence 只关闭 stale provider admission / stale handoff 复活路径；live attempt 仍只是 OPL runtime proof，不是 MAS owner receipt、paper progress、publication-ready、domain-ready、current package authority 或 provider completion claim。

Terminal closeout / successor handoff hardening：DHD materialized current-control 必须把同一 terminal attempt 的旧 running projection 清掉，并保留 different-identity successor handoff。具体表现是旧 `running_provider_attempt` / active attempt / workflow refs 不再作为 running proof，新 `current_executable_owner_action` 或 provider admission pending 继续按自身 work-unit identity 投影，同时携带 terminal precedence evidence 解释旧 running 被压制。OPL queue 侧同 identity running row 若已绑定 terminal attempt，只能修成 stale transport closeout observed / idempotent noop，清 lease，不 duplicate hydrate，不启动新 provider attempt。该规则吸收 controller observed state / durable terminal history / idempotent retry identity 的成熟工程口径，只关闭 stale-running / next-handoff 共存风险，不声明 paper progress 或 live closeout 已消费。

Same work-unit stop-loss currentness hardening：当 `progress_first_monitoring_summary.latest_terminal_stage` / `latest_terminal_stage_log` 已给出同一 `action_type + work_unit_id` 的 `repeat_suppressed`、typed blocker 或 stop-loss terminal closeout，`current_work_unit` 必须把它投影为 canonical `typed_blocker`，默认 blocker=`anti_loop_budget_exhausted`，并压住同一 provider admission / action queue / stale handoff。provider admission 只能在新的 work-unit identity、owner receipt、quality gate receipt、human gate、route-back evidence 或 MAS owner-authorized next owner 出现后重开；不得把同一 terminal closeout 当成下一轮可执行候选或 paper progress。

Readiness blocker-only dispatch hardening：`medical_paper_readiness_missing` / `current_execution_envelope.typed_blocker` 只能压住旧 transition、queue、handoff 和 persisted dispatch，不能自授权执行 `complete_medical_paper_readiness_surface`。readiness dispatch 只接受显式 `current_executable_owner_action`、带 authority binding 的 stage-native next action，或 `terminal_closeout_owner_answer_required` closeout dispatch；仅凭 source_ref / fingerprint 与旧 dispatch owner-route 匹配不得执行 owner callable。

MAS / OPL Agent OS 目标运行架构：[MAS / OPL Agent OS 目标运行架构与重构计划](./runtime/designs/mas_opl_agent_os_target_operating_architecture.md) 是当前顶层重构方向。目标态固定为 `OPL Agent OS + MAS Declarative Medical Research Pack + MAS Minimal Authority Kernel + Scientific Capability Registry`。OPL 持有 durable execution、StageRun、State Index、Route Reconciler、Lifecycle、Observability、Tool Arsenal 和 Workbench Shell；MAS 持有 study truth、source/data readiness、AI reviewer / auditor verdict、publication gate、artifact mutation authorization、publication-route memory decision、owner receipt 和 typed blocker；外部 Co-Scientist / Light / EvoScientist / PaperSpine / ARIS / ARK 等只进入 capability registry 或 refs-only advisory worker。External-learning 后续优化已折回 OPL family `W3-capability-registry-fail-open` / MAS `W4-domain-kernel-manifest` / `W7-production-evidence-soak`：不新增 MAS standalone selector、第二 active backlog 或 always-on advisory flow。该文档给出 Lane 0 文档/合同入口 + Pack Compiler、StageRun 上收、`current_owner_delta` 默认读面、Authority Kernel 收薄、evidence lineage、AI-first Quality OS、Agent Tool Arsenal / capability registry、workbench UX 和 production soak 实施 lane；它不声明 live paper-line 完成、publication-ready、artifact mutation authorization 或 production-ready。

Agent Tool Arsenal / Capability Invocation OS：`contracts/agent_tool_arsenal.json` 已把 MAS action catalog、MCP registry、owner callable registry、plugin skill、stage refs 和 Scientific Capability Registry 汇总成 agent-facing invocation ABI，并由 `src/med_autoscience/agent_tool_arsenal.py` 生成 `ToolArsenalIndex`、`AgentExecutionIndex`、`ToolUseCard` / `OperationalToolCard`、`CapabilityResolverReceipt`、`CapabilityInvocationPlan`、`ToolResultEnvelope` 和 `ToolAuditTrail` 的 source refs、authority boundary、risk annotations、allowed writes、forbidden authority、output schema、current-delta applicability 与操作恢复语义。合同现在定位为生成/校验材料；Agent ordinary path 不直接理解原始合同细节，而是消费 `agent_execution_index`、卡片内 `discovery_hint` / `fit_signal` / `invocation_gate` / `adaptation_policy` / `operational` 字段、`capability_resolver_receipt`、`primary_operational_card` 和 `mas_tool_result_envelope.recovery`。MCP 现在暴露只读 `agent_tool_arsenal` 工具，可让 autonomous agent 读取 index、单张 card、soft capability `resolve`、current-owner-delta hard invocation plan、result envelope schema、`completeness_diagnostic` 或 `hosted_consumption` evidence；`resolve` 模式固定三层语义：找工具阶段 soft match / high recall，不因缺 `required_refs` 丢候选；选工具阶段返回 explainable `fit_score`、`fit_policy`、`fit_reasons`、`missing_refs` 和 `next_safe_actions`；调用阶段由 `invocation_gate`、required refs、allowed writes、forbidden authority、owner receipt / typed blocker requirement fail closed。Display Pack 的 `adaptable_baseline_not_exact_contract` 已被提升为统一 adaptation policy precedent：可作为 quality-floor baseline 推进，但不能升级为 publication readiness、artifact authority、owner receipt 或 visual-audit replacement。实际 MCP `tools/call` / `call_tool()` 的 `structuredContent` 已统一包成 `mas_tool_result_envelope`，原结构化 payload 保留在 `structured_payload`，`content` 文本保持原工具输出，且 envelope 同时给出 `retryability`、`missing_refs`、`staleness`、`next_safe_actions`、`owner_needed`、receipt / blocker / diagnostic refs 和 `no_forbidden_authority_claim`，便于 Agent 失败后直接恢复而不解析 stderr。`ToolResultEnvelope` schema、`ToolUseCard`、`CapabilityInvocationPlan` 和 `completeness_diagnostic` 同时显式暴露可选 `executor_receipt_ref` 与 `lightweight_executor_receipt_contract_ref`，让 default executor / Tool Arsenal 可以引用 L0/L1 lightweight executor receipt evidence；该 ref 仍是 receipt-only，可缺失，不能阻断 current owner action，也不计为 owner receipt。`completeness_diagnostic` 已升级为 drift / parity doctor：证明 public runtime action cards 已映射到 MCP manifest，检查 output schema / input schema / annotations，并把 `doctor_audit`、`workspace_readiness`、`research_assets`、`publication_status`、`open_auto_research_soak` 和 `agent_tool_arsenal` 等非 current-owner-delta action cards 显式列为 support / diagnostic MCP tools。`scientific_capability_registry` 也已作为 public MCP runtime / CLI / product-entry action 落地，支持 `index / resolve / invoke`，把 Co-Scientist、Light、EvoScientist、PaperSpine、ARIS、ARK、AutoSci / OmegaWiki、PaperOrchestra 和 Display Pack 等能力收成 current-owner-delta-bound、fail-open、refs-only capability ABI；`build_capability_owner_consumption_evidence` 已提供 repo-side machine-readable owner-consumption / live-soak evidence shape，绑定 `current_owner_delta` identity、capability id、output refs、owner receipt / typed blocker / reviewer receipt / route-back refs、no-forbidden-write proof 和 fail-open flags。`contracts/hosted_ordinary_path_consumption.json`、`src/med_autoscience/hosted_ordinary_path_consumption.py` 与 MCP `agent_tool_arsenal(mode=hosted_consumption)` 已证明 OPL hosted ordinary path 可以消费 AgentExecutionIndex、OperationalToolCard、CapabilityInvocationPlan、ToolResultEnvelope recovery、Scientific Capability Registry resolution 和 owner-consumption evidence packet，且不要求人工手动选工具、读取 raw contract、默认新增 preflight、Docker / DinD 或等待 sidecar。人类用户仍只管理目标、授权和异常，不学习底层 CLI/MCP/helper 拼装。OPL 十大品牌模块 taxonomy 归 OPL 持有；MAS 只声明 domain intent、authority boundary、handler target 和 refs-only consumption，不新增 MAS 私有第 11 模块，也不复制 OPL brand module owner truth。该 landing 不声明 paper progress、domain-ready、production-ready、publication-ready、quality verdict、owner receipt 实例、artifact authority 或 current package freshness；剩余 tail 是 direct/hosted parity、current-owner-delta live owner-soak、真实 paper-line owner receipts / typed blockers / reviewer receipts / route-back evidence 和 production evidence。

Stage-route reconcile / arbiter surface：[Stage-route reconcile 目标设计](./runtime/designs/stage_route_reconcile_target.md) 和 `contracts/stage_route_reconcile_contract.json` 固定 MAS / OPL ordinary path 为 `current_owner_delta -> provider_admission_identity -> OPL StageRun attempt -> terminal closeout -> MAS closeout consumption -> next current_owner_delta`。currentness 优先级是 weak provider admission identity fail-closed、terminal closeout 压过同一 stage attempt 的 live 投影、strict live current identity 压过 pending、accepted typed closeout 消费同一 pending、fresh current owner action 投影 executable、stable typed blocker 投影 blocker，其余旧 route/queue/trace/sidecar 只进 audit。DHD materialized current-control 现在同步输出 `stage_route_arbiter` / `stage_route_arbiter_decisions[]`，把每个 provider admission identity 明确判为 `weak_provider_admission_identity`、`running_identity_observed`、`terminal_closeout_precedes_live_projection`、`accepted_closeout_consumed_pending` 或 `pending_provider_admission`，operator 不再从 action_queue 是否为空倒推原因。`terminal_closeout_precedes_live_projection.effect` 固定为 `suppress_provider_admission_pending`，另用 `stale_running_projection_effect=suppress_stale_running_projection` 表达 stale live 投影压制，避免消费者把 pending 抑制和 running 投影抑制读成两套 authority。provider admission 必须携带 `route_identity_key`、`attempt_idempotency_key`、显式 `stage_packet_ref` 或非空 `stage_packet_refs`，以及 strong `owner_route_currentness_basis`；`dispatch_ref` / `dispatch_path` 默认只证明 dispatch carrier，不能合成、兜底或替代 selected stage packet binding，`action_id` 也只是 action family，不能作为 dedupe/routing identity。唯一受限例外是 default-executor execution carrier 自身已是受 allowlist 约束且 owner-route 未 stale 的 StageRun identity enrichment source；该例外不得用于 queue residue、candidate root closeout backfill 或无 stage packet 的 OPL tick/hydrate。fresh current-owner-delta direct owner-action 可以作为 MAS owner callable / recovery action materialization 的强 identity，但不得满足 selected stage packet identity或 StageRun attempt identity。缺 stage packet binding 的普通 candidate 必须作为 `stage_packet_ref_or_refs` 缺失进入 `weak_provider_admission_identity`，`provider_admission_pending_count=0`，不得进入 OPL tick/hydrate/redrive。fingerprint-less stop-loss closeout 只有在 source-eval 相同，或无 source-eval 且共享 `truth_epoch` / `runtime_health_epoch` 全部一致时才能压 pending；generated_at-only 或任一 epoch 碰巧相等不能作为 consumption identity。record-only owner refs 现在只作为 closeout evidence：缺原生 fingerprint、route / attempt key 或 matching `owner_route_currentness_basis` / `source_eval_id` / truth+runtime epoch 时，`owner_receipt_ref`、`record_ref`、`publication_eval_record_ref` 不能消费 provider admission pending，只能留作 audit。2026-06-12 OPL 只读审计已把 terminal closeout precedence、`stage_run_currentness_identity`、schema-backed no-progress budget、`worker_source_stale` supervisor-safe restart 和 refs-only trace/span correlation 从“待 OPL 提升”收敛为“OPL covered, MAS consumes”；机器合同对应 `opl_covered_and_mas_consumes`、`desired_current_status_reconcile_policy` 和 `trace_span_correlation_policy`，并由 meta tests 验证 status/trace 不得反向生成 desired owner action、owner receipt、typed blocker、publication-ready 或 paper-progress credit。DM002 / DM003 recovery 另由合同内 Codex executor route policy 和 `#/dm002_dm003_recovery_acceptance_policy` 固定采信边界：Codex 前台或人工可以在用户明确选择 foreground/manual/fast-lane mode 时做 paper-local work product，但 MAS/OPL governed recovery 只采信 owner receipt、quality gate receipt、stable typed blocker、human gate、route-back evidence、同一 current identity strict provider running proof，或被 MAS/OPL 消费的 canonical changed surface ref；缺这些 refs 的 paper-local edit、旧 provider admission replay 或 docs-only claim 不能写成 MAS/OPL recovery。该 recovery policy 只保存验收规则和近期非权威样本，不能作为当前 blocker/owner/action truth；DM002 / DM003 运行态闭环必须按 `fresh study progress -> DHD dry-run/readback -> owner receipt / typed blocker / changed surface / provider running proof` 做 live verification。

PaperRecovery kernel：`contracts/paper_recovery_kernel_contract.json` 和 [PaperRecovery 事故 replay 与 authority 边界](./runtime/control/paper_recovery_replay.md) 是当前 paper recovery authority 入口。MAS 持有 `PaperRecovery` / `paper_recovery_state`、唯一 current `recovery_obligation_id`、phase、conditions、`next_safe_action`、owner receipt、quality gate receipt、stable typed blocker、human gate、route-back evidence 和 canonical changed surface adoption；OPL 只持有 StageRun execution、attempt ledger、queue/retry/dead-letter、provider liveness、terminal closeout transport 和 workbench shell。`study_progress`、DHD provider admission、operator status card、intervention lane、operator verdict、auto-runtime parked projection、recovery/autonomy contract、user-visible projection、OPL admission 与 human workbench card 都必须从 PaperRecovery 派生，不能从 queue、active run、transport status、operator card、parked runtime 或 docs-only claim 自造 recovery truth。合同硬性锁住 exactly-one current obligation、`pending + observe_only` fail closed、terminal closeout consume/reject、current-identity owner receipt `consume_owner_receipt` closure、stop-loss successor/human gate、projection inconsistency fail-closed、manual foreground adoption boundary 和 derived visible surface sanitization；runtime retry exhausted 时只允许分流为 `run_mas_owner_callable` 或 `authorize_opl_transport_recovery_or_stable_typed_blocker`。已存在同一 work-unit owner receipt、同一 repair receipt 派生的 gate replay / `gate_clearing_batch/latest.json` follow-up receipt，或 canonical `current_work_unit.status=owner_receipt_recorded` receipt 时，都必须收敛为 `owner_receipt_recorded` 而不是重复 callable，并由 contract meta test 校验 reducer 输出 action kind 全部进入 `next_safe_action_contract.allowed_values`。旧 `auto_runtime_parked` / `explicit_resume_pending` / `awaiting_explicit_wakeup` 残留不能覆盖当前 PaperRecovery phase。MAS 外智能体不迁移 MAS-specific `paper_recovery_state`：OPL 保持 generic stage-route/currentness/recovery-obligation substrate，MAG 保持 grant-native authority，RCA 保持 visual artifact recovery authority，OPL Meta Agent 保持 target work-order currentness 和 opaque owner refs。该 landing 不声明 live paper-line recovery、publication-ready、current package freshness 或 production-ready。

Standard OPL Agent completion acceptance：`contracts/standard_agent_completion_acceptance.json` 是当前“彻底落地”的机器验收定义，`contracts/standard_agent_completion_evidence_status.json` 是对应 evidence ledger。它把 MAS 历史残留清除和 OPL 标准化智能体复用防线拆成七个 gate：single default recovery/progress root、physical source morphology、active caller migration/no-resurrection、stage-route/stop-loss arbiter、negative false-completion tests、representative DM002/DM003 live owner evidence、family standard-agent feedback loop。ledger 对每个 gate 固定 current status、required evidence refs、observed refs、missing evidence tails 和 false-completion boundary；machine truth refs 不以 `docs/*.md` path 为真相源。当前状态是 `representative_live_owner_gate_satisfied_evidence_tail_open`：2026-06-15 fresh DHD apply / post-apply readback 已把 representative DM002/DM003 live owner evidence 更新为 `satisfied_with_live_owner_evidence`，但 family-level feedback loop、物理源码形态 scan、direct/hosted generated surface production consumption、真实 owner evidence scaleout 与 provider/operator soak 仍是开放证据尾项；owner receipt、typed blocker、human gate、route-back evidence 与 provider long-soak 不能由合同、文档或测试伪造完成。

Memory / artifact lifecycle owner follow-through：2026-06-14 fresh OPL `app-operator-drilldown` 显示 `memory_artifact_lifecycle_apply` 为 `refs_observed`，但 readiness 仍是 `typed_blocker_work_order_required_not_ready`，25 个 MAS `mas-artifact-lifecycle-typed-blocker:*canonical-regeneration-required-before-projection-removal:*` ref 需要 domain owner follow-through。MAS 侧已把这条 work order 落到 `contracts/functional_privatization_audit.json#/functional_followthrough_gap_summary/owner_followthrough_evidence/0` 与 `contracts/standard_agent_completion_evidence_status.json#/latest_owner_followthrough_evidence/0`，状态固定为 `typed_blocker_followthrough_recorded_not_ready`：它关闭的是“domain owner 已接收并记录 typed blocker follow-through”这条断链，不关闭 `artifact_lifecycle_receipt_scaleout`、memory/artifact/package/export readiness、physical delete authorization、domain-ready 或 production-ready。

Runtime supervision operator policy：`contracts/stage_route_reconcile_contract.json#/runtime_supervision_operator_policy` 是当前 operator/supervisor 命令边界合同。它把 current-control 四态拆成 desired / running / terminal / read_model，并固定 `domain-health-diagnostic --dry-run` 只观察运行真相且可能刷新诊断 evidence、`domain-health-diagnostic --apply` 只在 dry-run 已证明 terminal closeout 或 matching current-control identity 后消费 closeout / materialize current-control、`provider-slo tick` 只做 provider health / worker repair、`family-runtime tick --hydrate` 才可能消费已 materialized 的 MAS handoff。worker source stale restart 只允许在 Temporal reachable、attempt ledger readable、无 active attempt 时执行；有 active attempt、ledger 不可读或 Temporal 不可达时只能 fail closed 为 supervisor diagnostic。该合同用于防止 `provider-slo healthy`、worker heartbeat、queue empty、transport completed 或旧 active run 被误读为 provider admission consumed、paper progress 或 owner receipt。当前文档/合同 lane 不执行 DHD apply、hydrate、tick 或 redrive；具体 DM002 / DM003 当前 blocker、owner、action、work unit 和 provider proof 必须从 fresh live readback 读取。

Paper Autonomy Supervisor contract：`contracts/paper_autonomy_supervisor_contract.json` 是当前 MAS / OPL 自动论文推进监督协议的机器入口。它把每个 `current_owner_delta` 绑定到 `paper_autonomy_obligation`，并把监督结果限制为 `execute_current_owner_delta`、`consume_terminal_closeout`、`materialize_recovery_action`、`wait_for_owner_with_resume_token`、`stop_with_stable_typed_blocker`、`stop_with_owner_receipt` 六类 decision；`operator_decision_required`、`human_gate`、`typed_blocker`、`provider_admission_pending_count=0` 和 `action_queue=[]` 不能作为终点。该合同同时固定 paper semantic / platform repair / migration / observability 分账、OPL `StageRunIdentityPacket` / `HumanGateTransport` / `RecoveryObligationStore` / `SupervisorDecisionEngine` / `StateIndexKernel` / Workbench / Observability 基座职责，以及 MAS `PaperAutonomyAuthorityKernel` / `RecoveryMaterializer` / `PaperProgressLedger` / `AuthorityResultEnvelope` 职责。当前已落地的 repo 行为包括 OPL refs-only supervisor decision readback、DHD report `paper_recovery_state.supervisor_decision` recovery projection、identity-bound accepted owner-gate route-back provider-admission candidate、MAS `developer_apply_safe` 下的 accepted owner-gate quality repair materialization、same-work-unit owner receipt terminal readback，以及 DM002 / DM003 canary 对“每篇必须有非 idle `supervisor_decision`”的固定。`provider_admission_pending_count=0`、`action_queue=[]` 或 `action_class=observe_only` 只能作为某次 dry-run / fixture 形状和 forbidden interpretation 示例，不能作为稳定 live-shape 合同；当前 owner、action、work-unit、pending count、action queue、execution gate 与 running proof 必须以 fresh `study_progress`、DHD dry-run/readback、OPL current-control 和 owner receipt / typed blocker refs 为准。当前完成门已收紧为 fresh `supervisor_decision` readout、derived surfaces consume-only、OPL substrate readback、DM002/DM003 live-shape canary 和真实 paper-line tick evidence；该状态不声明 live DHD apply 已消费真实 study route-back、OPL provider 已执行恢复 action、DM002 / DM003 论文进展、publication-ready、provider running proof 或 OPL production readiness。

Paper Autonomy live supervisor canary：`contracts/paper_autonomy_live_supervisor_canary_contract.json` 与 `scripts/real-paper-autonomy-soak-inventory.py --mode live-supervisor-canary` 是当前 DM002/DM003 只读 live readback 入口。它对每篇 study 读取 `study_progress`，再读取 `domain-health-diagnostic --request-opl-stage-attempts --dry-run` 的 `progress_currentness`，要求 exactly-one `paper_autonomy_supervisor_decision`、decision 属于 PAS 闭合集合、decision identity 完整，且 study progress 与 DHD dry-run 的 obligation identity 一致；任何缺失或不一致都 fail closed 为 `stale_diagnostic` / identity split report。该 canary 的 read-only contract 显式禁止 DHD apply、hydrate、tick、redrive、provider attempt start，以及写 Yang study/runtime artifacts、owner receipt、typed blocker、human gate、paper body、`publication_eval/latest.json` 或 `controller_decisions/latest.json`。它只能证明当前读面是否有 identity-bound supervisor decision，不能证明论文恢复、publication-ready、provider running proof、terminal closeout 已消费、domain-ready、current package fresh 或 OPL production-ready；`queue_empty`、`dry_run`、旧 attempt completion、transport success、`provider_admission_pending_count=0`、read-model refresh 和 refs-only evidence 都列为 forbidden progress authority。

Paper Autonomy Supervisor successor dispatch / current work-unit landing：`paper_recovery_state.next_safe_action.successor_owner_action` 现在必须先进入 `study_progress.current_executable_owner_action`，再刷新 canonical `current_work_unit`、`current_execution_envelope`、provider-admission projection、DHD readback 和 `domain-handler export.pending_family_tasks`。`publication_gate_replay_blocked` 这类 canonical typed blocker 只有在同一 identity 的 PAS successor owner action 具备完整 action type、work-unit id、work-unit fingerprint，并且 `paper_recovery_successor.provider_admission_allowed=true` 时，才可被 supersede 成 executable owner action；否则继续作为 hard gate。已由 repair / gate replay 证据证明的 successor owner action 在 PaperRecovery 内优先于旧 `provider_admission_pending` residue、DHD `observe_only` 和 retry-exhausted admission 读面收敛，防止 verified successor 再被压回 `admission_blocked`。`execute_current_owner_delta` 的 pre-admission 硬门是完整 `paper_autonomy_obligation` identity 与同 identity provider admission identity；`stage_run_identity_packet` / strict running proof 是 post-admission evidence，不能被放在 admission 前形成循环依赖。这是 authority path 修复，不启动 provider、不执行 live DHD apply、不写 Yang study/runtime artifacts、不声明 DM002 / DM003 paper progress、publication-ready 或 provider running proof。

PAS / provider admission authority-path boundary：same-work-unit repair receipt、current-control admission identity、OPL authorization blocker、selected stage packet identity 和 domain action request materializer 必须共同服从 current-work-unit-bound authority path。current work-unit 优先投影 gate replay successor，而不是继续暴露旧 write repair action；provider admission current-control 不允许 retained queue action 用自身 identity 自证 admission；`opl_execution_authorization_required` blocked dispatch 必须按 action type、work-unit id 和 fingerprint 同时绑定 current identity；普通 provider admission 不得从 `dispatch_ref` / `dispatch_path` 合成 selected `stage_packet_ref`，缺 stage packet identity 继续进入 weak identity / fail closed；domain action request materializer 以 fresh `study_progress.paper_recovery_state.next_safe_action.successor_owner_action` 为准，压过 stale OPL scan state。该边界只修 MAS/OPL authority-path split，不写 study truth、paper body、`publication_eval/latest.json`、`controller_decisions/latest.json`、owner receipt、typed blocker、human gate 或 OPL provider attempt；具体机器真相归 source、focused tests、`docs/decisions.md` 和 fresh live readback。

Paper Autonomy Supervisor stable-blocker export landing：`domain-handler export` 消费 fresh `paper_recovery_state.supervisor_decision` 时，必须先通过同一 current execution hard gate。`stop_with_stable_typed_blocker` 或 owner-resolution handoff 只有在 supervisor obligation 与 canonical `current_work_unit` 的 study / quest / action / work-unit / fingerprint 完全一致，并且已成为 canonical executable owner action 或同 identity provider-admission carrier 时，才能作为 OPL-consumable owner handoff；否则只能作为 lineage、refs 或 diagnostic，不得作为普通 provider task 或 schedulable `pending_family_tasks` 导出。可接受返回仍固定为 domain owner receipt、quality gate receipt、typed blocker、human gate、route-back evidence 或既有 terminal / provider binding 族。这个落点关闭的是 read-model / owner-chain 断链；它不启动 provider、不执行 live DHD apply、不写 Yang study/runtime artifacts、不声明 DM002 / DM003 paper progress、publication-ready 或 provider running proof。

Stage-route currentness hardening：fresh `study_progress.current_owner_ticket` 缺 strong currentness identity 时，只能产生 `fresh_progress_current_owner_ticket_requires_strong_currentness_identity` diagnostic，`default_dispatch_allowed=false`，不能创建 default executor request；`study-progress-current-owner-ticket::*` synthetic id 只作 action id，不作 fingerprint。DHD scoped scan 对未扫描 study 的旧 handoff / action queue 只做 `unscanned_action_queue_retained_for_audit`，不得进入 active `action_queue`、不得增加 provider admission pending、不得触发 OPL tick。当前 action selector 的 readiness / stage-native authority 判断已集中到 `current_action_authority`，fresh progress current-owner ticket materialization 已集中到 `fresh_progress_current_action`，避免同一 currentness 规则在长选择器里重复漂移。fresh readiness barrier 只有在 canonical `current_work_unit` 仍投影为非 stale 的 `complete_medical_paper_readiness_surface` typed blocker 时，才可让带 authority binding 的 stage-native repair action 派生；若 `current_work_unit.state.stale_queue_or_handoff_can_override=true`、当前 work unit 已不是 readiness typed blocker，或只是旧 queued/handoff/envelope 残留，该 barrier 只能进入 ignored diagnostic / platform repair，不得重开同一 provider admission。Codex foreground session 的默认 governed role 是 live readback、repo docs/contracts/tests、补 MAS owner callable / derived repair action，或监督 OPL StageRun / provider attempt；显式 manual/fast-lane foreground work 可以处理 paper-local surface，但不能手写 study truth、publication eval 或 controller decision，也不能在未被 MAS/OPL 消费前声明系统恢复。DM002 / DM003 只读 live 回读里的 `active_run_id`、`running_provider_attempt`、`provider_admission_pending_count`、`provider_admission_candidates` 和 `action_queue` 必须与 canonical `current_work_unit`、execution gate、DHD readback 和 OPL current-control 一起读；0/[] 不能推出 idle/done，非空 pending 也不能推出 running 或 paper progress。后续 governed recovery 必须等 owner receipt、typed blocker 解除、human/operator gate、route-back、新 work-unit identity，或同一 current identity strict provider running proof，不能用 quest running、heartbeat、旧 attempt 或同一 action redrive 声称论文推进。

Progress-first owner action admission hardening：`owner_action_admission` 现在跟随 reducer 的 `current_executable_owner_action` 生成，而不是跟随顶层 `owner_action_visible` UI 规则生成。旧 `active_run_id` / handoff ref 被清成 non-running 时，admission 仍要显示 `admission_pending` 或 `provider_attempt_running_proven=false`；human gate、forbidden write 或 owner callable missing 时也要显示 hard-gate blocked，而不是返回 `null`。这条规则防止 DM003 类“旧 running 已清掉，但下一 owner admission 读面消失”的误判；它只修 projection，不启动 OPL、不消费 closeout、不声明 paper progress。

DM002 / DM003 live readback discipline：DM002 / DM003 的 current blocker、owner、action、work unit、active run、provider admission count 和 dispatch count 都必须从 fresh `study progress --format json`、`runtime domain-health-diagnostic --dry-run`、OPL current-control / queue / attempt 状态、owner receipt 与 typed blocker refs 读取。`docs/status.md` 不再冻结某日 live sample；2026-06-13 的 DM002 / DM003 样本只作为非权威 provenance 保留在 `docs/decisions.md` 与 [Progress-First Stage Outcome Runbook](./runtime/control/progress_first_stage_outcome.md)。稳定规则是：`active_run_id=null`、`provider_admission_pending_count=0`、`provider_admission_candidates=[]`、`action_queue=[]` 或 `codex_dispatch_count=0` 只能说明没有当前 provider admission / dispatch proof，不能推出 idle、done、paper recovery 完成或 publication-ready；同样，非空 pending 也不能推出 running 或 paper progress。若 fresh canonical current work unit 仍是 typed blocker，`progress_first_monitoring_summary.owner_action_admission` 可以保留 residual current owner action 诊断，但必须投影 `admission_requested=false` / `admission_pending=false`，并以 `current_work_unit_typed_blocker` 作为 hard-gate reason。后续 governed recovery 只能由 owner receipt、typed blocker 解除、human/operator gate、route-back、新 work-unit identity，或同一 current identity strict provider running proof 推进。

Provider admission currentness basis hardening：DHD / current-control materialization 会从同 identity 的 fresh `current_executable_owner_action`、canonical `current_work_unit.currentness_basis` 和 accepted closeout `owner_route_currentness_basis` 补齐 provider admission candidate 的 `truth_epoch`、`runtime_health_epoch`、`source_eval_id`、work-unit id 与 fingerprint。该规则防止当前 owner action 因 default-executor carrier 缺局部 basis 被降级成 weak identity；但补齐来源必须先通过 action/work-unit/fingerprint core identity 校验，且合成后的 basis 不能和 candidate 顶层 work-unit id / fingerprint 冲突。旧 queue label、action family、`action_id`、不同 current action 或 unscanned residue 仍不能自授权进入 OPL tick。它只修 provider admission identity/currentness，不执行 live apply，不写 study truth、paper、publication eval、controller decision、owner receipt、typed blocker 或 OPL runtime artifact。

Stage-log minimum viability / refs-only transport hardening：`contracts/stage_route_reconcile_contract.json#/stage_log_minimum_viability` 固定 terminal closeout 的 `paper_stage_log` / `user_stage_log` / `stage_log_summary` 最小字段集，至少说明 stage goal、实际工作、paper / deliverable / platform delta、duration、token / cost 状态、evidence refs 和 next forced delta。`latest_terminal_stage_log.stage_log_workbench_summary` 是当前 agent/operator workbench 的最小可用 read-model：只投影字段 present/missing、item count、refs、duration/token/cost observed-or-missing、delta 计数、next forced owner/action/target 和 missing domain fields；`body_policy=refs_only_body_free`，不复制 stage/paper 工作正文、paper body、artifact body、memory body、publication verdict body 或 transcript body。若 provider 明确给了用户可读 stage log 但缺 required domain fields，MAS closeout consumer 消费为 `domain_closeout_provided_incomplete_user_stage_log` typed blocker，投影 `missing_domain_fields` / `semantic_gap`：不给 paper-progress credit，不自动 redrive，不把 provider completion 写成 domain ready。旧 closeout 没有显式 stage log 时，MAS 只可从结构化 closeout 派生 fallback read-model log，并把 duration/token/cost 缺口写成 explicit missing；不能猜 token、费用或论文语义。OPL / Temporal activity completion payload 必须 refs-only：完整 closeout、stage log、transcript、paper body 和大数组留在文件/ledger，Temporal result 只保留 closeout refs、consumed refs、writeback refs、route impact 和 authority boundary。完整 closeout 可以继续作为 owner-result / story-surface / gate-replay evidence 被消费；该规则只约束 stage log read-model、closeout consumption 和 transport payload，不写 paper body、publication verdict、controller decision、current package、owner receipt 或 artifact authority，不新增 blocking preflight。该规则吸收 Kubernetes controller desired/current/status reconcile、Temporal 2MB/4MB payload limit、Airflow XCom small-metadata、AWS Step Functions idempotent execution name、OpenTelemetry links / OpenLineage refs 的成熟实践；官方来源见 [Kubernetes Controllers](https://kubernetes.io/docs/concepts/architecture/controller/)、[Temporal Activity return values](https://docs.temporal.io/develop/typescript/activities/basics)、[Airflow XComs](https://airflow.apache.org/docs/apache-airflow/stable/core-concepts/xcoms.html)、[AWS Step Functions StartExecution](https://docs.aws.amazon.com/step-functions/latest/apireference/API_StartExecution.html)、[OpenTelemetry traces](https://opentelemetry.io/docs/concepts/signals/traces/)。

Foundry Agent OS domain kernel manifest：`contracts/foundry-agent-os-domain-kernel-manifest.json` 是 W4 `domain-kernel-manifest` 的 MAS machine-readable 落点。它把 retained Medical Authority Kernel、OPL upcollect surfaces、`current_owner_delta` 默认读根、owner receipt / typed blocker / quality-publication signer 归属和 OPL/Vault/Console/Runway/Pack/Capability Registry false-authority flags 固定为可测试合同；它不声明 domain-ready、publication-ready、submission-ready、production-ready、App release-ready 或 physical delete authority。

Authority Kernel inventory：`contracts/authority_kernel_inventory.json` 与 `src/med_autoscience/authority_kernel_inventory.py::build_authority_kernel_inventory` 是 MAS retained Medical Authority Kernel 的机器可读 inventory/read-model。它列出 owner receipt signer、typed blocker materializer、source readiness、publication quality gate、artifact mutation authorization、publication-route memory accept/reject、no-forbidden-write proof、refs-only helper 和 diagnostic probe 的 representative surfaces，并固定 category、owner、active caller、allowed writes、forbidden authority、output refs、cannot-lift-to-OPL reason 与 retirement / upcollect target。第一批 physical thinning 已把 inventory generator 的 schema/dataclass、item builders 和 gap validation 下沉到 `src/med_autoscience/authority_kernel_inventory_parts/`，`authority_kernel_inventory.py` 只保留 thin read-model builder 与 public constants re-export；contract JSON 和 generated payload 不变。它仍只声明 `inventory_landed_physical_thinning_pending`，不声明 authority fully retired、physical thinning complete、production-ready、paper-line progress、publication-ready 或 artifact mutation authorized；后续旧 residue 删除、OPL upcollect 和真实 evidence scaleout 必须按该 inventory 继续验证。

Display Pack v2 landing status：[Display Pack v2 落地状态](./delivery/medical-display/contracts/display_pack_v2_landing_status.md) 是 medical display 当前完成度、目标态和 E2E 使用路径的人读入口。MAS 域内已落地的是 pack/template descriptor contract、paper-level figure-quality refs、article-level `paper/publication_style_profile.json` style-token lock、`paper/figure_spec.json` 单图 grammar、`paper/figure_specs.json` 批量 grammar、`paper/figure_polish_lifecycle.json` AI/VLM polish lifecycle、R/ggplot2-first subprocess runtime protocol、55 个 core evidence templates 的 `subprocess + render.R` 一等默认 R/ggplot2 入口、P2 Python plugin 兼容 renderer、template discovery/describe CLI、one-shot scaffold render、golden refresh/check lower-bound surface、multi-figure deterministic E2E render/QC/publication-manifest path、`paper/build/display_pack_lock.json#/publication_figure_quality_refs` 和 submission manifest refs preservation；style profile 字段覆盖 `style_profile_id`、`journal_palette_ref`、`palette`、`semantic_roles`、`typography`、`stroke` 和 `grid`，同一 sha256 进入 render request、R layout sidecar、每张 figure entry、display lock 和 publication manifest，用于整篇论文配色、字体、字号、线宽和网格一致性审计。OPL repo 已落地 `opl pack os install/registry/cache/distribute/lock/validate/mas-display-smoke` surfaces，可消费 `contracts/display-pack-contract.v2.json` 并输出 generic pack lock/audit smoke receipt；这些是 OPL-owned 外部 substrate refs，MAS 不拥有 generic Pack OS 或 publication authority。当前 core pack 的 renderer migration ledger 覆盖 84/84 evidence templates：P0 landed 22，P1 default R/ggplot2 promotion landed 33，P2 retained Python / later dual-stack 29。`display-pack-templates` / `display-pack-template` 是只读资产发现面；`display-pack-scaffold-render` 和 `display-pack-golden refresh/check` 会走正式 E2E runtime，默认 golden strict match 覆盖 PNG、layout sidecar、deterministic QC status 和 publication style profile hash，PDF bytes 作为 observed-only artifact hash 记录。`display-pack-render-candidate` 只保留为 P1 legacy comparison receipt，输出 request/stdout/stderr、PNG、PDF 和 layout sidecar，并固定 `candidate_only=true`、`comparison_only=true`、`publication_readiness_verdict=false`。AI/VLM visual audit 只作为 quality-loop / audit receipt evidence，不能修改 data/statistics/evidence marks，不能携带 publication verdict；external display projects 只能按 link-only exemplar、style/audit hint、template gap candidate、descriptor candidate、verified landed template/pack、OPL Pack OS handoff 的顺序吸收。该状态不声明 publication-ready、submission-ready、paper closure、artifact authority、domain-ready、production-ready、P2 Python templates 已全部切换默认 R renderer 或全 catalog 模板都有 checked-in golden。

External-learning adoption closure: [External Learning Adoption Closure Runbook](./runtime/control/external_learning_adoption_closure.md) 是当前所有外部自动研究框架 intake 的统一口径。Co-Scientist、Nature-skills、Academic Research Skills、AutoSci / OmegaWiki、EvoScientist / EvoSkills、ARK、ARIS、PaperSpine、PaperOrchestra、Open Auto Research 等材料只有 contract、reference intake 或 design doc 时，不能写成 learned / absorbed / landed；必须标注对应 landing status。只有进入 owner surface、generated/read-model projection、worker/sidecar execution slot、callable/action catalog、quality pack consumer、controller-authorized soak 或等价 repo-native surface，并有验证证明 allowed writes、forbidden authority 和 nonblocking / fail-open 边界，才算实际落地。当前 ARS、AutoSci / OmegaWiki、ARK、ARIS、PaperSpine 和 PaperOrchestra 已进入 single nonblocking `run_external_learning_sidecar` 的 refs-only advisory worker landing；`scientific_capability_registry` 已把该 worker、Light advisory、Evo sidecar、Co-Scientist affordance 和 Display Pack capability 暴露为 current-owner-delta-bound `index / resolve / invoke` ABI，并新增 refs-only owner-consumption evidence shape 记录 capability output refs 是否被 owner / reviewer / route-back refs 消费。它们不等于 owner receipt、study progress、publication quality、paper progress 或 external runtime landed。缺 external advisory 或 owner response refs 默认不阻断完整 `current_executable_owner_action`，除非命中 source/data/evidence、owner-route identity、forbidden write、不可逆 mutation、independent reviewer、publication gate、human gate 或 MAS hard gate。

Progress-first Co-Scientist affordance：`next-delta tournament`、`bounded micro-candidate generation`、`critique-as-repair-hint`、`reusable lesson extraction`、`triggered meta-review` 和 `opportunistic knowledge prefetch` 当前不是默认增强层，而是 current-owner-native JIT affordance。ordinary path 默认不运行额外探索、tournament、meta-review、prefetch 或 memory scan；当当前 owner action / owner route / route-back / typed blocker / reviewer gate 显式声明，或 current delta / gate 形态本身隐含需要 ref family、repair context、briefing 或 arbitration 时，系统即时调用对应 affordance。它们只帮助当前 owner 更快选下一步、reviewer / auditor 更快发现缺口、memory 复用失败路径；它们不写 study truth，不阻断 agent admission，不关闭 quality / publication / artifact / memory authority，也不把 platform repair、prefetch 或 review score 计为 paper progress。

EvoScientist / EvoSkills progress sidecar execution surface：[EvoScientist Progress-First Intake](./runtime/designs/evo_scientist_progress_first_intake.md) 当前已从完整目标态设计推进到 repo 可调用执行面，不再保留后续学习计划。EvoScientist v0.1.4 的 auxiliary background memory workers / tool selector / fire-and-forget observation memory 与 EvoSkills v1.0.0 的 IDE / IVE / ESE lifecycle memory，已一次性折回为 nonblocking current-owner-following sidecar architecture，并落地 `med_autoscience.runtime_protocol.evo_scientist_sidecar_refs.write_evo_scientist_sidecar_observation`、`medautosci evo-scientist-sidecar observe/read-latest`、`study_progress` materialize hook 和 refs-only index family `evo_scientist_sidecar_ref`。这些入口只写 `artifacts/runtime/evo_scientist_sidecar/` 下的 refs-only observation/latest ref；`tool_selector_helper`、`observation_memory_sidecar`、`failed_path_taxonomy`、`routing_eval` 和 `attempt_budget_stop_loss` 都只产生 refs-only hint、reviewer briefing、memory reuse candidate 或 no-loop signal；critical path 不等待 sidecar，缺失、失败、超时、预算耗尽或低置信不得阻断 current owner action，也不得替代 independent reviewer/auditor。

Light external-pattern / skill-content intake：[Light External Pattern Intake Runbook](./runtime/control/light_external_pattern_intake.md) 固定 Light 只作为 external pattern source：`verified_asset_ref`、`collision_check_ref`、`refusal_rehearsal_ref` 和 `fresh_evidence_gate_ref` 是基础 advisory refs；source search、data sink、citation locator、PRISMA reconciliation、figure manifest / integrity、experiment matrix backlink、statistical triage、overclaim lint、argument review 和 style fingerprint 是 payload-present 或 route-required 时才物化的 optional skill-content refs。当前可用入口是 `medautosci study light-advisory-materialize`，只把 refs-only bundle 写到 `artifacts/stage_outputs/<stage>/advisory/`。Skill-level 学习只承认 MAS-owned companion/templates 和 optional refs：progress passport / ref ledger、checkpoint gates、references/scripts/assets 分层、source map / literature matrix、experiment matrix / reproducibility plan、manuscript / rebuttal modes、citation locator、PRISMA template、figure export / integrity / panel template、style fingerprint、argument review 和 bounded mode registry。它不安装 Light runtime，不复制 27-skill router，不把 `db09` / 知识库 / API 表 / 期刊规格表 / font defaults / 示例项目写成 study truth，不把 review score 或 self-review checklist 写成质量/发表 gate。缺 Light-derived advisory 或 content template 默认只算 observability / platform repair；只有当前 delta 的 route-required ref 缺失且影响 source/data/evidence、owner-route identity、forbidden write、irreversible mutation 或 reviewer/publication hard gate 时，才能生成 typed-blocker candidate，正式 typed blocker 仍需当前 owner / reviewer 物化。

Progress-first safety envelope：`contracts/progress_first_safety_envelope.json` 现在把错误完成、伪证据、旧 read-model、重复 receipt 和 artifact authority 漂移固定为五类可测试风险。它同时定义 Light / Co-Scientist / EvoScientist / EvoSkills 的混合模式为 `current_owner_native_jit_affordance`，并把 PaperSpine、ARIS、Academic Research Skills、ARK、AutoSci / OmegaWiki 和 PaperOrchestra 固定为 single nonblocking external-learning sidecar 的 refs-only worker source family：ordinary path 没有常驻 sidecar、没有默认 scan / tournament / meta-review / prefetch / memory scan；触发语义固定为 `current_delta_declares_or_implies_affordance_need`。五类风险的全面审计走 meta / explicit audit lane；普通 live path 只做 JIT hard-gate 检查，继续锚定 `current_owner_delta -> concrete paper/evidence/reviewer/gate delta -> ProgressDeltaReceipt / OwnerReceipt / TypedBlocker -> next current_owner_delta`。

External-learning worker closure：`med_autoscience.external_learning_adoption_closure.build_external_learning_adoption_closure` 是当前统一口径，用来区分 Co-Scientist、Nature-skills、Academic Research Skills、AutoSci/OmegaWiki、EvoScientist/EvoSkills、ARK、ARIS、PaperSpine、PaperOrchestra 和 Open Auto Research 的真实落地层级。`contract_only_gap`、`thin_projection_landed_worker_scaleout_gap`、`history_only_gap` 和 `not_landed_gap` 不能再写成“已学完 / 已落地”；只有进入 owner surface、read-model、quality pack、controller-authorized soak 或 `run_external_learning_sidecar` 这类执行槽，才算有可运行落点。`external_learning_sidecar.run_external_learning_sidecar` 是 single nonblocking sidecar action/catalog 候选槽，只写 `artifacts/supervision/requests/external_learning_sidecar/latest.json` 与 `artifacts/advisory/external_learning_sidecar/latest.json`，输出 refs-only advisory candidates 和 refs-only advisory worker results；ARS、AutoSci / OmegaWiki、ARK、ARIS、PaperSpine 和 PaperOrchestra 的 worker callable refs 已写入 contract。mainline 不等待它，缺失、失败、超时或预算耗尽不阻断 current owner action，不写 `publication_eval/latest.json`、`controller_decisions/latest.json`、paper、submission package、`current_package`、memory body、owner receipt、typed blocker、quality verdict 或 artifact authority，也不声明 live paper progress、publication ready 或 external runtime landed。

Lightweight executor receipt contract：`med_autoscience.lightweight_executor_receipts` 与 action catalog 的 `lightweight_executor_receipt` 入口已落地为只读 executor evidence contract，并新增 `build_lightweight_executor_receipt_from_evidence` 作为 ToolResultEnvelope / default executor evidence dict 的结构化生成面。默认执行管理仍是现有 Codex / `uv` clean runner / process-workspace isolation：`L0_host_clean_runner` 与 `L1_process_workspace`；该层现在可记录 tool id / action type / status、stdout/stderr refs、output refs、artifact refs、changed-file refs、duration、env fingerprint、failure class、evidence source 和 isolation diagnostic。Docker、OpenHands 或同类 sandbox 只允许作为显式 `L3_containerized_sandbox` proof lane；MAS 在容器内运行时默认禁用 Docker / DinD / Docker socket mount 作为隐式授权。该 receipt 不执行命令、不写 owner receipt / typed blocker / `publication_eval/latest.json` / `controller_decisions/latest.json` / `current_package` / submission package，不关闭 stage、quality gate、publication readiness 或 artifact authority，缺 receipt 或缺 Docker 不阻断 current owner action。

Data asset v3 runbook：[Medical Data Asset Target Operating Model](./source/medical_data_asset_target_operating_model.md) 与 [Data Asset Storage Retention Runbook](./runtime/data_asset_storage_retention.md) 是当前 DPCC / MAS 数据资产长期读法。DPCC `restricted_raw` / `deidentified_longitudinal` / `standardized_longitudinal` release body 位于 `data/datasets/**` body plane；`dataset_manifest.yaml` 是 release contract；`memory/portfolio/data_assets/**/manifest_refs` 是 controller-rebuildable locator/projection；study 只通过 authorized study contract 绑定可消费 release。Runtime residue cleanup、restore-proof compaction、historical payload retention、cold-store semantic retention 和 runtime lifecycle SQLite compact 不得把 dataset body 或 release-local working-copy SQLite 当作默认清理对象。Storage 报告按 online workspace、dataset assets、cold store、remaining non-dataset large files 四桶分账；OPL substrate 只拿 locator/ref/projection/restore shell，不拿 MAS 医学数据 authority。

Paper clean-room rebuild contract：当 DM002 / DM003 同类论文线受 stale read-model、legacy `.ds` residue、重复 receipt 或旧 path currentness 影响而无法稳定推进时，当前 owner route 可以进入 `paper_clean_room_rebuild_required`。该 action 由 `MedAutoScience` owner surface 执行，只生成 `artifacts/supervision/paper_clean_room_rebuild/latest.json` descriptor、history record 和 `artifacts/stage_outputs/_paper_clean_room_rebuild/workspaces/<timestamp>/verified_inputs/` clean workspace；它只复制 verified current refs，不导入 `.ds`、legacy runtime/logs、dispatch ledger 或 stale read-model，不写 paper body、`publication_eval/latest.json`、`controller_decisions/latest.json`、submission package、`current_package` 或质量结论。命令入口是 `medautosci runtime paper-clean-room-rebuild` / `medautosci publication clean-room-rebuild`，dispatch action 是 `paper_clean_room_rebuild_required`。

Legacy `.ds` physical retirement contract：MAS 论文工作目录的当前目标态是 100% 新版目录结构；`.ds` 只作为一次性 legacy intake 输入。稳定入口是 `medautosci runtime legacy-ds-retire --profile <profile> --dry-run|--apply`：dry-run 规划 profile workspace 内最外层 `.ds`，apply 为每个 owner 写 source manifest、`legacy_ds.tar.gz`、restore proof 和 retirement receipt，必要时把旧 runtime state 物化到 `artifacts/runtime/state/runtime_state.json`，restore proof verified 后删除 `.ds`。`legacy_ds.tar.gz` 是可恢复 raw body，不是默认在线保留对象；加 `--archive-retention --archive-retention-apply --archive-retention-cold-store-root <path>` 后，MAS 在 proof/hash gate 通过时把 archive body 外置到 cold object store，在线只留 symlink、`.cold_ref.json`、manifest、proof 和 receipt。旧 `runtime maintain-storage --legacy-ds-root` / bare `.ds` helper 已退役；后续不再把 `.ds` 或其 raw tar body 作为 MAS 长期读取层、compatibility layer 或论文目录合法形态。

Historical retention / cold-store contract：当前 MAS storage governance 采用 online workspace / cold store 分账。在线论文目录只保留 current truth、数据资产、manifest、receipt、restore proof、cold ref、capsule ref、latest pointer、refs index 和必要运行依赖；大 raw body、legacy archive body、巨大 detail arrays 和派生 payload body 在 proof/hash gate 通过后进入 cold object store。`data/datasets/**` 不属于历史残留精简范围。稳定入口是 `medautosci semantic-cold-store-retention --root <cold-store> --reference-file-list <raw-ref-list> --dry-run|--apply` 与 `medautosci runtime-lifecycle-payload-retention --db <runtime_lifecycle.sqlite> --cold-store-root <cold-store> --retire-cold-payloads --compact --apply`。候选数量、释放空间、SQLite integrity 和 skipped samples 必须 fresh-run 或回到对应 receipt / runbook；本文只保留 current policy：剩余非 dataset 在线大文件和 workspace ops venv/cache 只能走 payload retention、SQLite integrity/compact 或 bootstrap/cache 生命周期规则，不能裸删。

Readiness blocker-derived repair contract：当 Stage 08 已稳定给出 `medical_paper_readiness_missing` / `medical_paper_readiness_not_ready` typed blocker，且 `publication_eval/latest.json.gaps` 已具体化到论文、证据、reviewer 或 submission/gate 差距时，当前长期读法不再把 `complete_medical_paper_readiness_surface` 作为 primary queued action。MAS owner-action reducer 必须从该 blocker 和 gaps 派生 `run_quality_repair_batch` 或 `run_gate_clearing_batch`，并携带 Stage typed blocker ref、publication eval id、gap ids、work-unit fingerprint 和 required output contract。重复 readiness completion 应标记为 `superseded_by_readiness_blocker_derived_repair`，只进入 ignored evidence / diagnostic；Progress-First operator surface 应展示所需 manuscript / evidence / reviewer / gate repair delta，而不是继续证明 not ready。该 contract 不写 study truth、`publication_eval/latest.json`、`controller_decisions/latest.json`、paper、submission package 或 `current_package`。

Current-control unique owner contract：MAS current work-unit reducer 是唯一下一工作单元 truth；OPL current-control / provider admission candidates 只承载 queue、attempt、provider transport 和 liveness evidence。OPL terminal closeout 必须先被 MAS controller 消费以释放旧 handoff 锁，再由 MAS 重新给出 `executable_owner_action`、`running_provider_attempt`、`typed_blocker` 或 `blocked_current_work_unit`。Root `provider_admission_candidates[]` 只能由 current-control action / owner-route currentness 与 MAS current work-unit identity 匹配后派生；ready dispatch 只是 callable carrier 和 authority check。Stage Native `control/next_action.json` 只有同时携带 OPL Stage Transition Authority 非写权边界和 `current_work_unit_binding.source=canonical_current_work_unit`、且 work-unit id/fingerprint 非空时，才能进入 default dispatch；缺 binding 的 next_action 只能作为 diagnostic ignored，reason 为 `stage_native_workspace_next_action_requires_authority_binding`。`paper_autonomy/guarded-apply` 的 owner answer 必须绑定 live OPL `current_owner_delta` lineage，accepted shape 固定为 `domain_owner_receipt_ref` / `quality_gate_receipt_ref` / `typed_blocker_ref` / `human_gate_ref` / `route_back_evidence_ref`；缺 live lineage 或缺 5-shape 时 MAS 只能返回 typed blocker receipt，不能把 export 的 expected contract、provider completion 或旧 default-executor lineage 当作论文推进。该 contract 不声明 paper closure、publication-ready、current package freshness、production-ready 或 provider 持续运行。

Current-control hardening：current-control provider admission 必须先取得同一 study 的 canonical current identity，优先使用 `current_work_unit`，其次才是 fresh `study_progress.current_executable_owner_action`；缺 current identity、work-unit id/action type 不匹配、或仅靠旧 action queue / ready dispatch / persisted default-executor execution 自证时一律 fail closed。`domain-health-diagnostic` 在 current-control fallback 前必须 fresh 读取 `study_progress` currentness；`domain-owner-action-dispatch` 只要 scan/latest 中存在 `current_work_unit`、owner route、action queue 或 strict running-provider evidence，就不得执行无 owner_route 的 legacy consumer dispatch。该 hardening 只关闭控制面绕过，不写 paper truth、publication verdict、package authority 或 provider running proof。

Current owner action export hardening：`domain-handler export` 在判断 default-executor dispatch 是否 dispatchable 前，必须先把 fresh `current_work_unit` / `current_executable_owner_action` 的 work-unit id、work-unit fingerprint、truth/runtime/source-eval currentness basis 覆盖到 dispatch owner route 和 prompt contract。Publication-gate replay family 的 work-unit id 可以按 `run_gate_clearing_batch` 等价匹配，但 fingerprint/currentness 不能等价化；旧 `publication_gate_replay` dispatch 不能因为 work-unit family 等价就遮蔽当前 `dpcc_publication_gate_replay_after_current_ai_reviewer_record`。当 progress owner 显示为 `finalize` 但 action 是 `run_gate_clearing_batch` 时，OPL default executor 的实际 executable owner 仍按 action policy 进入 `gate_clearing_batch`；`finalize` 只作为 MAS progress/global owner 语义，不授权 OPL 队列写 paper truth、publication verdict、current package、domain-ready 或 provider running proof。

Guarded-apply owner-answer hard gate：fresh OPL `current_owner_delta` 若为 `paper_autonomy/guarded-apply` / `domain_owner_receipt_quality_gate_or_typed_blocker_required`，且 `lineage_ref` 非空、`latest_owner_answer_ref=null`、`domain_ready_authorized=false`、`owner_answer_missing=true`，MAS 只能接受 `domain_owner_receipt_ref`、`quality_gate_receipt_ref`、`typed_blocker_ref`、`human_gate_ref` 或 `route_back_evidence_ref`。MAS export 只发布 expected contract，不发布 live delta；dispatch/provider admission 必须从 live identity 校验 lineage 和 shape，缺失或不匹配时 fail closed typed blocker。provider running/completion、OPL refs-only evidence、audit sidecar、expected contract、ready dispatch 或旧 ticket 都不能生成 owner answer、domain-ready、release-ready 或 production-ready。

Current-owner-delta owner-answer materializer：`medautosci current-owner-delta-owner-answer --current-owner-delta-file <json> --format json` 是 MAS 对 fresh OPL `paper_autonomy/guarded-apply` current owner delta 的 repo-owned answer 入口。它只校验 live lineage、desired delta、accepted shape、`latest_owner_answer_ref=null` 和 `domain_ready_authorized=false`，并物化 MAS-owned `typed_blocker_ref` 以及 OPL 可记录的 `domain-owner-payload-summary record` / `stage-run-authorization record` payload。该入口不写 study truth、paper body、`publication_eval/latest.json`、`controller_decisions/latest.json`、submission package、`current_package`、owner receipt 或 quality verdict；它只关闭 current pointer owner-answer handoff 的 refs/authorization binding，不能声明 paper progress、domain-ready、publication-ready、release-ready 或 production-ready。缺 live lineage、shape 不匹配或已有 owner answer ref 时必须 fail closed。

Stage-attempt typed-blocker owner-answer ref recovery：OPL stage attempt closeout 若已由 MAS/domain owner 产出 `surface_kind=mas_domain_typed_blocker` 的 typed blocker 正文，MAS `domain-handler dispatch-evidence-payload` 必须把它投影为 OPL current-owner-delta 可消费的 `typed_blocker_ref`，即使旧 closeout 使用 `typed_blocker` 字段、`status=closed_with_domain_owner_refs`，或缺少显式 `typed_blocker_ref`。缺 ref 时只能合成同一 closeout 内 fragment ref（例如 `...closeout.json#typed_blocker` 或 `...closeout.json#domain_blocker`），并仍要求 stage/study/action identity 匹配、closeout 不声明 domain completion / domain ready。该 recovery 只发布 owner-answer ref，不读取或写入 paper body、study truth、`publication_eval/latest.json`、`controller_decisions/latest.json`、package 或 quality verdict。

## 读法

- 本文只保留当前状态摘要、owner 边界、open evidence tail 和禁止误写口径。
- dated closeout、receipt id、OPL worklist 数字、命令流水、same-day follow-through 和过程性 proof 不再写入本文；文档生命周期记录见 [history/program](./history/program/README.md)，runtime/storage/stage-folder 证据见 [Domain Authority Refs Index Guard](./runtime/domain_authority_refs_index_guard.md)。
- 若本文与 live runtime/controller/workspace output 冲突，以 live output 为准；若本文与 [MAS 理想目标态差距与完善计划](./active/mas-ideal-state-gap-plan.md) 冲突，以 gap plan 对执行顺序和 evidence tail 的归属为准。

## 当前角色

`Med Auto Science` 是医学研究 domain agent，也是 OPL-compatible package。单一 MAS app skill 是 direct path 的稳定入口；经 OPL 托管时，OPL/Temporal 是默认 hosted autonomous runtime，承载 stage attempt、queue、wakeup、retry/dead-letter、resume、human gate transport、generated surface、projection 和 App/workbench shell。Codex App 只承担 direct entry / 人机操作面，不作为任务启动后的外围持续 driver。

MAS 持有医学研究 truth、stage semantics、AI reviewer / auditor quality gate、publication route、domain transition table、publication-route memory body/writeback decision、artifact/package authority、source readiness、owner receipt、typed blocker 和 safe action refs。OPL 不写 MAS study truth、memory body、publication verdict、artifact authority 或 `current_package`。

`Codex CLI` 是当前第一公民 executor。其他 executor adapter 只能通过 OPL 显式接入，并只保证接入、生命周期、回执与审计边界，不承诺行为效果等价。

MDS / DeepScientist 当前只作为 source provenance、historical fixture、explicit archive import、backend audit、upstream learning 和 parity oracle reference，不是 MAS 默认 backend。

历史 MAS-local scheduler/backend、runtime lifecycle/SQLite、workspace wrapper 和 alias 材料只在 `docs/history/**`、explicit archive/import reference 或 parity oracle 语境读取；当前默认面是 OPL/Temporal hosted runtime 加 MAS domain authority refs、owner receipts、typed blockers 和 minimal authority functions。

## DM002 / DM003 runtime truth 读法

DM002 / DM003 当前只按 live `study_progress`、owner-route、OPL current-control / provider attempt ledger、workspace artifacts 和 owner receipts 读取。历史 pause、Stage Native canary、readiness surface follow-through、dispatch evidence payload、clean-room rebuild 和 provider-hosted dispatch 授权记录只作为 provenance 或平台 repair 证据；当前执行顺序和 evidence tail 回到 [MAS 理想目标态差距与完善计划](./active/mas-ideal-state-gap-plan.md)。

当前稳定口径是：terminal `publication_handoff_owner_gate` 可以给出 MAS-owned typed blocker owner answer，并把下一步指向 `MedAutoScience / complete_medical_paper_readiness_surface`；该 owner surface 只按当前 `readiness.next_action.surface_key` 消费 matching dispatch / request / ref payload，物化 canonical readiness surface 或写 stable owner blocker。合法 closeout binding、stage-native owner-answer materialization、dispatch evidence payload 和 provider-hosted dispatch 都必须绑定同一 StageRun/source/idempotency、active lease、execution authorization 和 MAS owner result shape；缺任一关键 refs 时 fail closed，不写 paper body、publication eval、controller decision、submission package、`current_package`、owner receipt body 或 quality verdict。

这组平台收口只证明 MAS/OPL currentness、owner-surface carrier、typed blocker shape、refs-only accounting、clean workspace 入口和 authorization boundary。它不声明 DM002 / DM003 paper readiness、publication-ready、submission-ready、quality verdict、domain-ready、production-ready、OPL queue release/resume、provider running proof 或补偿链物理退役。真正 study closeout 仍必须由对应 owner run 在同一 current identity 下产出 matching owner receipt、quality gate receipt、typed blocker、human gate、route-back evidence 或 canonical changed surface ref。

当前读法固定为：

- 具体 study owner、action、route key、stage attempt、provider liveness 和 blocker 必须 fresh 读取 live `study_progress`、owner-route、OPL current-control / provider attempt ledger 和 owner receipts。
- `stage-artifact-materialize`、refs-only SQLite、restore-proof archive、stage-folder index、OPL dispatch task、held attempt 或曾经 running attempt 都不能单独声明 publication-ready、paper closure、artifact authority、domain-ready、production-ready 或 `current_package` 更新。
- 当前 platform lane 是黄金路径收口：保留 Stage Native / StageRun / current owner delta 的正确路线，减少默认面上的 read-model reconcile、receipt-only 和 audit tail；不是恢复 MAS generic runtime loop，也不是重建新的 MAS controller system。
- storage/index/stage-folder closeout 与 DM002/DM003 follow-through 细节由 [Domain Authority Refs Index Guard](./runtime/domain_authority_refs_index_guard.md) 承载；本文不保存 receipt/task/stage id ledger。
- paper-line pause closeout 细节由 [DM002 / DM003 paper-line pause closeout](./history/program/dm002_dm003_paper_line_pause_closeout_2026_06_05.md) 承载；本文只保存当前判断。
- 同类 receipt/currentness/read-model/preflight 空转按 [Progress-First Stage Outcome Runbook](./runtime/control/progress_first_stage_outcome.md) 判断 paper/deliverable progress、platform repair 和 stable typed blocker；platform repair 不计入论文实质进展。

## 当前机器事实

| Surface | 当前事实 |
| --- | --- |
| Semantic pack | `agent/` 是 canonical medical research semantic pack。 |
| Machine contracts | `contracts/foundry_agent_series.json`、`contracts/pack_compiler_input.json`、`contracts/stage_control_plane.json`、`contracts/golden_path_profile.json`、`contracts/action_catalog.json`、`contracts/generated_surface_handoff.json`、`contracts/stage_run_kernel_profile.json`、`contracts/stage_artifact_kernel_adoption.json`、`contracts/functional_privatization_audit.json` 和 `contracts/production_acceptance/mas-production-acceptance.json` 是当前主要 machine-readable truth。`contracts/foundry_agent_series.json#/workspace_topology_profile` 声明 MAS 按 OPL workspace topology envelope 的 `portfolio` mode / `studies` collection / `artifacts/stage_outputs` stage-native 输出根接入；它不声明 study truth、paper closure、publication-ready、domain-ready 或 production-ready。`golden_path_profile.json` 只声明 OPL ordinary default path 与 hidden-variant policy，不声明 paper closure、publication-ready、domain-ready 或 production-ready。 |
| Direct / hosted path | Direct MAS app skill path 与 OPL-hosted path 共享同一 MAS-owned stage、controller、durable truth、quality verdict、artifact authority、owner receipt 和 typed blocker。 |
| Hosted runtime | Hosted autonomous runtime 默认由 OPL/Temporal 承担；MAS 不持有 generic daemon、scheduler、attempt loop、queue、retry/dead-letter、resume、worker residency、generic lifecycle/index 或 App/workbench shell。`domain_owner_action_dispatch` 与 `paper_repair_executor` 只能在收到 OPL provider attempt、lease 或 receipt proof 时执行 `apply`；缺失时返回 `opl_execution_authorization_required` typed blocker。 |
| Generated surfaces | OPL generated / hosted surfaces 可以生成或托管 CLI / MCP / Skill / product-entry / status / workbench descriptor，并 dispatch allowlisted MAS task；它们不能写 MAS study truth、publication-route memory body、AI reviewer verdict、publication verdict、artifact authority、source body 或 `current_package`。 |
| Workspace lifecycle | 开发 checkout 只承载 semantic pack、机器合同、authority-function descriptor/receipt refs、domain handler/native helper 和人读治理；真实 workspace state、runtime artifact、receipt instance、paper/package/export artifact 与缓存/venv/pycache/pytest 副产物必须进入受控 study workspace/runtime artifact root 或用户级 runtime state。 |
| Workspace target-state cleanup | `ops/medautoscience/` 是唯一 MAS workspace ops 入口；`ops/mas/progress/` 只保留 Progress Portal 静态只读投影。`ops/mas/bin/*`、`ops/mas/config.env`、`ops/mas/README.md` 与 `ops/mas/behavior_equivalence_gate.yaml` 已退役为 legacy bridge surface；对应 renderer 源码已按无 active caller / replacement / tombstone proof 物理退役，并由 no-resurrection guard 防回流。`workspace-target-state-cleanup --visual-clean` 可受控归档已知 study-local / ops residue，未分类 entry fail closed 到 blocker。该清理是 platform repair / topology hygiene，不写 study truth、paper、publication eval、controller decisions、artifact body、memory body、owner receipt 或 publication readiness；`same_work_unit_live_evidence` 只保护当前 owner-answer compensation chain，不阻止静态 retired wrapper / renderer 删除。 |
| State index / restore-proof storage maintenance | MAS refs-only 小文件治理 pilot 和 restore-proof compaction 只作为 storage/index/provenance guard；细节、稳定命令和 2026-06-04 evidence 归 [Domain Authority Refs Index Guard](./runtime/domain_authority_refs_index_guard.md)。 |
| Stage-native paper workspace materialization | `stage-artifact-materialize` 只写 locator / hash / stage artifact receipt，不复制 legacy body，不改 paper/package/publication truth，不授权 quality verdict、publication-ready 或 submission-ready；对非 terminal stage 可写 stage-artifact projection，terminal `08-publication_package_handoff` 不写 `current.json` 或 `projection/current_owner_delta.json`，只能留下 `Terminalizing / publication_handoff_owner_gate`。terminal owner-answer projection 统一由 `publication_handoff_stage_projection.py` 在 owner receipt / typed blocker closeout 时写。2026-06-05 / 2026-06-06 DM002/DM003 stage-native materialization 与 readiness follow-up 是历史 provenance，不是当前 owner/action truth；当前 owner、action、work unit 和 provider proof 必须 fresh 读取 `study_progress.current_work_unit`、`current_execution_envelope`、DHD readback、OPL current-control 和 owner receipt / typed blocker refs。`stage_artifact_index` 现在还为每个 stage 投影 legacy taxonomy migration read-model：legacy route 只作为 tombstone/provenance/backfill 状态读取，缺 current pointer 或 tombstone/provenance 时 fail closed 给 next owner/action，Workbench 只能显示 `paper_study_stage_pack` 单一 current truth。stage-folder 证据归 [Domain Authority Refs Index Guard](./runtime/domain_authority_refs_index_guard.md)。 |
| StageRun Kernel foundation | 2026-06-05 MAS 已落地 StageRun Kernel foundation：`contracts/stage_run_kernel_profile.json` 固定 `StageRun` / `ArtifactRef` / `OwnerReceipt` / `TypedBlocker` / `ReadModel` 边界；`src/med_autoscience/controllers/stage_run_kernel.py` 和 `study_progress_parts/stage_kernel_projection.py` 从 stage folder / manifest / receipt / blocker 派生 refs-only StageRun projection 与 `current_owner_delta`。OPL 主干已落地 refs-only StageRun substrate contract / primitive / focused test。当前 live canary 已证明 DM002/DM003 terminal handoff owner-answer binding 可以写出 MAS typed blocker，并把默认 follow-up 转为 `complete_medical_paper_readiness_surface`；这仍不声明 paper closure、publication-ready、`current_package` fresh、OPL queue release/resume 或补偿链物理退役。 |
| Stage-native paper authority reader | `paper_authority_cutover/current_body` 是 reader authority surface；AI reviewer current-manuscript binding 优先消费该 current body，再回退到 root `paper/*`。当 `publication_eval/latest.json` 只是 `mechanical_projection` 且仍声明 `ai_reviewer_required=true` 时，已通过 reviewer OS currentness 的 AI reviewer record 可以作为当前 owner-route projection supersede 它。writer/materializer 仍必须走 MAS owner-authorized callable surface，不能把 reader authority、mechanical projection 或 reviewer currentness 升级为写权限、publication-ready 或 package authority。 |
| DM002/DM003 current owner handoff | 早期 stage-native read-model、readiness follow-through 与 2026-06-07 owner-surface 接入记录只作为历史 closeout / provenance 读取，不能作为当前 owner/action/work-unit truth。当前 DM002 / DM003 owner、action、work unit、provider admission、execution gate 和 running proof 必须 fresh 读取 `study_progress.current_work_unit`、`current_execution_envelope`、DHD dry-run/readback、OPL current-control 与 owner receipt / typed blocker refs。`study progress` 的 active attempt 可短时出现或消失，不能单独作为 owner answer；0/[] 也不能推出 idle、done 或恢复完成。真正 study closeout 仍取决于 live owner run 在同一 StageRun/source/idempotency 下产出 matching owner receipt、typed blocker、quality gate receipt、human gate、route-back evidence、canonical changed surface 或同一 current identity strict provider running proof；不得直接 tick、手写论文 truth，或把 stage-folder artifact receipt写成 publication-ready。 |
| Paper clean-room rebuild | `paper_clean_room_rebuild_required` 是当前 MAS owner-route 的干净重建 action。它 materialize descriptor、history 和 verified-input clean workspace，用 required refs `study.yaml`、`paper/draft.md`、`paper/evidence_ledger.json`、`paper/review/review_ledger.json` 作为最小输入，optional refs 包括 claim map、figure/table catalog、publication eval latest、controller decision latest、paper authority cutover 和 publishability gate latest。ready clean-room follow-up 会写 Stage Native `run_medical_publication_surface_from_clean_room` next action，并携带 OPL Stage Transition Authority boundary 与 canonical current-work-unit binding；这只授权 default executor dispatch 选中该 owner action，不授权 paper body、publication eval、controller decision、current package、submission package 或 promotion。后续必须由 clean workspace 上的 write/review/finalize owner、AI reviewer、publication gate 或 typed blocker 继续推进。 |
| Current execution envelope precedence | `current_execution_envelope` 只把 `state_kind` 作为顶层真相：非 superseded typed blocker > live running provider attempt > parked / executable action projection。live attempt 可以压过 stale parked/action queue 和 runtime-admission residue，但不能压过当前 MAS owner blocker；action queue 只能解释 evidence，不授权 running attempt work-unit identity。 |
| Progress-first Co-Scientist affordance | 六项机制当前是 semantic pack、machine contract、owner ticket projection、focused tests 和 runtime / active docs 中的 JIT affordance，不是 authority surface，也不是默认推进层：next-delta tournament、bounded micro-candidate generation、critique-as-repair-hint、reusable lesson extraction、triggered meta-review、opportunistic knowledge prefetch。它们只在 `current_delta_declares_or_implies_affordance_need` 时产生 affordance refs、reviewer briefing、memory reuse hint 或 no-loop suppression hint；缺失时不得阻断可执行 owner action，命中真实 hard gate 时必须转成 typed blocker、human gate、route-back 或 stop-loss。 |
| EvoScientist / EvoSkills progress sidecar | 目标态 sidecar execution architecture 已落地：`remaining_learning_plan=false`，后续只允许 implementation scaleout。async learning sidecar、auxiliary helper、fail-open tool selector、observation memory、failed-path taxonomy、routing eval 和 stop-loss candidate 都是 progress accelerator / audit sidecar，不是 authority surface。它们只能跟随当前 owner delta 产生 refs-only hint；critical path 不等待 sidecar，不能写 study truth、memory accept/reject、owner receipt、typed blocker、quality verdict、artifact authority、paper progress 或 reviewer/auditor output。 |
| External-learning adoption closure | `external_learning_adoption_closure` 是统一 machine surface：Co-Scientist / Nature-skills / Open Auto Research 等已有 owner surface、quality pack、read-model 或 soak 的项目按 landed surface 读取；ARS / AutoSci / ARK / ARIS / PaperSpine / PaperOrchestra 已有 refs-only advisory worker landing，`scientific_capability_registry` 现在还能生成 refs-only owner-consumption / live-soak evidence packet，记录 output refs、owner receipt / typed blocker / reviewer receipt / route-back evidence refs、no-forbidden-write proof 和 fail-open flags；这些 evidence packet 仍需真实 owner-consumed refs、owner receipt 或 typed blocker 后才能计入真实 study progress。`run_external_learning_sidecar` 是 single nonblocking advisory owner action，只能把 PaperSpine、ARIS、ARS、ARK、AutoSci / OmegaWiki 和 PaperOrchestra 暴露为 refs-only candidates / worker results，不能成为 external runtime、admission gate、quality gate、publication gate、stage closeout、current-owner blocker 或 paper progress。`lightweight_executor_receipt` 是独立只读 evidence contract：默认 L0/L1，显式 L3 proof lane 才能涉及 Docker/OpenHands，inside-container 不默认 Docker / DinD，receipt 不执行命令、不写 authority surface。 |
| Hosted OPL ordinary-path consumption | `repo_mcp_consumption_evidence_landed_not_live_progress` | `contracts/hosted_ordinary_path_consumption.json`、`src/med_autoscience/hosted_ordinary_path_consumption.py` 和 MCP `agent_tool_arsenal(mode=hosted_consumption)` 已把 AgentExecutionIndex、OperationalToolCard、CapabilityInvocationPlan、ToolResultEnvelope recovery、Scientific Capability Registry resolution 与 owner-consumption evidence packet 串成 OPL hosted ordinary path 可消费的 refs-only evidence。该面不要求 Docker / DinD，不要求人类手动拼工具，不新增默认 preflight，缺 capability 或 owner response refs 不阻断 current owner action；它也不能写 study truth、publication eval、controller decisions、paper/package、owner receipt、typed blocker、quality verdict、artifact authority 或 paper progress。 |
| Light external-pattern / skill-content intake | Light 只作为 external pattern source；MAS 可通过 `medautosci study light-advisory-materialize` 自动产出基础 advisory refs，并在 payload present 或 route-required 时产出 source / data / citation / PRISMA / figure / experiment / statistics / overclaim / argument / style 等 optional skill-content refs。Skill-level 学习只承认 MAS-owned companion/templates 和 optional refs；这些 ref 只能服务 stage-quality、stage knowledge、source readiness、reviewer briefing、display QA、advisory repair hints 和 next owner delta，不得引入 Light runtime、复制 27-skill route、把 `db09` / 知识库 / 期刊规格表 / font defaults / 示例项目写成 study truth，或把 score / checklist 写成 quality / publication gate。 |
| Display Pack v2 | MAS 域内 Display Pack v2 已落地为 descriptor + paper-quality refs + single/batch medical figure grammar + R/ggplot2-first subprocess runtime protocol + 55 个 core evidence templates 的 `subprocess + render.R` 一等默认 R/ggplot2 入口 + P2 Python plugin adapter + template discovery/describe CLI + one-shot scaffold render + golden refresh/check lower-bound surface + multi-figure AI/VLM polish lifecycle + deterministic E2E render/QC/publication manifest + display lock + submission refs preservation。core pack renderer migration ledger 覆盖 84/84 evidence templates：P0 landed 22，P1 default R/ggplot2 promotion landed 33，P2 retained Python / later dual-stack 29；`display-pack-render-candidate` 只保留 legacy comparison receipt，不替换默认 renderer，不授权 publication readiness。`contracts/display-pack-contract.v2.json` 明确 `mas_pack_descriptor_authority=true`、`mas_publication_quality_authority=true`，同时 `mas_owns_opl_generic_pack_os=false`、`display_pack_lock_can_authorize_publication_readiness=false`、`pack_descriptor_can_mutate_study_truth=false`、`ai_illustration_can_carry_scientific_claim=false`。OPL repo 已落地 `opl pack os install/registry/cache/distribute/lock/validate/mas-display-smoke` surfaces；MAS 只保留 refs-only handoff，不拥有 generic Pack OS。 |
| Standard Agent purity | `standard_agent_purity` 是 current product-entry / domain-handler / read-model 默认口径：MAS 目标形态是 `Declarative Medical Research Pack + OPL generated/hosted surfaces + minimal authority functions`。当前 retained code 只按 domain handler target、authority function、owner receipt / typed blocker producer 或 refs-only projection input 读取；repo-local dispatcher / repair executor 不再作为 MAS-private attempt loop 或 consumer 读取。 |
| Legacy names | MDS / DeepScientist、Hermes、legacy scheduler、`runtime_transport`、`mas_runtime_core*`、旧 lifecycle writer 和旧 alias 只能作为 provenance、explicit archive import、backend audit、parity oracle、tombstone 或旧输入测试语境读取；不作为当前 runtime owner、adapter、fallback、compatibility alias 或 active caller。 |

## 当前功能/结构状态

当前 MAS 目标源码形态按标准 OPL Agent 收口为三类长期 surface：

| 类别 | 当前口径 |
| --- | --- |
| `declarative_medical_pack` | `agent/` 和 contracts 声明医学 stage、prompt、skill、knowledge、quality gate、action catalog、receipt refs 和 forbidden authority boundary。 |
| `opl_generated_hosted_surfaces` | OPL 承担 generated / hosted CLI、MCP、Skill、product-entry、status、workbench、projection shell、attempt、queue、retry/dead-letter、watch shell、generic state-machine runner、locator/index/lifecycle 和 operator workbench。 |
| `minimal_authority_functions` | MAS 保留医学 study truth、publication/source/memory/artifact verdict、AI-first record validator、owner receipt signer、typed blocker materializer、safe action refs 和 body-free domain authority refs；当前 retained representative surfaces 由 `contracts/authority_kernel_inventory.json` 作为 inventory/read-model 固定。 |

`contracts/functional_privatization_audit.json` 当前固定 `classification_gap_count=0`、`functional_structure_gap_count=0`、`remaining_functional_followthrough_gate_ids=[]`、`active_private_generic_residue_count=0`、`repo_local_wrapper_tail_count=0`。这表示结构/功能 gap 当前关闭；它不等于真实 paper closure、publication-ready、artifact mutation authorization、provider long-soak 或 domain/production ready。

| Guard | 当前约束 |
| --- | --- |
| `generated_surface_default_owner_cutover` | 不把 MAS direct path、domain handler target 或 read-model projection 写回 MAS-owned generic wrapper。 |
| `authority_kernel_inventory` | `contracts/authority_kernel_inventory.json` 只作为 retained MAS authority function 的分类和后续物理收薄读根；不能把 inventory presence 写成 authority retired、production-ready、paper progress、publication-ready 或 artifact mutation authorization。 |
| `standard_agent_purity_guard` | 旧 wrapper tail 只作为 guarded provenance / delete-gate context；不新增 compatibility shim、alias、facade 或 wrapper。 |
| `domain_authority_refs_thinning` | storage、artifact、memory、source、owner-route、progress/status helper 只输出 refs、receipts、blockers、locators 或 diagnostic projection input。 |
| `state_contract_thinning_guard` | 稳定控制面只承认 `macro_state`、`owner_route`、`receipt_or_blocker` 和 `evidence_refs`；长 reason 和 supersession reason 只能作为 diagnostic detail。 |
| `ai_first_quality_record_boundary` | 程序只做 validator、materializer、receipt signer 和 guard；质量结论必须追到独立 reviewer/auditor invocation、context/task record、quality receipt、route-back 或 typed blocker。 |

## 当前测试/证据差距

以下差距是 production evidence tail。它们不改变上面的结构口径，也不能声明 publication-ready、domain-ready、artifact mutation authorization 或 `current_package` 更新。

| 证据差距 | 当前状态 | 仍需看到 |
| --- | --- | --- |
| 真实 paper-line provider apply | multi-profile guarded-apply、DM002 canary、research evidence pack read-model/schema/canary 已证明 refs-only owner-chain shape、stable blocker 和 fail-closed 审计链可见。`contracts/live_stage_run_progress_evidence.json` 现在把这组 MAS-owned owner receipt / typed blocker / human gate / quality gate / no-regression refs 暴露为 OPL 可消费的 live StageRun progress evidence contract；`medautosci current-owner-delta-owner-answer` 可从 fresh OPL `current_owner_delta` 物化 MAS-owned `typed_blocker_ref` answer，并输出 OPL `domain-owner-payload-summary` 与 `stage-run-authorization` record payload。该入口只关闭“domain owner 已给出 refs 或 blocker”这一 current pointer owner-answer handoff 缺口。 | 更多真实 paper line 产出 owner receipt、paper/artifact delta、independent reviewer/auditor record、human gate/resume、route decision、stop-loss、artifact/memory lifecycle receipt 或 stable typed blocker，并由 MAS owner 把 blocker 解析为 route-back、human gate 或下一 paper delta；该合同和 CLI 都不声明 publication-ready、domain-ready、artifact mutation authorization、current package freshness 或 production-ready。 |
| owner-chain dispatch ledger scaleout | `domain_dispatch_evidence_record_payload` 与 OPL refs-only identity preflight / record / verify 已覆盖 success refs path 和 typed-blocker path；2026-06-07 新增 readiness owner callable 的 payload exporter coverage，fresh OPL worklist 已读到 `open_worklist_item_count=0`。 | 只处理 fresh OPL worklist 重新暴露且能绑定 MAS current owner delta 的 workorder；已 verified typed-blocker receipt 不能升级成 success receipt，success refs receipt 也不能写成 domain-ready 或 production-ready。 |
| publication-route memory receipts | Router/writeback refs 已进入 body-free evidence packets、paper-line result、domain-dispatch payload 和 stage expected refs。 | 多条真实 paper line 持续产生 accepted/rejected/blocked writeback receipts，并由 owner route 明确 memory accept/reject/blocker 结论。 |
| artifact lifecycle receipts | Artifact lifecycle report / retention plan 已输出 bounded refs、physical-thinning handoff 和 stable typed blocker refs-only shape。 | OPL apply receipt、真实 cleanup/restore/freshness apply receipt 或 MAS artifact mutation permission；不能由 report/plan 直接授权 cleanup 或 artifact mutation。 |
| human gate / resume | Stage replay human-gate refs 已有 MAS-owned body-free typed blocker path，`finalize_and_publication_handoff` 有 refs-only success path。 | Human approval、resume chain receipt、新 owner success receipt 或 paper closure。 |
| provider SLO long soak | Provider/runtime read-model 能投影 live attempt、blocked closeout 和 admission/running distinction。 | 长时 provider-hosted run、restart/re-query、retry/dead-letter、no-forbidden-write 和 App/workbench drilldown 在真实 domain activity 中持续成立。 |
| family transition live receipt | `study_state_matrix` 可把 MAS-owned domain transition table materialize 给 OPL generic matrix runner。 | Route/work-unit 进入 MAS owner chain，并产出 owner receipt、typed blocker、progress delta、gate replay、human gate 或 stop-loss。 |

## 当前源码形态收口

MAS repo 的长期源码形态已经按 strict purity 口径收敛为标准 OPL Agent source shape：declarative pack、OPL generated/hosted surfaces、minimal authority functions、domain handler target、domain authority refs、owner receipt、typed blocker 和 refs-only evidence。

当前 source/test morphology 的完成口径只说明入口、case、helper 和 guard 回到 preferred boundary；它不生成 MAS owner receipt，不关闭 paper-line、memory/artifact/lifecycle、domain-ready 或 production-ready。dated line-budget、test split、helper deletion、receipt/worklist follow-through 和 closeout 过程不再写入本文，必要时读提交历史、focused tests、machine contracts 或 history closeout。

Progress-first consumed-receipt read-model、owner action selection、same-tick materialize/dispatch、closeout-first handoff、route obligation descriptor、publication route-back checklist、research evidence pack projection 和 evidence tail closure summary 是当前 active read-model / controller 口径。它们只让 operator 看见下一 owner、target surface、typed blocker、receipt consumption 和 evidence ref family；不能把 projection 或 refs-only ledger 写成 publication-ready、artifact mutation authorization、paper closure 或 `current_package` 更新。

Medical quality regression lane 已收口为 `make test-medical-quality-regression`。该 lane 只验证 MAS 可消费的质量/进度语义、owner-route 和 refs-only blocker 形状，不写真实 study truth、`publication_eval/latest.json`、`controller_decisions/latest.json`、`manuscript/current_package`、paper body、memory body 或 artifact body，也不授权 publication-ready、paper closure、domain-ready 或 production-ready。

默认开发验证策略：`scripts/verify.sh` 未传 lane 时只运行 `make test-smoke`，用于入口/行预算级 smoke 合同；`make test-regression` / `scripts/verify.sh regression` 是普通开发回归 lane，显式排除 `meta`、`display_heavy`、`submission_heavy`、`materialization_heavy`、`family` 和 `soak_or_golden`。显示物化、submission/package materialization、family shared boundary 以及 soak/golden/fixture-oracle 证据分别由 `make test-display`、`make test-submission`、`make test-family`、`make test-soak-golden` 或 `scripts/verify.sh <lane>` 显式触发；`scripts/verify.sh full` 仍通过并行 lane 覆盖这些重型证据。该分层只减少默认开发路径负担，不把 smoke/regression 通过写成 runtime currentness、release-ready、publication-ready、domain-ready 或 owner acceptance。

## 当前不能声明

- 不能声明 OPL provider proof 等于 MAS paper closure、publication-ready 或 artifact mutation authorization。
- 不能声明 MAS production acceptance receipt 等于具体论文线 publication-ready、medical-ready、artifact mutation authorization 或 `current_package` 更新。
- 不能把 descriptor ready、conformance passed、classification closed、provenance proof 或 selected proof 写成 production ready、paper closure 或物理源码清零。
- 不能把 OPL `stage_production_evidence_receipt_record|verify` 写成 MAS production ready；它只是 expected receipt / monitor freshness 的 refs-only record/verify route。
- 不能把 MDS/DeepScientist、Hermes、non-default executor proof lane 或 workspace archive 写成 MAS 默认 active runtime owner。
- 不能把 progress-first Co-Scientist affordance 写成默认前置流程、admission gate、quality closure、publication readiness、artifact authority、route blocking layer、paper progress、production-ready 或真实 paper-line closeout；`next-delta tournament`、bounded candidates、repair hints、reusable lesson extraction、triggered meta-review 和 opportunistic prefetch 只能在 `current_delta_declares_or_implies_affordance_need` 时服务推进选择、评审定位和失败路径复用。
- 不能把 EvoScientist / EvoSkills sidecar 写成 MAS 默认 runtime owner、executor backend、tool authority、memory authority、quality owner、admission gate、route blocking layer、paper progress、owner receipt、quality verdict、publication-ready、artifact authority、production-ready 或 independent reviewer/auditor output。
- 不能把 Light external-pattern / skill-content intake 写成 MAS runtime integration、skill router migration、study truth source、knowledge truth source、quality gate、publication gate、artifact authority、paper progress 或 dispatch blocker；除非当前 delta 缺 route-required ref 且命中真实 hard gate，否则 Light-derived advisory 或 content template 缺失不得阻塞 owner action。
- 不能把 Display Pack v2 descriptor、display pack lock、submission refs preservation、visual-audit clear、figure polish lifecycle、AI illustration receipt 或 link-only exemplar 写成 publication-ready、submission-ready、paper closure、artifact mutation authorization、scientific claim authority、domain-ready、production-ready 或 OPL generic Pack OS landed。
- 不能把 dated specs、dated closeout、follow-through 记录、receipt id、OPL worklist 数字或历史 full record 当成 current truth。
- 不能把未被 MAS/OPL 消费的 foreground/manual edit、docs-only claim、旧 provider admission / action queue、同 work-unit redrive、provider completion、active_run_id 或 stage artifact 文件存在写成 DM002 / DM003 governed recovery 完成；系统恢复只能由 MAS owner receipt、quality gate receipt、canonical changed surface、stable typed blocker、human gate、route-back evidence 或同一 current identity 的 strict provider running proof 证明。

## 下一跳

- 目标态：[MAS 理想目标态](./references/positioning/mas_ideal_state.md)
- 目标运行架构：[MAS / OPL Agent OS 目标运行架构与重构计划](./runtime/designs/mas_opl_agent_os_target_operating_architecture.md)
- 差距与顺序：[MAS 理想目标态差距与完善计划](./active/mas-ideal-state-gap-plan.md)
- 当前执行地图：[MAS 当前开发线路](./active/current-development-lines.md)
- Runtime 边界：[Runtime boundary](./runtime/contracts/runtime_boundary.md)
- Progress-first runbook：[Progress-First Stage Outcome Runbook](./runtime/control/progress_first_stage_outcome.md)
- Display Pack v2 status：[Display Pack v2 落地状态](./delivery/medical-display/contracts/display_pack_v2_landing_status.md)
- EvoScientist intake：[EvoScientist Progress-First Intake](./runtime/designs/evo_scientist_progress_first_intake.md)
- Light external-pattern intake：[Light External Pattern Intake Runbook](./runtime/control/light_external_pattern_intake.md)
- 文档治理：[MAS 文档组合治理](./docs_portfolio_consolidation.md)
