# MAS 私有实现与 OPL 迁移台账

Status: `active_migration_inventory`
Owner: `MedAutoScience`
Purpose: `opl_private_implementation_migration_inventory`
State: `active_support`
Machine boundary: 本文是 machine-adjacent human-readable 台账，不是机器真相。可执行真相继续归 `contracts/functional_privatization_audit.json`、`contracts/private_functional_surface_policy.json`、`contracts/generated_surface_handoff.json`、`contracts/pack_compiler_input.json`、CLI/MCP/API 行为、sidecar receipt、runtime/controller durable surfaces、owner receipt 和真实 workspace artifact。

## 目标态

MAS 的 OPL 标准智能体目标态是：

`Declarative Medical Research Pack + OPL hosted/generated surfaces + minimal authority functions`

这表示：

- `agent/` 和相关 contracts 声明医学研究 stage、prompt、skill、knowledge、quality gate、action catalog、receipt refs 和 forbidden authority boundary。
- OPL hosted/generated surfaces 承接通用 CLI、MCP、Skill、product-entry、sidecar、status、workbench、projection shell、attempt、queue、retry/dead-letter、watch shell、generic state-machine runner、locator/index/lifecycle 和 operator workbench。
- MAS repo 内长期保留的私有程序面只允许是医学 authority function、domain handler target、AI-first record validator、owner receipt signer、typed blocker materializer、body-free domain authority refs surface、diagnostic/provenance reader 或 historical fixture。
- OPL 可以托管、调度、索引、展示和 dispatch allowlisted task；不能写 MAS study truth、publication verdict、AI reviewer verdict、source readiness verdict、publication-route memory body、artifact authority、`current_package`、paper package 或 runtime/controller durable truth。

本台账只记录当前源码形态和迁移门槛。它不表示 OPL provider long soak、真实 paper closure、publication-ready、medical-ready、artifact mutation authorization 或物理删除已经完成。

## 分类词表

| class | 含义 | 允许输出 | 退役口径 |
| --- | --- | --- | --- |
| `domain_authority_retained` | MAS 必须保留的医学研究 authority 或 authority validator。 | AI-first reviewer/auditor record refs、publication/source/memory/artifact verdict refs、owner receipt、typed blocker、guarded apply ref。 | 不迁到 OPL；只可继续收窄接口和验证 provenance。 |
| `opl_framework_migration_candidate` | 当前仍有 MAS 手写外壳或 generic display/locator/projection 逻辑，但唯一 runtime/control owner 应为 OPL generated/hosted surface 或 OPL framework primitive。 | body-free task/locator/status/projection refs、dispatch receipt、diagnostic refs。 | 迁移或删除时以 OPL replacement proof、MAS receipt parity、focused tests、no-forbidden-write proof 和 tombstone/provenance refs 证明；不得写成 MAS active caller retained。 |
| `refs_only_authority_or_provenance` | MAS 只允许保留的 refs-only authority/provenance surface；它不是 runtime lifecycle adapter、compat alias 或 scheduler/read-model owner。 | owner receipt ref、surface ref、artifact/source/workspace locator ref、cleanup receipt ref、typed blocker。 | 只要该 surface 开始承担 generic runtime lifecycle/read-model/scheduler/recovery/worker lease 语义，即按旧控制面复活处理并退役。 |
| `needs_split_before_migration` | 文件或 surface 混合了 domain authority、read model、generic runner/watch/status shell、mutation guard 或 projection assembly；迁移前必须先按 owner 子域拆清。 | 当前只允许作为待拆控制面或迁移输入，不新增长期 authority claim。 | 拆成 MAS domain authority spec/validator 与 OPL-owned generic runner/watch/locator shell；不再把 generic 子域归为 MAS active adapter。 |

## 当前 inventory

| surface | current class | current caller / proof surface | MAS retains | OPL migration candidate | retirement / migration gate | verification entry |
| --- | --- | --- | --- | --- | --- | --- |
| `src/med_autoscience/controllers/study_runtime_decision_parts/domain_status_authority.py` | `needs_split_before_migration` | `study_runtime_router.progress_projection(...)`、`domain_health_diagnostic` status refresh、`study-progress` / product-entry / sidecar status projection；2026-05-21 已把 `_finalize_result` 的 refs-only runtime/read-model projection shell 拆出到 `status_projection_shell.py`，当前文件约 479 行，继续持有 runtime context、publication gate materialization、manual finish dominance、controller authorization 和 runtime decision authority。 | study truth interpretation、publication gate / manual finish dominance、controller authorization refs、runtime health semantics、owner receipt / typed blocker refs、source/readiness authority refs。 | generic status read-model assembly、generic state-machine runner inputs、runtime event diagnostic shell、family orchestration projection、supervision refresh shell、status serialization/projection shell。 | 本轮作为 control-plane thinning item：已先拆出 status finalization / projection shell；通用状态机、runner、queue、diagnostic shell 长线迁 OPL；医学 verdict、study truth、artifact mutation、owner receipt、source readiness 留 MAS。禁止把本项写成已迁移或已完成物理删除。 | focused status/runtime decision tests；`progress-projection` CLI；`domain-health-diagnostic` focused tests；`git diff --check`；后续触碰 machine surface 时跑 `scripts/verify.sh` 或对应 pytest slice。 |
| `src/med_autoscience/controllers/study_runtime_decision_parts/status_projection_shell.py` 与 `status_projection_shell_parts/read_model_projection_assembly.py` | `opl_framework_migration_candidate` | `domain_status_authority._finalize_result(...)` 的 current source；2026-05-21 已把 generic study truth snapshot、control-plane snapshot 与 progress projection 组装继续拆入 `status_projection_shell_parts/read_model_projection_assembly.py`，主 shell 只保留 MAS decision 之后的 runtime summary alignment、runtime supervision refresh、runtime event ref、family orchestration companion、runtime worker activity、auto-runtime parked projection 与薄 read-model attachment 调用。 | MAS-owned refs、study truth snapshot input、runtime health snapshot input、typed blocker / owner-route refs；不生成 publication verdict、source readiness verdict、artifact mutation authority 或 owner receipt。 | generic status/read-model finalization shell、projection hosting、runtime event relay、family companion/read-model carrier、App/workbench progress projection attachment。 | OPL generated status/workbench shell owns active/default read-model hosting and consumes MAS owner refs；MAS status decision authority、domain receipt parity、focused tests、no-forbidden-write proof 和 tombstone/provenance refs 保持；该 shell 不得承接 runtime current-state owner。 | `tests/test_progress_projection_evidence_adoption.py`、runtime event surface cases、submission metadata waiting cases、managed study projection focused tests；`scripts/verify.sh` for code changes。 |
| `src/med_autoscience/cli.py` 与 `src/med_autoscience/cli_parts/` | `refs_only_authority_or_provenance` with `opl_framework_migration_candidate` boundary | MAS CLI direct path、lazy controller loader、product/runtime/source/delivery command dispatch；2026-05-21 `cli.py` 已从约 1391 行收薄到约 941 行，stage-memory、workbench/portal、watch/supervision、workspace/data/bootstrap 等 generic shell dispatch 已拆入 `cli_parts/*_commands.py`，且旧 status 分支已删除，当前只保留 `progress-projection`。 | direct MAS skill target、domain handler dispatch、AI-first validator command、owner receipt signer / typed blocker reader。 | generic CLI/MCP/product wrapper、generated help/descriptor/status/workbench shell、test-lane harness。 | OPL generated CLI/Skill/MCP/product-entry surface 承担 hosted/default shell；MAS direct path 只可保留 authority callable 与 refs reader，不得复活 runtime lifecycle/read-model/scheduler/worker lease alias。 | public CLI tests、generated surface handoff checks、product-entry manifest tests、`scripts/verify.sh` for non-doc changes。 |
| `src/med_autoscience/controllers/domain_health_diagnostic.py` 与 `domain_health_diagnostic_parts/` | `refs_only_authority_or_provenance` with `opl_framework_migration_candidate` boundary | `domain-health-diagnostic` one-shot domain diagnostic、paper-progress SLO explanation、owner/blocker/status refs；2026-05-21 MAS repo-local long loop、`run_watch_loop` 与 `run_managed_supervisor_loop` 已物理退役，legacy workspace wrapper 重跑 `init-workspace` 后升级为单次 tick。 | paper-progress SLO semantics、owner route / safe action refs、typed blocker、AI doctor repair receipt refs、publication gate interaction constraints。 | generic watch shell、scheduler tick loop、outer-loop runner, retry/dead-letter transport, alert delivery shell, runtime scan/report assembly, provider cadence integration。 | OPL provider/runtime manager owns generic cadence/watch shell; MAS one-shot diagnostic 不得判断 worker lease、retry budget、attempt lifecycle 或 provider recovery。 | `tests/test_domain_health_diagnostic.py`、`tests/test_runtime_protocol_domain_health_diagnostic.py`、runtime supervision / domain SLO focused tests、workspace init wrapper focused tests。 |
| `src/med_autoscience/controllers/study_outer_loop.py` | `refs_only_authority_or_provenance` with `opl_framework_migration_candidate` boundary | `study_outer_loop_tick(...)`、`materialize_non_dispatching_outer_loop_decision(...)`、`refresh_parked_submission_milestone_controller_decision(...)`、domain diagnostic tick request caller；2026-05-22 已把 diagnostic outer-loop tick request / recommendation assembly 拆到 `study_outer_loop_parts/tick_request.py`，主文件约 605 行，只保留 controller decision materialization、dispatch adapter 和 parked submission refresh。 | study truth、publication / controller decision materialization、AI reviewer route-back refs、controller authorization、artifact/package authority refs、owner receipt、typed blocker。 | generic stage loop shell、attempt/queue/retry/dead-letter、operator progress shell、scheduler/watch carrier。 | OPL stage attempt / watch shell owns generic loop; MAS side 只输出 domain decision、owner receipt 或 typed blocker，不再保留 stage scheduler/read-model/recovery owner。 | `tests/test_study_outer_loop.py`、`tests/test_study_outer_loop_cases/*`、transition matrix focused tests、`domain-health-diagnostic` focused tests。 |
| `src/med_autoscience/controllers/study_outer_loop_parts/tick_request.py` | `opl_framework_migration_candidate` | `study_outer_loop.build_domain_health_diagnostic_outer_loop_tick_request` re-export import 的 current source；当前语义是组装 domain-health-diagnostic outer-loop tick request、recommended action priority、publication gate recheck handoff、bundle-stage finalize handoff、task-intake / quality-repair / domain-transition arbitration。 | MAS-owned publication eval refs、AI reviewer route-back refs、controller action refs、submission milestone route context、typed blocker / owner route refs；不写 study truth、publication verdict、source readiness verdict、artifact mutation authority 或 owner receipt。 | OPL generated watch/stage-attempt shell、generic recommendation carrier、attempt/queue/retry/dead-letter、operator progress/status projection、scheduler/watch carrier。 | OPL watch/stage attempt primitive owns tick scheduling and can consume MAS authority refs；MAS side keeps only authority refs / receipt / blocker semantics. | `tests/test_study_outer_loop.py`、`tests/test_domain_health_diagnostic.py`、domain-health-diagnostic outer-loop wakeup focused tests、OPL family-runtime attempt/watch parity tests。 |
| `src/med_autoscience/controllers/opl_unique_control_plane_boundary_parts/consumer_migration_inventory.py` | `refs_only_authority_or_provenance` with OPL-only control-plane boundary | `consumer_migration.py` / OPL unique control plane boundary inventory 的 import source；2026-05-22 已把 active-path residue cleanup gates、physical thinning evidence 和 physical morphology closeout registry 拆到 `consumer_migration_active_path_inventory.py`，主 inventory 从约 1000 行降到约 660 行，继续承载 functional surface classification、retired legacy residue tombstones 与 functional module inventory。 | MAS authority boundary refs、study/publication/artifact/source owner declarations、private authority policy guard refs、typed blocker / owner receipt refs；不生成 paper closure、publication verdict、source readiness verdict 或 artifact mutation authorization。 | OPL functional/private surface inventory hosting、generic scheduler/queue/attempt-ledger/runtime/workbench/lifecycle cleanup registry、read-model projection inventory。 | 该 inventory 不再作为 MAS 保留 runtime/control 面的理由；所有 generic scheduler/queue/runtime/workbench/lifecycle 条目必须指向 OPL owner 或 retired tombstone。 | `tests/test_opl_unique_control_plane_boundary.py`、`tests/test_ai_first_private_authority_contract.py`、`tests/test_test_lane_governance.py`、`tests/test_cli_cases/owner_route_handoff_command.py`、`make test-meta`、`scripts/verify.sh`。 |
| `src/med_autoscience/controllers/opl_unique_control_plane_boundary_parts/consumer_migration_active_path_inventory.py` | `opl_framework_migration_candidate` | `consumer_migration_inventory.py` re-export 的 source；当前约 355 行，集中记录 retired runtime transport/turn runner/worker lease/product-workbench/CLI/scheduler/legacy cleanup tombstones、physical thinning evidence 和 lane-D closeout registry。 | MAS authority refs、domain receipt requirement、focused test refs、no-forbidden-write guard refs；不写 study truth、publication verdict、AI reviewer judgment、source readiness verdict、artifact mutation authority、owner receipt 或 typed blocker。 | OPL queue/attempt-ledger/retry-dead-letter/runtime transport/workbench/status/scheduler/lifecycle cleanup primitive、private platform residue registry、operator read-model projection。 | 该 helper 只能作为 migration input / tombstone map；不得把 cleanup gate 写成 MAS generic runtime owner、scheduler owner、worker lease owner 或 retained active caller。 | `tests/test_opl_unique_control_plane_boundary.py`、`tests/test_ai_first_private_authority_contract.py`、`tests/test_test_lane_governance.py`、`tests/test_cli_cases/owner_route_handoff_command.py`。 |
| `active_run_id` / liveness / redrive arbitration / route hydration | `opl_framework_migration_candidate` | DM002 暴露出当前 AI reviewer route-back、analysis work unit 和 runtime failed/stopped/submission-metadata parking 之间的投影冲突；既有 MAS status surfaces 可观察这些 refs，但不应继续作为 generic runtime arbiter 扩写。 | domain transition refs、AI reviewer route-back refs、controller authorization refs、owner receipt、typed blocker、publication/artifact authority guard。 | provider liveness arbitration、active run ownership、attempt hydration、queue enqueue/dequeue、retry/dead-letter、resume/relaunch selection、operator status arbitration。 | 该类问题的修复入口是 OPL provider/runtime manager 或 OPL-generated queue/status/workbench shell。MAS 侧只可补缺失的 domain route ref、sidecar export field、owner callable 或 typed blocker；不得新增 MAS 私有 liveness/resume arbitration 来弥补 OPL hydration 缺口。 | OPL family-runtime queue/attempt tests；MAS sidecar export/dispatch tests；真实 paper-line canary owner receipt 或 stable typed blocker。 |
| live task intake / controller authorization message relay (`user_message`, `last_task_intake_delivery`, controller authorization relay marker) | `opl_framework_migration_candidate` | 这些路径仍可作为现有 live managed runtime 的迁移期 message-delivery compatibility surface，但它们不能继续扩展为 MAS 自有 durable queue、attempt ledger、retry/dead-letter 或 provider-resume truth。2026-05-22 已把 `study_task_intake.py` 的 task-intake artifact path、latest read、markdown/runtime-context render、startup-brief block 和 JSON/Markdown write shell 拆入 `study_task_intake_surfaces.py`，主入口降到约 842 行并继续持有 task-intake domain semantics、revision/manual-hold/stop-loss/closeout arbitration。 | study task intake truth、controller authorization refs、domain owner route refs、owner receipt、typed blocker、managed-runtime message ref provenance、reviewer_revision / stop-loss / manual-hold / submission revision authority。 | message queue ownership、delivery retry/dead-letter、attempt lifecycle、provider wakeup/resume、operator queue/status projection、task-intake artifact host/render shell、startup-brief block updater、generic intake locator/read-model shell。 | 新功能必须优先落到 OPL family-runtime queue / provider runtime manager；MAS 侧只能记录 domain authorization ref 或 message-ref provenance，不新增基于 pending user message 的 generic redrive/liveness 仲裁。当前 artifact surface split 只是 migration input 和文件边界收薄，不表示 OPL task-intake/default caller 已接管，也不表示 task-intake truth、quality closeout 或 live-message queue 已迁移。 | OPL family-runtime hydration / dispatch / dead-letter tests；MAS sidecar pending_family_tasks export tests；study task intake direct-path no-forbidden-write tests；`tests/test_study_task_intake.py`；`tests/product_entry_cases/manifest_launch_and_task_intake.py`。 |
| `src/med_autoscience/controllers/workspace_init.py` | `opl_framework_migration_candidate` | workspace init CLI/MCP/product setup；2026-05-22 已把 profile/config merge/render helper 与 legacy entry detector 拆入 `workspace_init_parts/`，主入口从约 1182 行降到约 736 行，只保留 workspace directory/file assembly、thin init orchestration、git boundary 与 MAS workspace policy shell。 | source readiness verdict、study charter/source truth interpretation、workspace policy boundary、owner refs。 | generic workspace locator、source intake shell、runtime artifact root bootstrap、lifecycle/index shell、generated workspace init surface。 | OPL locator/index and generated workspace shell own generic workspace lifecycle; MAS source-readiness authority tests remain green; development checkout must not produce runtime artifacts. | `tests/test_workspace_init.py`、`tests/test_workspace_init_cases/*`、`tests/test_workspace_git_boundary.py`、`tests/test_boundary_fitness.py`。 |
| `src/med_autoscience/controllers/workspace_init_parts/profile_config.py` | `opl_framework_migration_candidate` | `workspace_init._render_workspace_profile(...)` 与 `init_workspace(...)` 的 profile/config render/merge helper；当前约 238 行。 | MAS workspace policy values、medical overlay defaults、source/workspace readiness-related profile refs；不生成 source readiness verdict、study truth、publication verdict、artifact mutation authority、owner receipt 或 typed blocker。 | OPL generated workspace profile/config shell、workspace source locator defaults、runtime artifact root config transport。 | OPL generated workspace/source surface consumes MAS declarative pack values; this helper cannot become runtime artifact lifecycle or provider config owner. | `tests/test_workspace_init.py`、`tests/test_workspace_init_cases/*`、workspace config merge focused tests。 |
| `src/med_autoscience/controllers/workspace_init_parts/legacy_entries.py` | `opl_framework_migration_candidate` | `workspace_init._rendered_file_action(...)` 的 legacy workspace entry upgrade detector；当前约 227 行。 | MAS workspace policy boundary 和 legacy-to-current domain entry interpretation refs；不持有 source readiness verdict、study truth、publication verdict、artifact mutation authority、owner receipt 或 typed blocker。 | OPL generated workspace lifecycle / upgrade detector、legacy entry tombstone/provenance scanner、workspace bootstrap shell。 | OPL workspace lifecycle/index primitive owns legacy generated-entry upgrade/retirement; no-new-compat-alias proof remains mandatory. | `tests/test_workspace_init_cases/legacy_entry_upgrades.py`、`tests/test_workspace_git_boundary.py`、OPL generated workspace lifecycle parity tests。 |
| `src/med_autoscience/controllers/mainline_status.py` | `refs_only_authority_or_provenance` with `opl_framework_migration_candidate` boundary | `mainline_status` action、product-entry/status projection、CLI/MCP status read；2026-05-22 已把 Phase 2 user-product loop、Phase 3 clearance、Phase 4 backend deconstruction 和 platform target 的 program/status projection assembly 拆入 `mainline_status_parts/program_surfaces.py`，主入口降到约 661 行，只保留 MAS owner truth、phase ladder 和 thin read function。 | MAS owner truth、single-project boundary、capability owner boundary、active tranche truth、study/domain authority refs、publication route/readiness authority refs、controller decision refs、typed blocker。 | generic program/status read-model shell、App/operator summary projection、product-loop / clearance / backend-deconstruction display assembly、status serialization。 | OPL App/status/workbench shell owns generic projection/read-model hosting；MAS status payload 只可输出 refs 与 blockers，不得承接 runtime current-state owner。 | mainline status focused tests、product-entry status tests、`scripts/verify.sh` for code changes。 |
| `src/med_autoscience/controllers/mainline_status_parts/program_surfaces.py` | `opl_framework_migration_candidate` | `mainline_status._phase2_user_product_loop`、`_phase3_clearance_lane`、`_phase4_backend_deconstruction`、`_platform_target` 的 current source；当前约 452 行。 | MAS refs、owner boundary wording、domain retained-role refs、runtime owner/substrate labels；不生成 study truth、publication verdict、source readiness verdict、artifact mutation authority、owner receipt 或 typed blocker。 | OPL hosted/generated program companion surface、App/operator summary projection、generic product-loop / clearance / backend-deconstruction display shell。 | OPL generated program/status surface owns generic summary projection and consumes MAS-owned refs; MAS helper must not become current runtime owner/substrate authority. | mainline status focused tests、product-entry status tests、OPL generated/status handoff proof。 |
| `src/med_autoscience/controllers/stage_knowledge_plane.py` | `domain_authority_retained` with `opl_framework_migration_candidate` projection boundary | `stage_knowledge_plane` public API、stage-memory CLI/product-entry callers、publication-route memory closeout/router callers；2026-05-22 已把 paper-soak memory apply proof 的 receipt/ref projection shell 拆入 `stage_knowledge_plane_parts/paper_soak_memory_apply_proof.py`，主入口从约 1110 行降到约 878 行，继续持有 stage knowledge packet、publication-route memory selection、typed closeout normalization、memory router/writeback authority 和 thin proof wrapper。 | publication-route memory selection/writeback authority、typed closeout normalization、MAS memory accept/reject routing、stage knowledge semantics、study/source/artifact truth refs、owner receipt / typed blocker refs。 | generic stage proof/read-model projection shell、receipt ref index display、OPL/Aion read-only display policy、body-free receipt/input-ref assembly、stage knowledge locator/read-model hosting。 | OPL generated stage-knowledge / App workbench proof surface 可消费 MAS route-memory refs 与 receipt refs；MAS 不把 stage proof projection 扩展成 runtime read-model、scheduler 或 attempt owner。 | `tests/test_stage_knowledge_plane.py`、`tests/test_stage_knowledge_visibility.py`、product-entry stage descriptor parity cases、OPL generated stage-knowledge proof parity。 |
| `src/med_autoscience/controllers/stage_knowledge_plane_parts/paper_soak_memory_apply_proof.py` | `opl_framework_migration_candidate` | `stage_knowledge_plane.build_paper_soak_memory_apply_proof(...)` 的 current source；当前约 318 行，读取 stage closeout、MAS router receipt、workspace writeback receipt 与 sidecar dispatch receipt refs，组装 body-free paper-soak memory apply proof 和 OPL/Aion read-only refs projection。 | MAS-provided route-memory refs、memory router receipt refs、typed closeout proposal refs、authority boundary labels；不写 publication-route memory body、不接受或拒绝 writeback、不生成 publication verdict、source readiness verdict、artifact mutation authority、owner receipt 或 typed blocker。 | OPL hosted/generated proof projection primitive、receipt/input-ref indexer、App/workbench read-only receipt display shell、stage knowledge proof locator。 | OPL proof/read-model primitive can generate the same projection from MAS authority refs; this helper remains body-free and cannot become runtime attempt/read-model owner. | `tests/test_stage_knowledge_plane.py`、paper-soak memory proof focused cases、OPL App/workbench receipt projection parity。 |
| `src/med_autoscience/controllers/gate_clearing_batch.py` | `domain_authority_retained` with `opl_framework_migration_candidate` projection boundary | `run_gate_clearing_batch(...)` controller action、publication gate replay follow-through、runtime-watch/controller action callers；2026-05-22 已把 closed-batch replay report payload、current package freshness proof reuse、publication work-unit lifecycle normalization 与 current-gate duplicate check shell 拆入 `gate_clearing_batch_parts/closed_batch.py`，主入口从约 1004 行降到约 943 行，继续持有 gate-clearing action selection、repair-unit planning、controller route context、publication gate replay 和 write-route orchestration。 | publication gate replay authority、repair-unit selection semantics、artifact/package write-route guard、controller route context、current package authority interpretation、publication work-unit lifecycle authority refs、typed blocker / owner receipt refs。 | generic closed-batch read-model shell、duplicate-eval/currentness projection、lifecycle record normalization wrapper、current-package freshness proof carrier、OPL App/workbench batch status display shell。 | OPL generated controller/batch read-model primitive 可消费 MAS publication gate / owner refs 并生成 closed-batch projection；MAS 不把 lifecycle record wrapper 扩展成 runtime lifecycle/read-model owner。 | `tests/test_gate_clearing_batch.py`、gate-clearing current-package freshness cases、publication work-unit lifecycle focused cases、`make test-meta`。 |
| `src/med_autoscience/controllers/gate_clearing_batch_parts/closed_batch.py` | `opl_framework_migration_candidate` | `gate_clearing_batch` private wrapper helpers 的唯一 active source；当前约 220 行，封装 closed-batch duplicate/currentness、replay payload merge、current-package freshness proof reuse、publication work-unit lifecycle normalization。 | MAS-provided gate report refs、publication work-unit refs、current package freshness proof refs、lifecycle record refs；不运行 repair units、不 replay publication gate、不写 publication verdict、不授权 artifact/package mutation、不生成 owner receipt 或 typed blocker。 | OPL batch/read-model projection primitive、lifecycle record wrapper、freshness proof ref carrier、App/workbench closed-batch display shell。 | OPL read-model/batch primitive 可从 MAS authority refs 生成同等 projection，active/default caller 切换并证明 focused tests、MAS lifecycle/freshness receipt parity、no-forbidden-write proof 和 tombstone/provenance refs 后迁出或删除。 | `tests/test_gate_clearing_batch.py`、current-package freshness focused tests、OPL controller batch status parity。 |
| MAS 私有 console / operator drilldown read model 与 UI shell | `retired_runtime_control_plane_provenance` | 旧 MAS 私有 runtime drilldown、terminal/log observation shell 和 operator UI 已物理退役；当前不保留 CLI、module、read-model、HTML shell 或 compatibility alias。 | 无 active MAS authority；历史材料只可作为 provenance。 | OPL current-control-state、provider attempt projection、terminal transport 和 App/workbench drilldown。 | OPL runtime/workbench owns runtime drilldown；MAS active Portal 只暴露 study progress、route/decision、owner receipt、typed blocker 和 artifact/source refs。 | stale surface scan；Progress Portal no-private-console tests；OPL current-control-state parity evidence。 |
| `src/med_autoscience/controllers/study_progress_parts/projection.py` 与 `progress_portal_parts/` | `opl_framework_migration_candidate` | Progress Portal、study progress projection、workspace workbench drilldown；projection 已从约 1116 行降到 935 行。2026-05-22 `stage_review.py` 已把 workspace locator、latest review page proof、paper-line workspace proof 与 refs normalization 拆入 `stage_review_parts/locator.py`，主 stage-review read model 从约 926 行降到约 662 行；同日 workbench/study-workbench read-model 已收薄到 `progress_portal_parts/study_workbench.py` 与 `study_workbench_parts/`；本轮继续把 quality/module surface selection 拆入 `study_progress_parts/projection_quality_surfaces.py`，把 runtime supervision refs display shell 拆入 `study_progress_parts/projection_runtime_surfaces.py`，并将新 helper 纳入 control-plane preflight 分类与 canonical fact-resolver guard。 | study progress truth refs、publication/artifact/memory refs、route decision refs、typed blocker refs、stage review / deliverable index refs；不生成 quality verdict、source readiness verdict、publication ready 或 artifact mutation authority。 | generic operator progress/workbench shell、dashboard layout/display shell、workspace/source/artifact locator projection、stage-review locator/read-model hosting、runtime liveness display/read-model shell、quality-surface refs-only display selection。 | OPL workbench/default display caller 可消费 MAS refs；MAS 不再扩展 generic portal owner；direct MAS path parity、no-forbidden-write proof、stage review locator parity、runtime liveness read-model parity、owner receipt/typed blocker refs 保持后继续迁移或 tombstone 非 authority shell。当前拆分是收薄和迁移输入，不表示 OPL 已接管 study-progress default caller。 | `tests/test_progress_portal.py`、`tests/progress_portal_cases/*`、`tests/test_cli_cases/progress_portal_commands.py`、`tests/test_control_plane_structure.py`、`tests/test_dev_preflight_contract.py`。 |
| MAS Portal/Console combined soak runner | `retired_runtime_control_plane_provenance` | 旧 combined soak CLI、read-model evidence report 和 terminal/log cleanliness checks 已物理退役；当前不保留 runner、parts package、artifact output 或 command alias。 | 无 active MAS authority；历史材料只可作为 provenance。 | OPL App/workbench evidence shell、current-control-state user-path soak 和 generic display/read-model evidence aggregation。 | MAS direct path 只保留 Progress Portal refresh、source refs 和 no-forbidden-write proof；combined runtime drilldown soak 归 OPL。 | stale surface scan；Progress Portal focused tests；OPL App/workbench user-path evidence。 |
| `src/med_autoscience/controllers/domain_owner_action_dispatch.py` 与 `domain_owner_action_dispatch_parts/action_execution.py` | `domain_authority_retained` with OPL typed-queue boundary | MAS owner action dispatch / hard-methodology authority callable；当前约 999 / 992 行。 | medical methodology owner callable、owner evidence、typed blocker、safe action authorization。 | generic action request transport and queue shell only。 | Authority callable 留 MAS；transport/queue/attempt 归 OPL typed queue / generated action shell，dispatch envelope 不承接 retry/dead-letter 或 provider liveness。 | `tests/test_domain_owner_action_dispatch*.py`、domain route scan focused tests。 |
| scheduler / supervision lifecycle surfaces | `retired_runtime_control_plane_provenance` for old CLI/manager surfaces; `refs_only_authority_or_provenance` for MAS domain SLO refs | `runtime-supervision-status` / `runtime-ensure-supervision` / `runtime-remove-supervision` are not active CLI/MCP/product/workspace surfaces; local LaunchAgent and Hermes manager paths are tombstone/provenance only. | outer supervision SLO interpretation、owner receipt、typed blocker、safe action refs、no-forbidden-write evidence。 | scheduler cadence、job registry、wakeup, retry/dead-letter, provider lifecycle, default outer supervision owner。 | No MAS scheduler manager direct path may be restored; no install/status/remove/trigger active local or Hermes payload. OPL current_control_state / provider attempt ledger is the only current scheduler/runtime owner. | domain SLO tombstone tests; no-alias CLI focused tests; legacy tombstone contract。 |
| runtime transport / turn runner / worker lease | `retired_runtime_control_plane_provenance` | `runtime_transport_handoff_projection`、retired runtime core test references、stage closeout receipt provenance；2026-05-21 已删除 `runtime_transport/__init__.py` package-level generic callable facade，default provider caller 改由 OPL provider-backed stage runtime 验证。旧 worker wrapper / turn authorization / user-message / lease 子模块只作为 tombstone/provenance 或 deletion map 出现。 | typed closeout refs 后的 domain owner receipt、guarded apply / typed blocker bridge、artifact/source/quality refs。 | generic worker residency、attempt ledger、queue、transition runner、retry/dead-letter、provider transport。 | OPL current_control_state/provider attempt ledger 是唯一 runtime owner；MAS 不保留 runtime transport、turn runner、worker lease、diagnostic fallback、compat alias 或 active caller gate。 | stale surface scan, OPL provider/current_control_state tests, real paper-line owner receipt canary。 |
| `runtime_protocol/lifecycle_refs_adapter.py` and lifecycle CLI | `retired_runtime_lifecycle_provenance` | 旧 runtime lifecycle CLI、study runtime event/snapshot refs、owner-route handoff/product-entry lifecycle projections；2026-05-22 的 watch/report/event/archive SQLite record shell split 只作为 historical provenance。 | owner receipt refs、surface refs、artifact restore/cleanup proof refs、locator refs；MAS 保留 domain receipt / typed blocker / artifact restore proof 的 ref authority，不写 publication verdict、source readiness verdict、artifact mutation authority 或 owner receipt body。 | generic persistence/lifecycle engine、watch/report/event/archive record index shell、restore-retention orchestration、workspace lifecycle index owner、OPL App/workbench lifecycle projection host。 | Replacement 是 OPL lifecycle/index primitive + MAS `domain_authority_refs_index`；不保留 lifecycle refs adapter alias、runtime lifecycle CLI 或 SQLite active caller。 | stale surface scan, domain authority refs projection checks, `make test-meta` when contracts change。 |
| workspace / source locator and intake shell | `opl_framework_migration_candidate` | workspace init/readiness CLI, source readiness / external intake read models, sidecar substrate adapter refs。 | source readiness verdict、study charter and source truth interpretation、evidence ledger refs、body-free source refs。 | generic workspace locator/index, source intake shell, lifecycle/restore locator, generated workbench display。 | OPL locator/index and generated source/workspace surfaces can consume opaque refs; source body/readiness verdict stays MAS. Cutover requires no active generic shell caller in MAS and source readiness authority tests. | source readiness / workspace init focused tests; sidecar export contract tests。 |
| product-entry / workspace cockpit / workbench / portal shell | `opl_framework_migration_candidate` | product-entry manifest/status, workspace cockpit, Progress Portal, workbench drilldown projections；2026-05-22 已把 product-entry status operator brief、entry surface summary、workspace cockpit preview 与 refs-only workspace snapshots 拆入 `product_entry_parts/manifest_status_surface.py`，`manifest_surfaces.py` 降到约 829 行并从 boundary-fitness oversized baseline 移除。随后 product-entry runtime inventory、task lifecycle、session continuity、progress projection、artifact inventory 与 skill runtime continuity assembly 拆入 `product_entry_parts/generated_status_projection.py`，workspace attention queue / operator brief assembly 拆入 `product_entry_parts/attention_projection.py`，`program_surfaces.py` 继续保留 phase/entry/program 入口，`workspace_attention.py` 只保留兼容导出壳。 | MAS domain projection payload, study truth refs, publication/artifact/memory refs, typed blocker refs, safe action receipt refs。 | generic product-entry/status/workbench shell, App drilldown, operator dashboard, projection hosting, status payload assembly。 | OPL generated product/status/workbench shell owns production/default projection; MAS direct path keeps domain receipt refs only. No workbench/status shell may reintroduce runtime/watch/transport ownership. | product-entry manifest/status tests; App/workbench drilldown proof; `contracts/generated_surface_handoff.json` checks；boundary fitness / line-budget check。 |
| publication quality verdict / AI reviewer quality decision | `domain_authority_retained` | AI reviewer workflow, `publication_eval/latest.json`, review ledger/evidence ledger refs, publication gate. | independent reviewer/auditor quality judgment, publication route decision, route-back semantics, quality typed blocker。 | none; OPL may index refs only. | Cannot be converted to script/test/queue completion verdict. Missing independent reviewer/auditor record must fail closed or route back. | private authority policy validator, publication eval workflow tests, AI reviewer record provenance checks。 |
| artifact mutation / package authority | `domain_authority_retained` | delivery sync, artifact rebuild proof, submission package handoff, current package authority. | guarded artifact mutation authorization, rebuild/freshness proof interpretation, package authority receipt。 | generic artifact locator/gallery/lifecycle shell only. | OPL cannot write artifact body/package/current_package. Mutation requires MAS owner receipt or typed blocker. | artifact lifecycle tests, delivery sync tests, no-forbidden-write proof。 |
| owner receipt signer / typed blocker materializer | `domain_authority_retained` | sidecar dispatch receipt, controller decisions, guarded apply, human gate/resume, stop-loss refs。 | receipt signing, blocker materialization, safe action refs, owner route proof。 | generic receipt ledger/index and transport only. | OPL stores/queries refs; MAS signs and interprets domain receipt/blocker. | sidecar dispatch tests; owner route / controller decision tests。 |

## domain_status_authority.py thinning item

本轮 `domain_status_authority.py` 的正确处置不是直接迁到 OPL，也不是继续把它当成 MAS 私有控制面长期扩写。它应作为 `needs_split_before_migration` 的 control-plane thinning item。2026-05-21 已先把 `_finalize_result` 中的通用 refs/read-model/projection attachment 移入 `status_projection_shell.py`，让主文件更集中在 MAS domain decision authority；这只是拆分切点，不是 OPL 已接管 status/control plane。

1. 保留在 MAS 的 authority 子域：
   - study truth 与 `ProgressProjectionStatus` 的医学语义解释。
   - publication quality / publication gate / manual finish / submission authority 相关判断。
   - source readiness、startup/runtime reentry gate 的医学研究约束。
   - artifact mutation、owner receipt、typed blocker、controller authorization 和 forbidden-write 边界。

2. 可迁往 OPL 的 generic 子域：
   - generic status read-model assembly 和 serialization shell。
   - state-machine runner、transition runner、queue / attempt / retry / dead-letter。
   - watch shell、scheduler tick / provider cadence、runtime event relay 的通用外壳。
   - workspace/source/artifact locator/index、family orchestration projection、operator workbench display shell。

3. 拆分门槛：
   - 每个拆出的子模块必须能说明 owner：authority、read-model、adapter 或 materializer。
   - 新 OPL-facing shell 只能消费 MAS owner refs，不重新解释医学 verdict。
   - 任何迁移声明必须同时给出 OPL replacement proof、MAS receipt parity、focused tests、no-forbidden-write proof 和 tombstone/provenance refs。
   - generic runtime/read-model/scheduler/recovery/worker lease 子域不能再写成 MAS active adapter；proof 未完成时只写成 migration input、retired provenance 或 OPL-owned gap，不写成 MAS 当前 owner。

## 2026-05-21 status finalization shell split

本轮把 `domain_status_authority.py` 内部 `_finalize_result` 的通用状态投影组装拆到 `status_projection_shell.py`。新模块只在 MAS 已经完成 decision/reason 选择后附着 runtime summary、supervision refresh、runtime event ref、family companion、worker activity、auto-runtime parked projection、study truth snapshot、control-plane snapshot 与 progress projection。

该拆分减少了 `domain_status_authority.py` 的私有控制面混杂程度，并为 OPL generated status/workbench shell 后续接管 projection hosting 留出单独入口。它不迁移 publication gate、manual finish dominance、controller authorization、runtime health semantics、artifact authority、source readiness、owner receipt 或 typed blocker，也不声明 OPL status/control-plane active/default caller 已完成。

## 2026-05-21 status projection read-model assembly split

本轮继续收薄 `status_projection_shell.py`，把明显 generic 的 study truth snapshot、control-plane snapshot 与 progress projection 组装移入 `status_projection_shell_parts/read_model_projection_assembly.py`。主 shell 仍由 `domain_status_authority._finalize_result(...)` 唯一调用，仍只在 MAS 已经完成 decision/reason、runtime health、publication/manual-finish/controller authorization 等 domain 判定之后附着 refs-only read model。

该拆分是 `opl_framework_migration_candidate` 的迁移输入：它把可迁往 OPL generated status/workbench shell 的 read-model attachment 包成单独子域，但保留 MAS domain truth input、AI reviewer / auditor judgment、artifact mutation authority、source readiness verdict、owner receipt 与 typed blocker materialization 边界不变。最终进一步迁移或删除仍依赖 OPL read-model/default caller parity、MAS receipt parity、focused tests、no-forbidden-write proof 和 tombstone/provenance refs。

## 当前默认 owner / 退役门 / 验证入口

| gate id | current owner status | retirement gate | validation entry |
| --- | --- | --- | --- |
| `generated_surface_default_owner_cutover` | Generated descriptors and handoff exist; hand-written MAS shells only serve direct domain handler, authority ref, receipt, blocker or provenance roles. | OPL hosted/generated shell becomes production/default owner; old wrapper/alias/facade cannot reappear as a runtime control surface; MAS direct path receipt parity and no-forbidden-write proof remain green. | generated surface handoff checks, product-entry / CLI / MCP focused tests, OPL descriptor validation. |
| `refs_only_authority_boundary` | Storage maintenance、publication-route memory transport 和 artifact audit 只允许 refs-only authority/provenance 输出；terminal attach/read-model 已退役，runtime lifecycle/read-model 语义归 OPL。 | no body/verdict/blob output; OPL index/lifecycle/workbench/runtime drilldown owns generic primitives; MAS only emits authority refs / receipts / blockers. | storage / artifact focused tests; sidecar export contract tests; stale runtime wording scan. |
| `runtime_transport_sqlite_physical_retirement` | Runtime transport、turn runner、worker lease 和 lifecycle refs SQLite 已按旧 MAS runtime 控制面退役；若路径名仍出现在文档/fixture 中，只能是 tombstone/provenance 或删除清单。 | OPL current_control_state/provider attempt ledger is the only runtime owner; MAS paper-line receipt or stable typed blocker proves domain boundary; no compatibility alias or diagnostic fallback. | stale surface scan, OPL provider/current_control_state tests, real paper-line canary owner receipt or stable typed blocker. |
| `workbench_sidecar_status_retirement` | Product-entry/workspace cockpit/sidecar/status still provide direct MAS path, OPL handoff input or diagnostic read model. | OPL generated product/status/workbench shell is default/production caller; MAS retains only domain handler target, receipt signer, typed blocker and authority refs. | product-entry manifest/status tests, sidecar export/dispatch tests, App/workbench drilldown evidence. |
| `legacy_cleanup_physical_retirement` | Local LaunchAgent scheduler and workspace-local watch wrappers are tombstone/provenance only. | No compatibility alias, wrapper or active diagnostic bridge reintroduced; old refs stay history/tombstone only. | legacy tombstone contract, OPL unique control plane boundary tests, stale surface scan. |

## 2026-05-21 product/workbench wrapper alias cleanup

本轮最小真实清理只作用于 product-entry / workspace cockpit / workbench projection shell：移除 `needs_physician_decision`、`legacy_needs_physician_decision_field`、`legacy_approval_gate_field`、`study_physician_decision_gate` 和 `study_needs_physician_decision` 这组旧 human-gate alias 的 product/workbench 输出与 fallback 消费，保留 `needs_user_decision` 作为唯一 product/workbench human-gate 字段。

该清理属于 `workbench_shell_domain_projection_refs` 的 wrapper/facade thinning evidence：它减少 MAS 私有 generic workbench/status 包装面里的兼容别名，但不删除 `product_entry_parts/workspace_cockpit/`、Progress Portal、owner-route handoff adapter 或 status projection，也不改变 `progress_projection`、publication verdict、artifact authority、controller decision 或任何真实 workspace artifact。

## 2026-05-21 CLI aggregator natural split

本轮继续清理 domain repo 内手写大型 CLI 聚合器：`src/med_autoscience/cli.py` 不再内联 stage-memory、workbench/portal、watch/supervision、workspace/data/bootstrap 的 dispatch body，而是委托给 `src/med_autoscience/cli_parts/stage_memory_commands.py`、`watch_supervision_commands.py`、`workbench_commands.py` 和 `workspace_data_commands.py`。拆分后 `cli.py` 低于 1000 行；后续又删除末尾不可达的旧 status 分支，使 CLI 聚合器降到约 941 行，新增 parts 文件均为小型 direct dispatcher。

该清理只减少 MAS repo-local CLI wrapper 的平台化体积；它不改变任何 controller、study truth、publication verdict、artifact authority、source readiness、owner receipt、typed blocker 或真实 workspace artifact。OPL generated CLI / Skill / MCP / product-entry default owner 仍需 replacement parity、direct path receipt parity、no-forbidden-write proof 和 no-resurrection proof 后才能声明完成。

## 2026-05-21 runtime transport callable facade retirement

本轮继续清理 runtime transport 私有平台化 facade：`src/med_autoscience/runtime_transport/__init__.py` 不再通过 package-level `__getattr__` 暴露 `create_quest`、`resume_quest`、`schedule_turn` 等 generic runtime callables。默认 OPL provider 的调用证明迁到 explicit OPL provider path：`get_managed_runtime_backend(DEFAULT_MANAGED_RUNTIME_BACKEND_ID)` 返回 `opl_provider_backed_stage_runtime`，而 `runtime_transport` package 本身不再导出 generic runtime callable。

该清理已把 `runtime_transport/mas_runtime_core*`、turn runner、worker lease 归入 no-alias physical retirement 方向；旧路径若仍被引用，只能作为 tombstone/provenance、删除清单或 OPL provider replacement proof。MAS 当前可保留的是 owner receipt、typed blocker、artifact/source/quality refs 和 domain_authority_refs_index，不保留 runtime transport diagnostic fallback、compat alias 或 active caller gate。

## 2026-05-21 worker wrapper Codex isolation split

本轮继续把旧 worker wrapper 记录为 retired runtime substrate 的 historical split：Codex managed-home / config / virtualenv isolation helper 曾移入 `runtime_transport/mas_runtime_core_worker_codex_isolation.py`，用于说明旧文件边界来源。

该历史拆分不能再读成 worker lease / wrapper active caller retained。当前 runtime worker residency、session liveness、lease update 与 turn normalization 均归 OPL current_control_state / provider attempt ledger；MAS 只能消费 closeout refs 并产出 owner receipt、typed blocker 或 domain authority refs。

## 2026-05-22 mainline status program surface split

本轮继续清理 `mainline_status.py` 的大型私有 status / product-loop 控制面：Phase 2 user-product loop、Phase 3 clearance lane、Phase 4 backend deconstruction 和 platform target 的 program/status projection assembly 已移入 `mainline_status_parts/program_surfaces.py`。`mainline_status.py` 主入口降到约 661 行，继续持有 MAS owner truth、single-project boundary、capability owner boundary、active tranche truth、phase ladder 和 thin read function。

该拆分把可迁往 OPL hosted/generated program companion、App/operator summary projection 和 status/workbench display shell 的通用组装集中成单独 `opl_framework_migration_candidate`。它不迁移 MAS study truth、publication quality verdict、source readiness verdict、artifact mutation authority、owner receipt、typed blocker 或 runtime/controller durable truth，也不声明 OPL App/status/workbench default caller 已完成。

## 2026-05-22 turn user-message shell split

本轮继续把旧 turn lifecycle adapter 的历史拆分记录为 provenance：user-message queue、claim pending messages 与 restore claimed messages 曾移入 `runtime_transport/mas_runtime_core_turn_messages.py`。

该历史拆分不能再读成 message queue、turn runner、worker lease、runtime transport 或 lifecycle refs SQLite index 的 MAS active caller。当前 queue、provider resume、claim/restore、attempt lifecycle 与 closeout transport 均由 OPL stage runtime 负责；MAS 只处理 typed closeout 后的 owner receipt、route-back、typed blocker 或 artifact/source/quality authority refs。

## 2026-05-22 turn worker residency adapter split

本轮继续把旧 turn lifecycle / worker lease 控制面的拆分记录为 retired provenance：worker liveness check、orphan worker lease cleanup、delayed-turn drain/timer wiring、turn lifecycle inspection 和 stale liveness reconciliation 的调用壳曾移入 `runtime_transport/mas_runtime_core_turn_residency.py`。

该历史拆分不能再作为 worker lease、worker wrapper、runtime transport、message queue、turn runner 或 lifecycle refs SQLite index 的 retained gate。OPL worker residency manager / current_control_state 是唯一当前 owner；MAS 不再维护 stale liveness reconciliation 或 runtime recovery projection。

## 2026-05-22 workspace init profile/config and legacy-entry split

本轮继续清理 `workspace_init.py` 的 workspace/source/bootstrap 私有平台化控制面：profile/config TOML/env render 与 merge helper 已移入 `workspace_init_parts/profile_config.py`，legacy workspace entry upgrade detector 已移入 `workspace_init_parts/legacy_entries.py`。`workspace_init.py` 主入口降到约 736 行，并从 boundary fitness oversized baseline 中移除，后续不能再把该文件作为被允许的超长控制面继续扩写。

该拆分只把 generic workspace bootstrap shell、profile/config transport 和 legacy generated-entry detector 作为 OPL workspace/source/lifecycle primitive 的迁移输入单独成域。它不迁移 MAS source readiness verdict、study charter/source truth interpretation、publication verdict、artifact mutation authority、owner receipt、typed blocker 或真实 workspace artifact authority，也不声明 OPL generated workspace/default caller 已完成。

## 2026-05-22 consumer migration active-path inventory split

本轮继续清理 OPL unique control plane boundary 里的大型 migration inventory 控制面：active-path residue cleanup gates、physical thinning evidence 和 physical morphology lane-D closeout registry 已移入 `opl_unique_control_plane_boundary_parts/consumer_migration_active_path_inventory.py`。`consumer_migration_inventory.py` 主入口从约 1000 行降到约 660 行，继续作为 `consumer_migration.py` 消费的 functional surface classification / module inventory / retired residue tombstone 薄入口。

该拆分把 runtime transport、turn runner、worker lease、product/workbench shell、CLI wrapper、scheduler/watch 和 legacy cleanup gate 这类 generic 平台残留台账集中成 OPL migration input / retired tombstone map。它不迁移 MAS study truth、publication quality verdict、source readiness verdict、artifact/package authority、AI reviewer / auditor judgment、owner receipt 或 typed blocker，也不授权把旧 generic runtime 控制面继续写成 MAS active caller retained。

## 2026-05-22 stage review locator/proof split

本轮继续清理 Progress Portal / workbench 的 Stage Review 大型只读投影面：workspace root resolution、artifact locator path normalization、latest review page proof、paper-line workspace proof、source-ref normalization 和 read-only authority proof 已移入 `progress_portal_parts/stage_review_parts/locator.py`。`stage_review.py` 主入口从约 926 行降到约 662 行，继续承担 stage review index assembly、summary/read-model 和 HTML rendering。

该拆分只把可上收到 OPL workspace/source/artifact locator、App/workbench stage-review read model 和 operator projection 的 generic locator/proof shell 拆成独立 migration input。它不读取或写入 review body，不授权 publication quality verdict、submission readiness、source readiness、artifact authority、owner receipt 或 typed blocker，也不声明 OPL App/workbench/default caller 已完成。

## 2026-05-22 study workbench conversation projection split

本轮继续清理 Progress Portal / workbench 的 executor conversation 大型只读展示面：conversation read-model validation、timeline item projection、visible item selection、source-ref filtering、authority proof 和 HTML timeline rendering 已移入 `progress_portal_parts/study_workbench_parts/conversation.py`。`study_workbench.py` 主入口从约 969 行降到约 695 行，继续承担 workbench payload assembly、artifact group projection、stage knowledge section、route/status sections 和 thin rendering orchestration。

该历史拆分已被后续 physical retirement 吸收。当前 MAS active workbench 不消费或生成私有 executor conversation read model；执行器对话、turn receipt、runtime event 与 terminal/log drilldown 由 OPL current-control-state / provider attempt projection 承接。MAS active Portal 继续只展示 domain progress、route/decision、owner receipt、typed blocker 和 artifact/source refs。

## 2026-05-22 product-entry status payload split

本轮继续清理 product-entry manifest/status 大型私有 wrapper：product-entry status operator brief、entry surface summary、workspace cockpit preview、open-auto-research projection、OPL current-control-state handoff projection、AI-first feedback projection、study truth snapshot copy 和 runtime health snapshot copy 已移入 `product_entry_parts/manifest_status_surface.py`。`manifest_surfaces.py` 主入口降到约 829 行，继续承担 product-entry manifest construction、OPL provider-ready contract refs、skill catalog 与 thin status/preflight/start entrypoint。

该拆分把可上收到 OPL generated product/status/workbench shell 和 App drilldown 的 generic status payload assembly 单独成域。新 helper 只消费 `build_product_entry_manifest` 与 `read_workspace_cockpit` 产出的 refs/read-model，不写 study truth、publication verdict、source readiness verdict、artifact/package mutation、owner receipt、typed blocker 或 runtime/controller durable truth，也不声明 OPL generated product/status/workbench default owner 已完成。进一步迁移或删除仍依赖 OPL replacement parity、MAS direct-path receipt parity、focused product-entry status tests、no-forbidden-write proof、no-resurrection proof 和 tombstone/provenance refs。

## 2026-05-22 product/status/workbench projection assembly split

本轮继续收薄 product/status/workbench projection shell：`product_entry_parts/generated_status_projection.py` 承接 runtime inventory、task lifecycle、session continuity、progress projection、artifact inventory 和 skill runtime continuity 的 refs-only payload assembly；`product_entry_parts/attention_projection.py` 承接 workspace attention queue 与 operator brief assembly。`program_surfaces.py` 继续保留 phase lane、product-entry start 和 skill catalog registration 入口，`workspace_attention.py` 只保留兼容导出壳，`manifest_surfaces.py` 继续作为 manifest/status/preflight/start 入口。

该拆分只把 App/workbench/status 可消费的 refs-only projection assembly 单独成域。它不读取或写入 `publication_eval/latest.json`、`controller_decisions/latest.json`、paper/package/`current_package`，不授权 AI reviewer verdict、publication quality、submission readiness、source readiness、artifact/package mutation、owner receipt 或 typed blocker，也不声明 OPL generated product/status/workbench shell 已成为 production/default caller。runtime/watch/transport 仍需 OPL provider/default caller parity、MAS receipt parity、focused tests、no-forbidden-write proof 和 tombstone/provenance refs 后才能继续物理迁移或删除。

## 2026-05-22 lifecycle record index shell split

本轮把旧 `lifecycle_refs_adapter.py` / lifecycle refs SQLite index 记录为 retired runtime lifecycle provenance：watch state、runtime report、workspace storage audit、runtime event 和 archive ref 的 record/index implementation 曾移入 `lifecycle_refs_adapter_parts/runtime_record_indexes.py`。

该历史拆分不能再读成 lifecycle refs SQLite index active caller。Generic watch/report/event/archive record index、runtime lifecycle persistence、restore/retention orchestration 和 App lifecycle projection 均归 OPL/shared family primitive；MAS 当前 replacement 是 `domain_authority_refs_index`，只保存 owner receipt、typed blocker、artifact/source/status refs、cleanup/terminal gate receipts 或 authority locator refs，不生成 publication verdict、source readiness、artifact mutation、memory writeback、domain-ready 或 production-ready。

## 2026-05-22 runtime storage cache cleanup shell split

本轮继续收薄 `runtime_storage_maintenance.py` 的 workspace storage / cache cleanup 私有控制面：delete-safe cache scan、scoped scan-root resolution、apply/delete result assembly 和 workspace-boundary guard 已移入 `runtime_storage_maintenance_parts/cache_cleanup.py`。主入口从约 923 行降到约 775 行，继续保留 `audit_workspace_storage(...)`、`maintain_runtime_storage(...)`、runtime/storage report 写入、dataset/git/runtime refs 汇总和既有 `_delete_safe_candidates(...)` thin delegator。

该拆分只把可上收到 OPL storage lifecycle cleanup policy / artifact lifecycle storage-audit shell 的 generic cache cleanup 子域单独成迁移输入。它不删除 runtime storage maintenance active caller，不声明 OPL cleanup/default caller 已接管，不写 `publication_eval/latest.json`、`controller_decisions/latest.json`、paper/package/`current_package`，不授权 restore-ready、publication-ready、source-ready、artifact mutation 或 paper closure。后续迁移或删除仍需 active caller 清零、OPL cleanup policy parity、MAS artifact authority receipt parity、focused storage tests、no-forbidden-write proof 和 tombstone/provenance refs。

## 2026-05-22 stage knowledge proof projection split

本轮继续收薄 `stage_knowledge_plane.py` 的 stage-knowledge / publication-route memory 控制面：paper-soak memory apply proof 的 stage closeout refs、MAS router receipt refs、workspace writeback receipt refs、sidecar dispatch receipt refs、OPL/Aion read-only receipt projection 和 body-free input-ref assembly 已移入 `stage_knowledge_plane_parts/paper_soak_memory_apply_proof.py`。`stage_knowledge_plane.py` 主入口降到约 878 行，继续保留 stage knowledge packet、publication-route memory selection、typed closeout normalization、memory router/writeback authority 和 public wrapper。

该拆分只把可上收到 OPL generated stage-knowledge proof / App workbench receipt projection 的 read-only shell 单独成域。它不迁移 publication-route memory body、memory accept/reject routing、typed closeout authority、publication quality verdict、source readiness verdict、artifact/package mutation、owner receipt、typed blocker 或 runtime/controller durable truth，也不声明 OPL stage-knowledge/default caller 已完成。后续迁移或删除仍需 OPL proof/read-model parity、MAS memory/router receipt parity、focused stage-knowledge tests、no-forbidden-write proof、body-free proof 和 tombstone/provenance refs。

## 2026-05-22 gate-clearing closed-batch shell split

本轮继续收薄 `gate_clearing_batch.py` 的 controller/batch 私有控制面：closed-batch replay report payload、current package freshness proof reuse、publication work-unit lifecycle normalization、duplicate-eval/current-gate check 和 authority-sync settled projection 已移入 `gate_clearing_batch_parts/closed_batch.py`。`gate_clearing_batch.py` 主入口降到约 943 行，继续保留 gate-clearing action selection、repair-unit planning、controller route context、publication gate replay、write-route orchestration 和 public wrapper。

该拆分只把可上收到 OPL generated controller/batch read-model、lifecycle record wrapper 和 App/workbench closed-batch status display 的 generic shell 单独成域。它不迁移 publication gate replay authority、repair-unit selection semantics、artifact/package write-route guard、current package authority interpretation、publication verdict、source readiness verdict、artifact mutation authority、owner receipt 或 typed blocker，也不声明 OPL controller/batch/default caller 已完成。后续迁移或删除仍需 OPL batch/read-model parity、MAS gate/lifecycle receipt parity、focused gate-clearing tests、no-forbidden-write proof 和 tombstone/provenance refs。

## 禁止误写

- 不把 `active_private_generic_residue_count=0`、classification closed、descriptor ready、OPL cleanup ledger ready 或 production acceptance receipt 写成物理源码清零。
- 不把 `runtime_backend_id=mas_runtime_core` 写成 MAS generic runtime owner；当前只按 retired provenance / migration input 或 MAS domain authority refs 读取。
- 不把 OPL provider proof、stage production evidence receipt 或 generated surface proof 写成真实 paper closure、publication-ready、medical-ready、artifact mutation authorization 或 `current_package` 更新。
- 不把 `domain_status_authority.py` 本轮 thinning 写成 OPL 已接管 status/control plane；当前只是需要拆分后迁移的 control-plane item。
- 不把 `active_run_id`、worker liveness、failed/stopped redrive、resume/relaunch 或 route hydration 缺口写成新的 MAS 私有 runtime/status 修复；这些属于 OPL provider/runtime manager，MAS 只输出 domain refs、owner receipt 和 typed blocker。
- 不把 publication quality、AI reviewer quality、source readiness、publication-route memory accept/reject 或 artifact mutation authority 写成脚本、测试、queue completion、file presence 或同一 executor 自审可以决定。
