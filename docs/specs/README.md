# Specs Docs

Owner: `MedAutoScience`
Purpose: `active_spec_index`
State: `active_support`
Machine boundary: Human-readable spec index. Machine truth remains in contracts, schemas, source, runtime evidence, and generated artifacts.

This directory is the canonical OPL-family location for current MAS technical specs. Older dated specs must be classified as active, support reference, or history before expansion.

Current MAS runtime and product-boundary truth primarily lives in the core docs, `docs/runtime/`, contracts, source, and runtime evidence.
