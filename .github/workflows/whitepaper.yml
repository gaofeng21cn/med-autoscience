name: MAS Whitepaper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  whitepaper:
    uses: gaofeng21cn/one-person-lab/.github/workflows/reusable-whitepaper.yml@main
    with:
      profile: contracts/whitepaper_profile.json
      output_name: mas-whitepaper
      publish: false
    secrets: inherit
